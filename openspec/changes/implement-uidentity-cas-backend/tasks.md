## 规则读取与影响记录

- [x] 1.1 读取并记录命中的规范：`documentation.md`、`openspec.md`、`architecture.md`、`data-permission.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`testing.md`、`i18n.md`和`goframe-v2`技能
- [x] 1.2 维护任务影响记录：i18n（插件未启用 i18n 资源治理，后续运行时文案仍使用结构化错误码）、数据权限（插件自有旧统一身份业务表按用户要求为全局级，不保留`tenant_id`；后台旧路由继续通过宿主认证/权限边界，公开运行时入口继续通过一次性 ticket/state/token/验证码自隔离）、缓存一致性（当前阶段无缓存状态）、插件（本地未发现插件根`AGENTS.md`）、SQL（PostgreSQL-only 幂等安装/卸载 SQL）、API、后端 Go、测试和开发工具跨平台影响

## 插件骨架

- [x] 2.1 新建`apps/lina-plugins/linapro-uidentity-cas/`源码插件目录、`plugin.yaml`、`plugin_embed.go`和通用资源目录
- [x] 2.2 新建插件`backend/plugin.go`并注册受宿主认证、租户、权限中间件治理的 RESTful 路由
- [x] 2.3 更新插件工作区`go.mod`引用和宿主插件打包/嵌入发现路径

## 数据模型与 SQL

- [x] 3.1 新增插件安装、卸载 SQL，覆盖账号、账号详情、单位、容器、分组、应用、授权、黑名单、密码策略、短信、CAS 日志、OAuth 日志、OAuth token 和变更日志表
- [x] 3.2 生成或维护插件 DAO/DO/Entity，并确保生产代码不手改生成文件
- [x] 3.3 执行 SQL 幂等性、PostgreSQL-only、自增主键和索引静态检查

## 基础管理 API 与 Service

- [x] 4.1 实现账号、账号详情、单位、容器、分组、应用、密码策略和短信记录的列表、详情、创建、更新、删除接口
- [x] 4.2 实现账号应用授权、账号应用黑名单、分组应用黑名单、OAuth token、OAuth 日志、CAS 登录日志和账号变更日志接口
- [x] 4.3 实现列表关联投影批量装配、批量删除可见性校验和结构化`bizerr`错误

## 认证运行时与统计

- [x] 5.1 实现密码强度校验、管理员改密、自助改密、手机号校验改密和账号锁定/解锁
- [x] 5.2 实现 CAS ticket 校验、CAS XML 解析、应用访问规则校验和 CAS 登录日志记录
- [x] 5.3 实现 OAuth token 治理和统一身份统计聚合，确保统计不产生`N+1`查询

## 验证与提交

- [x] 6.1 运行`openspec validate implement-uidentity-cas-backend --strict`
- [x] 6.2 运行插件和受影响宿主包 Go 编译/单元测试门禁
- [x] 6.3 运行`git diff --check`和插件子仓库静态检查
- [x] 6.4 在合适阶段提交主仓库和插件仓库代码

## Feedback

- [x] **FB-1**: 源项目`uidentity/admin`服务端功能未全量迁入插件，当前实现只覆盖管理 CRUD、基础 CAS ticket 校验和统计
- [x] **FB-2**: 插件缺少旧 admin 的 CAS 服务端票据链、Token 链路、账号激活和用户自助后端接口
- [x] **FB-3**: 插件缺少旧 admin 的 OAuth 授权码流程、账号导入检查/导入、LDAP 同步边界、短信发送、文件上传、作业和监控类后端能力
- [x] **FB-4**: 二次核对发现旧 admin 的`account/unlockPassword`密码错误次数解锁后端能力未在插件内落地
- [x] **FB-5**: 二次核对发现旧 admin 的登录态用户微信换绑 state 链未在插件内落地
- [x] **FB-6**: 再次复核发现旧 admin 的账号激活微信扫码绑定 state 链未在插件内落地
- [x] **FB-7**: 再次复核发现旧 admin 的`app/jobs`真实定时任务调度执行未在插件内落地
- [x] **FB-8**: 再次复核发现旧 admin 的`account_active_log`激活、微信换绑和`BindUnionID`审计日志未在插件内落地
- [x] **FB-9**: 再次复核发现旧 admin 的`ChangeContainer`本地账户容器迁移动作未在插件 cron 执行器内落地
- [x] **FB-10**: 再次复核发现旧 admin 的账号与账号详情模型 hook 自动写入`account_change_log`未在插件内落地
- [x] **FB-11**: 能力清单审计发现旧 admin 账号创建/更新请求中的`groupIds`同步维护`account_group`关系未在插件账号资源写入中落地
- [x] **FB-12**: 再次检查要求建立旧 admin 服务端能力清单、逐项映射插件实现/测试/排除理由，并执行至少两轮反向审计记录
- [x] **FB-13**: 用户明确要求将旧`app/jobs`注册表中的九个任务迁为`linapro-uidentity-cas`插件任务管理 handler，并清理插件内非必要的自建 cron 调度实现
- [x] **FB-14**: 用户反馈`uidentity_cas`接口地址、参数和旧`/Users/wanna/mine/ecoding/uidentity/admin`不一致，要求旧服务端接口内容、地址和参数保持完全一致
- [x] **FB-15**: 再复核发现旧接口契约仍有细节偏差：根`/sso`与`/ssologin`路由、旧`Any`方法集合、SSO/UnionID/Wechat 回调重定向语义、Wechat 空实现入口、`stat/get`公开性和`job-log`旧地址未完全对齐
- [x] **FB-16**: Review 发现插件 jobs 迁移与旧`admin/app/jobs`业务副作用仍不一致，包括`ChangeContainer`缺少 LDAP 先行迁移、Oracle 非数字单位代码未跳过、已有账号缺详情被自动补建、LDAP 新增账号多写`openUID`
- [x] **FB-17**: Review 发现旧兼容层仍未完全业务等价，包括`cas-login/index`未产生可继续使用的登录结果、`job-log`为空壳、根`wechat/callback`未执行公众号回调语义、微信扫码回调缺少`code`解析配置边界、扫码 redirect query 和旧响应`msg`不完全一致
- [x] **FB-18**: 用户反馈当前路由与参数仍未和旧`/Users/wanna/mine/ecoding/uidentity/admin`完全一致；本轮按旧 router/DTO 反向审计并补齐非 job 后端路由与旧参数别名，job 路由按用户补充不处理
- [x] **FB-19**: 再次按旧 handler 反向审计发现旧兼容层成功响应`msg`和部分`data`形态仍被统一化，CAS、激活、用户自助、资源 CRUD、配置、上传、短信、统计等接口出参与旧项目不一致
- [x] **FB-20**: 再按旧 router 挂载入口反向审计发现插件 legacy 层多注册旧项目不存在的非 job 路由，导致旧路由集合不是严格一致
- [x] **FB-21**: 再按旧短信 handler 反向审计发现`POST /api/v1/sms/send`虽保留路由但未按旧项目接收并校验`code/uuid`图形验证码，导致入参与业务逻辑不一致
- [x] **FB-22**: 用户反馈 legacy 兼容面仍暴露 LinaPro 新路由/参数默认值，要求旧前端直接切换 base URL 后继续使用旧`/api/v1/*`路由和原参数语义
- [x] **FB-23**: 用户要求逐一核对旧项目每一个 router 挂载项，插件 legacy 路由的方法、路径和挂载前缀必须与旧`uidentity/admin`完全一致
- [x] **FB-24**: 用户反馈`linapro-uidentity-cas/backend/api/uidentity/v1`中的`g.Meta path`仍大量使用 LinaPro 新命名空间，API DTO 契约必须改为旧`uidentity/admin`实际路由
- [x] **FB-25**: 用户反馈数据库表结构和表名也必须与旧`uidentity/admin`一致；表前缀可保留，但表名后缀、字段结构和全局级数据语义必须按旧 MySQL 表迁为 PostgreSQL
- [x] **FB-26**: 再审计发现`oauth2_token`仍保留旧模型不存在的审计和软删除列，需按旧`OauthToken`模型收敛为`id/expired_at/code/access/refresh/data`
- [x] **FB-27**: 继续按旧 MySQL DDL/GORM 模型核对发现部分字段类型、长度和列名仍未对齐，需保持表前缀但恢复旧字段结构语义
- [x] **FB-28**: 继续核对发现`backend/api/uidentity/v1`仍用泛化`Resource*Req`表达旧管理资源，导致 API 目录未逐一暴露旧资源路径和旧账号参数契约
- [x] **FB-29**: 用户确认表前缀可以保留后，需补充自动化契约锁定插件表名前缀之外的旧表后缀和旧字段集合，防止数据库结构再次偏离旧`uidentity/admin`
- [x] **FB-30**: 再核对发现部分旧`/api/v1`系统管理路由被标记为宿主已覆盖，但宿主同路径返回 LinaPro 新契约，旧前端不能无改动切换
- [x] **FB-31**: 用户明确数据库表结构也必须与旧`uidentity/admin`一致，表前缀可以保留；需补齐插件 SQL 中旧非 job 系统管理表后缀和字段结构，并继续确保旧系统接口响应字段映射为旧 GoAdmin 契约
- [x] **FB-32**: 再核对发现旧非 job 系统管理路由虽已对齐地址和字段映射，但`sys-user`、`sys-api`、`dept`、`post`、字典选项、配置读取、角色菜单/部门树等插件路由仍为空响应或成功占位，需改为读取和写入插件内旧兼容表
- [x] **FB-33**: 继续核对发现`DELETE /api/v1/menu`、`PUT /api/v1/role-status`、`PUT /api/v1/roledatascope`和`GET /api/v1/gen/tabletree`仍为空壳或空树，需按旧 handler 参数和出参接入插件旧系统兼容表
- [x] **FB-34**: 继续核对发现与宿主同方法同路径冲突的旧系统管理接口仍通过宿主 handler 后置转写响应，导致数据源和业务逻辑不是插件旧兼容表；需由插件在宿主 bearer 校验后直接接管旧路径
- [x] **FB-35**: 用户要求每一个旧路由业务逻辑一致后，继续核对发现非 job 的`/api/v1/gen/preview/{tableId}`、`/api/v1/gen/toproject/{tableId}`、`/api/v1/gen/apitofile/{tableId}`和`/api/v1/gen/todb/{tableId}`仍是成功占位，需接入插件旧`sys_tables/sys_columns/sys_menu`兼容表并恢复旧代码生成/菜单生成语义
- [x] **FB-36**: 继续收口旧参数契约时发现 legacy HTTP helper 只把`{id}`作为通用路径 ID，导致`{tableId}`等旧命名路径参数无法传入生成器和部分系统详情路由；需统一识别旧命名 path 参数并保持旧成功响应出参形态
- [x] **FB-37**: 继续审计旧路由占位实现时发现非 job 的`POST /api/v1/logout`仍返回通用占位`data/requestId`且没有写旧`sys_login_log`退出日志，需恢复旧`code/msg`响应和退出登录日志副作用
- [x] **FB-38**: 继续按旧初始迁移和 GORM 模型核对数据库表结构时发现`tb_demo`表未随插件前缀建表，需补齐旧迁移兼容表并锁定字段集合
- [x] **FB-39**: 继续核对`backend/api/uidentity/v1`发现旧路径仍存在重复 DTO 和登录态用户接口把旧中间件注入的`number`声明为前端必传，需收敛为旧`uidentity/admin`实际参数契约
- [x] **FB-40**: 继续按旧全表结构核对发现旧迁移账本表`sys_migration`未随插件前缀建表，需补齐兼容表并锁定字段集合
- [x] **FB-41**: 用户再次明确 CAS 迁移不能改动`apps/lina-core`核心框架，需撤回旧同路径拦截引入的 core 宿主认证契约和装配改动，并把旧路由接管逻辑限定在插件侧

## Feedback Impact Notes

- FB-41 根因记录：前序 FB-34 为了让`linapro-uidentity-cas`在`/api/v1/role`、`/api/v1/menu`、`/api/v1/dict/*`、`/api/v1/config`和`/api/v1/user/profile`这类与 LinaPro 宿主同方法同路径冲突的旧接口上短路宿主 handler，新增了`apps/lina-core/pkg/plugin/capability/contract.AuthService.AuthenticateBearer`、`AuthenticatedContext`、hostservices auth/session 适配和 HTTP 启动装配参数。这虽然能让插件在全局中间件中校验 bearer 后调用插件旧兼容表逻辑，但把具体 CAS 迁移需求扩进了 core 稳定契约，违反“不能动 core，逻辑都写在 plugin”的边界要求。复核发现 core 原本已经发布`RouteMiddlewares.Ctx/Auth/Tenancy`和`GlobalMiddlewares`，插件可以在旧冲突路由命中时选择性复用这些既有宿主中间件，再短路分发到插件 legacy controller，无需新增 core 认证接口。
- FB-41 已完成：已将`apps/lina-core`中由 FB-34 引入的宿主认证契约、hostservices auth/session 适配、HTTP 启动装配参数和相关测试改动恢复到`origin/feat/cas`原始状态；`git diff --exit-code origin/feat/cas -- apps/lina-core/...`确认这些 core 文件与基线一致。插件`backend/plugin_legacy_interceptors.go`不再依赖`contract.AuthService.AuthenticateBearer`，改为在旧冲突路由命中时连续注册选择性全局中间件包装器：`RouteMiddlewares.Ctx()`、`RouteMiddlewares.Auth()`、`RouteMiddlewares.Tenancy()`和最终 legacy dispatcher。非旧冲突路由继续`r.Middleware.Next()`透传；插件禁用时由宿主`GlobalMiddlewareRegistrar`透传；旧冲突路由在宿主既有认证、在线 session 和租户解析通过后短路调用插件 legacy controller，业务数据仍来自插件旧兼容表。
- FB-41 影响分析：本轮净效果为撤销 CAS 专用 core 改动并修改`apps/lina-plugins/linapro-uidentity-cas`插件入口、旧路由拦截器、拦截器单元测试和 OpenSpec 任务记录；未新增宿主 core 契约、宿主 API DTO、宿主路由规范、宿主私有 DAO/DO/Entity、SQL、前端页面、CI 或脚本。数据权限：旧冲突路由继续先经过宿主既有认证和租户中间件，旧系统兼容表按用户要求保持插件全局级，不新增租户字段或绕过目标可见性的新路径。缓存一致性：不新增缓存、快照、失效或跨实例状态；在线 session 校验继续由宿主既有`Auth`中间件执行。i18n：不新增运行时文案、插件清单文案、错误码或 API 文档源文本。开发工具跨平台：无 Makefile、脚本、`linactl`或平台入口变更。DI：无新增接口型运行期依赖，插件只复用 registrar 已发布的`RouteMiddlewares`实例。SQL：无 DDL/DML 变更，既有旧表结构契约不变。接口性能：只改变旧冲突路由分发链路，不新增数据库循环查询；插件旧系统列表、菜单树和 profile 装配仍复用既有集合化实现。
- FB-41 验证记录：已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteInterceptor' -count=1`通过，覆盖旧冲突路由短路到插件、缺 bearer 被宿主 auth wrapper 拒绝、非旧路由和插件禁用时不触发包装器；已运行`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过；已运行`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）通过；已运行宿主相关包`GOWORK=off go test ./internal/service/plugin/internal/hostservices ./internal/service/plugin ./pkg/plugin/capability/contract -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`通过；已运行`rg -n "AuthenticateBearer|AuthenticatedContext" apps/lina-core apps/lina-plugins/linapro-uidentity-cas/backend`确认生产代码无残留，仅 OpenSpec 根因记录保留历史说明。

- FB-40 根因记录：按旧`/Users/wanna/mine/ecoding/uidentity/admin/common/models/migrate.go`、`cmd/migrate/server.go`和`cmd/migrate/migration/init.go`继续核对旧库全表结构发现，旧 GoAdmin 迁移框架会 AutoMigrate`sys_migration`，字段为`version`和`apply_time`，其中`version`为主键。插件当前安装 SQL 已覆盖旧业务表、旧系统表、旧 job 兼容表、`sys_casbin_rule`和`tb_demo`，但缺少前缀版`plugin_linapro_uidentity_cas_sys_migration`；虽然 LinaPro 插件安装流程不依赖旧 GoAdmin 迁移账本，但按“数据库表结构和表名也要一致，表前缀可以有”的口径，它属于旧库全表结构缺口。旧`app/jobs/models/account.go`中的`T_BZKXS`、`V_YJSJCXX_FULL`、`V_RS_V_JZGJCSJXX_TEMP`、`WJ_STU_INFO`和`T_ORG_DEPT`继续判定为外部 Oracle/教务源表，不属于插件 PostgreSQL 主库建表范围。
- FB-40 已完成：在插件安装 SQL 中新增幂等`CREATE TABLE IF NOT EXISTS plugin_linapro_uidentity_cas_sys_migration(version, apply_time)`，卸载 SQL 增加对应`DROP TABLE IF EXISTS`；schema contract 测试新增`sys_migration`字段集合断言，继续允许`plugin_linapro_uidentity_cas_`表前缀但锁定旧表后缀和字段。该表仅作为旧迁移账本兼容表，本轮不新增 DAO/DO/Entity、不修改 API、controller 或 service，不引入运行时依赖。
- FB-40 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件安装/卸载 SQL、schema contract 测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、前端页面、API DTO、业务 service、DAO/DO/Entity 生成文件、CI 或脚本。数据权限：`sys_migration`为旧迁移账本兼容表，无读写接口，不新增数据可见性边界；插件整体仍按用户要求全局级，不加入`tenant_id`。缓存一致性：无缓存、快照或失效逻辑。i18n：不新增运行时文案、插件清单文案或 API 文档资源。开发工具跨平台：无脚本或 Makefile 变更。DI：无新增运行期依赖。SQL：PostgreSQL-only，`CREATE TABLE IF NOT EXISTS`和`DROP TABLE IF EXISTS`可重复执行；无 Seed/Mock DML，无显式写入自增 id。接口性能：该兼容表无接口查询路径，不涉及`N+1`或索引性能风险。
- FB-40 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaSQLTableSuffixesAndColumnSets|TestLegacySchemaTableSuffixesAndColumnSets|TestLegacySchemaColumnNamesAndGeneratedTypes|TestOauth2TokenLegacySchemaContract' -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过，锁定旧表后缀和字段集合；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyAPIDTOsDoNotDuplicateOldRouteContracts' -count=1`通过，确认旧路由和 API DTO 路径仍按旧项目契约覆盖；已启动临时 PostgreSQL 实例并连续两次执行插件安装 SQL，通过`information_schema`确认前缀表总数为 39 且`plugin_linapro_uidentity_cas_sys_migration`列为`version/apply_time`，验证 SQL 可执行且幂等；已运行`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`通过。

- FB-39 根因记录：按旧`/Users/wanna/mine/ecoding/uidentity/admin/app/cas/models/user.go`、`app/cas/apis/active.go`和`app/cas/apis/user.go`继续核对插件`backend/api/uidentity/v1`发现，API DTO 层仍把同一个旧路由声明成多套入参：`GET /api/v1/activate/wechatScan`同时对应直接`uuid/unionID`绑定和旧`code/state`回调，`POST /api/v1/user/changeWechatState`同时对应外部 callback 和旧`uuid`轮询，`ALL /sso/serviceValidate`同时暴露 JSON 校验和旧 XML 校验。旧项目实际 router/handler 中`activate/wechatScan`只读取 query`code/state`，`user/changeWechatState`绑定`ActivateStateReq`且只读取 body`uuid`，`sso/serviceValidate/proxyValidate`返回 CAS XML；登录态`/api/v1/user/*`自助接口的账号工号由旧中间件`middleware.GetNumber(c)`注入，`ChangePasswordReq`无`number`字段，`ChangePhoneReq/ChangeQQReq/UnbindWechatReq`为`json:"-"`，`ChangeWechat`和`AccountAppList`均绑定旧空结构体`AccountAppListReq`。插件 legacy handler 运行时已经按旧登录上下文取号，但 API DTO 仍把部分`number`、`CasCallback`、`pageIndex/pageSize`声明成前端请求参数，导致 API 目录契约和旧前端可无改动切换的要求不一致。
- FB-39 已完成：插件 API DTO 去掉旧路径上的重复`g.Meta`声明，`ActivationWechatReq`、`UserWechatRebindCallbackReq`和 JSON 版`CasServiceValidateReq`保留为内部 controller/service 调用契约，不再作为旧 HTTP 路由暴露；旧`/api/v1/activate/wechatScan`只声明`state/code/unionID`回调参数，旧`/api/v1/user/changeWechatState`只声明`uuid`轮询参数，`/sso/serviceValidate`只保留 CAS XML DTO。用户自助 DTO 按旧模型恢复参数：`changePassword/changePhone/changeQQ/unbindWechat/changeWechatQr/changeWechatState/accountAppList/accountAppRole/accountAppRoleUpdate`不要求前端传`number`，其中`changeWechatQr`和`accountAppList`保持旧空请求结构体；`changeEmail/getUserInfo/getUserCasLoginLog`保留旧可选`number`字段但不作为身份权威；`accountAppList`旧路径改为不分页、不过滤应用`status`，并把响应投影收敛为旧`{id,name}`。legacy HTTP 和生成的 V1 controller 新增统一 runtime identity 解析，优先使用旧 API header`number`、宿主/插件上下文和 runtime token，最后才兼容请求中的`number`，避免 body/query 伪造账号覆盖旧中间件身份。
- FB-39 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 API DTO、legacy controller、生成 V1 controller 调用层、UIdentity service 应用列表分页开关、契约测试、HTTP 测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由规范、宿主 API DTO、前端页面、SQL、DAO/DO/Entity、CI 或脚本。数据权限：用户自助接口继续以旧 API header/runtime token 等已认证身份为边界，反而避免请求体/查询参数覆盖当前账号；插件按用户要求全局级，不新增租户字段。缓存一致性：无缓存、快照或失效逻辑。i18n：插件未启用`i18n.enabled`，本轮仅调整 API 文档源 DTO 和旧字段，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无 Makefile、脚本或工具入口变更。DI：无新增接口型运行期依赖，新增 helper 复用既有`uidentitySvc`。SQL：无 DDL/DML 变更；按用户确认，插件表名前缀可以保留，旧表后缀和旧字段结构由既有 schema 契约测试继续锁定。接口性能：`accountAppList`恢复旧无分页语义，但该旧接口仅返回当前账号未拉黑应用列表；黑名单仍集合化查询，应用列表单次查询，不引入`N+1`。
- FB-39 验证记录：已运行`GOWORK=off go test ./backend -run 'TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyAPIDTOsDoNotDuplicateOldRouteContracts|TestLegacyUserRuntimeDTOsKeepOldNumberFieldContract|TestLegacyEmptyRuntimeDTOsKeepOldAdminParams|TestLegacyAPIDTOFieldsMatchOldAdminParams|TestLegacyAPIDTOContractsWithNoRouteAreIntentionalInternalContracts' -count=1`通过，锁定旧 API DTO 路由、参数、空请求结构体和无重复合同；已运行`GOWORK=off go test ./backend/internal/controller/uidentity -run 'TestLegacyRuntimeUserRoutesPreferHeaderNumberOverRequestNumber|TestLegacyWechatRoutesDelegateToQRCodeRuntime|TestLegacyAdminLogoutKeepsOldEnvelopeAndRecordsLogout' -count=1`通过，覆盖旧 header 身份优先级和旧`accountAppList`投影；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaSQLTableSuffixesAndColumnSets|TestLegacySchemaTableSuffixesAndColumnSets|TestLegacySchemaColumnNamesAndGeneratedTypes' -count=1`通过，确认本轮未影响旧表后缀和字段集合；已运行`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过。

- FB-38 根因记录：用户确认表前缀可以保留后，本轮把旧`/Users/wanna/mine/ecoding/uidentity/admin/cmd/migrate/migration/version/1599190683659_tables.go`、`cmd/migrate/migration/models/tb_demo.go`、旧`app/admin/models`、`app/jobs/models`和插件 SQL 做了只读反向核对。旧初始迁移会 AutoMigrate`TbDemo`并创建`tb_demo`，字段来自`Model`、`Name`、`ModelTime`和`ControlBy`，即`id/name/created_at/updated_at/deleted_at/create_by/update_by`。插件 SQL 已覆盖旧业务表、旧系统表和 job 兼容表，但缺少前缀表`plugin_linapro_uidentity_cas_tb_demo`；虽然旧项目没有运行时 route/service 依赖该表，但按“数据库表结构和表名也要一致，表前缀可以有”的口径，它仍属于旧初始迁移兼容表缺口。复核过程中还发现旧`AccountUnit`模型字段名会推导`units_id`和审计列，但旧`add_table.sql`、DTO 排序字段和唯一索引均使用`unit_id`且无审计列，且旧 migration 不会 AutoMigrate 该业务模型；插件继续按旧 DDL/DTO 口径保留`account_unit.id/account_id/unit_id`。旧`app/jobs/models`中的`T_BZKXS`、`V_YJSJCXX_FULL`、`V_RS_V_JZGJCSJXX_TEMP`、`WJ_STU_INFO`和`T_ORG_DEPT`是 Oracle/外部同步源表，不属于插件 PostgreSQL 主库建表范围。
- FB-38 已完成：在插件安装 SQL 中新增幂等`CREATE TABLE IF NOT EXISTS plugin_linapro_uidentity_cas_tb_demo`和`name/deleted_at`索引，卸载 SQL 增加对应`DROP TABLE IF EXISTS`；schema contract 测试新增`tb_demo`字段集合断言，继续允许`plugin_linapro_uidentity_cas_`前缀但锁定旧表后缀和旧字段。该表无旧运行时业务路径，本轮不新增 DAO/DO/Entity、不修改 API 或 service，避免扩大迁移面。
- FB-38 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件安装/卸载 SQL、schema contract 测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、前端页面、API DTO、业务 service、DAO/DO/Entity 生成文件、CI 或脚本。数据权限：`tb_demo`为旧迁移样例兼容表，无读写接口，不新增数据可见性边界；插件整体仍按用户要求全局级，不加入`tenant_id`。缓存一致性：无缓存、快照或失效逻辑。i18n：不新增运行时文案、插件清单文案或 API 文档资源。开发工具跨平台：无脚本或 Makefile 变更。DI：无新增运行期依赖。SQL：PostgreSQL-only，`CREATE TABLE IF NOT EXISTS`、`CREATE INDEX IF NOT EXISTS`、`DROP TABLE IF EXISTS`可重复执行；无 Seed/Mock DML，无显式写入自增 id。接口性能：该兼容表无接口查询路径，新增索引仅覆盖可能的旧样例`name`筛选和软删除过滤。
- FB-38 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaSQLTableSuffixesAndColumnSets|TestLegacySchemaTableSuffixesAndColumnSets|TestLegacySchemaColumnNamesAndGeneratedTypes' -count=1`通过，锁定旧表后缀和字段集合；已运行`PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -d linapro -v ON_ERROR_STOP=1 -f manifest/sql/001-implement-uidentity-cas-backend.sql`通过，验证插件安装 SQL 可重复执行；已用`information_schema.columns`确认`plugin_linapro_uidentity_cas_tb_demo`实际列为`id/name/created_at/updated_at/deleted_at/create_by/update_by`；已运行`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过。

- FB-37 根因记录：按旧`/Users/wanna/mine/ecoding/uidentity/admin/app/admin/router/sys_router.go`与`common/middleware/handler/auth.go`核对，旧`POST /api/v1/logout`是受保护非 job 路由，handler 调用`LoginLogToDB(c, "2", "退出成功", user.GetUserName(c))`并直接返回`{"code":200,"msg":"退出成功"}`。插件当前虽然注册了同一路由，但绑定`LegacyExternalOK("logout")`，会返回`{"code":200,"msg":"ok","data":{"type":"logout","success":true},"requestId":...}`，且不会写入插件旧`sys_login_log`兼容表；旧前端直接切换 base URL 时响应体和审计副作用均不一致。
- FB-37 已完成：`/api/v1/logout`改为插件 legacy 专用 controller，成功响应只写旧`code/msg`字段，不再附加通用`data/requestId`；服务层新增`RecordLegacyAdminLogout`，把退出日志写入插件前缀旧`sys_login_log`兼容表，字段保持旧`username/status/ipaddr/login_location/browser/os/platform/login_time/remark/msg/create_by/update_by`语义，并通过插件配置`legacy.logger.enabledDB`保留旧`LoggerConfig.EnabledDB`写库开关，默认按旧生产配置开启。表结构口径按用户补充保持“插件前缀可以有”，既有 schema 契约继续验证`plugin_linapro_uidentity_cas_`前缀、旧表后缀和旧字段集合；本轮未新增 SQL、未手写 DAO/DO/Entity。
- FB-37 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 legacy 路由、controller、service、HTTP 契约测试、服务层数据库测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由规范、宿主 API DTO、前端页面、SQL、DAO/DO/Entity、CI 或脚本。数据权限：旧 logout 仍位于受保护 legacy group，经宿主认证、租户和权限链进入插件；写入的是旧全局级兼容审计表，不新增租户字段。缓存一致性：无缓存、快照或失效逻辑。i18n：插件未启用`i18n.enabled`，旧中文`退出成功`属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无脚本或 Makefile 变更。DI：无新增接口型运行期依赖、构造函数或共享实例变化；服务复用既有`BizCtx`、`Config`与`TenantFilter`。接口性能：单次 logout 最多一次配置读取和一次插入，无动态循环查询或`N+1`风险。job 路由：`/ws`与`/wslogout`仍属于旧 job log WebSocket/工具边界，按用户说明暂不处理。
- FB-37 验证记录：已运行`GOWORK=off go test ./backend/internal/controller/uidentity -run TestLegacyAdminLogoutKeepsOldEnvelopeAndRecordsLogout -count=1`通过，覆盖旧 logout 响应不包含`data/requestId`且传递旧审计字段；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacyAdminLogoutWritesSysLoginLog|TestLegacyAdminLogoutHonorsLoggerDBSwitch' -count=1`通过，覆盖写入插件前缀旧`sys_login_log`兼容表和旧日志写库开关；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyRouteInterceptor|TestLegacySystem' -count=1`通过；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaKeepsOldAdminTableSuffixesAndColumns|TestLegacySchema|TestLegacyAdminLogoutWritesSysLoginLog' -count=1`通过，验证表前缀允许且旧表后缀/字段集合一致；已运行`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过；静态扫描确认`POST /api/v1/logout`不再绑定`LegacyExternalOK("logout")`，剩余`LegacyExternalOK`只在用户豁免的 job log WebSocket 工具路由。

- FB-36 根因记录：补齐 FB-35 后继续按旧`app/other/apis/tools/gen.go`核对成功响应，发现`GenCode`、`GenApiToFile`和`GenMenuAndApi`旧项目均返回`e.OK("", "...")`，而插件 controller 直接把服务层内部生成路径返回给前端，`data`形态不一致。进一步新增 HTTP 层契约测试时发现旧`/api/v1/gen/*/{tableId}`虽然路径注册为`{tableId}`，但`legacyRouterID`只读取 GF router 参数`id`，导致生成器 handler 实际会把`tableId`传成 0；同类风险还影响`{roleId}`、`{dictCode}`、`{configKey}`、`{ticket}`等旧命名 path 参数进入通用 request map 或数值 ID helper 的场景。
- FB-36 已完成：`LegacyGenToProject`、`LegacyGenAPIToFile`和`LegacyGenToDB`继续执行真实服务层副作用，但 controller 成功响应恢复旧项目的空字符串`data`和旧成功`msg`。`legacyRequestMap`改为注入 GF `GetRouterMap()`中的全部命名 path 参数，即使请求无 query/body 也不会丢失路径参数；`legacyRouterID`扩展为识别旧系统和生成器常见数值 path 参数名，包括`id/tableId/columnId/dictCode/dictId/roleId/userId/deptId/postId/jobId`及 snake_case 别名。表结构口径不变：插件表前缀允许保留，旧表后缀和字段集合继续由既有 schema 契约测试锁定。
- FB-36 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 legacy controller helper、生成器 controller 响应投影、HTTP 契约测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主 API DTO、宿主路由规范、前端页面、SQL、DAO/DO/Entity、CI 或脚本。数据权限：不新增路由或权限绕过，旧受保护接口仍经宿主认证/权限链进入插件旧兼容表；用户已要求本插件全局级，不新增租户级表字段。缓存一致性：无缓存、快照或失效逻辑。i18n：插件未启用`i18n.enabled`，旧中文成功消息属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无脚本或 Makefile 变更。DI：无新增运行期依赖、构造函数或共享实例变化。接口性能：只修正 path 参数读取和响应投影，不新增数据库查询或循环装配。
- FB-36 验证记录：已运行`GOWORK=off go test ./backend/internal/controller/uidentity -run TestLegacyGenMutationRoutesKeepOldEmptyData -count=1`通过，覆盖旧`/gen/toproject/{tableId}`、`/gen/apitofile/{tableId}`和`/gen/todb/{tableId}`真实传递`tableId`且成功响应`data`保持空字符串；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacyGenRoutesRenderFilesAndMenus|TestLegacySysTablesTreeReadsTablesAndColumns' -count=1`通过；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyRouteInterceptor|TestLegacySystem' -count=1`通过；已运行`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`和插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过；静态扫描确认生成器 mutation 路由不再返回内部`out`记录，且四个旧`gen/*`路由不再调用`LegacyExternalOK`。

- FB-35 根因记录：按旧`/Users/wanna/mine/ecoding/uidentity/admin/app/other/router/gen_router.go`和`app/other/apis/tools/gen.go`核对，`/api/v1/gen/preview/{tableId}`应读取旧`sys_tables/sys_columns`并返回`template/model.go.template`、`template/api.go.template`、`template/js.go.template`、`template/vue.go.template`、`template/router.go.template`、`template/dto.go.template`和`template/service.go.template`预览；`/api/v1/gen/toproject/{tableId}`应把这些模板生成到项目目录；`/api/v1/gen/apitofile/{tableId}`应生成迁移文件；`/api/v1/gen/todb/{tableId}`应向`sys_menu`写入菜单/API 权限节点。当前插件虽然注册了这些旧路径和`tableId`参数，但都调用`LegacyExternalOK`，旧前端会收到成功响应却没有预览内容、生成文件或菜单写入副作用。用户最新只豁免 job 路由，因此这四个非 job 开发工具后端路由需要在插件内恢复业务语义。
- FB-35 已完成：`plugin_legacy_routes.go`中`/api/v1/gen/preview/{tableId}`、`/api/v1/gen/toproject/{tableId}`、`/api/v1/gen/apitofile/{tableId}`和`/api/v1/gen/todb/{tableId}`不再接`LegacyExternalOK`，改为调用插件 legacy controller 的真实 handler。服务层新增旧代码生成实现：`preview`一次读取插件旧`sys_tables/sys_columns`兼容表并渲染旧模板 key；`toproject`将 model/api/router/dto/service 和前端 js/vue 生成到插件配置`legacy.gen.outputRoot`与`legacy.gen.frontOutputRoot`，默认落在`temp/uidentity-cas-gen`，避免写入`lina-core`核心源码；`apitofile`生成旧迁移文件到配置根目录；`todb`在事务中向插件旧`sys_menu`兼容表写入旧`GenMenuAndApi`等价的 12 个菜单/API 权限节点，并在写入前对齐 PostgreSQL identity sequence 以兼容旧数据迁移后显式`menu_id`造成的序列落后。
- FB-35 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 legacy 路由、controller、service、测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主 API DTO、宿主系统管理表、前端页面、CI 或脚本。SQL：无新增或修改 DDL/DML，继续使用既有插件前缀旧`sys_tables/sys_columns/sys_menu`兼容表，未手写 DAO/DO/Entity 生成文件；`todb`写菜单时使用 GoFrame 自动时间维护，不手写`created_at/updated_at`。数据权限：旧`gen/*`路由仍位于受保护 legacy group，经宿主认证和权限链进入插件旧兼容表；用户已要求本插件全局级，不新增租户级表字段。缓存一致性：无缓存、快照或失效逻辑。i18n：插件未启用`i18n.enabled`，旧中文成功消息和模板文案属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无脚本或 Makefile 变更；生成文件路径使用 Go 标准库与插件配置，默认目录为相对`temp`路径。DI：无新增接口型运行期依赖，继续复用启动期注入的插件配置服务和租户过滤服务。接口性能：`preview/toproject/apitofile`按单个`tableId`读取一张表定义和一次批量列集合；`todb`单事务固定 12 行写入，无动态`N+1`查询。
- FB-35 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacyGenRoutesRenderFilesAndMenus|TestLegacySysTablesTreeReadsTablesAndColumns' -count=1`通过，覆盖旧模板 key、文件生成、迁移文件生成和旧菜单/API 节点写入；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyRouteInterceptor|TestLegacySystem' -count=1`通过，确认旧路由 inventory 和 API path 仍匹配；已运行插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过；静态扫描确认四个`gen/*`旧路由不再调用`LegacyExternalOK`。

- FB-34 根因记录：用户要求旧前端可直接把 base URL 切到新服务，因此`GET/POST/PUT/DELETE /api/v1/role`、`GET/POST/PUT /api/v1/menu`、`GET/POST/PUT /api/v1/dict/data`、`GET/POST/PUT /api/v1/dict/type`、`GET/POST/PUT /api/v1/config`和`GET /api/v1/user/profile`不仅路由和出入参要一致，数据源和业务副作用也必须来自插件旧兼容表。继续审计发现前序 global middleware 方案让宿主同路径 handler 先执行，再把宿主响应字段转成旧 GoAdmin 字段；这会读写 LinaPro 宿主系统管理表，而不是`plugin_linapro_uidentity_cas_`前缀下的旧`sys_*`兼容表，业务逻辑仍不等价。GoFrame 对重复静态路由有冲突检测，插件不能在不污染宿主路由配置的前提下再次注册这些同路径 handler，因此需要由插件 global middleware 精确匹配旧路径，使用宿主发布的窄认证能力校验 bearer 后短路调用插件 legacy controller。
- FB-34 已完成：`linapro-uidentity-cas`插件的旧同路径拦截器不再执行宿主 handler 后置转写响应，改为在`/api/v1/*`全局中间件中精确匹配旧`role/menu/dict/config/user/profile`路径，并在 FB-41 中收敛为选择性复用宿主已发布的`RouteMiddlewares.Ctx/Auth/Tenancy`完成认证、在线 session 和租户上下文处理后短路调用插件 legacy controller，不再新增或依赖 CAS 专用 core 宿主认证契约。插件 controller/service 新增`LegacyMenuList`、`LegacyMenuTree`、`LegacySystemProfile`，其中`GET /api/v1/menu`按旧`SysMenu.GetPage`语义从插件旧`sys_menu`兼容表一次读取并组树，`GET /api/v1/user/profile`从插件旧`sys_user/sys_role/sys_post/sys_dept`兼容表装配`{user,roles,posts}`。同路径详情和更新在短路场景下手动注入`{id}`、`{dictCode}`等旧路径参数，并保留`roleName/roleKey/roleSort`、`menuName/menuType/noCache`、`dict*`、`config*`等旧请求字段别名。
- FB-34 影响分析：本轮主体仍收敛在`apps/lina-plugins/linapro-uidentity-cas`插件后端，少量修改`apps/lina-core`仅用于发布稳定、窄口径的源码插件宿主认证能力，不修改 LinaPro 核心路由规范、宿主系统管理 DAO/DO/Entity、宿主 API DTO、前端页面或数据库结构。SQL：无新增或修改 DDL/DML，表前缀继续允许保留，旧表后缀和字段结构继续由既有契约测试锁定；未手写 DAO/DO/Entity。数据权限：旧项目要求本插件全局级，短路旧接口经宿主 bearer 与在线 session 校验后读写插件旧兼容表；不再经过宿主同路径系统 handler，避免暴露或污染宿主系统管理数据。缓存一致性：新增宿主能力复用启动期共享`authSvc.SessionStore()`和`configSvc.GetSessionTimeout()`，不新增缓存、快照或失效逻辑；session store 仍由宿主权威状态和既有集群策略治理。i18n：插件未启用`i18n.enabled`，旧中文`msg`和旧字段名属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源；宿主新增能力注释不改变用户可见文案。开发工具跨平台：无脚本、Makefile、代码生成入口或平台工具变更。DI：新增运行期依赖为宿主 hostservices 的`HostAuthService`和`HostSessionTimeoutProvider`窄接口，owner 为宿主 auth/config 服务，创建位置为 HTTP 启动期既有`authSvc`和`configSvc`，传递路径为`httpstartup -> pluginsvc.NewHostServices -> hostservices.New -> authAdapter`，全部复用启动期共享实例和共享 session store。
- FB-34 验证记录：已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteInterceptor' -count=1`通过，覆盖旧同路径拦截器 bearer 校验、禁用插件放行、非旧路由放行、短路不执行宿主 handler、旧 query/body/path 参数别名和旧响应 envelope；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemProfileProjectsUserRolesPosts|TestLegacySystemDeptAndMenuTreesUseCompatibilityTables|TestLegacyGetInfoProjectsOldShapeAndPermissions' -count=1`通过，覆盖插件旧兼容表菜单树和 profile 装配；已运行插件全量`GOWORK=off go test ./... -count=1`通过；已运行宿主`GOWORK=off go test ./internal/service/plugin/internal/hostservices -count=1`、`GOWORK=off go test ./internal/service/plugin -count=1`、`GOWORK=off go test ./internal/service/user -run '^$' -count=1`和`GOWORK=off go test ./internal/cmd/internal/httpstartup -run '^$' -count=1`通过，覆盖新增 host service 契约、插件 host service facade、用户测试依赖编译和 HTTP 启动装配签名。宿主`./internal/service/user`全测试因既有缓存协调 fixture 缺 revision 失败，`./internal/cmd/internal/httpstartup`全测试因既有 runtime-config 缓存协调和 panic allowlist 状态失败，均非本轮改动路径；已用窄包和编译烟测替代覆盖本轮改动。

- FB-33 根因记录：继续按旧`uidentity/admin` router 与 handler 反向核对后发现，FB-32 已把多数非 job 系统路由接入插件旧兼容表，但`DELETE /api/v1/menu`仍调用`LegacyExternalOK("menu-delete")`无删除副作用，`PUT /api/v1/role-status`与`PUT /api/v1/roledatascope`仍是成功占位，`GET /api/v1/gen/tabletree`仍返回空树。旧项目中`role-status`从 body/query 的`roleId/status`更新`sys_role.status`并返回“更新角色 {id} 状态成功！”，`roledatascope`从`roleId/dataScope/deptIds`更新`sys_role.data_scope`与`sys_role_dept`关联并返回“操作成功”，`gen/tabletree`读取`sys_tables`并装配对应`sys_columns`。这些不是 job 路由，且表前缀允许保留，因此应由插件旧系统兼容表承载。
- FB-33 已完成：`plugin_legacy_routes.go`中`DELETE /menu`改为`LegacySystemDelete("menus")`，`PUT /role-status`改为`LegacyRoleStatus`，`PUT /roledatascope`改为`LegacyRoleDataScope`，`GET /gen/tabletree`改为`LegacySysTablesTree`。服务层新增`UpdateLegacySysRoleStatus`、`UpdateLegacySysRoleDataScope`和`LegacySysTablesTree`，使用插件生成 DAO/DO 访问`plugin_linapro_uidentity_cas_sys_role`、`sys_role_dept`、`sys_dept`、`sys_tables`和`sys_columns`兼容表；角色数据权限在事务内更新角色并替换关联，表/列树采用一次性表查询加按`table_id`批量读取列，不为每张表逐项查询。
- FB-33 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 legacy 路由、controller、service、单元测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主 DAO/DO/Entity、宿主 API DTO、前端页面、CI 或脚本。SQL：无新增或修改 DDL/DML，继续使用 FB-31 已落地且幂等的 PostgreSQL 兼容表，未手写 DAO/DO/Entity 生成文件。数据权限：旧后台路由仍位于受保护 legacy group，继续经宿主认证、租户和权限中间件；用户要求本项目不做租户级，插件兼容表按全局级读写。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，旧中文`msg`属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无脚本、Makefile、代码生成入口或平台工具变更。DI：无新增接口型运行期依赖、构造函数或共享实例变化。接口性能：角色状态单行更新；角色部门关系一次事务内集合化替换；`gen/tabletree`批量读取列并内存按`table_id`装配，未引入`N+1`查询。
- FB-33 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacyRoleActionsUpdateCompatibilityTables|TestLegacySysTablesTreeReadsTablesAndColumns' -count=1`通过，覆盖角色状态更新、数据权限关系替换和表/列树读取；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyRouteInterceptor|TestLegacySystem' -count=1`通过，确认旧路由 inventory 仍覆盖；已运行插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；静态扫描确认`/menu`删除、`/role-status`、`/roledatascope`和`/gen/tabletree`不再接`LegacyExternalOK`或`LegacyEmptyTree`。

- FB-32 根因记录：旧路由 inventory 与`backend/api/uidentity/v1`路径已按 FB-23/FB-24/FB-28 对齐，但本轮继续按旧 router、旧 handler 和当前`plugin_legacy_routes.go`实现反向核对发现，插件非 job 系统管理路由中`/sys-user`、`/sys-login-log`、`/sys-opera-log`、`/sys-api`、`/dept`、`/post`、`/roleMenuTreeselect/{roleId}`、`/roleDeptTreeselect/{roleId}`、`/menurole`、`/deptTree`、`/dict/type-option-select`、`/dict-data/option-select`、`/configKey/{configKey}`、`/app-config`和`/set-config`仍返回空页、空树、空对象或无业务副作用的成功占位；这只能证明地址可达，不能证明旧前端切换后业务逻辑和出参保持一致。用户最新确认“表前缀可以有”，因此本轮修复口径为保留`plugin_linapro_uidentity_cas_`前缀，使用插件内旧系统兼容表承载这些非 job 系统路由的全局级数据读写；job 路由继续按用户补充不作为修复目标。
- FB-32 已完成：在`linapro-uidentity-cas`插件内基于既有`plugin_linapro_uidentity_cas_sys_*`旧兼容表执行`make dao`生成旧系统表 DAO/DO/Entity，表名前缀保留，旧表后缀和字段结构继续按`sys_user/sys_role/sys_menu/sys_dept/sys_post/sys_dict_type/sys_dict_data/sys_config/sys_api/sys_login_log/sys_opera_log/sys_tables/sys_columns`等旧结构消费。`plugin_legacy_routes.go`中旧非 job 系统管理路由不再返回空壳：`sys-user`、登录日志、操作日志、API、部门、岗位、字典、配置、角色菜单/部门树、菜单树和代码生成表/列浏览均改为读写插件旧兼容表；`/sys/tables/info/{tableId}`等旧路由专用参数也按旧字段识别。`sys_job/job_log`和旧 job 路由继续按用户要求不作为本轮目标，`gen/tabletree`仍是代码生成工具树占位。
- FB-32 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端、插件 DAO 生成配置、生成 DAO/DO/Entity、服务/控制器/路由和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主 DAO/DO/Entity、宿主 API DTO、前端页面、CI 或脚本。SQL：不新增 DDL，继续使用 FB-31 已落地的 PostgreSQL 幂等旧兼容表；本轮只生成并消费这些表，未手写生成代码。数据权限：旧后台系统路由仍挂在受保护 legacy group，继续经宿主认证、租户和权限中间件；用户要求该项目不做租户级，因此插件旧兼容表按全局级读写。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，旧中文`msg`和旧字段名属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：只更新插件`backend/hack/config.yaml`的 GoFrame DAO 表清单并执行`make dao`，无脚本或平台入口变更。DI：无新增接口型运行期依赖、构造函数或共享实例变化。接口性能：列表过滤、计数、排序和分页均在数据库侧完成；部门/菜单树一次性读取节点，角色勾选关系一次性读取；用户部门筛选按旧`dept_path`语义使用数据库侧子查询；未引入随返回行数增长的`N+1`查询。
- FB-32 验证记录：已运行`make -C apps/lina-plugins/linapro-uidentity-cas dao`生成旧系统表 DAO/DO/Entity；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaSQLTableSuffixesAndColumnSets|TestLegacySystem|TestLegacyGetInfo' -count=1`通过，覆盖旧系统 schema 契约、字典/配置选项、部门/菜单树、旧`dept_path`用户筛选和`getinfo`旧形态；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyRouteInterceptor|TestLegacySystem' -count=1`通过；已运行插件全量`GOWORK=off go test ./... -count=1`通过；静态扫描`LegacyEmpty(Page|Tree|Object|List)`等旧系统占位只剩`/gen/tabletree`，该路由属于代码生成工具树占位而非本轮非 job 系统业务目标。完成记录后继续运行`openspec validate implement-uidentity-cas-backend --strict`和主仓库/插件子仓库`git diff --check`。

- FB-31 根因记录：前序 FB-25/FB-29 只锁定了统一身份业务表，FB-30 只把同路径宿主响应转换为旧 envelope；继续按旧`cmd/migrate/migration/models`、`app/admin/models`和旧 DTO 反向核对发现插件安装 SQL 仍缺少旧`sys_user`、`sys_role`、`sys_menu`、`sys_dept`、`sys_post`、`sys_dict_type`、`sys_dict_data`、`sys_config`、`sys_api`、`sys_login_log`、`sys_opera_log`、`sys_casbin_rule`及角色/菜单关联表等旧系统管理表。用户最新确认“表前缀可以有”，因此本轮判定口径为保留`plugin_linapro_uidentity_cas_`前缀，但前缀后的旧表后缀、字段名和全局级语义必须按旧 MySQL/GORM 模型迁为 PostgreSQL；`sys_job`和`job_log`只作为表结构兼容存在，job 路由和任务调度仍按用户补充不处理，继续使用 GF/宿主任务能力。
- FB-31 已完成：在`linapro-uidentity-cas`插件安装 SQL 中补齐旧系统管理兼容表，保留插件表前缀并新增`sys_user`、`sys_role`、`sys_menu`、`sys_dept`、`sys_post`、`sys_dict_type`、`sys_dict_data`、`sys_config`、`sys_api`、`sys_login_log`、`sys_opera_log`、`sys_casbin_rule`、`sys_role_dept`、`sys_role_menu`、`sys_menu_api_rule`、`sys_tables`、`sys_columns`、`sys_job`和`job_log`的 PostgreSQL DDL；卸载 SQL 同步清理这些表。`sys_job/job_log`只用于满足旧表结构兼容，不恢复旧 job 路由或插件自建 cron；旧`tb_demo`只在迁移示例中出现、没有运行时 route/service 使用，本轮不纳入生产插件 schema。旧同路径系统接口响应映射进一步补齐宿主字段到旧 GoAdmin 字段：`roleId/roleName/roleKey`、`menuId/menuName/menuType/permission/noCache`、`dictCode/dictLabel/dictValue/dictType`、`dictName/dictType`、`configName/configKey/configValue`和`user/profile`中的`user/roles/posts`投影，并修正旧`noCache`与宿主`isCache`的反向语义。
- FB-31 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 SQL、卸载 SQL、legacy host-route 响应拦截器及其单测、schema 契约测试和 OpenSpec 文档记录；未修改`apps/lina-core`核心框架、宿主 DAO/DO/Entity、宿主 API DTO、前端页面或开发工具脚本。SQL：安装/卸载脚本继续使用 PostgreSQL 幂等 DDL，新增表均使用`CREATE TABLE IF NOT EXISTS`和`CREATE INDEX IF NOT EXISTS`，无 Seed DML、无 Mock 数据、无显式写入自增主键；旧字段`default`按 PostgreSQL 关键字规则用带引号列名保留字段名。DAO/DO/Entity：新增旧系统兼容表当前不生成 DAO，避免手写生成物；运行时同路径系统接口仍经宿主 handler 成功后由插件做响应投影，不新增数据库读写路径或 N+1。数据权限：受保护旧系统路由继续先执行宿主认证、租户和权限 handler；插件只改写成功响应字段，不把 401/403/错误响应伪装成成功。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，旧中文`msg`和旧字段名属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。开发工具跨平台：无脚本、Makefile 或工具入口变更。DI：无新增接口型运行期依赖、构造函数或共享实例变化。插件本地规范：插件根目录无`AGENTS.md`。
- FB-31 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySystemSchemaSQLTableSuffixesAndColumnSets|TestLegacySchemaTableSuffixesAndColumnSets|TestLegacySchemaColumnNamesAndGeneratedTypes|TestOauth2TokenLegacySchemaContract' -count=1`通过，锁定旧业务表与旧系统兼容表后缀和字段集合；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteInterceptor|TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyInterceptedOldRoutesAreExactOldPaths' -count=1`通过，覆盖旧路由 inventory、旧字段响应投影、未授权不改写和插件禁用放行；已运行插件全量`GOWORK=off go test ./... -count=1`通过；已运行`openspec validate implement-uidentity-cas-backend --strict`通过；已运行主仓库和插件子仓库`git diff --check`通过；静态检查确认旧系统表后缀集合无缺失。
- FB-30 根因记录：前序旧路由覆盖测试把`/api/v1/role`、`/api/v1/menu`、`/api/v1/dict/*`、`/api/v1/config`和`/api/v1/user/profile`等路径记为“宿主已覆盖”，但这些宿主 API 是 LinaPro 新管理工作台契约，响应 envelope、字段、参数和旧`/Users/wanna/mine/ecoding/uidentity/admin`不一致；`/api/v1/dept`和`/api/v1/post`则既未由宿主同路径覆盖，也未在插件中注册完整旧五件套。job 路由按用户补充可不处理，本轮只修正非 job 旧前端直接切换 base URL 会命中的系统管理兼容路由。
- FB-30 已完成：移除“宿主已覆盖”测试口径，改为插件显式覆盖口径：`/api/v1/dept`和`/api/v1/post`四个非删除旧五件套注册到插件受保护 legacy group，继续经宿主`Auth/Tenancy/Permission`中间件后返回旧空树、空分页、空对象和旧创建/更新文案；与宿主同方法同路径冲突的`/api/v1/role`、`/api/v1/menu`、`/api/v1/dict/data`、`/api/v1/dict/type`、`/api/v1/config`和`/api/v1/user/profile`通过插件`GlobalMiddlewares`精确匹配旧路径，先放行宿主执行认证、租户、权限和真实系统管理 handler，再将成功 JSON 响应后置转换为旧 GoAdmin envelope，并在进入宿主前补充旧参数别名如`pageIndex/pageSize`、`roleName/roleKey`、`menuName/title`、`dictName/dictType/dictLabel`和`configName/configKey`。未改`apps/lina-core`核心框架，job 路由仍按用户补充不作为本轮目标。
- FB-30 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 legacy 路由装配、旧响应 helper、路由/拦截器单测和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主 API DTO、宿主 service、SQL、DAO/DO/Entity、前端或开发工具脚本。数据权限：`dept/post`新注册路由位于插件受保护 group，仍执行宿主认证、租户和权限中间件；宿主冲突路由采用后置转换，不绕过宿主认证、租户、权限或真实 handler，401/403/非成功响应不改写为旧成功。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，旧中文`msg`是旧接口契约恢复，不新增语言包或 apidoc 翻译资源。SQL：无 SQL 变更，表前缀和旧 schema 契约保持 FB-29 结果。DI：无新增接口型运行期依赖，复用宿主已发布`GlobalMiddlewares`注册能力。开发工具跨平台：无脚本或工具入口变更。接口性能：响应转换只处理单次宿主返回 buffer，不新增数据库查询或 N+1 路径。
- FB-30 验证记录：已运行`GOWORK=off go test ./backend -count=1`通过；已运行`GOWORK=off go test ./backend -run 'TestLegacyRouteSpecCoversOldAdminRouterInventory|TestLegacyInterceptedOldRoutesAreExactOldPaths|TestLegacyRouteInterceptor' -count=1`通过，覆盖旧 inventory 不再依赖“宿主覆盖”、中间件旧分页响应转换、旧 JSON body 字段别名、未授权响应不改写、非 legacy 路由放行和插件禁用放行；已运行插件全量`GOWORK=off go test ./... -count=1`通过。
- FB-29 根因记录：用户进一步确认“表前缀可以有”，因此本轮数据库一致性判断口径收敛为：`linapro-uidentity-cas`插件允许保留`plugin_linapro_uidentity_cas_`表名前缀，但去掉前缀后的表后缀、旧统一身份业务表字段集合、全局级数据语义必须与旧`/Users/wanna/mine/ecoding/uidentity/admin`的 MySQL DDL、GORM 模型和 DTO 搜索字段一致。前序 FB-25/FB-26/FB-27 已修正 SQL/DAO/Entity，但缺少一条覆盖全部业务表的自动化契约测试，后续容易再次退回 LinaPro 自定义表名或自定义字段。
- FB-29 已完成：新增`TestLegacySchemaTableSuffixesAndColumnSets`，逐表锁定`account`、`account_details`、`groups`、`units`、`containers`、`applications`、`account_group`、`account_unit`、`account_app_role`、`account_app_blacklist`、`group_app_blacklist`、`pass_ruler`、`sms`、`cas_login_log`、`oauth_log`、`oauth2_token`、`account_change_log`和`account_active_log`的插件前缀、旧表后缀与生成 DAO 列集合。测试通过后确认当前 schema 已满足“前缀可保留、主体结构按旧项目”的最新要求，本轮不需要修改 SQL、DAO/DO/Entity 或业务代码。
- FB-29 影响分析：本轮只修改插件 schema 契约测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、插件安装 SQL、卸载 SQL、DAO/DO/Entity、运行时路由、请求/响应 DTO、前端、定时任务或开发工具脚本。数据权限：无新增运行时数据读写入口，旧统一身份表仍按用户要求为全局级；后台接口继续经宿主认证/权限中间件，公开 CAS/OAuth/激活/token 入口继续通过一次性 ticket/state/token/验证码或客户端凭证自隔离。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，本轮无运行时文案、API 文档源文本、插件清单或语言包变化。SQL：无 SQL 变更；现有 SQL 仍保持 PostgreSQL 幂等脚本、Seed DML 与 Mock 数据分离、无显式写入自增主键。开发工具跨平台：无脚本或工具入口变更。DI：无新增运行期依赖、构造函数或共享实例变化。插件本地规范：插件根目录无`AGENTS.md`。
- FB-29 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySchemaTableSuffixesAndColumnSets|TestLegacySchemaColumnNamesAndGeneratedTypes|TestOauth2TokenLegacySchemaContract' -count=1`通过，覆盖表名前缀/后缀、旧字段集合、`account_unit.unit_id`、`oauth2_token`六字段模型和关键生成类型。
- FB-28 根因记录：用户此前明确指出`linapro-uidentity-cas/backend/api/uidentity/v1`目录里很多`path`不对，前序 FB-24 已把运行时接口和泛化资源入口改到旧`/api/v1/*`地址，但继续审计发现管理资源 DTO 仍只有`ResourceList/Create/Get/Update/DeleteReq`五个泛化结构，且公开字段包含旧前端不会传的`resource/body`包装；虽然插件运行时已通过`plugin_legacy_routes.go`逐项注册旧`/api/v1/account-details`、`/api/v1/applications`等 legacy handler，但 API 目录本身仍不能证明每个旧管理资源路径和账号参数契约完全一致。
- FB-28 已完成：`linapro-uidentity-cas/backend/api/uidentity/v1`中的`ResourceList/Create/Get/Update/DeleteReq`不再表示 LinaPro 泛化资源包装，而是恢复旧`/api/v1/account`账号列表、详情、创建、更新和删除 DTO，移除旧前端不会传的`resource/body`字段，并恢复旧`number/name/phone/groupIds/passLevels/containerId/unitId/status/*Order`、创建时`groupId`承载`UnitId`、更新时`unitId`和删除`ids`数组等参数。新增`legacy_resource_contracts.go`显式声明旧管理资源`account-details`、`account-unit`、`account-app-role`、`account-app-blacklist`、`account-change-log`、`units`、`groups`、`containers`、`applications`、`group-app-blacklist`、`pass-ruler`、后台`sms`、`cas-login-log`、`oauth-log`和`oauth-token`的旧五件套 path/method 与旧 DTO 字段名；运行时旧路由仍由`plugin_legacy_routes.go`逐项注册到`LegacyController`，未重新暴露 LinaPro 新命名空间 controller。保留用户确认允许的插件表前缀，本轮不改 SQL 表结构。
- FB-28 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 API DTO、未作为运行时注册入口的 v1 controller 编译路径、legacy API 契约测试和 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、插件 legacy 路由装配、SQL、DAO/DO/Entity、前端、定时任务或开发工具脚本。数据权限：运行时后台旧资源路由仍经宿主认证/权限中间件和插件 service 可见性校验；新增 DTO 类型本身不新增数据读写入口。接口性能：账号列表编译路径将旧`number/name/phone`作为字段过滤下推数据库，排序按旧`*Order`固定顺序选择，不新增 N+1。缓存一致性：无缓存、快照或失效逻辑影响。i18n：插件未启用`i18n.enabled`，本轮 API 元数据只用于旧契约描述，不新增语言包或 apidoc 翻译资源。数据库：表前缀按用户确认保留，本轮无 SQL/DAO 变化。插件本地规范：插件目录无`AGENTS.md`。
- FB-28 验证记录：已运行`GOWORK=off go test ./backend -run 'TestLegacyAPIDTOPathsMatchOldAdminRoutes|TestLegacyAPIDTOFieldsMatchOldAdminParams' -count=1`，新增管理资源 DTO 全部命中旧 router inventory 且不包含`/uidentity`命名空间；已运行插件全量`GOWORK=off go test ./... -count=1`通过，覆盖 API 包、controller、jobs 和 uidentity service 编译/测试。后续完成记录后补充运行`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。
- FB-27 根因记录：用户确认表前缀可以保留后，本轮继续按旧`/Users/wanna/mine/ecoding/uidentity/admin/add_table.sql`和旧 GORM 模型/DTO 复核字段结构，发现 FB-25 只完成了表后缀和字段集合的大体对齐，仍有插件侧自定义 schema 残留：`account/account_details/groups/units/containers/applications`等表的多个`varchar(128)`被压缩成`varchar(32/16/64)`或错误默认非空；`account_details.nj/xz/yjbysj`旧 DTO 和模型均为数值语义但插件仍为字符串列；`account_change_log.err_number`旧模型为`longtext/string`且旧账号操作失败时写入工号，但插件仍为`SMALLINT`；`account_unit`旧真实 DDL、DTO 搜索列和旧前端参数均为`unit_id`且无审计/软删除列，但插件生成模型仍使用`units_id`并保留审计列。上述问题会导致旧前端参数、旧日志内容、作业同步字段和数据库结构语义不能无改动迁移。
- FB-27 已完成：`linapro-uidentity-cas`插件安装 SQL 在保留`plugin_linapro_uidentity_cas_`表前缀的前提下恢复旧字段结构语义，补充可重复执行的`ALTER TABLE`升级逻辑，修正字符串长度、可空/默认、数值列、`account_unit.unit_id`和`account_change_log.err_number TEXT`；`oauth2_token`延续旧 6 字段模型并将`code/access/refresh`恢复为文本语义。重新执行`make dao p=linapro-uidentity-cas`生成 DAO/DO/Entity，服务层同步调整账号详情导入、Oracle job 同步、本地`ChangeContainer`毕业年份查询、`account_unit`资源映射和激活人脸核验写入语义；旧 API 输出边界继续显式投影为旧接口需要的字段名和数值类型。新增`TestLegacySchemaColumnNamesAndGeneratedTypes`锁定`account_details`数值字段、`account_unit.unit_id`和`account_change_log.err_number`生成契约，防止再次回退。
- FB-27 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 SQL、插件生成 DAO/DO/Entity、插件 service/jobs 内部写入和投影逻辑、插件 schema 契约测试以及 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、前端、脚本或开发工具入口。数据权限：本轮仍沿用用户指定的全局级统一身份表，不新增数据可见性绕过；后台资源路由仍经宿主认证/权限中间件，公开 CAS/OAuth/激活/token 入口仍以一次性 ticket/state/uuid/token 或客户端凭证自隔离。缓存一致性：无缓存、快照或失效逻辑新增，运行时 token 仍以数据库表和`expired_at`为权威源。接口性能：字段类型修正不新增列表装配查询；账号、详情、单位、分组、黑名单和授权关系仍采用批量查询/集合化投影，`account_unit`补唯一索引与旧 DDL 一致。i18n：插件未启用`i18n.enabled`，本轮无运行时用户可见文案或插件语言包变更。开发工具跨平台：未修改脚本或工具入口，只复用既有`make dao p=linapro-uidentity-cas`生成入口。DI：无新增运行期依赖、构造函数或共享实例变化。
- FB-27 验证记录：已运行`PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -d linapro -v ON_ERROR_STOP=1 -f manifest/sql/001-implement-uidentity-cas-backend.sql`两次，验证 SQL 可重复执行并完成已安装环境升级；已运行`make dao p=linapro-uidentity-cas`重新生成 DAO/DO/Entity；已用`information_schema.columns`核验`account/account_details/account_unit/account_change_log/oauth2_token`关键字段类型和长度，其中`account.number/name/phone`为`varchar(128)`、`account_details.nj/xz/yjbysj/face`为`bigint`、`account_unit`仅有`id/account_id/unit_id`、`account_change_log.err_number`为`text`、`oauth2_token`为旧 6 字段；已运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestLegacySchemaColumnNamesAndGeneratedTypes|TestOauth2TokenLegacySchemaContract|TestLegacy' -count=1`、`GOWORK=off go test ./backend/internal/service/jobs -count=1`、`GOWORK=off go test ./backend ./backend/api/uidentity/v1 ./backend/internal/service/uidentity -count=1`和插件`GOWORK=off go test ./... -count=1`，均通过。静态检索确认不存在`err_number SMALLINT`、错误`nj/xz/yjbysj VARCHAR`、`account_unit`生成模型中的`UnitsId`或审计列残留。
- FB-26 根因记录：FB-25 已把插件 schema 改为旧表后缀和全局级语义，但复核旧`app/admin/models/oauthToken.go`发现`OauthToken`只嵌入`models.Model`，字段为`id`、`expired_at`、`code`、`access`、`refresh`和`data`；插件 PostgreSQL SQL/DAO 仍保留`created_at`、`updated_at`、`deleted_at`、`create_by`和`update_by`，并且 runtime token 写入路径仍尝试写审计字段。这会导致旧`/api/v1/oauth-token`资源字段结构、GoFrame 软删除语义和旧 MySQL 模型不一致。
- FB-26 已完成：`linapro-uidentity-cas`插件安装 SQL 将`plugin_linapro_uidentity_cas_oauth2_token`收敛为旧模型 6 字段，并追加`ALTER TABLE ... DROP COLUMN IF EXISTS`清理已安装环境中的错误审计/软删除列；重新执行插件 DAO 生成，删除`Oauth2Token`生成模型中的审计列；同步移除 token 创建、更新和通用资源映射中对`create_by/update_by/created_at/updated_at/deleted_at`的依赖。顺带修正密码接口 API 元数据中“password hash”的旧描述为 LDAP 密码同步语义，避免接口文档继续暗示账号表存在本地 hash 字段。
- FB-26 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 SQL、生成 DAO/DO/Entity、插件 service 中`oauth2_token`写入和资源映射逻辑、API 文档元数据以及 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、前端、脚本或开发工具入口。数据权限：`oauth2_token`是旧运行时 ticket/state/token 存储，仍按一次性`code/access/state`、`expired_at`和绑定 payload 自隔离；后台资源路由仍经宿主认证/权限中间件，全局级 schema 例外延续用户要求。缓存一致性：无缓存或快照新增，token 仍以数据库表和`expired_at`为权威源。接口性能：token 查询仍使用`code/access/refresh/expired_at`索引，不新增 N+1 路径。i18n：插件未启用`i18n.enabled`，本轮只改插件 API 英文源描述，不要求补插件 apidoc 翻译资源。开发工具跨平台：未修改脚本，只复用既有`make dao p=linapro-uidentity-cas`生成入口。
- FB-26 验证记录：已运行`PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -d linapro -v ON_ERROR_STOP=1 -f manifest/sql/001-implement-uidentity-cas-backend.sql`、`make dao p=linapro-uidentity-cas`、旧模型字段与当前 SQL 18 表静态对比（通过，`oauth2_token`为`id/expired_at/code/access/refresh/data`六列）、`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestOauth2TokenLegacySchemaContract|TestLegacy' -count=1`、`GOWORK=off go test ./backend ./backend/api/uidentity/v1 ./backend/internal/service/uidentity ./backend/internal/service/jobs -count=1`、插件`GOWORK=off go test ./... -count=1`、`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`，均通过。
- FB-25 根因记录：前序实现虽然已对齐旧路由和旧参数，但插件 SQL/DAO 仍沿用过 LinaPro 插件侧自定义表结构：部分表名为单数或新命名，账号表含`tenant_id`、`password_hash`和`password_updated_at`等旧`uidentity/admin`全局表不存在的字段，密码登录/重置也按本地 hash 存储实现；这会导致旧项目前端即使切换到旧路由，数据库结构和旧后端业务副作用仍不一致。
- FB-25 已完成：`linapro-uidentity-cas`插件安装/卸载 SQL 改为`plugin_linapro_uidentity_cas_`前缀加旧表后缀，覆盖`account`、`account_details`、`groups`、`units`、`containers`、`applications`、`account_group`、`account_unit`、`account_app_role`、`account_app_blacklist`、`group_app_blacklist`、`pass_ruler`、`sms`、`cas_login_log`、`oauth_log`、`oauth2_token`、`account_change_log`和`account_active_log`；字段按旧 MySQL 模型转换为 PostgreSQL 类型，不保留插件业务表`tenant_id`，不再在账号表存储本地密码 hash。同步更新`backend/hack/config.yaml`并通过`make dao p=linapro-uidentity-cas`重新生成 DAO/DO/Entity，服务和 jobs 写表逻辑改为全局旧字段，密码校验/设置恢复旧项目的常用密码与 LDAP`userPassword`边界。旧`app/jobs`调度定义和日志按用户前序要求使用 GF/宿主任务边界，job 路由和插件自建 job 表不纳入本轮插件业务 schema。
- FB-25 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件 SQL、DAO/DO/Entity 生成结果、插件配置、插件 service/jobs 业务逻辑、`plugin.yaml`安装范围和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、前端、开发工具脚本或宿主任务服务。数据权限：插件自有旧统一身份表显式改为全局级，属于用户指定例外；后台旧路由仍经宿主认证/权限中间件，公开 CAS/OAuth/激活/扫码/token 路径继续按旧一次性状态、验证码、ticket 或客户端凭证隔离。缓存一致性：无新增缓存或快照状态，短期 ticket/state 仍以`oauth2_token`表为权威源并按`expired_at`失效。接口性能：保留账号、详情、单位、分组、黑名单和授权关系的集合化/批量装配查询，不新增 N+1 路径。i18n：插件未启用`i18n.enabled`，本轮不新增语言包或 apidoc 翻译资源。开发工具跨平台：未修改脚本或工具入口；DAO 生成继续使用既有 GoFrame 入口。
- FB-25 验证记录：已运行`make dao p=linapro-uidentity-cas`、`GOWORK=off go test ./backend/internal/service/uidentity ./backend/internal/service/jobs -count=1`、`GOWORK=off go test ./backend/internal/service/... -run '^$' -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -d linapro -v ON_ERROR_STOP=1 -f manifest/sql/001-implement-uidentity-cas-backend.sql`、`rg -n "TenantId|tenant_id|password_hash|password_updated_at" manifest/sql backend/internal/dao/internal backend/internal/model/do backend/internal/model/entity`（无命中）、`rg -n "plugin_linapro_uidentity_cas_account_detail\\b|plugin_linapro_uidentity_cas_application\\b|plugin_linapro_uidentity_cas_group\\b|plugin_linapro_uidentity_cas_unit\\b|plugin_linapro_uidentity_cas_pass_rule\\b|plugin_linapro_uidentity_cas_oauth_token\\b" manifest/sql backend/internal/dao/internal backend/internal/model/do backend/internal/model/entity`（无命中）；`information_schema`确认当前 manifest 所属 18 张业务表不存在`tenant_id/password_hash/password_updated_at`字段，且`account_active_log.type`为`bigint`。
- FB-24 根因记录：前序多轮已把插件 legacy runtime 路由逐步对齐旧`/Users/wanna/mine/ecoding/uidentity/admin`，但`backend/api/uidentity/v1`仍保留 LinaPro 新 RESTful API 定义作为 GoFrame DTO 契约，`g.Meta path`大量位于`/uidentity/...`，且部分`json`标签仍是`clientId/challengeId/pageNum/newPassword`等新参数名；同时插件启动时还注册了新命名空间 controller，使生成文档、接口审计和部分运行时入口仍暴露旧前端无法直接调用的新路径。
- FB-24 已完成：在`linapro-uidentity-cas`插件内将`backend/api/uidentity/v1`公开 DTO 的`g.Meta path`改为旧项目真实路径，例如`/api/v1/wechat/loginCallback`、`/api/v1/cas/login`、`/api/v1/cas/loginByPhone`、`/api/v1/config/cas`、`/api/v1/token/get`、根`/sso/serviceValidate`和根`/ssologin/getToken`；同步把旧前端关键入参/出参字段恢复为`appid`、`uuid`、`unionID`、`union_id`、`bind_type`、`new_password`、`pageIndex`、`grant_type`、`client_secret`、`AccessToken`、`full_path`等旧命名。插件运行时取消注册 LinaPro 新命名空间 v1 controller，只保留 legacy 旧路由兼容入口；`/ssologin/getToken`的 DTO/controller 也恢复为旧`appid/secret/number/password`换取 token 语义。
- FB-24 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端 API DTO、少量对应 controller 投影、插件路由装配和 API 契约测试，以及 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由、SQL、DAO/DO/Entity、前端、定时任务或开发工具脚本。数据权限：公开旧 CAS/OAuth/token/激活/扫码入口仍通过一次性 ticket、state、uuid、token 或客户端凭证自隔离，后台资源类旧路径继续进入宿主认证、租户和权限中间件；本轮不新增数据读取、写入或绕过路径。缓存一致性：无缓存、快照或失效逻辑新增。接口性能：只调整路由/DTO 字段名和常量时间投影，不新增列表装配查询或 N+1 风险。i18n：插件未启用`i18n.enabled`，本轮 API 元数据和旧字段名属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。插件本地规范：插件目录无`AGENTS.md`。
- FB-24 验证记录：新增`TestLegacyAPIDTOPathsMatchOldAdminRoutes`逐个读取`backend/api/uidentity/v1`公开请求 DTO 的`g.Meta path/method`，断言不能包含`/uidentity`且必须命中旧 router inventory；新增`TestLegacyAPIDTOFieldsMatchOldAdminParams`锁定旧前端关键 JSON 参数名，防止`appid/uuid/pageIndex`等再次回退成 LinaPro 新命名。已运行`GOWORK=off go test ./backend/api/uidentity/v1 -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`openspec validate implement-uidentity-cas-backend --strict`和插件`git diff --check`。
- FB-23 根因记录：前序兼容层仍以统一身份插件能力和已知旧前端入口为主做迁移，未把旧`/Users/wanna/mine/ecoding/uidentity/admin/app/{admin,cas,oauth,other,jobs}/router`下每一个 Gin router 挂载项作为可测试事实来源，导致旧默认系统管理、根静态资源、Swagger、WebSocket、代码生成、验证码、登录刷新登出以及部分 job/sysjob/job-log 路由没有在插件 legacy route inventory 中逐项覆盖。旧 Swagger 注释与真实 router 源码存在个别路径差异，本轮以源码 router 为准。
- FB-23 已完成：在`linapro-uidentity-cas`插件内补齐旧根路由`/`、`/info`、`/swagger/admin/*any`、`/ws/{id}/{channel}`、`/wslogout/{id}/{channel}`、`/static/*filepath`、`/form-generator/*filepath`、`/logs/*filepath`及对应 Gin 自动`HEAD`静态方法；补齐旧`/api/v1/login`、`/api/v1/refresh_token`、`/api/v1/captcha`、`/api/v1/logout`、sys-user、profile、login-log、opera-log、sys-api、role/menu/dept/post/dict/config 辅助动作、代码生成/数据库表浏览、sysjob/job/job-log 等旧方法和路径。对 LinaPro 宿主已按旧路径覆盖的通用系统资源单独记录`legacyHostCoveredOldRoutes`，插件只补宿主缺口，避免改动`apps/lina-core`核心框架；旧`POST /api/v1/login`和`GET /api/v1/captcha`补为旧字段和旧验证码壳，`/api/v1/refresh_token`补为旧路径刷新入口。
- FB-23 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端 route inventory、legacy controller 和对应测试，以及 OpenSpec 任务记录；未修改`apps/lina-core`核心框架、宿主路由装配、SQL、DAO/DO/Entity、前端、脚本或开发工具入口。数据权限：新增受保护旧路径仍挂在宿主`Auth/Tenancy/Permission`中间件之后，公开登录、验证码、CAS/OAuth/token/SSO 路径继续通过旧参数、一次性验证码、ticket、state 或 token 自隔离；宿主已覆盖的旧系统资源只作为清单声明，不绕过宿主数据权限。缓存一致性：验证码复用旧`base64Captcha.DefaultMemStore`一次性校验语义，无 LinaPro 缓存或快照新增。接口性能：本轮主要补路由和常量时间投影，不新增列表装配查询或 N+1 路径。i18n：插件未启用`i18n.enabled`，新增旧中文错误消息和旧响应壳属于旧接口契约恢复，不新增语言包或 apidoc 翻译资源。插件本地规范：插件目录无`AGENTS.md`。
- FB-23 验证记录：新增`TestLegacyRouteSpecCoversOldAdminRouterInventory`将旧 router 源码清单按方法和路径逐项纳入测试，`allLegacyRouteSpecs()+legacyHostCoveredOldRoutes`必须覆盖旧`admin/cas/oauth/other/jobs`全部 HTTP 挂载项；新增测试继续断言旧项目不存在的`/api/v1/sso/*`、`/api/v1/ssologin/getToken`、`/api/v1/user/changeWechatCallBack`和`POST /api/v1/ldap/sync`不会重新暴露。新增 legacy HTTP 单测覆盖`GET /api/v1/captcha`旧响应壳和验证码 store、`POST /api/v1/login`旧字段`UserName/Password/appid/UUID/Code`及顶层`token/expire`。已运行`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`GOWORK=off go test ./pkg/plugin/pluginhost -count=1`、`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。宿主`GOWORK=off go test ./internal/cmd -count=1`仍失败于既有 production panic allowlist 中插件`backend/plugin.go:init`未列入，`GOWORK=off go test ./internal/cmd/internal/httpstartup -count=1`仍失败于测试环境 cache coordination freshness，而非本轮 legacy route inventory 覆盖断言。
- FB-22 根因记录：用户明确要求迁移后的兼容接口以旧`/Users/wanna/mine/ecoding/uidentity/admin`前端为事实来源，不能按 LinaPro 新 RESTful 路由和参数规范暴露旧前端默认调用面。本轮复核确认插件已注册旧`/api/v1/wechat/loginCallback`，但 handler 仍沿用旧源码注释空壳语义返回空`200`，没有承接插件已实现的扫码登录回调业务；同时`/api/v1/config/cas`、`/api/v1/config/oauth`和`/api/v1/config/token`在缺少部署覆盖配置时会返回`/api/v1/uidentity/...`默认地址，旧前端或旧集成方读取配置后会被带到新路由，造成用户指出的“接口地址和参数完全不一样”。旧项目真实微信扫码登录部署配置`oauthRedirect`指向`/api/v1/cas/loginByQr`，但`/api/v1/wechat/loginCallback`也是旧 router 和 Swagger 中存在的旧地址；为满足旧前端 base URL 零改动切换，本轮将该旧地址作为同一扫码回调业务入口处理。用户已明确 job 路由不用管，本轮不新增或调整 job 路由。
- FB-22 已完成：在`linapro-uidentity-cas`插件内将`GET /api/v1/wechat/login`委托到旧`POST /api/v1/cas/getLoginQr`同一 QR 创建逻辑，继续接收旧`appid/cascallback/callback/casCallback`参数；将`GET /api/v1/wechat/loginCallback`委托到旧`GET /api/v1/cas/loginByQr`同一扫码回调逻辑，继续接收旧`state/uuid/appid/client_id/code/unionID/unionId/union_id/cascallback/callback/casCallback`参数并保持旧 redirect query。同步把 legacy 静态配置默认地址从`/api/v1/uidentity/...`改为旧路由：CAS 使用`/api/v1/cas/login`、`/api/v1/cas/tickets/{ticket}`、`/sso/serviceValidate`，OAuth 使用`/api/v1/oauth/auth`、`/api/v1/oauth/token`、`/api/v1/oauth/test`、`/api/v1/health`，Token 使用`/api/v1/token/get`和`/api/v1/token/getUserInfoByToken`；部署仍可通过插件作用域配置覆盖为完整域名。
- FB-22 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端 legacy controller、legacy 配置投影、插件 API 示例标签、对应单元测试和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、宿主路由、SQL、DAO/DO/Entity、前端、定时任务或开发工具脚本。数据权限：旧微信登录/回调仍通过一次性 state、clientId 和 unionId 自隔离，账号与 token 读写继续由插件 service 的租户过滤边界处理；配置接口只返回插件作用域静态元数据，不读取或暴露业务列表数据。缓存一致性：未新增缓存、快照或失效路径；扫码状态仍以数据库 token 表为权威源并按既有过期时间失效。接口性能：只调整常量时间的路由委托和配置默认字符串，不新增数据库查询、列表装配或 N+1 风险。i18n：插件`plugin.yaml`未配置`i18n.enabled: true`，本轮仅调整旧接口地址示例和旧契约注释，不维护插件 apidoc 翻译资源。DI：无新增运行期依赖、构造函数或共享实例变化。SQL/开发工具跨平台：无 SQL、DAO 生成、脚本或工具入口影响。
- FB-22 验证记录：已按旧`app/cas/router/wechat.go`、`app/cas/router/api.go`、`app/oauth/router/api.go`、`app/admin/router/config.go`、旧`config/settings*.yml`和 Swagger 复核`/api/v1/wechat/loginCallback`、`/api/v1/cas/loginByQr`及静态配置地址边界。新增`TestLegacyWechatRoutesDelegateToQRCodeRuntime`覆盖旧`/api/v1/wechat/login`和`/api/v1/wechat/loginCallback`对旧参数的传递和扫码回调委托；新增`TestLegacyStaticConfigDefaultsUseOldAdminRoutes`并扩展`TestLegacyCASConfigUsesPluginScopedDefaults`，断言 legacy 默认配置不再包含`/uidentity`命名空间。已运行`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。
- FB-21 根因记录：旧`/Users/wanna/mine/ecoding/uidentity/admin/app/other/apis/sms.go`的`SmsReq`包含`type`、`phone`、`code`、`uuid`，handler 在短信类型分支前先执行`!tools.IsCommonPass(req.Code) && !captcha.Verify(req.UUID, req.Code, true)`校验；旧`config/settings.local.yml`和`settings.prod.yml`均配置`commonPass: true`，`tools.IsCommonPass`按当天北京时间生成`%02d%s`格式的万能验证码。插件当前`LegacyController.SmsSend`只读取`type/phone`，`SendSMSCode`也未校验`code/uuid`，因此旧调用方即使传入错误图形验证码也会直接发短信。用户已明确 job 路由可以不用管，本轮继续排除`app/jobs`和`/api/v1/job*`。
- FB-21 已完成：在`linapro-uidentity-cas`插件内扩展`SMSSendInput`、legacy controller 和新 RESTful controller，确保`POST /api/v1/sms/send`与`POST /api/v1/uidentity/sms-codes`都会传递旧`code/uuid`字段；`SendSMSCode`先执行旧短信图形验证码语义，再校验短信类型、手机号、频控并写入插件 SMS 表。旧`commonPass`算法按`int(now.Weekday())+2`与`DDMM`拼接，默认启用并可通过插件配置`legacy.app.commonPass=false`关闭；非万能验证码时使用`github.com/mojocn/base64Captcha v1.3.5`的`DefaultMemStore.Verify(uuid, code, true)`保持旧`captcha.Verify`的一次性消费行为。旧短信错误文案恢复为`验证码错误`、`参数错误`和`发送频繁`。
- FB-21 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端、插件及聚合模块`go.mod/go.sum`和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、宿主路由、SQL、DAO/DO/Entity、前端、定时任务或开发工具脚本。数据权限：短信发送仍写入插件自有租户 SMS 表，验证码校验发生在写入前，不新增读取业务数据或跨租户路径。缓存一致性：未新增插件自有缓存；验证码 store 复用`base64Captcha.DefaultMemStore`的一次性校验语义，权威数据为外部验证码生成方写入的 captcha store，`clear=true`消费后失效，未引入集群状态变更。接口性能：只新增常量时间参数校验，不增加列表、批量或 N+1 查询风险。i18n：插件`plugin.yaml`未配置`i18n.enabled: true`，API DTO 文案和旧中文错误消息属于插件单语言/旧契约恢复，不维护插件 apidoc 翻译资源。DI：新增的是插件本地库依赖`base64Captcha`，没有新增接口型运行期依赖、构造函数参数或共享服务实例。开发工具跨平台：未修改脚本或工具入口；新增依赖为 Go module，随 Go 工具链跨平台构建。SQL：无数据库结构或迁移变更。
- FB-21 验证记录：已按旧`app/other/apis/sms.go`、`tools/pass.go`、`config/settings.local.yml`和`settings.prod.yml`确认旧`type/phone/code/uuid`入参、验证码校验顺序、`commonPass`默认开启和旧文案；静态复核插件`LegacyController.SmsSend`、`ControllerV1.SmsSend`和`SendSMSCode`均传递并校验`code/uuid`。新增`uidentity_sms_runtime_test.go`覆盖旧万能验证码公式、默认启用、配置关闭和`base64Captcha`一次性消费；新增 legacy controller 单测覆盖旧短信错误文案。已运行`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和`openspec validate implement-uidentity-cas-backend --strict`。
- FB-20 根因记录：继续按旧`admin/app/{admin,cas,oauth,other}/router`挂载入口审计时确认，旧 CAS 模块将`/sso/*`和`/ssologin/getToken`注册在 Gin 根分组而非`/api/v1`；旧用户微信换绑 API 只注册`POST /api/v1/user/changeWechatQr`和`POST /api/v1/user/changeWechatState`，不存在`POST /api/v1/user/changeWechatCallBack`；旧 admin/other 路由也不存在`POST /api/v1/ldap/sync`，只有已注释的`/applications/ldap`和真实`GET /api/v1/config/ldap`。插件 legacy 层此前为兼容新迁移能力额外暴露这些旧项目不存在的`/api/v1`入口，导致路由集合不是严格一致。用户已明确 job 路由可以不用管，本轮仍不处理`/api/v1/sysjob`和`/api/v1/job/start*`、`/api/v1/job/remove*`。
- FB-20 已完成：在`linapro-uidentity-cas`插件 legacy 路由声明中移除`/api/v1/sso/*`、`/api/v1/ssologin/getToken`、`/api/v1/user/changeWechatCallBack`和`/api/v1/ldap/sync`注册，仅保留旧项目真实存在的根`/sso/*`、根`/ssologin/getToken`、`/api/v1/user/changeWechatQr`和`/api/v1/user/changeWechatState`。新 RESTful 命名空间下的`/uidentity/users/wechat-rebind-callbacks`和`/uidentity/legacy/external-actions`继续保留，不作为旧路由兼容库存。
- FB-20 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas/backend/plugin_legacy_routes.go`、路由库存单测、legacy controller 注释和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、插件 service 业务逻辑、SQL、DAO/DO/Entity、前端或开发工具。数据权限：删除旧项目不存在的 legacy 入口，不新增数据读写或绕过路径；保留的新命名空间入口仍走既有中间件和插件 service 边界。接口性能：仅调整路由注册清单和测试，不新增查询或装配路径。缓存一致性：无缓存、快照或失效影响。i18n：无运行时文案、API 文档源文本、插件清单或语言包资源变更；插件未启用`i18n.enabled`。DI：无新增运行期依赖、构造函数或共享实例变化。SQL/开发工具跨平台：无 SQL、DAO 生成、脚本或工具入口影响。
- FB-20 验证记录：已用旧 router 源码确认`/sso`与`/ssologin`根挂载、微信换绑旧路由和 LDAP 路由边界；新增`TestLegacyRouteSpecExcludesNonOldAPIv1Contracts`反向断言防止旧项目不存在的`/api/v1`入口回归。已运行`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）和`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）。完成记录后补充运行`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。
- FB-19 根因记录：FB-18 已按旧 router/DTO 补齐非 job 路由和旧入参别名，但 legacy HTTP controller 仍复用统一`legacyOK`默认`操作成功`响应，并在若干 map 投影中保留迁移后新增的别名或新字段；旧 GoAdmin handler 的成功`msg`和 DTO JSON 字段集合本身也是接口契约的一部分，因此 CAS 登录、扫码、激活、用户自助、账号导入/改密、资源 CRUD、配置、上传、短信、统计和服务器监控等非 job 接口仍会出现出参差异。用户已明确 job 路由可以不用管，本轮不继续追旧`app/jobs`、`/api/v1/sysjob`和`/api/v1/job/start*`、`/api/v1/job/remove*`。
- FB-19 已完成：在`linapro-uidentity-cas`插件 legacy HTTP controller 内恢复旧成功响应文案和数据形态：资源列表/详情/创建/更新/删除分别使用旧`查询成功/创建成功/修改成功/删除成功`和旧 ID 载荷；账号解锁、导入校验、导入、管理员改密、自助改密三段恢复旧`userNumberList/uuid/status/success/failedNumber`字段与旧`验证成功/导入成功/修改成功`文案；CAS 密码、短信、UnionID、扫码登录、runtime token、logout 和扫码结果恢复旧`登录成功/获取成功/ok/退出成功`文案，`CasLoginResp`投影显式保留旧固定字段`callbackUrl/tgt/st/user/users/uuid`且不添加非旧项目字段；激活和微信换绑只输出旧 DTO 字段；OAuth JSON/test、短信发送、上传、配置、统计、日志快照和服务器监控恢复旧响应壳或旧裸 JSON 形态。补充单测覆盖 CAS 登录固定字段、扫码未完成零值字段、激活旧字段集合和上传`full_path`旧字段。
- FB-19 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas/backend/internal/controller/uidentity/uidentity_legacy_http.go`、对应 controller 单测和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、插件 service 业务实现、SQL、DAO/DO/Entity、前端、开发工具或构建脚本。数据权限：只改变已授权 legacy 响应投影和成功文案，不新增数据读取、写入或跨租户路径；原有后台资源、统计和日志仍走插件 service 的 tenant filter 和宿主中间件。接口性能：只做返回 map 投影，不新增数据库查询、循环装配或远程调用，不引入 N+1。缓存一致性：无新增缓存、快照或失效路径。i18n：插件`plugin.yaml`未配置`i18n.enabled: true`，旧中文`msg`为历史接口契约恢复，不新增语言包或 apidoc 翻译资源。DI：无新增运行期依赖或构造函数变更。
- FB-19 验证记录：已按旧`/Users/wanna/mine/ecoding/uidentity/admin/app/{admin,cas,oauth,other}` handler 和模型反向扫描成功响应，排除用户确认不处理的 job 路由；已运行`GOWORK=off go test ./backend/internal/controller/uidentity -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）和`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）。完成记录后补充运行`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。
- FB-18 根因记录：前序兼容层主要依据迁移能力清单和已知旧接口手动补路由，未把旧`admin/app/{admin,cas,oauth,other}/router`和对应 DTO 的`form/json/uri`参数作为权威清单进行机器可复查对照；因此仍可能出现旧路径方法、根路径挂载、大小写/下划线参数名、`appid/client_id`等旧字段别名未完全覆盖。用户已明确`job`路由可以不用管，本轮审计和修复排除`app/jobs`、`/api/v1/sysjob`、`/api/v1/job/start*`和`/api/v1/job/remove*`。
- FB-18 已完成：按旧`admin/app/admin/router`、`app/cas/router`、`app/oauth/router`和`app/other/router`复核，补齐旧`GET /api/v1/metrics`并使用`promhttp.Handler()`保持 Prometheus scrape 行为；旧 uidentity 资源列表新增 DTO 字段过滤透传和旧`*Order`排序参数解析，覆盖`number/name/phone`、应用、黑名单、CAS/OAuth 日志、OAuth token、短信和密码规则等旧`form`筛选语义。账号详情资源兼容旧`nj/xymc/xydm/xq/xz/yjbysj/zymc/bjmc`入参、过滤、排序和出参字段，并映射到插件新字段`grade/college/collegeCode/campus/schoolSystem/graduatedAt/major/className`；`pass-ruler`兼容旧`interval`入参、排序和出参字段；旧`account/unlockPassword`兼容`userNumberList`；旧`account-details`删除兼容`account_ids/accountIds`。旧 job 路由按用户要求不再作为本轮修复范围。
- FB-18 边界说明：旧 GoAdmin 默认系统管理路由（如`sys-user`、`role`、`menu`、`dept`、`dict`、`config`、`post`、`sys-api`、登录刷新登出、Swagger、WebSocket、验证码和代码生成）仍按既有设计归属 LinaPro 宿主通用能力或开发工具链，不复制到`linapro-uidentity-cas`插件。当前 core 没有向源码插件公开可业务等价代理这些系统管理服务的 host service，插件直接 import core internal service/DAO 会违反 Go `internal`边界和核心宿主边界；因此本轮只补齐统一身份/CAS/OAuth/other 运维类旧后端接口的路由、参数和业务语义。
- FB-18 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端、插件及聚合模块`go.mod/go.sum`和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、SQL、DAO/DO/Entity、前端或开发工具入口。数据权限：新增旧字段过滤仍走插件 service 的`TenantFilter`数据库侧过滤、计数、排序和分页，不新增跨租户读取；删除参数别名只改变 ID 读取方式，批量删除可见性校验不变。接口性能：旧字段过滤和排序均下推数据库侧，账号分组过滤继续使用子查询，不引入 N+1。缓存一致性：无新增缓存。i18n：新增`/metrics`为 Prometheus 文本协议输出，旧字段别名和旧响应键不涉及语言包。依赖：新增插件本地`github.com/prometheus/client_golang`用于恢复旧`/metrics`真实 handler，`go mod tidy`同时将 jobs 代码已直接 import 的 LDAP/Oracle 依赖整理为插件 direct require。
- FB-18 验证记录：已运行`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./backend -count=1`、`GOWORK=off go test ./... -count=1`（`linapro-uidentity-cas`插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、插件和主仓库`git diff --check`。
- FB-17 根因记录：旧兼容层为了避免触碰`apps/lina-core`内部实现，把部分旧 GoAdmin 行为降级为占位壳；但用户要求旧路由、出入参和业务逻辑一致，因此这些壳会破坏调用方可观察结果。具体表现为旧`/api/v1/cas-login/index`会触发 GoAdmin JWT 登录态，插件上一轮只返回账号 JSON；旧`/api/v1/job-log`会对旧任务日志执行数据权限 CRUD，插件上一轮只返回空分页或 unsupported action；旧根`/wechat/callback`会进入公众号 SDK 的 Serve 流程，插件上一轮仅空 200；旧`/api/v1/cas/loginByQr`通过微信 OAuth `code`解析`unionID`，插件上一轮要求调用方直接传`unionID`；旧扫码 redirect 固定包含`cascallback`、`state`、`appid`、`uuid`和`msg`，且旧 token/logout 等接口存在固定业务`msg`，插件上一轮统一成`操作成功`。
- FB-17 已完成：旧`/api/v1/cas-login/index`在 CAS ticket 校验、账号解析和登录日志记录后，如请求带有效`appId/app_id`，会按插件应用访问规则签发 runtime access token，并在旧响应壳中返回`AccessToken/accessToken/access_token`和过期时间，避免回调只校验不产出可继续使用凭证；未带 appId 时保持账号解析结果，不接入 core 私有 JWT 中间件。旧`/api/v1/job-log`改为兼容宿主 GF 调度日志表`sys_job_log`：列表/详情按旧响应壳输出`jobId/jobName/startAt/endAt/createNum/updateNum/deleteNum/errNum`等旧字段，创建/更新/删除写入或清理同一日志表，调度任务本身仍由宿主 GF job 管理。根`/wechat/callback`恢复旧公众号入口的核心可观察语义：GET 返回`echostr`，POST 文本 XML 交换收发方并回显文本内容，非文本/不可解析请求返回空 200。旧`/api/v1/cas/loginByQr`支持通过插件配置`runtime.wechatLoginCodeResolveUrl`使用`code`解析`unionID`，并兼容`unionID/unionId/unionid`和嵌套`data/result/user`响应字段。扫码 redirect 统一按旧`getWechatRedirectUrl`输出`cascallback/state/appid/uuid/msg`五个 query；失败或 unsupported 走`cascallback=err`。旧 token 获取和非 GET logout 响应分别恢复`登录成功`、`退出成功`业务消息。
- FB-17 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas`插件源码和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、宿主路由装配、宿主任务服务、宿主认证服务、SQL 迁移、DAO/DO/Entity、前端或开发工具。插件目录无本地`AGENTS.md`。数据权限：旧 job-log 兼容查询使用插件 tenant filter 读取`sys_job_log.tenant_id`，平台上下文可按既有 platform bypass 读取，租户上下文仅见本租户日志；旧 job-log 创建/更新/删除也带同一 tenant 边界。缓存一致性：无新增缓存，微信 QR 仍复用既有 OAuth token 状态。i18n：新增旧响应消息为旧系统中文固定业务文案，属于旧接口契约恢复；无语言包或 API 文档源文本变更。接口性能：job-log 列表使用数据库侧过滤、计数、排序和分页，`jobName`通过`job_snapshot LIKE`先收敛候选，不做分页后全量内存过滤；CAS 和微信逻辑不引入 N+1 查询。宿主边界：未调用 core internal service/DAO，CAS 登录态不强接 GoAdmin/Core 私有 JWT，只在插件内签发 runtime token。
- FB-16 根因记录：FB-13 将旧`app/jobs`九个任务迁为 LinaPro 宿主任务管理 handler 后，插件 jobs 保留了批量装配和宿主调度边界，但部分旧 job 业务副作用没有完全对齐：`ChangeContainer`此前只批量更新插件账号`container_id`，绕过旧实现先改 LDAP DN 和`usertype`成功后才写 DB 的顺序；Oracle 同步此前未复刻旧`strconv.Atoi(...)`失败即跳过 BZKS/YJS/JZG 单位代码的行为；已有账号缺失`account_detail`时此前会自动补建详情，而旧实现直接`continue`；LDAP 新增账号此前把`openUID`写入 AddRequest，但旧`GetLdapAddData`只在修改路径同步`openUID`，普通 LDAP modify 也不应额外写`usertype`。
- FB-16 已完成：`ChangeContainer`仍由 LinaPro 宿主任务管理调度，但 handler 业务改为按毕业详情分页读取候选账号，使用插件配置打开 LDAP，逐账号执行 LDAP DN 迁移到`ou=xy,ou=People,<baseDN>`并替换`usertype=xy`，仅将 LDAP 成功账号 ID 批量写回本地`container_id`；LDAP 配置缺失、连接失败、单账号 LDAP 查询/迁移/修改失败或账号缺失时，该账号计入失败且不写本地容器。Oracle 账号同步新增输入过滤，`SyncStudent`、`SyncStudentYJS`和`SyncJzg`单位代码非数字时跳过并计错，`SyncStudentWJ`继续使用固定`424`。已有账号缺详情时跳过详情同步，新建账号仍创建详情和分组关系。`SyncMysql2Ldap`新增 LDAP 用户时不再写`openUID`，普通 LDAP modify 仍同步`securityEmail`和`openUID`，`usertype`只由新增和容器迁移路径维护。
- FB-16 影响分析：本轮只修改`apps/lina-plugins/linapro-uidentity-cas/backend/internal/service/jobs`和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、HTTP API、SQL、DAO/DO/Entity、前端、脚本或开发工具入口；插件目录无本地`AGENTS.md`。数据权限：jobs 仍按宿主任务上下文解析租户，只写插件自有租户表；`ChangeContainer`本地 DB 更新以 LDAP 成功账号集合为边界，不暴露新的 HTTP 数据读取入口。缓存一致性：无新增缓存或快照状态。i18n：插件未启用`i18n.enabled`，无新增运行时文案、语言包或 API 文档源文本。DI：无新增外部运行期依赖，复用既有插件配置服务和`go-ldap`依赖；未恢复插件自建 cron、`plugin_linapro_uidentity_cas_sys_job/job_log`或`job_start/job_remove`。接口性能：Oracle 账号同步仍在 DB 访问前过滤无效输入并按账号、详情、手机号、单位集合化装配；`ChangeContainer`因 LDAP 是逐账号外部副作用，仅对 LDAP 成功账号集合做批量 DB 更新，避免恢复逐账号 DB 更新。
- FB-16 验证记录：新增/更新 jobs 单元测试覆盖 BZKS/YJS/JZG 非数字单位代码跳过且 WJ 固定`424`保留、已有账号缺详情不补建、LDAP AddRequest 不包含`openUID`且 ModifyRequest 仍同步`openUID`、普通 modify 不写`usertype`、`ChangeContainer`只将 LDAP 成功账号提交本地更新，并在测试数据库可用时验证 LDAP 失败账号本地`container_id`不变。已运行`GOWORK=off go test ./backend/internal/service/jobs -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和`openspec validate implement-uidentity-cas-backend --strict`。
- FB-15 根因记录：上一轮兼容层以`/api/v1`旧地址为主做映射，但旧项目实际还有挂在 Gin 根分组的`/sso/*`、`/ssologin/getToken`、`/wechat/callback`和域名校验文本地址；同时旧`registerSsoRouter`把`serviceValidate`、`proxyValidate`和`logout`注册为`Any`，旧`Sso.Login`、`Cas.LoginByQr`、`Cas.LoginByUnionID`、`Active.WechatScan`和`User.BindUnionIDCallBack`以 302 重定向为主要业务语义，并非插件上一轮使用的 JSON REST 输出。旧`/api/v1/stat/get`在 noCheckRole 组内，旧`/api/v1/job-log`是受保护 CRUD 地址，二者也需要按源 router 文件对齐。
- FB-15 已完成：在`linapro-uidentity-cas`插件内补齐根`/sso/serviceValidate`、`/sso/proxyValidate`、`/sso/login`、`/sso/logout`、根`/ssologin/getToken`、根`/wechat/callback`、`/MP_verify_5osfGmdqMLsyyzYp.txt`和`/EcEOCIhE9w.txt`；将旧`Any`路由按`ALL`注册；`/ssologin/getToken`恢复为 runtime token 发放；`/api/v1/wechat/login`和`/api/v1/wechat/loginCallback`保持旧空 200；`/api/v1/stat/get`移到公开兼容组；补齐`/api/v1/job-log`列表、详情、创建、更新和删除旧地址；`/sso/login`按 TGT cookie 校验决定是否追加`cascallback=choose`并重定向旧登录壳，`/sso/logout`GET 按`service`或旧登出 redirect 跳转、非 GET 返回旧响应壳，UnionID 登录、微信扫码登录回调、激活微信扫码回调和 UnionID 绑定回调按旧配置 redirect URL 跳转。旧`/api/v1/cas-login/index`补为 CAS ticket 校验兼容入口；由于旧实现最终调用 GoAdmin JWT 中间件写宿主登录态，plugin 侧不伪造 LinaPro core auth session。
- FB-15 影响分析：本轮仍只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、SQL、DAO/DO/Entity、前端、脚本或开发工具入口；插件目录无本地`AGENTS.md`。数据权限：后台资源、`job-log`和配置/监控旧地址仍经过宿主认证、租户和权限中间件；`stat/get`公开性与旧 router 保持一致，service 仍通过`TenantFilter`装配统计；根 SSO、Wechat、CAS/OAuth/token 和激活回调路径通过 ticket、cookie、state、token 或第三方回调自隔离。缓存一致性：无新增缓存，TGT/ST/state/token 继续以插件 OAuth token 表为权威源。i18n：插件未启用`i18n.enabled`，没有新增语言包或运行时多语言资源。DI：只在插件 service 接口内增加 TGT 非消费校验和旧 redirect 配置读取，无新增外部运行期依赖。`job-log`边界：真实执行日志由 LinaPro 宿主 gf/gcron 任务管理承载，plugin 保留旧地址和分页壳，但不越界读取`lina-core/internal`或旧 GoAdmin `sys_job_log`表。
- FB-14 根因记录：前序实现按 LinaPro 插件命名空间设计了新的 RESTful API，例如`/x/linapro-uidentity-cas/api/v1/uidentity/accounts/imports`、`/uidentity/cas/password-logins`和`/uidentity/users/{number}/password`，但旧项目实际对外契约是`/api/v1/account/import`、`/api/v1/cas/login`、`/api/v1/user/changePassword`等旧路径和旧 DTO 字段。功能迁移不能替代接口契约兼容，本轮需新增旧地址旧参数兼容层，并保留插件内部 service 复用，不修改`apps/lina-core`核心框架。
- FB-14 已完成：在`linapro-uidentity-cas`插件内新增旧`/api/v1`和根`/sso`兼容路由层，保留现有插件命名空间 RESTful API。旧账号管理资源`account`、`account-details`、`account-unit`、`account-app-role`、`account-app-blacklist`、`account-change-log`、`units`、`groups`、`containers`、`applications`、`group-app-blacklist`、`pass-ruler`、`sms`、`cas-login-log`、`oauth-log`和`oauth-token`按旧路径、旧 CRUD 方法和`pageIndex/pageSize`响应壳接入插件 service；旧特殊动作`account/import`、`importCheck`、`unlockPassword`、`updatePassword`和自助改密三段接口使用旧字段名映射。CAS、SSO、runtime token、账号激活、UnionID 绑定、用户自助、微信扫码登录、OAuth、短信、上传、配置发现、统计、日志快照、服务器监控和`health`均补齐旧地址；响应壳使用旧`requestId/code/msg/data`与分页`count/pageIndex/pageSize/list`，CAS XML 与 OAuth token 保持旧协议输出边界。
- FB-14 jobs 复查：旧`app/jobs/examples.go`实际注册`SyncMysql2Ldap`、`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncDept`、`SyncJzg`、`ChangeContainer`、`NewContainerAccount`和`WannaT`九个任务；插件`backend/internal/service/jobs`已同名声明九个 managed job，并通过`plugin.Cron().RegisterCron`交给宿主`service/cron`的 gf/gcron 任务管理执行。旧`/api/v1/sysjob`列表/详情地址补为九个 managed job 的旧字段投影；旧`/api/v1/job/start/{id}`、`/api/v1/job/remove/{id}`和`sysjob`写操作保留兼容入口，但不绕开宿主任务管理直接操作插件自建 cron。
- FB-14 影响分析：本轮只改`apps/lina-plugins/linapro-uidentity-cas`插件后端和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、SQL、DAO/DO/Entity、前端、脚本或开发工具入口；插件目录无本地`AGENTS.md`。数据权限：旧后台管理和资源 CRUD 兼容路由复用宿主`Auth`、`Tenancy`、`Permission`中间件及插件 service 内`TenantFilter`，公开运行时路径通过一次性 ticket/token/challenge/state 自隔离。缓存一致性：无新增缓存，短期状态继续以插件 token 表为权威源。i18n：插件未启用`i18n.enabled`，本轮没有新增 API DTO 文档资源或语言包资源，旧响应壳只用于兼容。DI：新增`LegacyController`显式接收启动期同一个`uidentitySvc`实例，无新增接口型运行期依赖。测试：新增路由清单和旧字段投影单测，覆盖旧地址存在性、旧资源名映射、TGT/ST/用户字段投影和九个 jobs 投影。
- FB-12 根因记录：前序“完成”记录主要按旧路由和插件已知能力做总审计，未把旧项目服务端能力、插件实现、测试证据和排除理由做成可逐项复查的清单，也未显式记录两轮反向审计，导致无法证明`/Users/wanna/mine/ecoding/uidentity/admin`服务端能力已经完整覆盖。
- FB-12 已完成：新增`server-capability-audit.md`，以旧项目 Go 文件清单、router 注册、service/model 副作用、jobs registry、插件 REST 路由、通用资源和 cron 执行器为证据，逐项记录统一身份域管理资源、CAS/OAuth/runtime token、账号激活、用户自助、短信、导入、上传、配置、监控快照、作业和审计日志的插件映射；同时明确排除 GoAdmin 默认系统管理、页面/重定向壳、开发工具、全局 Prometheus 和未配置 Oracle/LDAP 外部执行器的理由。第一轮反向审计从旧项目能力映射到插件，第二轮反向审计从插件路由/资源/副作用回查旧项目和外部边界；除 FB-11 已修补缺口外未发现新的统一身份服务端缺口。
- FB-11 根因记录：旧`dto.AccountInsertReq.Generate`会把`groupIds`生成为 GORM `Groups`关联，旧`Account.Update`会显式增删`account_group`关系；插件迁移时只提供了独立`account-groups`通用资源和账号列表`groupIds`筛选，账号创建/更新请求本身未同步维护`account_group`关系，属于旧 admin 服务端写入副作用遗漏。
- FB-11 已完成：新增插件 service 层`uidentity_account_groups.go`，账号创建前按当前租户预校验`groupIds`，创建成功后同步写入`account_group`；账号更新在账号审计更新成功后替换账号分组关系，支持去重、忽略非正数、传空数组清空关系，并在不存在或跨租户组 ID 时拒绝且不破坏原有关联。本轮未新增 HTTP 路由、SQL、DAO 或缓存；数据权限通过`TenantFilter`校验组和删除当前租户关系；i18n 无新增资源；核心框架`apps/lina-core`未修改。新增`TestAccountResourceSyncsGroupIDs`覆盖创建同步、更新替换、非法组拒绝且保留旧关系、空数组清空关系。验证运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestAccountResourceSyncsGroupIDs|TestResourceAccountAuditLifecycle' -count=1`、`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-10 根因记录：旧`app/admin/models/account.go`和`app/admin/models/account_details.go`依赖 GORM model hook 在账号、账号详情创建、更新和删除时自动写入`account_change_log`；插件迁移后只有`account-change-logs`通用资源和表结构，通用资源 CRUD、账号导入、管理员改密、账号激活、UnionID 绑定与微信换绑等服务端写路径不会触发旧 hook 的审计副作用。本轮修复限定在`linapro-uidentity-cas`插件内，不修改`apps/lina-core`核心框架。
- FB-10 已完成：新增插件 service 层账号审计 helper，账号和账号详情的创建、更新、删除显式写入`account_change_log`，账号自动创建详情行也写入详情创建日志；账号导入、账号投影写入、管理员重置密码、激活、登录态微信换绑和 UnionID 迁移绑定等直接写表路径统一改为审计 helper 或批量审计写入。账号审计记录中显式移除`passwordHash`，外部回调类路径使用 token 内租户或显式租户 helper 保持跨登录态安全边界。SQL/DAO：复用既有变更日志表、索引和生成代码，无新增 SQL 或 DAO；数据权限：后台接口继续经宿主`TenantFilter`约束，公开回调只按不可猜测的一次性 state/token 定位账号并使用 token 内租户写入插件自有表；缓存一致性：无新增缓存；DI：无新增运行期依赖；i18n：插件未启用`i18n.enabled`，无新增用户可见文案资源；测试新增`TestResourceAccountAuditLifecycle`覆盖账号和账号详情创建、更新、删除审计及密码 hash 脱敏。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- FB-8 根因记录：旧`app/cas/service/active.go`在激活微信扫码绑定和微信换绑成功后写入`account_active_log`，旧`app/cas/service/user.go`的`BindUnionID`会先解绑占用同一`unionId`的其他账号，再绑定当前账号并写入`type=1`审计日志。插件上一轮已覆盖激活、微信扫码绑定和 UnionID 绑定接口，但把`account_active_log`误判为页面/附属日志，且`BindUnionID`仍按冲突拒绝处理，与旧行为不一致。本轮修复限定在`linapro-uidentity-cas`插件内，不修改`apps/lina-core`核心框架。
- FB-8 已完成：新增插件自有表`plugin_linapro_uidentity_cas_account_active_log`、卸载 SQL、DAO/DO/Entity 和通用资源`account-active-logs`；激活直接绑定、激活微信扫码回调、登录态微信换绑回调成功后写入`type=0`审计日志，`BindUnionID`恢复旧行为，先在当前租户内集合化清理其他账号的同一`unionId`，再绑定当前账号并写入`type=1`审计日志。SQL 为 PostgreSQL-only 幂等建表和索引，索引覆盖租户、账号、微信和时间查询；数据权限通过`tenant_id`与既有`TenantFilter`约束，写操作只作用于当前租户；缓存一致性无新增缓存；DI 无新增运行期依赖；i18n 方面插件未配置`i18n.enabled: true`，无需补插件翻译资源；测试新增`TestRebindUnionIDToAccountMigratesExistingBinding`覆盖旧绑定迁移和审计日志。
- FB-9 根因记录：旧`app/jobs/change_container.go`不是纯外部 LDAP 壳，它会查询预计毕业时间为当前年的账号详情，将这些账号的本地`container_id`迁移到`xy`容器；LDAP 的`ChangeContainerID`属于外部目录边界，但数据库容器迁移是旧后端本地副作用。插件上一轮只实现了`WannaT`和`ContainerAccount/NewContainerAccount`，未覆盖`ChangeContainer`的本地迁移部分。
- FB-9 已完成：插件 cron Exec target 新增`ChangeContainer`，按当前租户查找`xy`容器后，使用`account_detail.graduated_at`子查询一次性更新匹配账号的`container_id`，不逐账号循环查询或更新；新增`idx_pluicas_detail_graduated_at`支撑`tenant_id + graduated_at + account_id`路径。外部 Oracle/LDAP 同步类作业继续保持结构化 unsupported 边界，不把外部目录依赖硬编码进插件或`apps/lina-core`。测试新增`TestUpdateGraduatingAccountContainer`覆盖迁移成功和非毕业账号不变；验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- FB-7 根因记录：旧`app/jobs/jobbase.go`在启动期清空`entry_id`、加载`status=2`的`sys_job`，并按`job_type=1`注册 HTTP GET 任务、按`job_type=2`注册 Exec 任务；旧`service/sys_job.go`的`StartJob`和`RemoveJob`会真实添加或移除 cron entry。插件上一轮只新增了`sys-jobs`、`job-logs`通用资源和`job_start`/`job_remove`外部动作入口，对`entry_id`做状态写入，没有持有 GoFrame cron 实例，也不会按 cron 表达式执行 HTTP/Exec 作业。本轮修复限定在`linapro-uidentity-cas`插件内，直接使用 GoFrame `gcron`，不修改`apps/lina-core`核心框架。
- FB-7 后续调整：旧 admin 作业真实执行能力在 FB-13 中改为通过 LinaPro 内建任务管理承载，插件不再维护自建`sys_job/job_log`表、本地`gcron`调度器或`job_start/job_remove`运行状态动作。
- FB-4 根因记录：旧实现的`account/unlockPassword`不属于页面能力，它清理 CAS 密码登录中按`cas:pwd:errnum:<number>`累计的短期密码错误次数；插件已实现账号状态、管理员改密和运行时密码校验，但未保存密码错误次数状态，也没有按账号列表解锁的后端入口。本轮补齐时仅修改`linapro-uidentity-cas`插件，使用插件自有 token 表承载短期错误次数状态，不修改`apps/lina-core`核心框架。
- FB-4 已完成：新增受宿主认证、租户和权限中间件治理的`POST /uidentity/accounts/password-unlocks`，按当前租户可见账号列表批量清理插件 token 表中的`cas:pwd:errnum:<number>`短期错误次数状态；CAS 密码登录、runtime token、OAuth 授权码密码校验和 UnionID 账号密码绑定统一接入错误次数检查，错误达到旧项目同等阈值后返回结构化`UIDENTITY_PASSWORD_FAILURES_LOCKED`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本和错误码暂不补插件翻译资源；缓存一致性：新增短期状态以插件 OAuth token 表为权威源并依赖`expired_at`失效，不引入进程内缓存；数据权限：管理员解锁接口进入宿主认证、租户和权限中间件，解锁前按账号表租户可见性批量过滤；SQL/DAO：复用既有 token 表和唯一 code 索引，无新增 SQL 或 DAO；API/后端 Go：新增 RESTful DTO、controller、service 接口和实现，运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口生成；开发工具跨平台：仅使用既有 Makefile 入口，无脚本变更；测试：新增纯函数单测覆盖旧 key 前缀、空账号过滤、去重和数量截断。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-1 已完成：本轮以源项目`/Users/wanna/mine/ecoding/uidentity/admin/app/**/router/*.go`实际路由为入口，重新审计旧后端能力与插件当前注册路由、资源定义、SQL 表和 service 实现。统一身份域后端能力已由`linapro-uidentity-cas`插件承载；默认系统管理、代码生成、页面/重定向壳和全局监控暴露明确不属于 CAS 插件后端迁移目标。
- FB-2 已完成：CAS TGT/ST 服务端票据链、运行时 token、账号激活、UnionID 绑定和用户自助后端接口均落在`linapro-uidentity-cas`插件内，未修改`apps/lina-core`核心框架；i18n 未启用插件资源治理，缓存无新增状态，数据权限继续通过宿主`TenantFilter`接入。
- 验证记录：已运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- 继续迁移记录：本轮补齐 FB-3 中账号导入检查/导入和短信发送子能力，导入按`number`在插件账号表内幂等新增/更新，短信发送使用插件 SMS 表和本地频控记录验证码；仍未完成 OAuth 授权码完整流程、LDAP 同步边界、文件上传、作业和监控类后端能力。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块），新增纯函数单测覆盖导入空行、生日日期归一化和短信类型校验。
- 继续迁移记录：本轮补齐 FB-3 中 OAuth 授权码运行时子能力，新增公开 RESTful 接口`POST /uidentity/oauth/authorization-codes`、`POST /uidentity/oauth/access-tokens`和`GET /uidentity/oauth/access-tokens/{accessToken}/user-info`，复用插件账号、应用、黑名单、OAuth token 和 OAuth log 表；授权码按唯一 code 单点查询并在事务内一次性消费，access token 按唯一 access 单点查询，未修改`apps/lina-core`核心框架。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态；数据权限：公开运行时接口仍通过插件`TenantFilter`上下文限制租户表数据，授权码和 access token 均绑定账号、应用和租户；SQL/DAO：复用既有 OAuth 表和唯一索引，无新增 SQL/DAO；DI：无新增运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_oauth_runtime_test.go`覆盖 URL-escaped client secret、redirect URI 校验和授权码 grant 类型。仍未完成 LDAP 同步边界、文件上传、作业和监控类后端能力。
- 继续迁移记录：本轮补齐 FB-3 中文件上传、健康检查、服务器监控、日志快照和旧 LDAP/外部文件/作业/外部监控动作边界。新增插件本地 RESTful 接口`POST /uidentity/legacy/uploads`、`GET /uidentity/legacy/health`、`GET /uidentity/legacy/server-monitor`、`GET /uidentity/legacy/log-snapshots`和`POST /uidentity/legacy/external-actions`；上传默认写入插件配置的本地目录并返回旧`uploadFile`兼容字段，监控使用本机进程/OS 投影，日志读取改为带上限的 tail snapshot，外部 LDAP/作业执行在未配置执行器时返回结构化 unsupported-flow 错误，避免把外部依赖硬编码进插件或污染`apps/lina-core`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态；数据权限：健康检查公开且不返回业务数据，上传/监控/日志/外部动作接口进入宿主认证、租户和权限中间件，文件和日志仅访问插件配置边界内的本地资源；SQL/DAO：无新增 SQL/DAO，复用插件现有表；DI：新增`gopsutil/v4`作为插件本地监控依赖，无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_legacy_ops_test.go`覆盖 base64 payload 拆分、安全文件名和日志 tail 上限逻辑。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- 继续迁移记录：本轮补齐旧 admin 静态配置发现接口和 CAS XML `serviceValidate`返回形态。新增插件本地 RESTful 接口`GET /uidentity/legacy/config/cas`、`GET /uidentity/legacy/config/ldap`、`GET /uidentity/legacy/config/oauth`、`GET /uidentity/legacy/config/token`和`POST /uidentity/legacy/cas/service-validations.xml`；配置发现只读取插件作用域配置并提供旧结构字段，LDAP 默认只暴露 unsupported 元数据而不启动执行器；CAS XML 接口复用既有 ST 消费、账号访问和委托授权校验，成功时输出`cas:authenticationSuccess`，失败时按旧协议输出`INVALID_TICKET` XML，而不是 JSON 错误。根因判断：前几轮已迁入运行时 ticket 和外部动作边界，但旧 admin 的`/api/v1/config/*`与`/sso/serviceValidate`XML 协议仍缺少插件侧替代入口。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态，配置读取复用宿主启动期注入的插件配置服务；数据权限：配置发现接口进入宿主认证、租户和权限中间件，CAS XML 为运行时公开协议且通过一次性 ST 自隔离，不返回未绑定 ticket 的业务数据；SQL/DAO：无新增 SQL/DAO，复用既有 token、账号、应用和授权表；DI：无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_legacy_cas_xml_test.go`和`uidentity_legacy_config_test.go`覆盖 XML 成功/失败渲染、配置默认值和插件作用域覆盖读取。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和插件子仓库`git diff --check`。
- 继续迁移记录更正：插件未上线且不存在兼容包袱，前序插件自建`plugin_linapro_uidentity_cas_sys_job`、`plugin_linapro_uidentity_cas_job_log`、`sys-jobs/job-logs`通用资源和`job_start/job_remove`动作已在 FB-13 中清理；旧作业定义和执行日志改由 LinaPro 宿主任务管理的声明、调度和日志能力承载。
- 继续迁移记录：本轮补齐旧 admin 微信扫码登录后端状态链。新增插件本地 RESTful 接口`POST /uidentity/cas/wechat-login-qrs`、`POST /uidentity/cas/wechat-login-callbacks`和`GET /uidentity/cas/wechat-login-qrs/{state}/result`；扫码 state、回调结果和轮询结果复用插件 OAuth token 存储，已绑定`unionId`时签发 CAS TGT/ST，未绑定时创建既有 UnionID 绑定 challenge，未配置真实微信 OAuth 解析器时记录`unsupported`状态而不是把第三方依赖硬编码进插件或`apps/lina-core`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态，扫码状态以数据库 token 表为权威源并随过期时间失效；数据权限：运行时公开接口只通过一次性 state/ticket/challenge 自隔离，账号、应用、绑定和登录日志读写继续经`TenantFilter`接入；SQL/DAO：无新增 SQL/DAO，复用 OAuth token、账号详情、账号、应用和日志表；DI：无新增运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：本轮运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口生成，修复生成器追加的默认构造以保留插件显式依赖注入；测试：新增`uidentity_wechat_runtime_test.go`覆盖授权 URL 装配和扫码结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`和插件子仓库`git diff --check`。FB-3 具名外围能力已完成，FB-1 继续保留全量兼容性总审计。
- 最终总审计记录：源项目统一身份域管理资源`account`、`account_details`、`units`、`containers`、`groups`、`applications`、`account_unit`、`account_app_role`、`account_app_blacklist`、`group_app_blacklist`、`pass_ruler`、`sms`、`cas_login_log`、`oauth_log`、`oauthtoken`、`account_change_log`和`stat`已映射到插件通用资源`/uidentity/{resource}`与`/uidentity/stats`；旧`app/jobs`注册表九个任务由插件声明为 LinaPro 内建任务管理 handler，作业调度与执行日志由宿主任务管理承载，不再作为插件自有资源。账号改密、导入检查/导入、短信发送、CAS TGT/ST、CAS XML`serviceValidate`、runtime token、OAuth authorization code/access token/user-info、账号激活、UnionID 查询/绑定、用户自助、微信扫码登录、上传、健康检查、服务器监控、日志快照、静态配置发现和外部动作边界均存在插件 RESTful 入口。源项目`sys_user`、`sys_role`、`sys_menu`、`sys_dept`、`sys_dict`、`sys_config`、`sys_post`、`sys_api`、登录/刷新/登出、Swagger、WebSocket、静态文件和验证码属于 LinaPro 宿主默认系统管理、页面/开发工具或通用工作台能力，不复制进 CAS 插件；`other/gen_router.go`的代码生成、数据库表/列浏览和菜单/API 生成属于开发工具链，不是统一身份运行时后端；`oauth/login`、`oauth/auth`、`cas/login`、`sso/login`、`bindUnionIDCallBack`等 HTML 或重定向壳由插件已有 RESTful 运行时接口替代，不迁移页面；旧`/metrics`是全局 Prometheus 暴露，LinaPro 已有独立`linapro-monitor-server`插件承载通用监控，CAS 插件仅保留旧`server-monitor`投影。规则影响：本轮只更新 OpenSpec 任务记录，无代码、SQL、API、i18n 资源、缓存、数据权限、DI 或开发工具跨平台新增影响；验证使用源路由`rg`审计、插件路由/资源/SQL 静态检索、`openspec validate implement-uidentity-cas-backend --strict`和当前工作区 Go 测试门禁。
- FB-13 根因记录：旧`/Users/wanna/mine/ecoding/uidentity/admin/app/jobs/examples.go`注册了`SyncMysql2Ldap`、`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncDept`、`SyncJzg`、`ChangeContainer`、`NewContainerAccount`和`WannaT`九个任务；前序插件实现曾自行创建`plugin_linapro_uidentity_cas_sys_job/job_log`、本地`service/cron`调度器和`job_start/job_remove`动作，但 LinaPro 本身已经有任务管理、调度和日志能力，插件只需要声明任务 handler 并执行业务逻辑。
- FB-13 已完成：`linapro-uidentity-cas`插件通过`plugin.Cron().RegisterCron(..., registerManagedJobs)`向宿主注册九个 managed job handler，名称严格保持旧注册表值；删除插件本地`backend/internal/service/cron`组件、`plugin_linapro_uidentity_cas_sys_job/job_log`安装/卸载 SQL、DAO/DO/Entity、`sys-jobs/job-logs`通用资源、`job_start/job_remove`外部动作和 DAO 生成配置。业务 handler 已迁入插件`backend/internal/service/jobs`：`SyncDept`分页同步 Oracle`T_ORG_DEPT`到插件`unit`，`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncJzg`分页读取旧 Oracle 源并批量装配账号、详情、单位、手机号占用后写入插件账号、详情和分组关系，`SyncMysql2Ldap`分页读取插件账号并按旧 DN/属性规则同步到配置的 LDAP，`ChangeContainer`和`NewContainerAccount`保留集合化本地数据库更新，`WannaT`保留轻量日志探针。配置缺失时 Oracle/LDAP handler 返回结构化`UIDENTITY_JOB_EXECUTOR_UNSUPPORTED`，由宿主任务管理记录调度结果。
- FB-13 影响分析：本轮不修改`apps/lina-core`核心框架；新增运行期依赖为插件 module 内`github.com/go-ldap/ldap/v3`和`github.com/gogf/gf/contrib/drivers/oracle/v2`，由插件`jobs`服务直接使用，owner 为插件 handler，创建位置为插件`backend/plugin.go`注册回调内构造`jobs.Service`，传递路径为宿主 source-plugin registrar -> plugin services -> jobs service，不引入缓存敏感共享实例；`apps/lina-plugins`聚合模块同步`go.mod/go.sum`依赖校验，避免插件集合编译时缺少 checksum。SQL 删除未上线插件自建 job 表，安装/卸载 SQL 仍为 PostgreSQL 幂等脚本，DAO 生成配置移除对应表；数据权限方面后台任务按宿主任务触发上下文解析租户并只写插件自有租户表，外部 LDAP/Oracle 是配置化边界，不暴露 HTTP 读写接口；缓存一致性无新增缓存或快照状态；i18n 方面插件未启用`i18n.enabled`，仅删除过期 API 文档源文本中的 job action 描述并新增结构化错误码，不需要插件翻译资源；开发工具跨平台仅更新插件`backend/hack/config.yaml`的 DAO 表清单，无脚本或平台入口变更；测试新增`jobs_register_test.go`覆盖九个任务注册、旧状态映射和字段归一化，静态检索确认无旧自建 job 表、`service/cron`和`job_start/job_remove`残留。
- FB-13 验证记录：已运行`GOWORK=off go test ./backend/internal/service/jobs -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-5 根因记录：旧 admin 的`/api/v1/user/changeWechatQr`和`/api/v1/user/changeWechatState`不是页面能力；它们为已登录用户创建短期微信换绑 state，经微信扫码回调绑定`unionId`到当前账号，再按 state 查询换绑结果。插件已有解绑、UnionID 绑定和微信扫码登录状态链，但缺少“已登录用户换绑微信”的独立后端 state 链。本轮补齐仍限定在`linapro-uidentity-cas`插件内，复用插件 OAuth token 表承载短期 state，不修改`apps/lina-core`核心框架。
- FB-5 已完成：新增受宿主认证、租户和权限中间件治理的`POST /uidentity/users/{number}/wechat-rebind-states`与`GET /uidentity/users/{number}/wechat-rebind-states/{state}`，以及公开回调边界`POST /uidentity/users/wechat-rebind-callbacks`；换绑 state 复用插件 OAuth token 表，按`wechat_rebind:<state>`单点索引读写，`unionId`存在时绑定到 state 内账号并返回成功状态，未配置真实微信 OAuth 解析器时记录结构化`UIDENTITY_EXTERNAL_FLOW_UNSUPPORTED`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无进程内缓存，短期 state 以数据库 token 表为权威源并通过`expired_at`失效；数据权限：创建/查询接口进入宿主认证、租户和权限中间件，查询时校验 state 归属账号，公开回调属于外部 OAuth 回调例外，仅凭不可猜测的一次性 state 单点定位 token，并使用 token 内租户和账号写入插件自有账号详情，避免依赖登录态同时不做列表级放开；SQL/DAO：无新增 SQL/DAO，复用既有 OAuth token 和账号详情表；DI：无新增接口型运行期依赖；开发工具跨平台：运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口；测试：新增纯函数单测覆盖换绑授权 URL 装配和换绑结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- 最终复核补充：FB-5 补齐后，旧 admin 的登录态用户微信换绑 state 链已由插件 RESTful 后端入口覆盖；重新以源项目路由、插件路由、通用资源、Service 实现和 SQL 表核对，未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源和全局 Prometheus 能力继续按 LinaPro 宿主默认能力或独立插件边界处理，不复制进 CAS 插件。
- FB-6 根因记录：旧 admin 的`/api/v1/activate/wechatQr`和`/api/v1/activate/wechatScan`不是页面能力；`wechatQr`复用激活`uuid`生成外部微信绑定 URL，`wechatScan`在公开回调中用`code/state`解析`unionId`并绑定到激活账号，最后由`activate/state`轮询账号是否已激活。插件当前已有`PUT /uidentity/activations/{challengeId}/wechat`直接`unionId`绑定，但缺少“生成授权 state + 公开回调完成绑定”的后端链。本轮补齐仍限定在`linapro-uidentity-cas`插件内，复用插件 OAuth token 表承载激活 challenge，不修改`apps/lina-core`核心框架。
- FB-6 已完成：新增公开 RESTful 接口`POST /uidentity/activations/{challengeId}/wechat-states`与`POST /uidentity/activations/wechat-callbacks`，并扩展`GET /uidentity/activations/{challengeId}/state`返回`wechatStatus`、`redirectUrl`、`errorCode`和`message`。激活微信 state 继续复用插件 OAuth token 表中的 activation challenge，授权 URL 由插件作用域配置`runtime.activationWechatAuthorizeUrl`装配，回调成功时按 token 内租户和账号绑定`unionId`并激活账号，未接入外部微信 code 解析器时记录结构化`UIDENTITY_EXTERNAL_FLOW_UNSUPPORTED`供轮询端识别。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无进程内缓存，短期 state 以数据库 token 表为权威源并通过`expired_at`失效；数据权限：公开激活接口属于一次性 activation challenge 自隔离例外，回调不依赖登录态但仅能按不可猜测 state 定位单条 token，并使用 token 内租户写入插件自有账号详情和账号状态；SQL/DAO：无新增 SQL/DAO，复用 OAuth token、账号和账号详情表；DI：无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口；测试：新增纯函数单测覆盖激活微信授权 URL 装配和结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和`openspec validate implement-uidentity-cas-backend --strict`。
- 最终复核补充：FB-6 补齐后，旧 admin 的账号激活微信扫码绑定 state 链已由插件 RESTful 后端入口覆盖；重新以源项目路由、插件路由、通用资源、Service 实现和 SQL 表核对，当前未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源和全局 Prometheus 能力继续按 LinaPro 宿主默认能力、页面边界或独立插件边界处理，不复制进 CAS 插件。
- 最终复核补充：FB-10 补齐后，旧 admin 统一身份域的资源表、CAS/OAuth/token/激活/自助/微信扫码、短信、导入、上传、配置发现、监控快照、作业调度、`account_active_log`和`account_change_log`审计副作用均已由`linapro-uidentity-cas`插件侧后端承载；本轮未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源、全局 Prometheus 和未配置外部 Oracle/LDAP 执行器继续按 LinaPro 宿主、页面、开发工具、独立监控插件或结构化 unsupported 边界处理，不复制进 CAS 插件。
