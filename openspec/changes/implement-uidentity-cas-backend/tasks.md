## 规则读取与影响记录

- [x] 1.1 读取并记录命中的规范：`documentation.md`、`openspec.md`、`architecture.md`、`data-permission.md`、`plugin.md`、`api-contract.md`、`backend-go.md`、`database.md`、`testing.md`、`i18n.md`和`goframe-v2`技能
- [x] 1.2 维护任务影响记录：i18n（插件未启用 i18n 资源治理，后续运行时文案仍使用结构化错误码）、数据权限（所有租户数据表保留`tenant_id`并通过宿主`TenantFilter`接入）、缓存一致性（当前阶段无缓存状态）、插件（本地未发现插件根`AGENTS.md`）、SQL（PostgreSQL-only 幂等安装/卸载 SQL）、API、后端 Go、测试和开发工具跨平台影响

## 插件骨架

- [x] 2.1 新建`apps/lina-plugins/linapro-uidentity-cas/`源码插件目录、`plugin.yaml`、`plugin_embed.go`和通用资源目录
- [x] 2.2 新建插件`backend/plugin.go`并注册受宿主认证、租户、权限中间件治理的 RESTful 路由
- [x] 2.3 更新插件工作区`go.mod`引用和宿主插件打包/嵌入发现路径

## 数据模型与 SQL

- [x] 3.1 新增插件安装、卸载 SQL，覆盖账号、账号详情、单位、容器、分组、应用、授权、黑名单、密码策略、短信、CAS 日志、OAuth 日志、OAuth token 和变更日志表
- [x] 3.2 生成或维护插件 DAO/DO/Entity，并确保生产代码不手改生成文件
- [x] 3.3 执行 SQL 幂等性、PostgreSQL-only、自增主键和索引静态检查

## 基础管理 API 与 Service

- [x] 4.1 实现账号、账号详情、单位、容器、分组、应用、密码策略和短信记录的列表、详情、创建、更新、删除接口
- [x] 4.2 实现账号应用授权、账号应用黑名单、分组应用黑名单、OAuth token、OAuth 日志、CAS 登录日志和账号变更日志接口
- [x] 4.3 实现列表关联投影批量装配、批量删除可见性校验和结构化`bizerr`错误

## 认证运行时与统计

- [x] 5.1 实现密码强度校验、管理员改密、自助改密、手机号校验改密和账号锁定/解锁
- [x] 5.2 实现 CAS ticket 校验、CAS XML 解析、应用访问规则校验和 CAS 登录日志记录
- [x] 5.3 实现 OAuth token 治理和统一身份统计聚合，确保统计不产生`N+1`查询

## 验证与提交

- [x] 6.1 运行`openspec validate implement-uidentity-cas-backend --strict`
- [x] 6.2 运行插件和受影响宿主包 Go 编译/单元测试门禁
- [x] 6.3 运行`git diff --check`和插件子仓库静态检查
- [x] 6.4 在合适阶段提交主仓库和插件仓库代码

## Feedback

- [x] **FB-1**: 源项目`uidentity/admin`服务端功能未全量迁入插件，当前实现只覆盖管理 CRUD、基础 CAS ticket 校验和统计
- [x] **FB-2**: 插件缺少旧 admin 的 CAS 服务端票据链、Token 链路、账号激活和用户自助后端接口
- [x] **FB-3**: 插件缺少旧 admin 的 OAuth 授权码流程、账号导入检查/导入、LDAP 同步边界、短信发送、文件上传、作业和监控类后端能力
- [x] **FB-4**: 二次核对发现旧 admin 的`account/unlockPassword`密码错误次数解锁后端能力未在插件内落地
- [x] **FB-5**: 二次核对发现旧 admin 的登录态用户微信换绑 state 链未在插件内落地
- [x] **FB-6**: 再次复核发现旧 admin 的账号激活微信扫码绑定 state 链未在插件内落地
- [x] **FB-7**: 再次复核发现旧 admin 的`app/jobs`真实定时任务调度执行未在插件内落地
- [x] **FB-8**: 再次复核发现旧 admin 的`account_active_log`激活、微信换绑和`BindUnionID`审计日志未在插件内落地
- [x] **FB-9**: 再次复核发现旧 admin 的`ChangeContainer`本地账户容器迁移动作未在插件 cron 执行器内落地
- [x] **FB-10**: 再次复核发现旧 admin 的账号与账号详情模型 hook 自动写入`account_change_log`未在插件内落地
- [x] **FB-11**: 能力清单审计发现旧 admin 账号创建/更新请求中的`groupIds`同步维护`account_group`关系未在插件账号资源写入中落地
- [x] **FB-12**: 再次检查要求建立旧 admin 服务端能力清单、逐项映射插件实现/测试/排除理由，并执行至少两轮反向审计记录
- [x] **FB-13**: 用户明确要求将旧`app/jobs`注册表中的九个任务迁为`linapro-uidentity-cas`插件任务管理 handler，并清理插件内非必要的自建 cron 调度实现
- [x] **FB-14**: 用户反馈`uidentity_cas`接口地址、参数和旧`/Users/wanna/mine/ecoding/uidentity/admin`不一致，要求旧服务端接口内容、地址和参数保持完全一致
- [x] **FB-15**: 再复核发现旧接口契约仍有细节偏差：根`/sso`与`/ssologin`路由、旧`Any`方法集合、SSO/UnionID/Wechat 回调重定向语义、Wechat 空实现入口、`stat/get`公开性和`job-log`旧地址未完全对齐

## Feedback Impact Notes

- FB-15 根因记录：上一轮兼容层以`/api/v1`旧地址为主做映射，但旧项目实际还有挂在 Gin 根分组的`/sso/*`、`/ssologin/getToken`、`/wechat/callback`和域名校验文本地址；同时旧`registerSsoRouter`把`serviceValidate`、`proxyValidate`和`logout`注册为`Any`，旧`Sso.Login`、`Cas.LoginByQr`、`Cas.LoginByUnionID`、`Active.WechatScan`和`User.BindUnionIDCallBack`以 302 重定向为主要业务语义，并非插件上一轮使用的 JSON REST 输出。旧`/api/v1/stat/get`在 noCheckRole 组内，旧`/api/v1/job-log`是受保护 CRUD 地址，二者也需要按源 router 文件对齐。
- FB-15 已完成：在`linapro-uidentity-cas`插件内补齐根`/sso/serviceValidate`、`/sso/proxyValidate`、`/sso/login`、`/sso/logout`、根`/ssologin/getToken`、根`/wechat/callback`、`/MP_verify_5osfGmdqMLsyyzYp.txt`和`/EcEOCIhE9w.txt`；将旧`Any`路由按`ALL`注册；`/ssologin/getToken`恢复为 runtime token 发放；`/api/v1/wechat/login`和`/api/v1/wechat/loginCallback`保持旧空 200；`/api/v1/stat/get`移到公开兼容组；补齐`/api/v1/job-log`列表、详情、创建、更新和删除旧地址；`/sso/login`按 TGT cookie 校验决定是否追加`cascallback=choose`并重定向旧登录壳，`/sso/logout`GET 按`service`或旧登出 redirect 跳转、非 GET 返回旧响应壳，UnionID 登录、微信扫码登录回调、激活微信扫码回调和 UnionID 绑定回调按旧配置 redirect URL 跳转。旧`/api/v1/cas-login/index`补为 CAS ticket 校验兼容入口；由于旧实现最终调用 GoAdmin JWT 中间件写宿主登录态，plugin 侧不伪造 LinaPro core auth session。
- FB-15 影响分析：本轮仍只修改`apps/lina-plugins/linapro-uidentity-cas`插件后端和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、SQL、DAO/DO/Entity、前端、脚本或开发工具入口；插件目录无本地`AGENTS.md`。数据权限：后台资源、`job-log`和配置/监控旧地址仍经过宿主认证、租户和权限中间件；`stat/get`公开性与旧 router 保持一致，service 仍通过`TenantFilter`装配统计；根 SSO、Wechat、CAS/OAuth/token 和激活回调路径通过 ticket、cookie、state、token 或第三方回调自隔离。缓存一致性：无新增缓存，TGT/ST/state/token 继续以插件 OAuth token 表为权威源。i18n：插件未启用`i18n.enabled`，没有新增语言包或运行时多语言资源。DI：只在插件 service 接口内增加 TGT 非消费校验和旧 redirect 配置读取，无新增外部运行期依赖。`job-log`边界：真实执行日志由 LinaPro 宿主 gf/gcron 任务管理承载，plugin 保留旧地址和分页壳，但不越界读取`lina-core/internal`或旧 GoAdmin `sys_job_log`表。
- FB-14 根因记录：前序实现按 LinaPro 插件命名空间设计了新的 RESTful API，例如`/x/linapro-uidentity-cas/api/v1/uidentity/accounts/imports`、`/uidentity/cas/password-logins`和`/uidentity/users/{number}/password`，但旧项目实际对外契约是`/api/v1/account/import`、`/api/v1/cas/login`、`/api/v1/user/changePassword`等旧路径和旧 DTO 字段。功能迁移不能替代接口契约兼容，本轮需新增旧地址旧参数兼容层，并保留插件内部 service 复用，不修改`apps/lina-core`核心框架。
- FB-14 已完成：在`linapro-uidentity-cas`插件内新增旧`/api/v1`和根`/sso`兼容路由层，保留现有插件命名空间 RESTful API。旧账号管理资源`account`、`account-details`、`account-unit`、`account-app-role`、`account-app-blacklist`、`account-change-log`、`units`、`groups`、`containers`、`applications`、`group-app-blacklist`、`pass-ruler`、`sms`、`cas-login-log`、`oauth-log`和`oauth-token`按旧路径、旧 CRUD 方法和`pageIndex/pageSize`响应壳接入插件 service；旧特殊动作`account/import`、`importCheck`、`unlockPassword`、`updatePassword`和自助改密三段接口使用旧字段名映射。CAS、SSO、runtime token、账号激活、UnionID 绑定、用户自助、微信扫码登录、OAuth、短信、上传、配置发现、统计、日志快照、服务器监控和`health`均补齐旧地址；响应壳使用旧`requestId/code/msg/data`与分页`count/pageIndex/pageSize/list`，CAS XML 与 OAuth token 保持旧协议输出边界。
- FB-14 jobs 复查：旧`app/jobs/examples.go`实际注册`SyncMysql2Ldap`、`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncDept`、`SyncJzg`、`ChangeContainer`、`NewContainerAccount`和`WannaT`九个任务；插件`backend/internal/service/jobs`已同名声明九个 managed job，并通过`plugin.Cron().RegisterCron`交给宿主`service/cron`的 gf/gcron 任务管理执行。旧`/api/v1/sysjob`列表/详情地址补为九个 managed job 的旧字段投影；旧`/api/v1/job/start/{id}`、`/api/v1/job/remove/{id}`和`sysjob`写操作保留兼容入口，但不绕开宿主任务管理直接操作插件自建 cron。
- FB-14 影响分析：本轮只改`apps/lina-plugins/linapro-uidentity-cas`插件后端和 OpenSpec 任务记录，未修改`apps/lina-core`核心框架、SQL、DAO/DO/Entity、前端、脚本或开发工具入口；插件目录无本地`AGENTS.md`。数据权限：旧后台管理和资源 CRUD 兼容路由复用宿主`Auth`、`Tenancy`、`Permission`中间件及插件 service 内`TenantFilter`，公开运行时路径通过一次性 ticket/token/challenge/state 自隔离。缓存一致性：无新增缓存，短期状态继续以插件 token 表为权威源。i18n：插件未启用`i18n.enabled`，本轮没有新增 API DTO 文档资源或语言包资源，旧响应壳只用于兼容。DI：新增`LegacyController`显式接收启动期同一个`uidentitySvc`实例，无新增接口型运行期依赖。测试：新增路由清单和旧字段投影单测，覆盖旧地址存在性、旧资源名映射、TGT/ST/用户字段投影和九个 jobs 投影。
- FB-12 根因记录：前序“完成”记录主要按旧路由和插件已知能力做总审计，未把旧项目服务端能力、插件实现、测试证据和排除理由做成可逐项复查的清单，也未显式记录两轮反向审计，导致无法证明`/Users/wanna/mine/ecoding/uidentity/admin`服务端能力已经完整覆盖。
- FB-12 已完成：新增`server-capability-audit.md`，以旧项目 Go 文件清单、router 注册、service/model 副作用、jobs registry、插件 REST 路由、通用资源和 cron 执行器为证据，逐项记录统一身份域管理资源、CAS/OAuth/runtime token、账号激活、用户自助、短信、导入、上传、配置、监控快照、作业和审计日志的插件映射；同时明确排除 GoAdmin 默认系统管理、页面/重定向壳、开发工具、全局 Prometheus 和未配置 Oracle/LDAP 外部执行器的理由。第一轮反向审计从旧项目能力映射到插件，第二轮反向审计从插件路由/资源/副作用回查旧项目和外部边界；除 FB-11 已修补缺口外未发现新的统一身份服务端缺口。
- FB-11 根因记录：旧`dto.AccountInsertReq.Generate`会把`groupIds`生成为 GORM `Groups`关联，旧`Account.Update`会显式增删`account_group`关系；插件迁移时只提供了独立`account-groups`通用资源和账号列表`groupIds`筛选，账号创建/更新请求本身未同步维护`account_group`关系，属于旧 admin 服务端写入副作用遗漏。
- FB-11 已完成：新增插件 service 层`uidentity_account_groups.go`，账号创建前按当前租户预校验`groupIds`，创建成功后同步写入`account_group`；账号更新在账号审计更新成功后替换账号分组关系，支持去重、忽略非正数、传空数组清空关系，并在不存在或跨租户组 ID 时拒绝且不破坏原有关联。本轮未新增 HTTP 路由、SQL、DAO 或缓存；数据权限通过`TenantFilter`校验组和删除当前租户关系；i18n 无新增资源；核心框架`apps/lina-core`未修改。新增`TestAccountResourceSyncsGroupIDs`覆盖创建同步、更新替换、非法组拒绝且保留旧关系、空数组清空关系。验证运行`GOWORK=off go test ./backend/internal/service/uidentity -run 'TestAccountResourceSyncsGroupIDs|TestResourceAccountAuditLifecycle' -count=1`、`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-10 根因记录：旧`app/admin/models/account.go`和`app/admin/models/account_details.go`依赖 GORM model hook 在账号、账号详情创建、更新和删除时自动写入`account_change_log`；插件迁移后只有`account-change-logs`通用资源和表结构，通用资源 CRUD、账号导入、管理员改密、账号激活、UnionID 绑定与微信换绑等服务端写路径不会触发旧 hook 的审计副作用。本轮修复限定在`linapro-uidentity-cas`插件内，不修改`apps/lina-core`核心框架。
- FB-10 已完成：新增插件 service 层账号审计 helper，账号和账号详情的创建、更新、删除显式写入`account_change_log`，账号自动创建详情行也写入详情创建日志；账号导入、账号投影写入、管理员重置密码、激活、登录态微信换绑和 UnionID 迁移绑定等直接写表路径统一改为审计 helper 或批量审计写入。账号审计记录中显式移除`passwordHash`，外部回调类路径使用 token 内租户或显式租户 helper 保持跨登录态安全边界。SQL/DAO：复用既有变更日志表、索引和生成代码，无新增 SQL 或 DAO；数据权限：后台接口继续经宿主`TenantFilter`约束，公开回调只按不可猜测的一次性 state/token 定位账号并使用 token 内租户写入插件自有表；缓存一致性：无新增缓存；DI：无新增运行期依赖；i18n：插件未启用`i18n.enabled`，无新增用户可见文案资源；测试新增`TestResourceAccountAuditLifecycle`覆盖账号和账号详情创建、更新、删除审计及密码 hash 脱敏。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- FB-8 根因记录：旧`app/cas/service/active.go`在激活微信扫码绑定和微信换绑成功后写入`account_active_log`，旧`app/cas/service/user.go`的`BindUnionID`会先解绑占用同一`unionId`的其他账号，再绑定当前账号并写入`type=1`审计日志。插件上一轮已覆盖激活、微信扫码绑定和 UnionID 绑定接口，但把`account_active_log`误判为页面/附属日志，且`BindUnionID`仍按冲突拒绝处理，与旧行为不一致。本轮修复限定在`linapro-uidentity-cas`插件内，不修改`apps/lina-core`核心框架。
- FB-8 已完成：新增插件自有表`plugin_linapro_uidentity_cas_account_active_log`、卸载 SQL、DAO/DO/Entity 和通用资源`account-active-logs`；激活直接绑定、激活微信扫码回调、登录态微信换绑回调成功后写入`type=0`审计日志，`BindUnionID`恢复旧行为，先在当前租户内集合化清理其他账号的同一`unionId`，再绑定当前账号并写入`type=1`审计日志。SQL 为 PostgreSQL-only 幂等建表和索引，索引覆盖租户、账号、微信和时间查询；数据权限通过`tenant_id`与既有`TenantFilter`约束，写操作只作用于当前租户；缓存一致性无新增缓存；DI 无新增运行期依赖；i18n 方面插件未配置`i18n.enabled: true`，无需补插件翻译资源；测试新增`TestRebindUnionIDToAccountMigratesExistingBinding`覆盖旧绑定迁移和审计日志。
- FB-9 根因记录：旧`app/jobs/change_container.go`不是纯外部 LDAP 壳，它会查询预计毕业时间为当前年的账号详情，将这些账号的本地`container_id`迁移到`xy`容器；LDAP 的`ChangeContainerID`属于外部目录边界，但数据库容器迁移是旧后端本地副作用。插件上一轮只实现了`WannaT`和`ContainerAccount/NewContainerAccount`，未覆盖`ChangeContainer`的本地迁移部分。
- FB-9 已完成：插件 cron Exec target 新增`ChangeContainer`，按当前租户查找`xy`容器后，使用`account_detail.graduated_at`子查询一次性更新匹配账号的`container_id`，不逐账号循环查询或更新；新增`idx_pluicas_detail_graduated_at`支撑`tenant_id + graduated_at + account_id`路径。外部 Oracle/LDAP 同步类作业继续保持结构化 unsupported 边界，不把外部目录依赖硬编码进插件或`apps/lina-core`。测试新增`TestUpdateGraduatingAccountContainer`覆盖迁移成功和非毕业账号不变；验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- FB-7 根因记录：旧`app/jobs/jobbase.go`在启动期清空`entry_id`、加载`status=2`的`sys_job`，并按`job_type=1`注册 HTTP GET 任务、按`job_type=2`注册 Exec 任务；旧`service/sys_job.go`的`StartJob`和`RemoveJob`会真实添加或移除 cron entry。插件上一轮只新增了`sys-jobs`、`job-logs`通用资源和`job_start`/`job_remove`外部动作入口，对`entry_id`做状态写入，没有持有 GoFrame cron 实例，也不会按 cron 表达式执行 HTTP/Exec 作业。本轮修复限定在`linapro-uidentity-cas`插件内，直接使用 GoFrame `gcron`，不修改`apps/lina-core`核心框架。
- FB-7 后续调整：旧 admin 作业真实执行能力在 FB-13 中改为通过 LinaPro 内建任务管理承载，插件不再维护自建`sys_job/job_log`表、本地`gcron`调度器或`job_start/job_remove`运行状态动作。
- FB-4 根因记录：旧实现的`account/unlockPassword`不属于页面能力，它清理 CAS 密码登录中按`cas:pwd:errnum:<number>`累计的短期密码错误次数；插件已实现账号状态、管理员改密和运行时密码校验，但未保存密码错误次数状态，也没有按账号列表解锁的后端入口。本轮补齐时仅修改`linapro-uidentity-cas`插件，使用插件自有 token 表承载短期错误次数状态，不修改`apps/lina-core`核心框架。
- FB-4 已完成：新增受宿主认证、租户和权限中间件治理的`POST /uidentity/accounts/password-unlocks`，按当前租户可见账号列表批量清理插件 token 表中的`cas:pwd:errnum:<number>`短期错误次数状态；CAS 密码登录、runtime token、OAuth 授权码密码校验和 UnionID 账号密码绑定统一接入错误次数检查，错误达到旧项目同等阈值后返回结构化`UIDENTITY_PASSWORD_FAILURES_LOCKED`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本和错误码暂不补插件翻译资源；缓存一致性：新增短期状态以插件 OAuth token 表为权威源并依赖`expired_at`失效，不引入进程内缓存；数据权限：管理员解锁接口进入宿主认证、租户和权限中间件，解锁前按账号表租户可见性批量过滤；SQL/DAO：复用既有 token 表和唯一 code 索引，无新增 SQL 或 DAO；API/后端 Go：新增 RESTful DTO、controller、service 接口和实现，运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口生成；开发工具跨平台：仅使用既有 Makefile 入口，无脚本变更；测试：新增纯函数单测覆盖旧 key 前缀、空账号过滤、去重和数量截断。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-1 已完成：本轮以源项目`/Users/wanna/mine/ecoding/uidentity/admin/app/**/router/*.go`实际路由为入口，重新审计旧后端能力与插件当前注册路由、资源定义、SQL 表和 service 实现。统一身份域后端能力已由`linapro-uidentity-cas`插件承载；默认系统管理、代码生成、页面/重定向壳和全局监控暴露明确不属于 CAS 插件后端迁移目标。
- FB-2 已完成：CAS TGT/ST 服务端票据链、运行时 token、账号激活、UnionID 绑定和用户自助后端接口均落在`linapro-uidentity-cas`插件内，未修改`apps/lina-core`核心框架；i18n 未启用插件资源治理，缓存无新增状态，数据权限继续通过宿主`TenantFilter`接入。
- 验证记录：已运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- 继续迁移记录：本轮补齐 FB-3 中账号导入检查/导入和短信发送子能力，导入按`number`在插件账号表内幂等新增/更新，短信发送使用插件 SMS 表和本地频控记录验证码；仍未完成 OAuth 授权码完整流程、LDAP 同步边界、文件上传、作业和监控类后端能力。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块），新增纯函数单测覆盖导入空行、生日日期归一化和短信类型校验。
- 继续迁移记录：本轮补齐 FB-3 中 OAuth 授权码运行时子能力，新增公开 RESTful 接口`POST /uidentity/oauth/authorization-codes`、`POST /uidentity/oauth/access-tokens`和`GET /uidentity/oauth/access-tokens/{accessToken}/user-info`，复用插件账号、应用、黑名单、OAuth token 和 OAuth log 表；授权码按唯一 code 单点查询并在事务内一次性消费，access token 按唯一 access 单点查询，未修改`apps/lina-core`核心框架。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态；数据权限：公开运行时接口仍通过插件`TenantFilter`上下文限制租户表数据，授权码和 access token 均绑定账号、应用和租户；SQL/DAO：复用既有 OAuth 表和唯一索引，无新增 SQL/DAO；DI：无新增运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_oauth_runtime_test.go`覆盖 URL-escaped client secret、redirect URI 校验和授权码 grant 类型。仍未完成 LDAP 同步边界、文件上传、作业和监控类后端能力。
- 继续迁移记录：本轮补齐 FB-3 中文件上传、健康检查、服务器监控、日志快照和旧 LDAP/外部文件/作业/外部监控动作边界。新增插件本地 RESTful 接口`POST /uidentity/legacy/uploads`、`GET /uidentity/legacy/health`、`GET /uidentity/legacy/server-monitor`、`GET /uidentity/legacy/log-snapshots`和`POST /uidentity/legacy/external-actions`；上传默认写入插件配置的本地目录并返回旧`uploadFile`兼容字段，监控使用本机进程/OS 投影，日志读取改为带上限的 tail snapshot，外部 LDAP/作业执行在未配置执行器时返回结构化 unsupported-flow 错误，避免把外部依赖硬编码进插件或污染`apps/lina-core`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态；数据权限：健康检查公开且不返回业务数据，上传/监控/日志/外部动作接口进入宿主认证、租户和权限中间件，文件和日志仅访问插件配置边界内的本地资源；SQL/DAO：无新增 SQL/DAO，复用插件现有表；DI：新增`gopsutil/v4`作为插件本地监控依赖，无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_legacy_ops_test.go`覆盖 base64 payload 拆分、安全文件名和日志 tail 上限逻辑。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）。
- 继续迁移记录：本轮补齐旧 admin 静态配置发现接口和 CAS XML `serviceValidate`返回形态。新增插件本地 RESTful 接口`GET /uidentity/legacy/config/cas`、`GET /uidentity/legacy/config/ldap`、`GET /uidentity/legacy/config/oauth`、`GET /uidentity/legacy/config/token`和`POST /uidentity/legacy/cas/service-validations.xml`；配置发现只读取插件作用域配置并提供旧结构字段，LDAP 默认只暴露 unsupported 元数据而不启动执行器；CAS XML 接口复用既有 ST 消费、账号访问和委托授权校验，成功时输出`cas:authenticationSuccess`，失败时按旧协议输出`INVALID_TICKET` XML，而不是 JSON 错误。根因判断：前几轮已迁入运行时 ticket 和外部动作边界，但旧 admin 的`/api/v1/config/*`与`/sso/serviceValidate`XML 协议仍缺少插件侧替代入口。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态，配置读取复用宿主启动期注入的插件配置服务；数据权限：配置发现接口进入宿主认证、租户和权限中间件，CAS XML 为运行时公开协议且通过一次性 ST 自隔离，不返回未绑定 ticket 的业务数据；SQL/DAO：无新增 SQL/DAO，复用既有 token、账号、应用和授权表；DI：无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：未修改脚本或工具入口；测试：新增`uidentity_legacy_cas_xml_test.go`和`uidentity_legacy_config_test.go`覆盖 XML 成功/失败渲染、配置默认值和插件作用域覆盖读取。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和插件子仓库`git diff --check`。
- 继续迁移记录更正：插件未上线且不存在兼容包袱，前序插件自建`plugin_linapro_uidentity_cas_sys_job`、`plugin_linapro_uidentity_cas_job_log`、`sys-jobs/job-logs`通用资源和`job_start/job_remove`动作已在 FB-13 中清理；旧作业定义和执行日志改由 LinaPro 宿主任务管理的声明、调度和日志能力承载。
- 继续迁移记录：本轮补齐旧 admin 微信扫码登录后端状态链。新增插件本地 RESTful 接口`POST /uidentity/cas/wechat-login-qrs`、`POST /uidentity/cas/wechat-login-callbacks`和`GET /uidentity/cas/wechat-login-qrs/{state}/result`；扫码 state、回调结果和轮询结果复用插件 OAuth token 存储，已绑定`unionId`时签发 CAS TGT/ST，未绑定时创建既有 UnionID 绑定 challenge，未配置真实微信 OAuth 解析器时记录`unsupported`状态而不是把第三方依赖硬编码进插件或`apps/lina-core`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无新增缓存或快照状态，扫码状态以数据库 token 表为权威源并随过期时间失效；数据权限：运行时公开接口只通过一次性 state/ticket/challenge 自隔离，账号、应用、绑定和登录日志读写继续经`TenantFilter`接入；SQL/DAO：无新增 SQL/DAO，复用 OAuth token、账号详情、账号、应用和日志表；DI：无新增运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：本轮运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口生成，修复生成器追加的默认构造以保留插件显式依赖注入；测试：新增`uidentity_wechat_runtime_test.go`覆盖授权 URL 装配和扫码结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`和插件子仓库`git diff --check`。FB-3 具名外围能力已完成，FB-1 继续保留全量兼容性总审计。
- 最终总审计记录：源项目统一身份域管理资源`account`、`account_details`、`units`、`containers`、`groups`、`applications`、`account_unit`、`account_app_role`、`account_app_blacklist`、`group_app_blacklist`、`pass_ruler`、`sms`、`cas_login_log`、`oauth_log`、`oauthtoken`、`account_change_log`和`stat`已映射到插件通用资源`/uidentity/{resource}`与`/uidentity/stats`；旧`app/jobs`注册表九个任务由插件声明为 LinaPro 内建任务管理 handler，作业调度与执行日志由宿主任务管理承载，不再作为插件自有资源。账号改密、导入检查/导入、短信发送、CAS TGT/ST、CAS XML`serviceValidate`、runtime token、OAuth authorization code/access token/user-info、账号激活、UnionID 查询/绑定、用户自助、微信扫码登录、上传、健康检查、服务器监控、日志快照、静态配置发现和外部动作边界均存在插件 RESTful 入口。源项目`sys_user`、`sys_role`、`sys_menu`、`sys_dept`、`sys_dict`、`sys_config`、`sys_post`、`sys_api`、登录/刷新/登出、Swagger、WebSocket、静态文件和验证码属于 LinaPro 宿主默认系统管理、页面/开发工具或通用工作台能力，不复制进 CAS 插件；`other/gen_router.go`的代码生成、数据库表/列浏览和菜单/API 生成属于开发工具链，不是统一身份运行时后端；`oauth/login`、`oauth/auth`、`cas/login`、`sso/login`、`bindUnionIDCallBack`等 HTML 或重定向壳由插件已有 RESTful 运行时接口替代，不迁移页面；旧`/metrics`是全局 Prometheus 暴露，LinaPro 已有独立`linapro-monitor-server`插件承载通用监控，CAS 插件仅保留旧`server-monitor`投影。规则影响：本轮只更新 OpenSpec 任务记录，无代码、SQL、API、i18n 资源、缓存、数据权限、DI 或开发工具跨平台新增影响；验证使用源路由`rg`审计、插件路由/资源/SQL 静态检索、`openspec validate implement-uidentity-cas-backend --strict`和当前工作区 Go 测试门禁。
- FB-13 根因记录：旧`/Users/wanna/mine/ecoding/uidentity/admin/app/jobs/examples.go`注册了`SyncMysql2Ldap`、`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncDept`、`SyncJzg`、`ChangeContainer`、`NewContainerAccount`和`WannaT`九个任务；前序插件实现曾自行创建`plugin_linapro_uidentity_cas_sys_job/job_log`、本地`service/cron`调度器和`job_start/job_remove`动作，但 LinaPro 本身已经有任务管理、调度和日志能力，插件只需要声明任务 handler 并执行业务逻辑。
- FB-13 已完成：`linapro-uidentity-cas`插件通过`plugin.Cron().RegisterCron(..., registerManagedJobs)`向宿主注册九个 managed job handler，名称严格保持旧注册表值；删除插件本地`backend/internal/service/cron`组件、`plugin_linapro_uidentity_cas_sys_job/job_log`安装/卸载 SQL、DAO/DO/Entity、`sys-jobs/job-logs`通用资源、`job_start/job_remove`外部动作和 DAO 生成配置。业务 handler 已迁入插件`backend/internal/service/jobs`：`SyncDept`分页同步 Oracle`T_ORG_DEPT`到插件`unit`，`SyncStudent`、`SyncStudentYJS`、`SyncStudentWJ`、`SyncJzg`分页读取旧 Oracle 源并批量装配账号、详情、单位、手机号占用后写入插件账号、详情和分组关系，`SyncMysql2Ldap`分页读取插件账号并按旧 DN/属性规则同步到配置的 LDAP，`ChangeContainer`和`NewContainerAccount`保留集合化本地数据库更新，`WannaT`保留轻量日志探针。配置缺失时 Oracle/LDAP handler 返回结构化`UIDENTITY_JOB_EXECUTOR_UNSUPPORTED`，由宿主任务管理记录调度结果。
- FB-13 影响分析：本轮不修改`apps/lina-core`核心框架；新增运行期依赖为插件 module 内`github.com/go-ldap/ldap/v3`和`github.com/gogf/gf/contrib/drivers/oracle/v2`，由插件`jobs`服务直接使用，owner 为插件 handler，创建位置为插件`backend/plugin.go`注册回调内构造`jobs.Service`，传递路径为宿主 source-plugin registrar -> plugin services -> jobs service，不引入缓存敏感共享实例；`apps/lina-plugins`聚合模块同步`go.mod/go.sum`依赖校验，避免插件集合编译时缺少 checksum。SQL 删除未上线插件自建 job 表，安装/卸载 SQL 仍为 PostgreSQL 幂等脚本，DAO 生成配置移除对应表；数据权限方面后台任务按宿主任务触发上下文解析租户并只写插件自有租户表，外部 LDAP/Oracle 是配置化边界，不暴露 HTTP 读写接口；缓存一致性无新增缓存或快照状态；i18n 方面插件未启用`i18n.enabled`，仅删除过期 API 文档源文本中的 job action 描述并新增结构化错误码，不需要插件翻译资源；开发工具跨平台仅更新插件`backend/hack/config.yaml`的 DAO 表清单，无脚本或平台入口变更；测试新增`jobs_register_test.go`覆盖九个任务注册、旧状态映射和字段归一化，静态检索确认无旧自建 job 表、`service/cron`和`job_start/job_remove`残留。
- FB-13 验证记录：已运行`GOWORK=off go test ./backend/internal/service/jobs -count=1`、`GOWORK=off go test ./backend/internal/service/uidentity -count=1`、`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- FB-5 根因记录：旧 admin 的`/api/v1/user/changeWechatQr`和`/api/v1/user/changeWechatState`不是页面能力；它们为已登录用户创建短期微信换绑 state，经微信扫码回调绑定`unionId`到当前账号，再按 state 查询换绑结果。插件已有解绑、UnionID 绑定和微信扫码登录状态链，但缺少“已登录用户换绑微信”的独立后端 state 链。本轮补齐仍限定在`linapro-uidentity-cas`插件内，复用插件 OAuth token 表承载短期 state，不修改`apps/lina-core`核心框架。
- FB-5 已完成：新增受宿主认证、租户和权限中间件治理的`POST /uidentity/users/{number}/wechat-rebind-states`与`GET /uidentity/users/{number}/wechat-rebind-states/{state}`，以及公开回调边界`POST /uidentity/users/wechat-rebind-callbacks`；换绑 state 复用插件 OAuth token 表，按`wechat_rebind:<state>`单点索引读写，`unionId`存在时绑定到 state 内账号并返回成功状态，未配置真实微信 OAuth 解析器时记录结构化`UIDENTITY_EXTERNAL_FLOW_UNSUPPORTED`。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无进程内缓存，短期 state 以数据库 token 表为权威源并通过`expired_at`失效；数据权限：创建/查询接口进入宿主认证、租户和权限中间件，查询时校验 state 归属账号，公开回调属于外部 OAuth 回调例外，仅凭不可猜测的一次性 state 单点定位 token，并使用 token 内租户和账号写入插件自有账号详情，避免依赖登录态同时不做列表级放开；SQL/DAO：无新增 SQL/DAO，复用既有 OAuth token 和账号详情表；DI：无新增接口型运行期依赖；开发工具跨平台：运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口；测试：新增纯函数单测覆盖换绑授权 URL 装配和换绑结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）、`openspec validate implement-uidentity-cas-backend --strict`、主仓库和插件子仓库`git diff --check`。
- 最终复核补充：FB-5 补齐后，旧 admin 的登录态用户微信换绑 state 链已由插件 RESTful 后端入口覆盖；重新以源项目路由、插件路由、通用资源、Service 实现和 SQL 表核对，未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源和全局 Prometheus 能力继续按 LinaPro 宿主默认能力或独立插件边界处理，不复制进 CAS 插件。
- FB-6 根因记录：旧 admin 的`/api/v1/activate/wechatQr`和`/api/v1/activate/wechatScan`不是页面能力；`wechatQr`复用激活`uuid`生成外部微信绑定 URL，`wechatScan`在公开回调中用`code/state`解析`unionId`并绑定到激活账号，最后由`activate/state`轮询账号是否已激活。插件当前已有`PUT /uidentity/activations/{challengeId}/wechat`直接`unionId`绑定，但缺少“生成授权 state + 公开回调完成绑定”的后端链。本轮补齐仍限定在`linapro-uidentity-cas`插件内，复用插件 OAuth token 表承载激活 challenge，不修改`apps/lina-core`核心框架。
- FB-6 已完成：新增公开 RESTful 接口`POST /uidentity/activations/{challengeId}/wechat-states`与`POST /uidentity/activations/wechat-callbacks`，并扩展`GET /uidentity/activations/{challengeId}/state`返回`wechatStatus`、`redirectUrl`、`errorCode`和`message`。激活微信 state 继续复用插件 OAuth token 表中的 activation challenge，授权 URL 由插件作用域配置`runtime.activationWechatAuthorizeUrl`装配，回调成功时按 token 内租户和账号绑定`unionId`并激活账号，未接入外部微信 code 解析器时记录结构化`UIDENTITY_EXTERNAL_FLOW_UNSUPPORTED`供轮询端识别。i18n：插件未启用`i18n.enabled`，新增 API 文档源文本不要求补插件 apidoc 翻译资源；缓存一致性：无进程内缓存，短期 state 以数据库 token 表为权威源并通过`expired_at`失效；数据权限：公开激活接口属于一次性 activation challenge 自隔离例外，回调不依赖登录态但仅能按不可猜测 state 定位单条 token，并使用 token 内租户写入插件自有账号详情和账号状态；SQL/DAO：无新增 SQL/DAO，复用 OAuth token、账号和账号详情表；DI：无新增接口型运行期依赖，继续复用启动期注入的`BizCtx`、插件`Config`和`TenantFilter`服务；开发工具跨平台：运行`make ctrl p=linapro-uidentity-cas`同步 GoFrame 控制器接口；测试：新增纯函数单测覆盖激活微信授权 URL 装配和结果投影。验证补充运行`GOWORK=off go test ./... -count=1`（插件独立模块）、`GOWORK=off go test ./... -count=1`（`apps/lina-plugins`聚合模块）和`openspec validate implement-uidentity-cas-backend --strict`。
- 最终复核补充：FB-6 补齐后，旧 admin 的账号激活微信扫码绑定 state 链已由插件 RESTful 后端入口覆盖；重新以源项目路由、插件路由、通用资源、Service 实现和 SQL 表核对，当前未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源和全局 Prometheus 能力继续按 LinaPro 宿主默认能力、页面边界或独立插件边界处理，不复制进 CAS 插件。
- 最终复核补充：FB-10 补齐后，旧 admin 统一身份域的资源表、CAS/OAuth/token/激活/自助/微信扫码、短信、导入、上传、配置发现、监控快照、作业调度、`account_active_log`和`account_change_log`审计副作用均已由`linapro-uidentity-cas`插件侧后端承载；本轮未发现新的统一身份服务端功能缺口。此前明确排除的页面/重定向壳、GoAdmin 默认系统管理、代码生成、Swagger、WebSocket、验证码、静态资源、全局 Prometheus 和未配置外部 Oracle/LDAP 执行器继续按 LinaPro 宿主、页面、开发工具、独立监控插件或结构化 unsupported 边界处理，不复制进 CAS 插件。
