## 1. 基线调查与风险清单

- [x] 1.1 扫描宿主和源码插件生产 Go 文件中的关键服务构造调用，按宿主 Controller、宿主 Service、Middleware、`pkg/pluginservice/*`、源码插件 Controller/Service、源码插件 `backend/plugin.go`、WASM host service 分类输出清单
- [x] 1.2 标记高风险缓存一致性路径，至少覆盖 auth/session、role/datascope、config runtime params、i18n runtime bundle、plugin runtime cache、cachecoord/kvcache/locker、notify、source route bindings 和 plugin enabled snapshot
- [x] 1.3 为每个高风险路径记录权威数据源、共享实例要求、当前隐式构造点、可能的不一致结果、故障策略和建议迁移顺序
- [x] 1.4 明确允许局部构造的无状态 helper/纯查询服务候选，并为每个候选记录无缓存、无订阅、无 session/token、无插件状态和无跨实例协调影响的理由

## 2. 规范与审查标准

- [x] 2.1 更新 `AGENTS.md` 后端代码规范，新增显式依赖注入、禁止隐式关键服务构造、缓存敏感服务共享实例和测试豁免边界
- [x] 2.2 更新 `.agents/skills/lina-review/SKILL.md`，新增后端依赖注入审查项，覆盖 Controller、Service、Middleware、源码插件、`pkg/pluginservice/*`、WASM host service 和缓存敏感实例来源
- [x] 2.3 在审查标准中要求每次后端变更明确记录 i18n、缓存一致性和数据权限影响；无影响时也必须说明
- [x] 2.4 增加静态扫描脚本或等价治理验证，识别生产路径中对关键服务 `New()` 的新增违规调用，并维护启动构造边界、测试文件和无状态豁免允许列表
- [x] 2.5 为静态扫描脚本或治理验证添加测试/示例，覆盖违规调用、允许的启动边界、允许的测试文件和允许的无状态 helper

## 3. 宿主核心依赖注入改造

- [x] 3.1 改造 `auth.Service` 构造函数，显式接收 config、plugin、orgcap、role、tenant、session store、token state 等依赖，移除内部关键服务隐式构造
- [x] 3.2 改造 `middleware.Service` 构造函数，显式接收 auth、bizctx、config、i18n、plugin、role、tenant 等依赖，并确保中间件使用同一套运行期实例
- [x] 3.3 改造 role、menu、user、dict、file、usermsg、notify、sysconfig、i18n 等宿主服务构造入口，移除内部对缓存敏感依赖的隐式构造
- [x] 3.4 改造 datascope、tenantcap、orgcap 等能力服务构造入口，确保插件 enablement reader、role service 和 org capability 通过显式依赖传递
- [x] 3.5 更新 `cmd_http_runtime.go` 现有 runtime 结构，使其持有需要共享的宿主服务实例和共享后端，不新增独立组装层
- [x] 3.6 更新 cron、job management、plugin runtime、cachecoord、kvcache、locker、session coordination 的启动注入路径，确保单机/集群模式共享后端选择仍由现有 `cluster.enabled` 和 `coordination.Service` 控制

## 4. 宿主 Controller 与路由绑定改造

- [x] 4.1 改造宿主所有 Controller `NewV1` 构造函数，使其通过逐项参数接收接口型服务依赖，禁止使用聚合依赖结构体整体传递
- [x] 4.2 更新 `cmd_http_routes.go`，在路由绑定前从现有 runtime 共享实例构造 Controller，并禁止 Controller 构造函数内部创建关键服务
- [x] 4.3 更新 health、sysinfo、plugin、job、joblog、jobgroup、jobhandler 等已支持部分注入的控制器，使构造风格与其他控制器一致
- [x] 4.4 为宿主路由构造添加单元测试或静态断言，确认 Controller 与 Middleware 引用同一 fake auth/session/role/plugin/config/i18n 实例

## 5. 插件宿主服务与源码插件改造

- [x] 5.1 为 `pluginhost` 的 HTTP/Cron registrar 或等价上下文新增宿主发布服务目录，暴露源码插件可用的稳定 `pkg/pluginservice/*` 适配器
- [x] 5.2 改造 `pkg/pluginservice/auth/session/bizctx/config/i18n/notify/pluginstate/route` 等适配器，使生产路径由宿主传入内部依赖并统一构造
- [x] 5.3 迁移源码插件 `backend/plugin.go` 路由、全局中间件和 Cron 注册回调，使插件 Controller/Service 从 registrar 获取宿主发布依赖
- [x] 5.4 改造源码插件 Controller 构造函数，移除控制器内部对插件 service 或宿主 pluginservice adapter 的隐式构造
- [x] 5.5 改造源码插件 Service 构造函数，使其显式接收 bizctx、notify、config、i18n、auth、session、pluginstate、tenantfilter 等宿主能力依赖
- [x] 5.6 为源码插件添加编译期和单元测试验证，确认插件注册路径不再调用无参宿主 service adapter 构造函数

## 6. WASM Host Service 与动态插件路径

- [x] 6.1 梳理 `internal/service/plugin/internal/wasm/hostfn_service_*` 包级默认实例和 `ConfigureXxxHostService` 调用点，标记生产启动路径与测试恢复路径
- [x] 6.2 改造 cache、lock、notify、storage、config、runtime 等 WASM host service，使启动期显式注入共享宿主服务或共享后端
- [x] 6.3 确保动态插件 host service handler 不在每次调用中创建独立 cache、lock、notify、config、plugin 或 runtime 服务实例
- [x] 6.4 添加 WASM host service 单元测试，使用 fake shared backend 验证多次 host service 调用复用同一实例和同一租户/插件作用域

## 7. 测试与回归验证

- [x] 7.1 运行宿主核心服务单元测试，至少覆盖 auth、session、middleware、role、datascope、config、i18n、plugin、cachecoord、kvcache、locker、cron
- [x] 7.2 运行宿主 Controller 和 cmd 路由相关单元测试，确认路由绑定、公开 API 和系统诊断行为不变
- [x] 7.3 运行所有源码插件后端单元测试，至少覆盖 org-center、multi-tenant、content-notice、monitor-online、monitor-loginlog、monitor-operlog、monitor-server、demo-control、plugin-demo-source 和 plugin-demo-dynamic
- [x] 7.4 在 Redis 可用环境下运行 coordination/cache/session/plugin runtime 相关集成测试，确认集群模式共享后端和失效策略未退化
- [x] 7.5 如果实现影响登录、在线用户、插件管理、系统信息或运行时配置页面的用户可观察行为，按 `lina-e2e` 规范新增或更新对应 TC 并运行相关 E2E；若无前端可见变化，在验证结论中说明
- [x] 7.6 运行新增静态扫描或治理验证，确认生产路径不存在新增关键服务隐式构造
- [x] 7.7 运行 `openspec validate explicit-service-dependency-injection --strict`
- [x] 7.8 运行 `git diff --check -- openspec/changes/explicit-service-dependency-injection AGENTS.md .agents/skills/lina-review/SKILL.md apps/lina-core apps/lina-plugins`
- [x] 7.9 完成实现后调用 `lina-review`，重点审查依赖注入合规性、缓存一致性、i18n 影响、数据权限影响和测试覆盖

## Feedback

- [x] **FB-1**: `file.Service` 与 `jobmgmt.Service` 在构造函数内部创建 `datascope.Service`，数据权限依赖未显式注入
- [x] **FB-2**: WASM host service 配置入口在 `nil` 依赖时静默创建默认实例，掩盖共享运行期依赖注入错误
- [x] **FB-3**: `cmd_http_plugin_services.go` 在 HTTP 初始化包中实现源码插件宿主服务目录，插件 host service 发布边界未与启动编排解耦
- [x] **FB-4**: `content-notice` 控制器同时暴露 `NewV1` 与 `NewControllerV1`，插件路由绑定绕过统一接口构造入口
- [x] **FB-5**: `plugin.New` 在构造函数内部创建运行期关键依赖，且显式注入参数不得使用额外结构体封装
- [x] **FB-6**: `plugin_startup_consistency.go` 在启动一致性校验中临时创建 `tenantcap.Service` 与 `bizctx.Service`
- [x] **FB-7**: `monitor-server` 配置加载同时暴露 `Load` 与 `LoadWithReader`，且 `Load` 内部隐式创建宿主配置服务
- [x] **FB-8**: `multi-tenant` auth 控制器同时暴露 `NewV1` 与 `NewControllerV1`，插件路由绑定绕过统一接口构造入口
- [x] **FB-9**: `multi-tenant` platform 与 tenant 控制器同时暴露 `NewV1` 与 `NewControllerV1`，插件路由绑定存在绕过统一接口构造入口的风险
- [x] **FB-10**: `role.New` 构造函数内部创建 `datascope.Service`，数据权限依赖未显式注入
- [x] **FB-11**: `plugin-demo-source` 卸载回调通过 `demosvc.New(nil)` 构造服务，依赖形状与实际清理职责不一致
- [x] **FB-12**: `pkg/sourceupgrade.New` 在 facade 初始化中隐式创建配置、上下文、缓存协调、i18n、session 与插件服务图
- [x] **FB-13**: `plugin-demo-source` demo 控制器同时暴露 `NewV1` 与 `NewControllerV1`，插件路由绑定绕过统一接口构造入口
- [x] **FB-14**: `user.New` 构造函数内部创建 `datascope.Service`，用户管理数据权限依赖未复用启动期共享实例
- [x] **FB-15**: `pluginservice/tenantfilter` 通过包级全局配置读取宿主 `bizctx`，源码插件租户过滤依赖未通过 `HostServices` 显式注入
- [x] **FB-16**: `pkg/pluginservice/*` 中纯契约源码文件分散且单薄，插件宿主服务契约未统一收敛到 `contract` 组件
- [x] **FB-17**: `ConfigureWasmHostServices` 与 `pluginhostservices.New` 使用聚合依赖结构体传参，显式注入参数未直接展开
- [x] **FB-18**: 项目规范、OpenSpec 规范和 `lina-review` 未明确禁止通过聚合结构体整体传递接口型运行期依赖
- [x] **FB-19**: `role_new.go` 被误追加无参 `NewV1` 和空 `ControllerV1`，导致角色控制器重复声明且绕过显式依赖注入
- [x] **FB-20**: `internal/service` 运行时初始化入口在依赖缺失时直接 `panic`，未通过显式 `error` 返回暴露初始化失败
- [x] **FB-21**: 源码插件注册与 registrar API 仍在内部 `panic`，未将错误返回给最上层入口决策
- [x] **FB-22**: `pkg/pluginhost/pluginhost_source_plugin.go` 文件过长，源码插件注册、输入包装、descriptor 与 snapshot 职责混杂
- [x] **FB-23**: `multi-tenant` 生命周期预检查通过 `newTenantService(nil)` 构造半初始化服务，租户计数依赖未按实际职责窄化注入
- [x] **FB-24**: panic allowlist 仍引用拆分前文件路径，导致 `TestProductionPanicsMatchAllowlist` 误报新增与过期 panic 例外

## Feedback Verification

- [x] 2026-05-16: FB-24 验证通过：`cmd_panic_allowlist_test.go` 中 `configureRuntimeCacheDomain` 与 `MustNormalizeHostServiceSpecs` 的 panic allowlist 路径已同步为当前实现文件 `pluginruntimecache_controller.go` 与 `hostservice_validation.go`，修复文件拆分后扫描器按路径+函数名匹配产生的新增/过期例外误报；未新增、删除或改变任何生产 panic 行为。执行通过：`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`cd apps/lina-core && go test ./internal/service/pluginruntimecache ./pkg/pluginbridge/hostservice -count=1`。i18n 影响：本轮仅更新治理测试 allowlist 与 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：未修改缓存域注册逻辑、失效范围、共享修订号或跨实例协调机制，仅同步静态治理测试路径。数据权限影响：不新增 REST/API 数据操作面或数据访问路径。E2E 影响：无前端可见行为变化，不新增 E2E；使用定向后端单元测试和治理验证覆盖。
- [x] 2026-05-16: FB-23 验证通过：`multi-tenant` 生命周期预检查从依赖完整 `tenant.Service` 改为依赖只包含 `CountExisting(ctx)` 的 `lifecycleprecondition.TenantCounter`，新增零值可用的 `tenant.ExistingCounter` 承担非删除租户计数；删除 `backend/plugin.go` 中 `newTenantService(nil)` 半初始化构造路径，`newLifecyclePrecondition` 显式注入 `tenant.ExistingCounter{}`，HTTP 路由路径继续通过 `hostServices.PluginLifecycle()` 注入完整 tenant/tenantplugin 运行期依赖，并复用同一个 `resolverConfigSvc`，未增加插件注册文件的依赖治理基线构造计数。执行通过：`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend/... -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/multi-tenant/backend/plugin.go:apps/lina-plugins/multi-tenant/backend/internal/service/lifecycleprecondition:apps/lina-plugins/multi-tenant/backend/internal/service/tenant node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-plugins/multi-tenant/backend/plugin.go apps/lina-plugins/multi-tenant/backend/internal/service/lifecycleprecondition/lifecycleprecondition.go apps/lina-plugins/multi-tenant/backend/internal/service/lifecycleprecondition/lifecycleprecondition_test.go apps/lina-plugins/multi-tenant/backend/internal/service/tenant/tenant.go apps/lina-plugins/multi-tenant/backend/internal/service/tenant/tenant_counter.go openspec/changes/explicit-service-dependency-injection/tasks.md`。根仓库直接在插件模块执行 `go test` 会被当前 `go.work` 边界阻断，因此插件测试按既有验证方式使用 `GOWORK=off` 在插件模块内执行。i18n 影响：本轮仅调整后端内部依赖形状和测试，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存域、失效范围或跨实例协调机制；路由路径继续使用宿主发布的共享插件生命周期服务，生命周期预检查仅执行数据库计数读取。数据权限影响：本轮不新增 REST/API 数据操作面，不改变租户 CRUD、租户插件启停或数据权限过滤语义。E2E 影响：无前端可见行为变化，不新增 E2E；使用后端全量测试、依赖治理扫描、OpenSpec 校验和 diff check 覆盖。
- [x] 2026-05-16: FB-22 验证通过：将 `apps/lina-core/pkg/pluginhost/pluginhost_source_plugin.go` 从 1324 行拆分为 207 行的源码插件核心定义，并新增职责明确的 `pluginhost_source_plugin_callbacks.go`、`pluginhost_source_plugin_inputs.go`、`pluginhost_source_plugin_descriptors.go`、`pluginhost_source_plugin_manifest.go`、`pluginhost_source_plugin_registration.go`、`pluginhost_source_plugin_values.go`；仅移动同包类型、构造函数、注册方法、descriptor、manifest snapshot 和 map helper，未改变公开 API、错误返回语义、缓存、数据权限或 i18n 资源。额外验证 `internal/cmd` 时发现当前工作区既有 `apps/lina-plugins/plugin-demo-source/backend/plugin.go:mustRegisterSourceLifecycle` 非顶层 panic 不符合 FB-21 治理规则，已改为 `registerLifecycleDebugHandlers` / `registerLifecycleEventDebugHooks` 返回 `error`，由包级 `init` 顶层显式选择 `panic`，并同步 panic allowlist 计数。执行通过：`cd apps/lina-core && go test ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/service/plugin ./internal/service/i18n ./internal/service/plugin/internal/catalog -count=1`、`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、临时 workspace 覆盖 `plugin-demo-source` 执行 `go test ./backend/... -count=1`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/pkg/pluginhost openspec/changes/explicit-service-dependency-injection/tasks.md`。i18n 影响：纯 Go 文件结构拆分和错误传播收敛，不新增或修改用户可见文案、manifest i18n 或 apidoc i18n。缓存一致性影响：未新增缓存域、失效范围或跨实例协调机制。数据权限影响：不新增 REST/API 数据操作面或查询路径。E2E 影响：无前端可见行为变化，不新增 E2E。
- [x] 2026-05-16: FB-21 验证通过：`pluginhost.RegisterSourcePlugin`、`SourcePluginLifecycle/Hooks/HTTP/Cron/Governance` 注册 facade、`sourcePlugin` 内部注册方法、callback execution mode 校验、`GlobalMiddlewareRegistrar.Bind` 与 `CronRegistrar.AddWithMetadata` 均改为返回 `error`；官方源码插件包级 `init` 显式处理注册错误并作为顶层静态注册入口选择 `panic`，运行期 HTTP/Cron/Hook/中间件回调缺少宿主发布依赖时改为返回 `gerror`；panic allowlist 删除 `pluginhost` API 内部 panic 例外与运行期回调 panic 例外，仅保留源码插件 `init` 顶层失败退出条目。已更新 `AGENTS.md`、`.agents/skills/lina-review/SKILL.md`、OpenSpec 增量规范，明确初始化与注册 API 必须返回错误给调用方，是否 `panic` 只能由调用栈最上层决定。验证通过：`cd apps/lina-core && go test ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/service/i18n ./internal/service/plugin/internal/catalog -count=1`；根 `go.work` 未纳入源码插件模块，直接执行插件测试会被工作区边界阻断，因此官方插件后端使用临时 workspace 纳入 `apps/lina-core` 与对应插件模块后验证通过，覆盖 `content-notice`、`demo-control`、`monitor-loginlog`、`monitor-online`、`monitor-operlog`、`monitor-server`、`org-center`、`plugin-demo-source`、`multi-tenant` 的 `go test ./backend/... -count=1`，临时目录已清理。静态扫描 `rg -n "\\bpanic\\s*\\(" apps/lina-core/pkg/pluginhost apps/lina-plugins -g '*.go' -g '!**/*_test.go'` 确认 `pkg/pluginhost` 生产路径无 panic，剩余源码插件 panic 均位于包级 `init` 顶层失败退出；注册 API 调用扫描确认新增 `error` 返回值均已显式处理，测试路径也不再静默丢弃注册错误。执行 `cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1` 初次被同一工作区既有动态插件生命周期改动中 `plugin_lifecycle.go` 缺少 `pluginbridge` 导入阻断，已补齐导入后通过。i18n 影响：本轮仅调整后端错误传播与治理文档，不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存域、失效范围或跨实例协调机制；源码插件继续使用启动期发布的共享 host services。数据权限影响：本轮不新增 REST/API 数据操作面；源码插件租户过滤等数据权限依赖来源不变，仅错误传播方式从 panic 改为 error。E2E 影响：无前端可见行为变化，不新增 E2E；使用后端编译测试、panic allowlist、OpenSpec 校验和静态扫描覆盖。
- [x] 2026-05-15: FB-20 验证通过：`plugin.New`、`sysinfo.New`、`hostlock.New`、`jobmgmt.NewScheduler`/`internal/shellexec.New`、`plugin.ConfigureWasmHostServices` 与 WASM `ConfigureCache/Config/Lock/Notify/StorageHostService` 均改为显式返回 `error`，HTTP 启动装配逐项处理并向上返回初始化失败；同时将相邻的 `pkg/sourceupgrade.New`、`pkg/pluginservice/tenantfilter.New` 与 `pluginhostservices.New` 收敛为 error-returning，避免继续保留运行时 `New` panic 例外。已更新 `AGENTS.md` 与 `.agents/skills/lina-review/SKILL.md`，明确禁止运行时初始化入口用 `panic` 处理可预期错误，仅保留 `Must*`、静态打包配置/编译期插件注册契约损坏、进程入口最终兜底和 panic rethrow 等例外，并从 panic allowlist 移除本轮改造入口。执行通过：`cd apps/lina-core && go test ./internal/service/sysinfo ./internal/service/hostlock ./internal/service/jobmgmt/internal/shellexec ./pkg/sourceupgrade ./pkg/pluginservice/tenantfilter -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/service/jobmgmt -count=1`、`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、临时 `go.work` 覆盖 `monitor-loginlog` 与 `monitor-operlog` 的 tenant 单元测试、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- AGENTS.md .agents/skills/lina-review/SKILL.md apps/lina-core/internal/service/plugin apps/lina-core/internal/service/sysinfo apps/lina-core/internal/service/hostlock apps/lina-core/internal/service/jobmgmt apps/lina-core/internal/cmd apps/lina-core/pkg/sourceupgrade apps/lina-core/pkg/pluginservice/tenantfilter apps/lina-core/internal/service/pluginhostservices apps/lina-plugins/monitor-loginlog/backend/internal/service/loginlog/loginlog_tenant_test.go apps/lina-plugins/monitor-operlog/backend/internal/service/operlog/operlog_tenant_test.go openspec/changes/explicit-service-dependency-injection/tasks.md`。静态扫描 `rg -n "\\bpanic\\s*\\(" apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/sysinfo/sysinfo.go apps/lina-core/internal/service/hostlock/hostlock.go apps/lina-core/internal/service/jobmgmt/internal/shellexec/shellexec.go apps/lina-core/internal/service/plugin/internal/wasm apps/lina-core/pkg/sourceupgrade apps/lina-core/pkg/pluginservice/tenantfilter -g '*.go' -g '!**/*_test.go'` 无输出。`cd apps/lina-core && go test ./internal/service/plugin -count=1` 当前被同一工作区既有 `pkg/pluginhost/lifecycle_guard.go` 删除/替换后的旧测试引用阻断，失败点为 `plugin_lifecycle_test.go` 仍引用 `RegisterLifecycleGuard`、`GuardHookCanDisable`、`ensureLifecycleGuardAllowed` 等旧 API；该阻断与本轮初始化错误返回改造无直接关系，相关 panic allowlist 已按新的 `BeforeTenantDelete` 编译期注册契约更新。i18n 影响：本轮仅调整后端初始化错误返回和治理文档，不新增用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存域或失效范围；WASM cache/lock/notify/config/storage 继续复用启动期显式注入的共享服务，未配置时返回 host-call internal error 而非构造孤立默认实例。数据权限影响：本轮不新增 REST/API 数据操作面；`tenantfilter.New` 改为显式错误返回后仍由宿主发布目录注入共享 bizctx/tenant 能力，插件租户过滤测试已覆盖。E2E 影响：无前端可见行为变化，不新增 E2E；使用后端单元测试、panic allowlist、静态扫描、OpenSpec 校验和 diff check 覆盖。
- [x] 2026-05-14: FB-19 验证通过：删除 `apps/lina-core/internal/controller/role/role_new.go` 末尾误追加的空 `ControllerV1` 和无参 `NewV1()`，保留 `NewV1(roleSvc rolesvc.Service)` 显式注入入口；执行 `cd apps/lina-core && go test ./internal/controller/role -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./internal/controller/role ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`rg -n "func NewV1\\(\\) role\\.IRoleV1|type ControllerV1 struct\\{\\}" apps/lina-core/internal/controller/role apps/lina-core/internal/controller -g '*.go'` 无输出、`git diff --check -- apps/lina-core/internal/controller/role/role_new.go openspec/changes/explicit-service-dependency-injection/tasks.md AGENTS.md .agents/skills/lina-review/SKILL.md` 均通过。根因复盘：该文件修改时间晚于本变更任务记录的最终收口验证，说明最终验证后又产生了未验证的未暂存 Go 改动；原有 `lina-review` 已强调规范审查和测试覆盖，但没有把“基于当前工作区重新运行变更包 Go 编译门禁”写成阻塞项，导致简单重复声明没有被流程拦截。治理措施：已在 `AGENTS.md` 与 `.agents/skills/lina-review/SKILL.md` 增加后端 Go 编译门禁，要求 Go 生产代码变更必须运行覆盖变更包的 `go test`，控制器/路由/启动绑定变更必须覆盖 `internal/cmd` 或等价启动绑定包。i18n 影响：本轮仅修正后端控制器构造和治理文档，不新增用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存或失效路径；修复恢复角色控制器复用启动期传入的共享 `roleSvc`。数据权限影响：本轮不新增 REST/API 数据操作面；角色相关接口继续通过注入的角色服务执行业务与数据权限逻辑。E2E 影响：无前端可见行为变化，使用后端编译测试、路由启动包测试、静态扫描和 diff check 覆盖。
- [x] 2026-05-13: FB-18 验证通过：执行聚合依赖旧表述残留扫描无输出，`openspec validate explicit-service-dependency-injection --strict` 通过，`git diff --check -- AGENTS.md .agents/skills/lina-review/SKILL.md openspec/changes/explicit-service-dependency-injection/proposal.md openspec/changes/explicit-service-dependency-injection/design.md openspec/changes/explicit-service-dependency-injection/specs/service-dependency-injection-governance/spec.md openspec/changes/explicit-service-dependency-injection/specs/backend-conformance/spec.md openspec/changes/explicit-service-dependency-injection/tasks.md` 通过。确认项目规范、`lina-review` 和 OpenSpec 文档已明确禁止通过聚合结构体整体传递多个接口型运行期依赖，并要求接口型依赖拆分为独立构造函数参数，以便依赖新增、删除或替换在编译阶段暴露未同步调用点；仅纯值配置结构体保留例外。i18n 影响：本轮仅修改治理文档和审查规则，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增 REST/API 数据操作面。E2E 影响：无前端可见行为变化，使用 OpenSpec 校验、静态扫描和 diff check 覆盖。
- [x] 2026-05-13: FB-17 验证通过：`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/internal/service/plugin/plugin_wasm_host_services.go:apps/lina-core/internal/service/pluginhostservices node hack/tests/scripts/validate-service-dependencies.mjs`、`rg -n "WasmHostServiceDependencies|type Dependencies struct|pluginhostservices\\.Dependencies|ConfigureWasmHostServices\\([^\\n]*\\{|New\\(deps Dependencies\\)" apps/lina-core/internal/service/plugin apps/lina-core/internal/service/pluginhostservices apps/lina-core/internal/cmd -g '*.go'` 无输出、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/plugin_wasm_host_services.go apps/lina-core/internal/service/pluginhostservices/pluginhostservices.go apps/lina-core/internal/cmd/cmd_http_runtime.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `ConfigureWasmHostServices` 与 `pluginhostservices.New` 删除聚合依赖结构体，HTTP 启动期直接展开传入运行期共享服务实例；实际服务来源、共享实例边界和动态插件/source 插件发布能力保持不变。i18n 影响：本轮仅调整后端内部构造签名和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存、失效范围或跨实例协调机制；WASM host service 与源码插件 host service 仍复用启动期传入的 `kvcache`、`notify`、`config`、`i18n`、`session`、`plugin` 等共享实例。数据权限影响：本轮不新增 REST/API 数据操作面；`pluginhostservices.New` 内部仍基于启动期传入的 `bizctx`、`role`、`orgcap` 构造插件会话数据权限投影，行为不变。E2E 影响：无前端可见行为变化，使用后端单元测试、定向 cmd 测试、依赖治理扫描、OpenSpec 校验和 diff check 覆盖。
- [x] 2026-05-13: FB-16 验证通过：`cd apps/lina-core && go test ./pkg/pluginservice/contract ./pkg/pluginservice/bizctx ./pkg/pluginservice/config ./pkg/pluginservice/tenantfilter ./pkg/pluginservice/pluginstate ./pkg/pluginhost ./internal/service/pluginhostservices -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/wasm ./internal/service/plugin -count=1`、`cd apps/lina-plugins/content-notice && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-loginlog && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-online && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-server && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-operlog && go test ./backend/... -count=1`、`cd apps/lina-plugins/org-center && go test ./backend/... -count=1`、`cd apps/lina-plugins/plugin-demo-source && go test ./backend/... -count=1`、`cd apps/lina-plugins/multi-tenant && go test ./backend/... -count=1`、`rg -n "pkg/pluginservice/(apidoc|auth|i18n|notify|route|session)" apps/lina-core apps/lina-plugins -g '*.go'` 无输出、scoped `LINAPRO_SERVICE_DEP_SCAN_ROOTS=... node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/pkg/pluginservice apps/lina-core/pkg/pluginhost apps/lina-core/internal/service/pluginhostservices apps/lina-core/internal/service/plugin/plugin_wasm_host_services.go apps/lina-core/internal/service/plugin/internal/wasm/hostfn_service_config.go apps/lina-plugins/content-notice apps/lina-plugins/monitor-loginlog apps/lina-plugins/monitor-online apps/lina-plugins/monitor-operlog apps/lina-plugins/monitor-server apps/lina-plugins/multi-tenant apps/lina-plugins/org-center apps/lina-plugins/plugin-demo-source openspec/changes/explicit-service-dependency-injection/tasks.md`。确认插件宿主服务契约统一收敛到 `pkg/pluginservice/contract`，`HostServices` 返回 `contract.APIDocService/AuthService/BizCtxService/ConfigService/I18nService/NotifyService/PluginStateService/RouteService/SessionService/TenantFilterService`，原 `apidoc/auth/i18n/notify/route/session` 空壳契约包移除，`config/bizctx/pluginstate/tenantfilter` 仅保留 adapter/helper 实现并引用 contract 类型；源码插件改为直接依赖 contract 类型。i18n 影响：本轮不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n，仅迁移后端 Go 契约类型与引用路径。缓存一致性影响：本轮未新增缓存、失效范围或跨实例协调机制；契约收敛不改变启动期共享 adapter 的实例来源，继续通过 `pluginhostservices.New` 从宿主运行期依赖构造。数据权限影响：本轮不新增 REST/API 数据操作面；在线会话数据权限、租户过滤和插件自有表查询语义保持不变，由宿主/插件后端测试覆盖。E2E 影响：无前端可见行为变化，使用 Go 单元测试、插件后端编译测试、依赖治理扫描、OpenSpec 校验和 diff check 覆盖。
- [x] 2026-05-13: FB-15 验证通过：`cd apps/lina-core && go test ./pkg/pluginservice/tenantfilter -count=1`、`cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/pluginhostservices -count=1`、`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`cd apps/lina-plugins/content-notice && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-loginlog && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-operlog && go test ./backend/... -count=1`、`cd apps/lina-plugins/plugin-demo-source && go test ./backend/... -count=1`、`cd apps/lina-plugins/org-center && go test ./backend/... -count=1`、`cd apps/lina-plugins/monitor-server && go test ./backend/... -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/pkg/pluginservice/tenantfilter:apps/lina-core/internal/service/pluginhostservices:apps/lina-core/pkg/pluginhost:apps/lina-plugins/content-notice/backend:apps/lina-plugins/monitor-loginlog/backend:apps/lina-plugins/monitor-operlog/backend:apps/lina-plugins/plugin-demo-source/backend:apps/lina-plugins/org-center/backend node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/pkg/pluginservice/tenantfilter apps/lina-core/pkg/pluginhost/pluginhost_services.go apps/lina-core/internal/service/pluginhostservices apps/lina-core/internal/cmd/cmd_panic_allowlist_test.go apps/lina-plugins/content-notice apps/lina-plugins/monitor-loginlog apps/lina-plugins/monitor-operlog apps/lina-plugins/plugin-demo-source apps/lina-plugins/org-center apps/lina-plugins/monitor-server/backend/plugin_test.go hack/tests/config/service-dependency-baseline.json openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `tenantfilter` 改为 `HostServices.TenantFilter()` 显式发布的 `Service`，源码插件服务构造函数显式接收并使用同一租户过滤实例，`content-notice`、`monitor-loginlog`、`monitor-operlog`、`plugin-demo-source`、`org-center` 不再调用 `tenantfilter` 包级全局状态；`orgcap` provider 改为路由注册期拿到 host tenant-filter 后注册。i18n 影响：本轮仅调整后端依赖注入和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存或失效路径；租户过滤读取启动期共享的 `bizctx` 和可选 `tenantcap.PlatformBypass` 策略，避免源码插件各自持有孤立宿主上下文。数据权限影响：本轮不新增 REST/API 数据操作面；租户过滤仍在插件自有表查询阶段注入租户谓词，并补充平台 bypass 单元测试覆盖。E2E 影响：无前端可见行为变化，使用后端单元测试、插件后端编译测试、panic allowlist、依赖治理扫描和 OpenSpec 校验覆盖。完整 `node hack/tests/scripts/validate-service-dependencies.mjs` 当前仍受同一大迭代中未完成的 `cmd_http_runtime.go`、`multi-tenant/backend/plugin.go`、`monitor-online/backend/plugin.go` 等既有 baseline 差异阻断，本轮采用上述 scoped 扫描验证改动面。
- [x] 2026-05-13: FB-14 验证通过：`cd apps/lina-core && go test ./internal/service/user -count=1`、`cd apps/lina-core && go test ./internal/cmd -run 'TestUploadedFileAccessRouteIsPublic|TestBindHostedOpenAPIDocsDisablesBuiltInEndpointsAndBindsConfiguredPath|TestBindHostedOpenAPIDocsUsesRequestOrigin' -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/internal/service/user/user.go node hack/tests/scripts/validate-service-dependencies.mjs`、`rg -n "datascope\\.New" apps/lina-core/internal/service/user/user.go` 无输出，确认 `user.New` 不再隐式创建 `datascope.Service`，HTTP 启动期和测试构造边界将共享 `scopeSvc` 传入用户服务。`cd apps/lina-core && go test ./internal/service/user ./internal/cmd -count=1` 中 `internal/service/user` 已通过，`internal/cmd` 当前被既有 `TestProductionPanicsMatchAllowlist` 阻断，失败项为 `apps/lina-core/pkg/sourceupgrade/sourceupgrade.go:New`，与本轮用户服务数据权限注入改造无直接关系。`openspec validate explicit-service-dependency-injection --strict` 与 `git diff --check -- apps/lina-core/internal/service/user/user.go apps/lina-core/internal/service/user/user_test_dependencies_test.go apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/cmd/cmd_http_test.go openspec/changes/explicit-service-dependency-injection/tasks.md` 通过。i18n 影响：本轮仅改后端内部服务注入和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：用户管理数据权限操作改为复用启动期共享 `datascope.Service`，避免 `user.New` 内部创建孤立数据权限服务图，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮修正用户管理数据权限依赖来源，不扩大 REST/API 数据操作面；用户管理数据权限行为由 `internal/service/user` 包测试覆盖。E2E 影响：无前端可见行为变化，使用后端单元测试、定向 cmd 测试和治理扫描覆盖。
- [x] 2026-05-13: FB-13 验证通过：`rg -n "NewControllerV1|func NewV1" apps/lina-plugins/plugin-demo-source/backend/internal/controller/demo/demo_new.go apps/lina-plugins/plugin-demo-source/backend/plugin.go` 确认 demo 控制器只保留 `NewV1(demoSvc)` 构造入口且路由注册调用 `democtrl.NewV1(demoSvc)`；`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/plugin-demo-source/backend/internal/controller/demo/demo_new.go:apps/lina-plugins/plugin-demo-source/backend/plugin.go node hack/tests/scripts/validate-service-dependencies.mjs`；`openspec validate explicit-service-dependency-injection --strict`；`git diff --check -- apps/lina-plugins/plugin-demo-source/backend/internal/controller/demo/demo_new.go apps/lina-plugins/plugin-demo-source/backend/plugin.go openspec/changes/explicit-service-dependency-injection/tasks.md`。`cd apps/lina-plugins/plugin-demo-source && go test ./backend/internal/controller/demo -count=1` 当前被既有 import cycle 阻断，循环路径经过 `lina-core/pkg/pluginhost`、`lina-core/pkg/pluginservice/apidoc`、`lina-core/internal/service/apidoc`、`lina-core/internal/service/i18n` 与 `lina-core/pkg/pluginservice/i18n`，与本轮移除 `NewControllerV1` 无直接关系。i18n 影响：本轮仅调整后端构造入口和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮消除控制器额外构造入口，依赖继续来自插件注册期传入的共享 host i18n service adapter，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用静态扫描、OpenSpec 校验和路由构造入口扫描覆盖。
- [x] 2026-05-13: FB-12 验证通过：`cd apps/lina-core && go test ./pkg/sourceupgrade -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/pkg/sourceupgrade node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/pkg/sourceupgrade/sourceupgrade.go apps/lina-core/pkg/sourceupgrade/sourceupgrade_test.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `sourceupgrade.New` 改为接收显式 `sourceupgrade.UpgradeGovernanceService`，不再导入或调用宿主内部 `plugin.New`，也不在 facade 初始化中自行构造配置、上下文、缓存协调、i18n、session 或插件服务图；`nil` 依赖通过单元测试验证为 fail-fast。i18n 影响：本轮仅调整后端内部 facade 构造入口和测试，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮消除 `pkg/sourceupgrade` 独立插件服务图入口，调用方必须传入启动期或工具入口显式构造的共享升级治理服务，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或权限判定路径；E2E 影响：无前端可见行为变化，使用后端单元测试、依赖治理扫描和 OpenSpec 校验覆盖。
- [x] 2026-05-13: FB-11 验证通过：`cd apps/lina-plugins/plugin-demo-source && go test ./backend/... -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/plugin-demo-source/backend/plugin.go node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-plugins/plugin-demo-source/backend/plugin.go apps/lina-plugins/plugin-demo-source/backend/internal/service/demo/demo_storage.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `plugin-demo-source` 卸载回调不再调用 `demosvc.New(nil)`，插件自有附件清理改为使用无宿主依赖的 `demo.PurgeStorageData(ctx)` 包级入口，`Service.PurgeStorageData` 保持委托以兼容已有服务契约。i18n 影响：本轮仅移除卸载清理路径中的伪造 nil i18n 依赖，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮未新增缓存、失效范围或跨实例协调机制；变更消除了卸载回调中的虚假服务构造，不影响运行期共享 host service 目录。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或权限判定路径；卸载清理仍仅处理插件自有附件存储。E2E 影响：无前端可见行为变化，使用插件后端测试、依赖治理扫描和 OpenSpec 校验覆盖。
- [x] 2026-05-13: FB-10 验证通过：`cd apps/lina-core && go test ./internal/service/role ./internal/cmd ./internal/service/cron ./internal/service/file ./internal/service/jobmgmt ./internal/service/user ./internal/controller/i18n`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/internal/service/role node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/internal/service/role/role.go apps/lina-core/internal/service/role/role_test_dependencies_test.go apps/lina-core/internal/service/role apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/cmd/cmd_http_test.go apps/lina-core/internal/service/user/user_test_dependencies_test.go apps/lina-core/internal/controller/i18n/i18n_v1_runtime_test.go apps/lina-core/internal/service/jobmgmt/jobmgmt_test.go apps/lina-core/internal/service/file/file_data_scope_test.go apps/lina-core/internal/service/cron/cron_single_node_test.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `role.New` 不再调用 `datascope.New`，HTTP 启动期和测试构造边界先创建共享 `role.Service`，再创建共享 `datascope.Service` 并通过 `roleSvc.SetDataScopeService(scopeSvc)` 显式完成环形依赖装配。i18n 影响：本轮仅改后端内部服务注入和测试，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：角色授权用户操作继续使用同一个启动期共享 `datascope.Service`，避免 role 内部创建孤立数据权限服务图，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮修正数据权限依赖来源，不扩大 REST/API 数据操作面；角色授权用户相关数据权限行为由 `internal/service/role` 包测试覆盖。E2E 影响：无前端可见行为变化，使用后端单元测试和治理扫描覆盖。
- [x] 2026-05-13: FB-6 验证通过：`cd apps/lina-core && go test ./internal/service/plugin -run 'TestValidateStartupConsistency' -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/internal/service/plugin/plugin_startup_consistency.go node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_startup_consistency.go apps/lina-core/internal/service/plugin/plugin_test.go apps/lina-core/internal/service/plugin/plugin_startup_consistency_test.go apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/cmd/cmd_http_test.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认启动一致性校验改为使用启动期注入的 `tenantcap.Service`，缺失注入时 fail-fast，不再在 `plugin_startup_consistency.go` 中临时创建 `tenantcap.Service` 或 `bizctx.Service`。i18n 影响：本轮仅改后端内部服务注入和测试，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮消除租户能力启动校验的孤立服务图，权威插件启用状态和 tenantcap provider 仍来自启动期共享 `pluginSvc`/`tenantSvc`，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用后端单元测试和治理扫描覆盖。
- [x] 2026-05-13: FB-5 验证通过：`cd apps/lina-core && go test ./internal/service/plugin ./internal/cmd ./pkg/sourceupgrade ./internal/controller/i18n ./internal/service/auth ./internal/service/user`、`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-core/internal/service/plugin/plugin.go node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_runtime_cache.go apps/lina-core/internal/service/plugin/plugin_test.go apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/cmd/cmd_http_test.go apps/lina-core/internal/cmd/cmd_panic_allowlist_test.go apps/lina-core/pkg/sourceupgrade/sourceupgrade.go apps/lina-core/internal/controller/i18n/i18n_v1_runtime_test.go apps/lina-core/internal/service/auth/auth_runtime_params_test.go apps/lina-core/internal/service/user/user_test_dependencies_test.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `plugin.New` 改为直接接收 `topology`、`config`、`bizctx`、`cachecoord`、`i18n`、`sessionStore` 参数，不新增 `Dependencies` 结构，也不在缺失运行期关键依赖时内部 `New()` 补齐；HTTP 启动期和相关测试改为传入共享依赖。i18n 影响：本轮复用启动期共享 `i18n.Service`，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：插件 runtime cache controller 改为使用传入的 `cachecoord.Service`，动态 route session store 改为使用传入的共享 `session.Store`，避免独立配置、i18n、cachecoord 和 session 实例。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用后端单元测试和治理扫描覆盖。
- [x] 2026-05-13: FB-7 验证通过：`cd apps/lina-plugins/monitor-server && go test ./backend/internal/service/config -count=1`、`cd apps/lina-plugins/monitor-server && go test ./backend/... -count=1`、`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/monitor-server/backend/internal/service/config/config.go node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- apps/lina-plugins/monitor-server/backend/internal/service/config/config.go apps/lina-plugins/monitor-server/backend/internal/service/config/config_test.go apps/lina-plugins/monitor-server/backend/plugin.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 `monitor-server` 配置加载收敛为单一 `Load(ctx, reader)` 入口，生产路径从 `registrar.HostServices().Config()` 显式传入宿主配置服务，测试路径也显式传入 reader，不再由配置加载函数内部调用 `configsvc.New()`。i18n 影响：本轮仅改后端内部依赖传递和测试调用，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮移除运行期配置服务隐式构造入口，配置读取继续使用宿主发布的同一配置服务实例，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用后端单元测试和治理扫描覆盖。
- [x] 2026-05-13: FB-8 验证通过：`rg -n "NewControllerV1" apps/lina-plugins/multi-tenant/backend/internal/controller/auth apps/lina-plugins/multi-tenant/backend/plugin.go` 未发现残留引用；`cd apps/lina-plugins/multi-tenant && go test ./backend ./backend/internal/controller/auth -count=1`；`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/multi-tenant/backend/internal/controller/auth/auth.go node hack/tests/scripts/validate-service-dependencies.mjs`；`openspec validate explicit-service-dependency-injection --strict`；`git diff --check -- apps/lina-plugins/multi-tenant/backend/internal/controller/auth/auth.go apps/lina-plugins/multi-tenant/backend/plugin.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 auth 控制器只保留 `NewV1` 入口，插件路由绑定改为调用 `authcontroller.NewV1(...)`，控制器不再通过额外 `NewControllerV1` 绕过统一接口构造入口。`cd apps/lina-plugins/multi-tenant && go test ./... -count=1` 当前仍因既有测试未同步新服务构造签名失败，失败包包括 `lifecycleguard`、`membership`、`tenant` 和 `tenantplugin`，与本轮移除 auth 控制器 `NewControllerV1` 无直接关系。i18n 影响：本轮仅改后端构造入口和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮消除 auth 控制器孤立构造入口，依赖继续来自插件注册期传入的共享 host auth、membership 和 provider 实例，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用后端编译、静态扫描和 OpenSpec 治理验证覆盖。
- [x] 2026-05-13: FB-9 验证通过：`rg -n "NewControllerV1" apps/lina-plugins/multi-tenant/backend/internal/controller/platform apps/lina-plugins/multi-tenant/backend/internal/controller/tenant apps/lina-plugins/multi-tenant/backend/plugin.go` 未发现残留引用；`cd apps/lina-plugins/multi-tenant && go test ./backend ./backend/internal/controller/platform ./backend/internal/controller/tenant -count=1`；`LINAPRO_SERVICE_DEP_SCAN_ROOTS=apps/lina-plugins/multi-tenant/backend/internal/controller/platform/platform.go:apps/lina-plugins/multi-tenant/backend/internal/controller/tenant/tenant.go node hack/tests/scripts/validate-service-dependencies.mjs`；`openspec validate explicit-service-dependency-injection --strict`；`git diff --check -- apps/lina-plugins/multi-tenant/backend/internal/controller/platform/platform.go apps/lina-plugins/multi-tenant/backend/internal/controller/tenant/tenant.go apps/lina-plugins/multi-tenant/backend/plugin.go openspec/changes/explicit-service-dependency-injection/tasks.md`。确认 platform 与 tenant 控制器只保留 `NewV1` 入口，并由 `NewV1` 直接使用插件注册期传入的共享 service 实例构造控制器，不再保留额外 `NewControllerV1` 入口。`cd apps/lina-plugins/multi-tenant && go test ./... -count=1` 当前仍因既有测试未同步新服务构造签名失败，失败包包括 `lifecycleguard`、`membership`、`tenant` 和 `tenantplugin`，与本轮移除 platform/tenant 控制器 `NewControllerV1` 无直接关系。i18n 影响：本轮仅改后端构造入口和 OpenSpec 任务记录，不新增或修改用户可见文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：本轮消除 platform/tenant 控制器额外构造入口，依赖继续来自插件注册期传入的共享 tenant、impersonate 和 tenantplugin 实例，未新增缓存、失效范围或跨实例协调机制。数据权限影响：本轮不新增或修改 REST/API 数据操作、列表/详情/写入/聚合查询或插件数据访问路径；E2E 影响：无前端可见行为变化，使用后端编译、静态扫描和 OpenSpec 治理验证覆盖。
- [x] 2026-05-14: 最终收口验证通过：`cd apps/lina-core && go test ./internal/service/auth ./internal/service/session ./internal/service/middleware ./internal/service/role ./internal/service/datascope ./internal/service/config ./internal/service/i18n ./internal/service/plugin ./internal/service/cachecoord ./internal/service/kvcache ./internal/service/locker ./internal/service/cron ./internal/service/hostlock ./internal/service/pluginhostservices ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、所有官方源码插件后端 `go test ./backend/... -count=1`、`node hack/tests/scripts/validate-service-dependencies.mjs`、`openspec validate explicit-service-dependency-injection --strict`、`git diff --check -- openspec/changes/explicit-service-dependency-injection AGENTS.md .agents/skills/lina-review/SKILL.md apps/lina-core apps/lina-plugins hack/tests/config/service-dependency-baseline.json hack/tests/scripts/validate-service-dependencies.mjs` 均通过。当前环境未配置 `LINA_TEST_REDIS_*`，且本地 `127.0.0.1:6379` 拒绝连接，因此未执行真实 Redis 集成测试；本轮已通过非 Redis coordination/cache/session/plugin runtime 单元测试和依赖治理扫描覆盖单机路径与共享实例回归。i18n 影响：本轮无用户可见文案、前端语言包、manifest i18n 或 apidoc i18n 变更，仅移除 i18n fallback 中的隐式 host config 构造。缓存一致性影响：本轮未新增缓存域或失效范围，改为复用启动期共享的 data scope、host lock、OpenAPI bizctx、auth token store 相关依赖，避免孤立服务图。数据权限影响：本轮不新增 REST/API 数据操作面，`pluginhostservices` session adapter 改为复用启动期共享 `datascope.Service`，数据权限投影来源更一致。E2E 影响：无前端或用户可观察页面行为变化，不新增 E2E；使用后端单元测试、插件后端测试、治理扫描和 OpenSpec 校验覆盖。
