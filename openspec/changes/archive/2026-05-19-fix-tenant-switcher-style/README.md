# Tenant Switcher Style Fix

This change fixes the management workbench header tenant switcher style
regression after the control moved from the host layout into the official
`multi-tenant` source plugin header slot.
