## Feedback

- [x] **FB-1**: 管理工作台顶部租户切换下拉框迁移到插件头部插槽后样式发生变化，应恢复迁移前的固定宽度、间距、截断和图标样式
- [x] **FB-2**: `multi-tenant`插件 mock 数据缺少用于演示本租户数据隔离的`tenant-user`账号，该账号应拥有除平台管理外的其他菜单权限，并使用本租户数据权限
- [x] **FB-3**: `tenant-user`演示账号只关联单个租户，无法在登录时演示多租户选择效果，应关联 5 个活跃租户并保持每个租户下的本租户权限角色

## Verification

- 2026-05-17：FB-1验证通过。修复将`multi-tenant`头部插槽租户切换器的固定宽度、右侧间距、代操作提示、选项截断、暗色样式和建筑图标约束收敛到组件本地实现，并在`TC0222`页面对象中新增顶部切换器宽度、位置和间距断言。已运行`cd apps/lina-vben && pnpm -F @lina/web-antd typecheck`、`cd hack/tests && pnpm exec tsc --noEmit --pretty false`、`cd hack/tests && pnpm test:validate`、`openspec validate fix-tenant-switcher-style --strict`、`cd apps/lina-vben && LINAPRO_SOURCE_PLUGINS=1 pnpm -F @lina/web-antd build`、`cd hack/tests && E2E_BROWSER_CHANNEL=chrome pnpm exec playwright test apps/lina-plugins/multi-tenant/hack/tests/e2e/platform-admin/TC0222-management-workbench-routes.ts --config playwright.config.ts --project=chromium`，均通过。前端构建仍输出既有`Rollup`分块警告，与本修复无关。
- 2026-05-17：影响评估：本次不新增或修改用户可见文案、运行时`i18n`键、`manifest/i18n`资源或`apidoc i18n JSON`；不涉及后端接口、数据库、数据权限、租户隔离业务逻辑或缓存一致性路径；不修改默认开发工具、脚本或跨平台执行入口。
- 2026-05-17：`/lina-review`审查通过，未发现阻塞问题。审查范围包含`multi-tenant`插件头部插槽组件、`MultiTenantPage`页面对象和本次`OpenSpec`反馈文档；确认样式修复保持在插件组件边界内，`E2E`覆盖固定宽度、顺序和间距回归，未引入`Go`生产代码、`REST API`、数据库、缓存、数据权限或跨平台脚本变更。
- 2026-05-17：FB-2验证通过。`multi-tenant`插件 mock 数据新增`tenant-user`演示账号，密码沿用 mock 数据统一 bcrypt（明文：admin123），账号归属`alpha-retail`租户；新增同名租户角色，`data_scope=2`（本租户数据）；角色菜单授权使用递归 CTE 排除`platform`平台管理顶层菜单及其子树，并授予其余所有启用菜单，避免后续新增非平台菜单时遗漏授权。同步更新`mock_data_test.go`，覆盖账号昵称、成员关系、角色绑定、本租户数据权限和平台管理子树排除策略。已运行`cd apps/lina-plugins/multi-tenant && GOWORK=off go test . -count=1`、`cd apps/lina-core && go test ./pkg/dialect -count=1`、`openspec validate fix-tenant-switcher-style --strict`、`git -C apps/lina-plugins diff --check -- multi-tenant/manifest/sql/mock-data/001-multi-tenant-demo-data.sql multi-tenant/mock_data_test.go`、`git diff --check -- openspec/changes/fix-tenant-switcher-style/tasks.md`，均通过。默认`go test`会因根`go.work`未包含`apps/lina-plugins/multi-tenant`而拒绝执行，本次按插件模块自身`go.mod`使用`GOWORK=off`完成验证。i18n影响：仅新增 mock seed 中的演示账号/角色展示值，不新增或修改前端运行时语言包、`manifest/i18n`资源或`apidoc i18n JSON`。数据权限影响：新增演示角色显式使用本租户数据权限，用于验证租户隔离演示，不修改生产数据权限逻辑。缓存一致性影响：仅修改可重复加载的 mock SQL，不新增运行时缓存、失效或跨实例协调路径。开发工具脚本影响：无新增或修改默认开发工具、脚本或跨平台执行入口。
- 2026-05-17：FB-2 `/lina-review`审查通过，未发现阻塞问题。审查范围限定为`apps/lina-plugins/multi-tenant/manifest/sql/mock-data/001-multi-tenant-demo-data.sql`、`apps/lina-plugins/multi-tenant/mock_data_test.go`和本反馈记录；确认新增账号、成员关系、租户角色、用户角色绑定和菜单授权均保持 mock SQL 幂等，`tenant-user`角色显式使用本租户数据权限，平台管理菜单通过递归子树排除而非易漂移的权限白名单/黑名单处理。确认本次不新增生产`Go`代码、`REST API`、前端页面、运行时缓存路径或默认开发脚本；Go 测试只覆盖插件 mock 资产，且已按插件独立模块使用`GOWORK=off`完成编译与单测验证。
- 2026-05-17：FB-3验证通过。`tenant-user`现关联 5 个活跃租户：`alpha-retail`、`beta-manufacturing`、`one-click-triple-media`、`cyber-wellness-health`、`clock-in-on-time-hr`，用于登录时展示租户选择效果；同时为这 5 个租户分别创建同 key 的`tenant-user`本租户角色，均为`data_scope=2`，复用既有非平台菜单动态授权块，保证切换任一租户后仍具备除平台管理外的常规菜单权限。同步更新`mock_data_test.go`，覆盖 5 个 membership、5 个租户本地角色和唯一角色绑定。已运行`cd apps/lina-plugins/multi-tenant && GOWORK=off go test . -run 'TestMultiTenantMockData' -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test . -count=1`、`cd apps/lina-core && go test ./pkg/dialect -count=1`、`openspec validate fix-tenant-switcher-style --strict`、`git -C apps/lina-plugins diff --check -- multi-tenant/manifest/sql/mock-data/001-multi-tenant-demo-data.sql multi-tenant/mock_data_test.go`、`git diff --check -- openspec/changes/fix-tenant-switcher-style/tasks.md`，均通过。i18n影响：仅新增 mock seed 中的租户角色展示值，不新增或修改前端运行时语言包、`manifest/i18n`资源或`apidoc i18n JSON`。数据权限影响：每个新增租户角色均显式使用本租户数据权限，用于演示租户隔离，不修改生产数据权限逻辑。缓存一致性影响：仅修改可重复加载的 mock SQL，不新增运行时缓存、失效或跨实例协调路径。开发工具脚本影响：无新增或修改默认开发工具、脚本或跨平台执行入口。
- 2026-05-17：FB-3 `/lina-review`审查通过，未发现阻塞问题。审查范围限定为`multi-tenant` mock SQL、`mock_data_test.go`和本反馈记录；确认 5 个 membership 均为活跃租户，5 个同 key 角色均保持`data_scope=2`且依赖`(tenant_id, key)`唯一约束幂等写入，`sys_user_role`通过用户名和角色 key 绑定后会绑定到用户可选租户下的全部同 key 角色；非平台菜单授权仍通过递归排除`platform`子树，不引入平台管理权限。确认本次不新增生产`Go`代码、`REST API`、前端页面、运行时缓存路径或默认开发脚本。
