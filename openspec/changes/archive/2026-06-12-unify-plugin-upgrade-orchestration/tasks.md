## 1. 基线盘点与边界测试

- [x] 1.1 盘点当前`plugin_runtime_upgrade_preview.go`、`plugin_runtime_upgrade_execute.go`、`internal/sourceupgrade`、`internal/runtimeupgrade`、`internal/runtime`升级/release 切换和`internal/store`升级状态投影，记录 owner、调用路径、失败 phase、DI 来源、缓存和数据权限影响。

  记录：当前升级 owner 仍分散。根门面`plugin_runtime_upgrade_preview.go`负责 runtime upgrade preview：读取 desired manifest、registry、`store.BuildRuntimeUpgradeState`，调用`runtimeupgrade.CanExecute`、`BuildSQLSummary`、`BuildHostServicesDiff`和`BuildRiskHints`，并在门面内执行依赖检查。根门面`plugin_runtime_upgrade_execute.go`负责公开`UpgradeSourcePlugin`、`ExecuteRuntimeUpgrade`、分布式锁、running 状态标记、失败后 stable state 恢复和 source/dynamic 分派，其中 source 分支仍通过公开`UpgradeSourcePlugin`再入。`internal/sourceupgrade`负责源码插件升级状态、执行、before/after callback、升级 SQL、治理资源同步、release 提升和 source 失败迁移账本。`internal/runtimeupgrade`只承载 runtime preview/lock 纯函数与常量。`internal/runtime`负责 dynamic artifact/release 切换、`UpgradeDynamicPluginRequest`、reconciler upgrade/rollback 和动态路由升级阻断。`internal/store`负责 registry/release 读写、`BuildRuntimeUpgradeState`、失败 release/migration 投影和`PromoteSourceRelease`。

  调用路径：管理查询调用根门面 source status 或 runtime preview；管理执行调用根门面`ExecuteRuntimeUpgrade`，锁内复查后按 manifest type 分派，source 路径再入公开 source 升级入口，dynamic 路径调用 runtime upgrade request。失败 phase 当前由 sourceupgrade synthetic migration、store runtime upgrade projection 和 runtime failed release/rollback 共同解释，存在 D 阶段待统一的多 owner 语义。

  DI 来源检查：本任务只盘点基线，不新增运行期依赖。现有 sourceupgrade 由`plugin.New()`和`internal/testutil.NewServices()`构造，依赖 catalog、store、lifecycle、runtime、integration、i18n 和 dependency validator 窄契约；runtimeupgrade 无运行期实例；runtime/store/lifecycle 等均复用启动期构造的同一实例。缓存影响：本任务不改变缓存语义；当前升级成功/失败仍经根门面`publishPluginChange`兼容 wrapper 或`syncEnabledSnapshotAndPublishRuntimeChange`发布`plugin-runtime`revision。数据权限影响：本任务不改变升级入口平台治理守卫和租户边界；后续迁移必须保持公开入口守卫单次执行且内部策略不得放宽可见性。
- [x] 1.2 扩展插件服务静态边界测试，先覆盖 D 阶段最终目标：`internal/upgrade`必须存在，生产代码不得导入`internal/sourceupgrade`或`internal/runtimeupgrade`，旧目录不得保留，根门面不得通过公开`UpgradeSourcePlugin`再入升级流程。

  记录：已在`apps/lina-core/internal/service/plugin/plugin_boundary_test.go`新增`TestPluginUpgradeOrchestrationStaticBoundaries`。当前 1.x 处于 D 阶段过渡起点，`internal/upgrade`尚未建立，因此测试先确认旧 source/runtime upgrade 路径和根门面升级文件仍显式存在，避免迁移前出现隐式半删除状态；一旦`internal/upgrade`建立，测试会检查 package owner，并在旧目录删除后阻断生产代码重新 import`internal/sourceupgrade`/`internal/runtimeupgrade`和根门面 source 再入公开`UpgradeSourcePlugin`。同时删除 C 阶段“不允许引入`internal/upgrade`”的旧护栏。

  验证：`cd apps/lina-core && go test ./internal/service/plugin -run TestPluginUpgradeOrchestrationStaticBoundaries -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。
- [x] 1.3 记录本变更无 HTTP API、DTO、SQL schema、前端页面、插件 manifest wire、动态插件 guest 协议和`apps/lina-plugins/*`文件影响；记录`i18n`、数据权限、缓存一致性、开发工具跨平台和测试策略影响判断。

  记录：本变更 D 只修改`apps/lina-core/internal/service/plugin/**`内部 Go 代码、测试和 OpenSpec 文档；不修改`api/`、HTTP 路由、DTO、OpenAPI 源文本、数据库 schema、SQL 文件、前端页面、插件 manifest wire、动态插件 guest 协议或`apps/lina-plugins/*`文件。`i18n`资源无新增文案；若后续错误 owner 迁移触及 code/message key/fallback，必须保持既有 key/fallback 或在对应任务记录影响。数据权限语义不变，公开升级入口仍由根门面平台治理守卫保护，内部策略不得绕过租户或平台边界。缓存一致性不新增缓存域，升级成功/失败继续通过统一插件变化发布入口和`plugin-runtime`revision controller；后续任务需记录权威源、触发点、跨实例同步和失败降级是否保持不变。开发工具跨平台无影响，未修改脚本、Makefile、CI 或 linactl。测试策略为后端单元测试、静态治理测试、OpenSpec 严格校验和 diff 检查；无 UI 或用户可观察页面变化，未触发 E2E。
- [x] 1.4 完成 1.x 后调用`lina-review`审查基线记录、静态边界测试和影响判断。

  审查记录：已按`lina-review`任务级范围审查 1.x 当前变更，范围来源为`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change unify-plugin-upgrade-orchestration --json`和本任务上下文；本阶段实际审查文件为`apps/lina-core/internal/service/plugin/plugin_boundary_test.go`、`openspec/changes/unify-plugin-upgrade-orchestration/proposal.md`、`design.md`、`specs/plugin-upgrade-governance/spec.md`、`specs/plugin-service-layout/spec.md`和`tasks.md`。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`和`goframe-v2`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，任务 1.1-1.3 仅在实现和验证后标记完成；后端 Go 通过，新增静态测试可单独运行；架构通过，D 阶段 owner 收敛方向与方案一致；缓存、数据权限、`i18n`和开发工具跨平台本阶段无语义变更且已记录；E2E 未触发，原因是本阶段无 UI 或用户可观察工作流变更。

  验证：`cd apps/lina-core && go test ./internal/service/plugin -run TestPluginUpgradeOrchestrationStaticBoundaries -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。

## 2. 建立统一 upgrade 组件

- [x] 2.1 新建`apps/lina-core/internal/service/plugin/internal/upgrade`主契约、`Service`接口、`serviceImpl`和构造函数，逐项接收 catalog、store、lifecycle、runtime、integration、dependency、i18n、locker、cache publisher、topology 等窄依赖，不使用`Deps`/`Options`聚合接口型依赖。

  记录：已新增`apps/lina-core/internal/service/plugin/internal/upgrade`，包含`upgrade.go`、`upgrade_code.go`、`upgrade_source_code.go`、`upgrade_source_status.go`、`upgrade_preview.go`、`upgrade_dependency.go`和`upgrade_source_execute.go`。`upgrade.Service`当前提供 source upgrade status、source readiness、runtime upgrade preview 和过渡期 source execute 包装能力；`serviceImpl`通过构造函数逐项接收`catalog.Service`、`store.Service`、`lifecycle.Service`、`runtime.Service`、`integration.Service`、`migration.Service`、`dependency.Resolver`、`I18nService`、`coordination.LockStore`、`CachePublisher`、`RuntimeCacheFreshener`、`Topology`、`MetadataReader`和过渡期`SourceUpgradeExecutor`，未使用`Deps`/`Options`聚合接口型依赖。运行期 DI 来源：owner 为`upgrade.Service`；生产创建位置为`plugin.New()`；传递路径为启动组合根创建`plugin.Service`时显式传入既有 catalog/store/lifecycle/runtime/integration/migration/dependency/i18n/cache/topology/lock 实例；缓存发布继续经根门面 adapter 回到`publishPluginChange`和`plugin-runtime`revision controller；`coordination.LockStore`仍复用启动期传入的共享锁后端，单节点允许为`nil`。本任务未新增 HTTP API、DTO、SQL、前端或插件 manifest wire。
- [x] 2.2 将 source upgrade status 查询迁入`upgrade`，保持有效版本、发现版本、待升级、异常和失败状态投影语义不变。

  记录：已将 source upgrade status 查询迁入`upgrade_source_status.go`，根门面`ListSourceUpgradeStatuses`和`ValidateSourcePluginUpgradeReadiness`只委托`upgradeSvc`。实现保持原有批量读取策略：一次`ScanManifests`加一次`ListAllRegistries`，按 plugin ID 在内存映射合并，避免随 source 插件数量增长产生逐项 registry 查询。有效版本、发现版本、未安装 no-op、`NeedsUpgrade`和`DowngradeDetected`语义保持不变；旧`internal/sourceupgrade`中的执行长流程仍作为过渡期 source executor 保留，后续 3.x 迁移。
- [x] 2.3 将 runtime upgrade preview 纯函数和 host service diff、SQL summary、risk hints 规划迁入`upgrade`，保持现有 preview 输出字段和风险提示语义不变。

  记录：已将`PreviewRuntimeUpgrade`、manifest snapshot 装配、dependency check、host service diff、SQL summary 和 risk hints 迁入`upgrade_preview.go`与`upgrade_dependency.go`。根门面`plugin_runtime_upgrade_preview.go`仅保留风险提示常量别名和一行委托；`RuntimeUpgradePreview`、`RuntimeUpgradeSQLSummary`、`RuntimeUpgradeHostServicesDiff`、`RuntimeUpgradeHostServiceChange`和 runtime upgrade state 由根包别名导出到`upgrade`，保持调用契约不变。dependency projection 在`upgrade`内用一次 manifest scan、一次 registry 批量读取和 release snapshot hydrate 完成，不反向持有根门面。错误码 owner 已迁到`upgrade_code.go`，根包继续别名导出既有 code/fallback/message key，`i18n`资源无新增文案。
- [x] 2.4 更新`plugin.New()`和`internal/testutil`显式构造并注入`upgrade.Service`，根门面公开查询/preview 方法仅委托 upgrade。

  记录：已在`plugin.New()`中显式构造`upgrade.Service`并赋给`serviceImpl.upgradeSvc`；新增`plugin_upgrade_adapters.go`将根门面私有缓存发布和 freshness 能力适配为`upgrade.CachePublisher`与`upgrade.RuntimeCacheFreshener`，子组件不反向持有根门面公开服务契约，也不绕过`publishPluginChange`。`internal/testutil.NewServices()`同步构造`Services.Upgrade`，使用同一 test catalog/store/lifecycle/runtime/integration/migration/dependency/i18n 图和 no-op cache publisher/freshener，保持测试服务图与生产装配形态一致。根门面`PreviewRuntimeUpgrade`、`ListSourceUpgradeStatuses`和`ValidateSourcePluginUpgradeReadiness`只委托`upgradeSvc`；公开`UpgradeSourcePlugin`也先委托`upgradeSvc`包装旧 source executor，平台治理守卫仍只在根门面公开入口执行。
- [x] 2.5 完成 2.x 后运行相关 upgrade/status/preview 测试和`lina-review`。

  验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/testutil -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin -run 'TestPreviewRuntimeUpgrade|TestValidateSourcePluginUpgradeReadiness|TestListSourceUpgradeStatuses|TestPluginUpgradeOrchestrationStaticBoundaries' -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。影响判断：本阶段仅调整`apps/lina-core/internal/service/plugin/**`内部 Go 代码和 OpenSpec 任务记录，不修改 HTTP API、DTO、SQL schema、前端页面、插件 manifest wire、动态插件 guest 协议或`apps/lina-plugins/*`。数据权限语义不变，公开升级入口仍执行平台治理守卫，read-only preview/status 不新增业务数据暴露范围。缓存一致性不新增缓存，升级 cache publisher 通过根门面 adapter 复用既有`plugin-runtime`revision、管理读模型失效、frontend/i18n/WASM 派生缓存失效；集群模式继续依赖启动期共享`coordination.LockStore`和 cachecoord。开发工具跨平台无影响，未修改脚本、Makefile、CI 或 linactl。无 UI 或用户可观察页面变化，未触发 E2E。

  审查记录：已按`lina-review`任务级范围审查 2.x 当前变更，范围来源为`git status --short`、`git ls-files --others --exclude-standard -- apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/plugin_upgrade_adapters.go openspec/changes/unify-plugin-upgrade-orchestration`、`openspec status --change unify-plugin-upgrade-orchestration --json`和本任务上下文；本阶段实际审查文件为`apps/lina-core/internal/service/plugin/internal/upgrade/*.go`、`apps/lina-core/internal/service/plugin/plugin.go`、`plugin_code.go`、`plugin_runtime_upgrade_preview.go`、`plugin_runtime_upgrade_execute.go`、`plugin_upgrade_adapters.go`、`internal/testutil/testutil_services.go`和`openspec/changes/unify-plugin-upgrade-orchestration/tasks.md`。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`和`goframe-v2`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，2.1-2.4 均在实现和验证后标记完成；后端 Go 通过，新增`upgrade.New()`显式逐项接收运行期依赖，初始化错误返回`error`，未使用`Deps`/`Options`聚合接口型依赖；架构通过，新增`upgrade`抽象由当前方案 D 的单一升级 owner 支撑，根门面 preview/status 已瘦身为委托；缓存一致性通过，升级发布仍经根门面`PublishPluginChange`/`syncEnabledSnapshotAndPublishRuntimeChange`进入`publishPluginChange`和共享 revision；数据权限无语义变化，公开入口治理守卫保持在根门面，preview/status 为只读查询；`i18n`无资源变化，风险提示 key、错误 code/message key/fallback 保持既有值；开发工具跨平台未命中；E2E 未触发，原因是无 UI 或用户可观察页面工作流变化。剩余风险：本阶段仍保留`sourceupgrade`执行长流程和`runtimeupgrade`纯函数依赖，这是 3.x/5.x 的计划内过渡状态，不阻塞 2.x 完成。

## 3. 迁移源码插件升级执行

- [x] 3.1 将`UpgradeSourcePlugin`执行编排迁入`upgrade`source 策略，保留 before/after callback、upgrade SQL、治理资源同步、release 提升、失败记账和缓存发布语义。

  记录：已将 source 插件升级执行长流程迁入`apps/lina-core/internal/service/plugin/internal/upgrade/upgrade_source_execute.go`，新增`ExecuteSourcePluginUpgrade`作为无公开治理守卫、无 cache publisher 的内部 source 策略入口，公开`UpgradeSourcePlugin`由`upgrade`包负责调用内部策略并发布 source 专属成功/失败缓存变化。before/after lifecycle callback、插件自定义 upgrade callback、`ExecuteManifestSQLFiles`升级 SQL、`SyncPluginMenusAndPermissions`、`SyncPluginResourceReferences`、`PromoteSourceRelease`、release 状态回写、node state 同步和升级后 hook dispatch 均已迁入`upgrade`。失败记账迁入`upgrade_source_failure.go`，保持`sys_plugin_migration`的`phase=upgrade`、`migration_key=upgrade-phase-*`和 release failed 语义；source SQL 失败额外记录`upgrade-phase-upgrade-step-source-sql`，使运行时失败投影可以稳定归一为`sql`phase。
- [x] 3.2 删除根门面 source 升级长流程，公开`UpgradeSourcePlugin`只保留平台治理守卫、必要输入校验和一行委托。

  记录：根门面`UpgradeSourcePlugin`现在只执行`ensurePlatformGovernance`后委托`upgradeSvc.UpgradeSourcePlugin`；`plugin.New()`和`internal/testutil.NewServices()`不再构造`sourceupgrade.New()`或向`upgrade.New()`传入过渡期`SourceUpgradeExecutor`。`upgrade.Service`不再包含`SourceUpgradeExecutor`依赖，source 错误码和`SourceUpgradeResult`由`upgrade`包自有定义，根包继续通过别名导出既有 code/fallback/message key 语义。
- [x] 3.3 确认 runtime upgrade execute 分派到 source 类型时直接调用 upgrade 内部 source 策略，不再调用公开`UpgradeSourcePlugin`入口。

  记录：`executeRuntimeUpgradeByType`的 source 分支已改为` s.upgradeSvc.ExecuteSourcePluginUpgrade(ctx, targetManifest.ID)`，不再调用公开`UpgradeSourcePlugin`，因此公开 runtime upgrade 入口执行一次平台治理守卫后，source 策略不会重复守卫或重复执行 source 专属 cache publisher。runtime upgrade 外层仍负责`markRuntimeUpgradeRunning`、失败后`restoreRuntimeUpgradeStableState`、`runtime_upgrade_failed`发布和成功后的`runtime_upgrade_succeeded`发布。
- [x] 3.4 补充或更新测试，验证 source 升级成功、callback veto、callback 失败、SQL 失败、治理同步失败和缓存发布失败下有效 release、失败 phase、message key 和缓存发布语义。

  记录：保留并通过既有 source 升级成功、callback veto、callback 失败和 retry 测试；新增`TestExecuteRuntimeUpgradeSourceSQLFailureKeepsEffectiveRelease`覆盖 runtime upgrade 入口直接分派 source 策略、SQL 失败后有效 release 保持旧版本、失败 phase 为`sql`且 message key 为`plugin.runtimeUpgrade.failure.migrationFailed`；新增`TestUpgradeSourcePluginGovernanceSyncFailureIsDiagnosable`覆盖治理资源同步失败后有效 release 保持旧版本、失败 phase 为`governance`且 message key 不变；新增`TestUpgradeSourcePluginCachePublisherFailureReturnsError`覆盖 source 成功切换 release 后 cache publisher 失败会向调用方返回错误。测试通过替换`upgrade.Service`的窄 integration/cache publisher 依赖注入故障，不修改生产装配。
- [x] 3.5 完成 3.x 后运行 source upgrade 相关测试、插件根包升级测试和`lina-review`。

  验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/store -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin -run 'TestUpgradeSourcePlugin|TestExecuteRuntimeUpgradeSource|TestExecuteRuntimeUpgrade.*Source|TestListSourceUpgradeStatuses|TestValidateSourcePluginUpgradeReadiness|TestPluginUpgradeOrchestrationStaticBoundaries|TestSourcePluginDiscoveryKeepsEffectiveVersion|TestExecuteRuntimeUpgrade.*Dynamic' -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。影响判断：本阶段仅修改`apps/lina-core/internal/service/plugin/**`内部 Go 代码、测试和 OpenSpec 任务记录，不修改 HTTP API、DTO、SQL schema、SQL 文件、前端页面、插件 manifest wire、动态插件 guest 协议或`apps/lina-plugins/*`。DI 来源变化为删除过渡期`SourceUpgradeExecutor`，`upgrade.Service`继续由`plugin.New()`和`internal/testutil.NewServices()`显式构造并复用启动期 catalog/store/lifecycle/runtime/integration/migration/dependency/i18n/cache/topology/lock 实例；未新增运行期服务 owner。缓存一致性保持既有权威源和触发点：source 专属公开入口成功/失败由`upgrade`通过 cache publisher 回到根门面`publishPluginChange`，runtime upgrade 外层继续发布`runtime_upgrade_succeeded/failed`，集群同步仍复用`plugin-runtime`revision controller。数据权限语义不变，公开入口平台治理守卫只执行一次，内部 source 策略不放宽租户或平台边界。`i18n`资源无新增文案，source result message key、错误 code/fallback 和 runtime failure message key 保持既有值。开发工具跨平台无影响，未修改脚本、Makefile、CI 或 linactl。无 UI 或用户可观察页面工作流变化，未触发 E2E。

  审查记录：已按`lina-review`任务级范围审查 3.x 当前变更，范围来源为`git status --short`、`git ls-files --others --exclude-standard -- apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin openspec/changes/unify-plugin-upgrade-orchestration`、`openspec status --change unify-plugin-upgrade-orchestration --json`和本任务上下文；本阶段实际审查文件为`apps/lina-core/internal/service/plugin/internal/upgrade/*.go`、`apps/lina-core/internal/service/plugin/internal/store/store_runtime_upgrade_state.go`、`apps/lina-core/internal/service/plugin/plugin.go`、`plugin_code.go`、`plugin_runtime_upgrade_execute.go`、`plugin_runtime_upgrade_execute_test.go`、`internal/testutil/testutil_services.go`和`openspec/changes/unify-plugin-upgrade-orchestration/tasks.md`。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`以及`goframe-v2`、`openspec-apply-change`、`lina-review`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，3.1-3.4 均在实现和验证后标记完成；后端 Go 通过，source execute 长流程迁入`upgrade`，根门面公开入口仅保留治理守卫和委托，`ExecuteRuntimeUpgrade`的 source 分支直接调用内部 source 策略；架构通过，source 升级 owner 已收敛到`upgrade`，旧`sourceupgrade`目录仅作为 5.x 清理前的过渡残留且生产路径不再依赖；缓存一致性通过，公开 source 入口成功/失败和 runtime 外层成功/失败发布语义保持既有作用域化发布；数据权限通过，公开治理守卫仍只在根门面执行一次，内部策略未新增数据可见性路径；`i18n`无资源变化，错误码、message key 和 fallback 保持兼容；开发工具跨平台未命中；E2E 未触发，原因是本阶段无前端页面、路由或用户可观察工作流变化。剩余风险：`runtimeupgrade`纯函数和旧目录清理仍属于 4.x/5.x 计划内任务，不阻塞 3.x 完成。

## 4. 迁移动动态插件升级执行

- [x] 4.1 将根门面 runtime upgrade execute 编排迁入`upgrade`dynamic 策略，保留分布式锁内状态复查、authorization 持久化、runtime upgrade request、release 切换和 rollback 语义。

  记录：已新增`apps/lina-core/internal/service/plugin/internal/upgrade/upgrade_runtime_execute.go`并将根门面 runtime upgrade execute 状态机迁入`upgrade`包。根门面`ExecuteRuntimeUpgrade`现在只执行`ensurePlatformGovernance`后委托`upgradeSvc.ExecuteRuntimeUpgrade`；`upgrade`内部负责确认标记、单机/集群锁、锁内 cache fresh、目标 manifest/registry/projection 重读、running 状态标记、source/dynamic 分派、失败后 stable state 恢复和成功/失败 cache publisher。dynamic 策略保留 authorization 持久化、`runtimeSvc.UpgradeDynamicPluginRequest`、release 切换和 runtime rollback 语义；source 分派继续调用`ExecuteSourcePluginUpgrade`内部策略，不再再入公开`UpgradeSourcePlugin`。根门面已删除 runtime upgrade lock map 字段，`plugin.New()`仍保存启动期`runtimeUpgradeLockStore`用于构造`upgrade.Service`和测试重建。
- [x] 4.2 将 dynamic upgrade 失败诊断和旧有效 release 保留逻辑纳入统一升级失败模型，确保失败目标 release 不成为缓存权威来源。

  记录：已在`runtime`包新增结构化 dynamic upgrade phase error，`applyUpgrade`对 upgrade callback、SQL、governance、release switch 和 cache invalidation 失败打上`plugintypes.RuntimeUpgradeFailurePhase`，rollback 追加诊断时仍保留原始失败 phase。`upgrade`dynamic 策略在`runtimeSvc.UpgradeDynamicPluginRequest`返回失败后重读目标 release，只有 runtime 已将目标 release 标记为`failed`时，才写入统一`sys_plugin_migration`失败账本；已成功切换 release 后发生的最终 cache publisher 错误不会写失败账本，也不会把有效 release 降级为失败目标。`store`运行时升级投影继续以 registry 当前有效 release 作为缓存权威来源，以 failed target release 作为失败诊断来源，并补齐`upgrade-phase-sql`、`upgrade-phase-release_switch`和`upgrade-phase-cache_invalidation`等统一账本 key 的 phase 归一化。
- [x] 4.3 补充或更新测试，验证 dynamic 升级成功、SQL 失败、release switch 失败、rollback 失败、cache invalidation 失败和同版本刷新失败语义。

  记录：已保留并通过 dynamic 升级成功测试`TestExecuteRuntimeUpgradeUpgradesDynamicPlugin`。已更新`TestExecuteRuntimeUpgradeFailureKeepsEffectiveDynamicVersion`，验证 dynamic SQL 失败后旧有效 release 保持、`LastUpgradeFailure.Phase=sql`、message key 为`plugin.runtimeUpgrade.failure.migrationFailed`且写入`upgrade-phase-sql`失败账本。已新增`TestExecuteRuntimeUpgradeDynamicCachePublisherFailureReturnsError`，验证最终 cache publisher 失败返回错误但 release 已正常切换、runtime state 保持 normal，且不写 failed migration ledger。已新增`TestExecuteRuntimeUpgradeDynamicSameVersionRefreshUnavailable`，验证同版本动态刷新不会被 runtime upgrade execute 入口执行。已新增`runtime`同包`TestDynamicUpgradeFailurePhaseSurvivesJoinedDiagnostics`和`TestTagDynamicUpgradeFailureKeepsExistingPhase`，验证 rollback 诊断不会覆盖原始失败 phase；并更新`TestRollbackInstallOrUpgradeReturnsRollbackDiagnostics`断言 rollback SQL 失败后仍能读取原始 SQL phase。已在 store 投影测试中新增`TestRuntimeUpgradeStateMapsUnifiedFailurePhaseLedger`，覆盖 release switch 和 cache invalidation 失败账本映射为统一 migration failure message key。
- [x] 4.4 完成 4.x 后运行 runtime upgrade、runtime reconciler、store projection 和插件全包相关测试，并调用`lina-review`。

  验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/store -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin -run 'TestExecuteRuntimeUpgrade|TestRuntimeUpgradeClusterLock|TestDynamicPluginRuntimeUpgrade|TestPluginUpgradeOrchestrationStaticBoundaries|TestUpgradeSourcePlugin' -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。影响判断：本阶段仅修改`apps/lina-core/internal/service/plugin/**`内部 Go 代码、测试和 OpenSpec 任务记录，不修改 HTTP API、DTO、SQL schema、SQL 文件、前端页面、插件 manifest wire、动态插件 guest 协议或`apps/lina-plugins/*`。DI 来源变化为 runtime upgrade execute 状态机 owner 从根门面迁入`upgrade.Service`；owner 为`upgrade.Service`，生产创建位置为`plugin.New()`，测试创建位置为`internal/testutil.NewServices()`和根包测试`replaceUpgradeServiceForTest`，传递路径继续显式接收启动期 catalog/store/lifecycle/runtime/integration/migration/dependency/i18n/cache/topology/lock 实例，未新增运行期依赖或独立服务图。缓存一致性保持既有权威源和触发点：成功/失败 runtime upgrade 经 cache publisher 回到根门面`syncEnabledSnapshotAndPublishRuntimeChange`和`plugin-runtime`revision；dynamic runtime 内部 release 切换仍通过`notifyRuntimeCacheChanged`和 reconciler revision 通知；最终 cache publisher 失败不会把已成功切换的 release 写成 failed migration。数据权限语义不变，公开入口平台治理守卫仍只在根门面执行一次，内部 dynamic 策略不新增数据读取接口或放宽租户边界。`i18n`资源无新增文案，runtime failure message key 继续使用既有`plugin.runtimeUpgrade.failure.migrationFailed`和 release fallback key。开发工具跨平台无影响，未修改脚本、Makefile、CI 或 linactl。无 UI 或用户可观察页面工作流变化，未触发 E2E。

  审查记录：已按`lina-review`任务级范围审查 4.x 当前变更，范围来源为`git status --short`、`git ls-files --others --exclude-standard -- apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin openspec/changes/unify-plugin-upgrade-orchestration`、`git diff --name-only -- apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_execute.go apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_execute_test.go openspec/changes/unify-plugin-upgrade-orchestration/tasks.md`、`openspec status --change unify-plugin-upgrade-orchestration --json`和本任务上下文；本阶段实际审查文件为`apps/lina-core/internal/service/plugin/internal/upgrade/upgrade_runtime_execute.go`、`upgrade_source_failure.go`、`upgrade_runtime_execute_test.go`、`apps/lina-core/internal/service/plugin/internal/runtime/reconciler_upgrade.go`、`reconciler_safety_test.go`、`upgrade_failure.go`、`upgrade_failure_test.go`、`apps/lina-core/internal/service/plugin/internal/store/store_runtime_upgrade_state.go`、`store_startup_snapshot_test.go`、`apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_execute.go`、`plugin_runtime_upgrade_execute_test.go`和`openspec/changes/unify-plugin-upgrade-orchestration/tasks.md`。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`以及`goframe-v2`、`openspec-apply-change`、`lina-review`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，4.1-4.3 均在实现和验证后标记完成；后端 Go 通过，新增文件有文件职责注释，运行期依赖仍通过构造函数显式注入；架构通过，runtime upgrade execute 编排已迁入`upgrade`，root facade 保持治理守卫和委托；缓存一致性通过，失败诊断写账本只在目标 release 已 failed 时发生，避免失败目标成为缓存权威源；数据权限通过，公开入口治理边界不变；`i18n`无资源变化，错误 key/fallback 维持既有语义；插件目录规则无`apps/lina-plugins/*`影响；开发工具跨平台未命中；E2E 未触发，原因是本阶段无前端页面、路由或端到端用户工作流变化。剩余风险：旧`internal/sourceupgrade`和`internal/runtimeupgrade`目录清理、失败诊断 owner 的最终单一化和 cache publisher 最终收敛仍属于 5.x 计划内任务，不阻塞 4.x 完成。

## 5. 失败诊断、缓存和包清理

- [x] 5.1 将`sys_plugin_migration`升级失败 phase 归一化、失败码、message key 和 fallback 构造收敛到单一 owner，删除 source/dynamic 分散重复逻辑。

  记录：已将 source/dynamic synthetic 失败账本写入收敛到`upgrade`包的`recordRuntimeUpgradeFailureMigration`，source 失败和 dynamic runtime 失败均写入同一`sys_plugin_migration`约定：`phase=upgrade`、`migration_key=upgrade-phase-<phase>`、`status=failed`、`error_message=<原始错误>`。`store.BuildRuntimeUpgradeFailureWithLatestMigration`继续作为治理投影 owner，统一将失败 release 叠加最新 failed upgrade migration，输出`RuntimeUpgradeFailureCodeMigrationFailed`和`plugin.runtimeUpgrade.failure.migrationFailed`。已补齐`upgrade-phase-sql`、`upgrade-phase-release_switch`、`upgrade-phase-cache_invalidation`等 key 映射，删除旧`sourceupgrade`包内重复失败写入逻辑。
- [x] 5.2 将升级成功、升级失败和失败诊断变化后的缓存发布统一为 upgrade 组件调用 cache publisher，并继续经根门面`publishPluginChange`发布`plugin-runtime`revision。

  记录：`upgrade.Service`通过构造函数接收`CachePublisher`窄契约，source 公开升级入口和 runtime upgrade 外层成功/失败路径均通过该 publisher 回到根门面`publishPluginChange`/`syncEnabledSnapshotAndPublishRuntimeChange`，继续复用`plugin-runtime`revision controller、管理读模型失效、runtime 派生缓存、frontend bundle、i18n runtime bundle 和 WASM 派生缓存失效。dynamic runtime 底层 release 切换仍使用 runtime 的`notifyRuntimeCacheChanged`和 reconciler revision 发布；外层最终 cache publisher 失败只向调用方返回错误，不写 failed release migration，避免失败目标成为缓存权威来源。
- [x] 5.3 删除`internal/sourceupgrade`和`internal/runtimeupgrade`目录，迁移或删除对应测试，更新 import 和类型别名。

  记录：已将`internal/runtimeupgrade`中的 preview SQL summary、hostServices diff、risk hints、`CanExecute`和分布式锁命名迁入`internal/upgrade/upgrade_runtime_plan.go`；`upgrade.go`中的公开类型别名改为指向同包`SQLSummary`、`HostServicesDiff`和`HostServiceChange`；`upgrade_preview.go`和`upgrade_runtime_execute.go`不再 import`internal/runtimeupgrade`。旧`internal/sourceupgrade`执行、状态和失败测试已由`internal/upgrade`测试与根包 runtime/source upgrade 测试覆盖，旧目录和旧`internal/runtimeupgrade`目录均已删除。
- [x] 5.4 收紧静态边界测试，阻断旧升级包、根门面升级长流程、source 再入公开方法和重复失败诊断 owner 回流。

  记录：`TestPluginUpgradeOrchestrationStaticBoundaries`已从过渡模式改为最终严格模式：要求`internal/upgrade`目录存在且 package 名为`upgrade`；阻断`internal/sourceupgrade`和`internal/runtimeupgrade`目录恢复；阻断生产代码重新 import 旧升级包；阻断根门面 runtime upgrade execute 再入公开`UpgradeSourcePlugin`；阻断`recordSourceUpgradeFailureMigration`等旧失败诊断 owner helper 回流。
- [x] 5.5 记录最终 DI 来源检查：`upgrade.Service`owner、创建位置、传递路径、共享实例策略和缓存后端复用情况。

  记录：`upgrade.Service`是统一升级编排 owner，生产创建位置为`apps/lina-core/internal/service/plugin/plugin.go`的`plugin.New()`；测试创建位置为`internal/testutil.NewServices()`和根包测试`replaceUpgradeServiceForTest`。传递路径为启动组合根显式传入 catalog、store、lifecycle、runtime、integration、migration、dependency resolver、i18n、coordination lock store、cache publisher、runtime cache freshener、topology、metadata reader，未使用`Deps`/`Options`聚合接口型依赖，也未在业务路径临时`New()`关键服务。共享实例策略：catalog/store/lifecycle/runtime/integration/migration/i18n/cachecoord/topology/lock 均复用启动期同一服务图或共享后端；`cluster.enabled=true`时 runtime upgrade lock 使用启动期`coordination.LockStore`，缺失时 fail closed；`cluster.enabled=false`时允许本地进程锁。缓存后端复用根门面`publishPluginChange`和`plugin-runtime`revision controller，未新增本地孤立缓存。
- [x] 5.6 完成 5.x 后运行静态边界测试、插件全包测试和`lina-review`。

  验证：`test ! -e apps/lina-core/internal/service/plugin/internal/sourceupgrade && test ! -e apps/lina-core/internal/service/plugin/internal/runtimeupgrade`通过；`rg 'lina-core/internal/service/plugin/internal/(sourceupgrade|runtimeupgrade)' apps/lina-core/internal/service/plugin --glob '*.go' -n`仅命中静态边界测试中的 forbidden import 字符串；`cd apps/lina-core && go test ./internal/service/plugin -run 'TestPluginUpgradeOrchestrationStaticBoundaries|TestPreviewRuntimeUpgrade|TestExecuteRuntimeUpgrade|TestUpgradeSourcePlugin|TestRuntimeUpgradeClusterLock' -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin/internal/upgrade ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/store -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。影响判断：本阶段仅修改`apps/lina-core/internal/service/plugin/**`内部 Go 代码、测试和 OpenSpec 任务记录，不修改 HTTP API、DTO、SQL schema、SQL 文件、前端页面、插件 manifest wire、动态插件 guest 协议或`apps/lina-plugins/*`。数据权限语义不变，公开入口治理守卫仍在根门面执行一次。`i18n`资源无新增文案，错误 code/message key/fallback 保持既有值。开发工具跨平台无影响，未修改脚本、Makefile、CI 或 linactl。无 UI 或用户可观察页面工作流变化，未触发 E2E。

  审查记录：已按`lina-review`任务级范围审查 5.x 当前变更，范围来源为`git status --short`、`git ls-files --others --exclude-standard -- apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin openspec/changes/unify-plugin-upgrade-orchestration`、旧目录缺失检查、`openspec status --change unify-plugin-upgrade-orchestration --json`和本任务上下文；本阶段实际审查文件为`apps/lina-core/internal/service/plugin/internal/upgrade/upgrade_runtime_plan.go`、`upgrade.go`、`upgrade_preview.go`、`upgrade_runtime_execute.go`、`upgrade_source_failure.go`、`apps/lina-core/internal/service/plugin/internal/runtime/upgrade_failure.go`、`reconciler_upgrade.go`、`apps/lina-core/internal/service/plugin/internal/store/store_runtime_upgrade_state.go`、`store_source_upgrade.go`、`apps/lina-core/internal/service/plugin/plugin_boundary_test.go`、`plugin_runtime_upgrade_execute_test.go`和`openspec/changes/unify-plugin-upgrade-orchestration/tasks.md`。已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`以及`goframe-v2`、`openspec-apply-change`、`lina-review`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，5.1-5.5 均在实现和验证后标记完成；后端 Go 通过，旧平行包已清零，`upgrade`导出面承载对外别名所需类型，具体流程仍在职责文件中；架构通过，统一升级 owner 形成，根门面无升级长流程回流；缓存一致性通过，升级发布仍经根门面统一 publisher 和共享 revision；数据权限通过，未新增数据访问入口或绕过治理守卫；`i18n`无资源变化；插件目录规则无`apps/lina-plugins/*`影响；开发工具跨平台未命中；E2E 未触发，原因是无前端或端到端用户工作流变化。剩余风险：6.x 仍需运行完整 plugin、i18n、cachecoord、internal/cmd 和最终 OpenSpec/diff 验证后才能判定整个变更完成。

## 6. 验证与归档准备

- [x] 6.1 运行`cd apps/lina-core && go test ./internal/service/plugin/... -count=1`。

  验证：通过。覆盖`plugin`根包、`capabilityhost`、`catalog`、`datahost`、`dependency`、`frontend`、`integration`、`lifecycle`、`management`、`migration`、`openapi`、`runtime`、`store`、`testutil`、`upgrade`、`wasm`和`wasm/hostservicedispatch`等插件服务全包。
- [x] 6.2 运行`cd apps/lina-core && go test ./internal/service/i18n/... -count=1`。

  验证：通过。
- [x] 6.3 运行`cd apps/lina-core && go test ./internal/service/cachecoord/... -count=1`。

  验证：通过，覆盖`cachecoord`和`cachecoord/revisionctrl`。
- [x] 6.4 运行`cd apps/lina-core && go test ./internal/cmd -count=1`。

  验证：通过，覆盖宿主启动绑定包编译门禁。
- [x] 6.5 运行`openspec validate unify-plugin-upgrade-orchestration --strict`和`git diff --check`。

  验证：`openspec validate unify-plugin-upgrade-orchestration --strict`通过，输出`Change 'unify-plugin-upgrade-orchestration' is valid`；`git diff --check`通过。
- [x] 6.6 完成实现、验证和影响记录后调用最终`lina-review`；审查通过后再考虑后续可选 E 阶段。

  审查记录：已按`lina-review`对`unify-plugin-upgrade-orchestration`执行最终变更范围审查。范围来源为`git status --short`、`git ls-files --others --exclude-standard -- apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin openspec/changes/unify-plugin-upgrade-orchestration`、`git diff --name-only -- apps/lina-core/internal/service/plugin/internal/upgrade apps/lina-core/internal/service/plugin/internal/runtime apps/lina-core/internal/service/plugin/internal/store apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_upgrade_adapters.go apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_execute.go apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_preview.go apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_execute_test.go apps/lina-core/internal/service/plugin/plugin_runtime_upgrade_preview_test.go apps/lina-core/internal/service/plugin/plugin_boundary_test.go apps/lina-core/internal/service/plugin/internal/testutil/testutil_services.go openspec/changes/unify-plugin-upgrade-orchestration`、旧目录缺失检查、`openspec status --change unify-plugin-upgrade-orchestration --json`和 apply instructions 当前进度。实际审查重点为`internal/upgrade`统一升级 owner、根门面升级委托、`runtime`动态升级 phase error、`store`失败投影、cache publisher adapter、静态边界测试、相关升级测试和 OpenSpec 任务记录；未将无关 dirty 变更作为本任务结论依据，未触碰`apps/lina-plugins/*`。

  已读取规则文件和技能说明：`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/backend-go.md`、`.agents/rules/plugin.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`以及`goframe-v2`、`openspec-apply-change`、`lina-review`技能说明。审查未发现阻塞问题。规则域结论：OpenSpec 通过，30 个任务均在实现和验证完成后标记；后端 Go 通过，`upgrade.Service`显式逐项接收运行期依赖，生产创建在`plugin.New()`，测试创建在`internal/testutil.NewServices()`和根包测试替换点，未使用`Deps`/`Options`聚合接口依赖，也未在业务路径临时创建关键服务图；架构通过，source/dynamic 升级编排已收敛到`internal/upgrade`，根门面仅保留平台治理守卫和委托，旧`internal/sourceupgrade`、`internal/runtimeupgrade`目录和生产 import 已清零；缓存一致性通过，升级成功/失败仍经 cache publisher 回到根门面`publishPluginChange`/`plugin-runtime`revision，dynamic release switch 内部继续使用 runtime/reconciler revision，最终 cache publisher 失败不把失败目标 release 写成权威缓存来源；数据权限通过，公开升级入口平台治理守卫执行一次，内部策略未新增数据暴露路径或放宽租户边界；`i18n`无资源变更，错误 code、message key 和 fallback 保持既有语义；插件目录规则无`apps/lina-plugins/*`影响；文档规则通过，本次只更新 OpenSpec 任务记录并已通过严格校验；开发工具跨平台未命中，未修改脚本、Makefile、CI、linactl 或工具入口；API/DTO、SQL schema、前端页面、动态 guest wire 和插件 manifest wire 均无影响；E2E 未触发，原因是无前端页面、路由或端到端用户工作流变化。

  验证：`test ! -e apps/lina-core/internal/service/plugin/internal/sourceupgrade && test ! -e apps/lina-core/internal/service/plugin/internal/runtimeupgrade`通过；`rg 'lina-core/internal/service/plugin/internal/(sourceupgrade|runtimeupgrade)' apps/lina-core/internal/service/plugin --glob '*.go' -n`仅命中`plugin_boundary_test.go`中的 forbidden import 字符串；`cd apps/lina-core && go test ./internal/service/plugin/... -count=1`通过；`cd apps/lina-core && go test ./internal/service/i18n/... -count=1`通过；`cd apps/lina-core && go test ./internal/service/cachecoord/... -count=1`通过；`cd apps/lina-core && go test ./internal/cmd -count=1`通过；`openspec validate unify-plugin-upgrade-orchestration --strict`通过；`git diff --check`通过。剩余风险：本变更已达到方案 D 验收口径，后续只剩用户确认后可选执行归档或继续推进方案 E；当前未 commit、未 push。
