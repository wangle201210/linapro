## 1. Workflow 实现

- [x] 1.1 新增仅支持`workflow_dispatch`的手动`nightly`镜像发布 workflow
- [x] 1.2 让手动 workflow 直接调用`.github/workflows/reusable-image-publish.yml`，并确认不存在测试 job 或`needs`测试依赖
- [x] 1.3 配置与现有 nightly 一致的镜像名称、平台、`tag-mode`、浮动标签和官方插件完整构建模式

## 2. 验证与审查

- [x] 2.1 运行 workflow YAML 解析和`actionlint`静态检查
- [x] 2.2 运行`openspec validate manual-nightly-image-build --strict`
- [x] 2.3 运行`git diff --check -- .github/workflows openspec/changes/manual-nightly-image-build`
- [x] 2.4 记录本变更不新增后端生产代码、业务 API、数据库 schema、前端运行时文案、运行时缓存、数据权限逻辑或默认开发工具脚本，因此无需新增 Go 编译门禁、i18n、缓存一致性或数据权限实现变更
- [x] 2.5 调用`lina-review`完成代码和规范审查

## Feedback

- [x] **FB-1**: 通过`.github/workflows/nightly-image-build.yml`构建的插件完整镜像启动后，所有插件工作台页面显示插件页面未找到
- [x] **FB-2**: nightly 插件完整镜像启动自动启用监控插件后，租户级监控菜单只显示服务监控，登录日志、在线用户和操作日志菜单缺失
- [x] **FB-3**: nightly 镜像中的服务监控页面样式与本地开发环境不一致
- [x] **FB-4**: 缺少内存态`Docker Compose`演示启动文件，体验者仍需要自行拼装镜像运行命令或持久化挂载
- [x] **FB-5**: `hack/deploy/docker-compose.yaml`缺少必要注释，不利于体验者理解镜像覆盖、端口覆盖、内存态数据和初始化流程
- [x] **FB-6**: `hack/deploy/docker-compose.yaml`内联运行时配置过长，应剥离为`hack/deploy/config.yaml`单独维护
- [x] **FB-7**: 演示`Docker Compose`仍使用 SQLite，应改为使用 PostgreSQL 服务运行 LinaPro
- [x] **FB-8**: `hack/deploy/tests/docker-compose.yaml`仍保留演示应用启动逻辑，应改为 PostgreSQL 加手动验证用 `loads/ubuntu:24.04-npm` 开发容器

## 验证记录

- [x] 2026-05-18: FB-8 验证通过。将`hack/deploy/tests/docker-compose.yaml`调整为`postgres`与`dev`两个服务：`postgres`继续使用内存态`tmpfs`数据目录和`pg_isready`健康检查；`dev`默认使用`loads/ubuntu:24.04-npm`，等待`postgres`健康后常驻运行，挂载当前仓库到`/workspace/linapro`，设置`GF_GCFG_PATH=/workspace/linapro/hack/deploy/tests`，并创建`/app/data/upload`与`/app/data/wasm`内存态运行目录，便于开发者进入容器后手动执行初始化、构建、测试或其他验证命令。同步新增`release-image-build`增量规范，明确测试 Compose 不自动执行`lina init`、`lina mock`或启动 LinaPro HTTP 服务。验证命令：`docker compose -f hack/deploy/tests/docker-compose.yaml config`、`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "yaml ok"' hack/deploy/tests/docker-compose.yaml hack/deploy/tests/config.yaml`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- hack/deploy/tests/docker-compose.yaml openspec/changes/manual-nightly-image-build/specs/release-image-build/spec.md openspec/changes/manual-nightly-image-build/tasks.md`。i18n 影响：仅调整部署测试 Compose 和 OpenSpec 记录，不新增或修改产品运行时文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例同步策略；测试配置仍为单实例手动验证入口。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：不新增宿主平台脚本或默认开发工具入口；Compose 仅提供容器化手动验证环境。
- [x] 2026-05-17: 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/manual-nightly-image-build.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/manual-nightly-image-build.yml`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- .github/workflows openspec/changes/manual-nightly-image-build`，并对新增未跟踪文件补充执行`git diff --no-index --check`空白检查。确认新增 workflow 仅包含`workflow_dispatch`触发和直接调用`.github/workflows/reusable-image-publish.yml`的镜像发布 job，不包含测试 job 或测试`needs`依赖。i18n 影响：不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例一致性策略。数据权限影响：不新增或修改业务 API、数据库查询或角色数据权限边界。开发工具与脚本影响：仅新增`GitHub Actions`编排文件，不新增仓库默认开发工具、跨平台脚本或长期维护脚本入口。
- [x] 2026-05-17: FB-1 验证通过。根因不是默认`go.work`未引用插件；`plugins=1`镜像构建会通过`linactl`生成临时`temp/go.work.plugins`并设置`official_plugins`构建标签。实际根因为前端构建走`turbo build`且`turbo.json`处于 strict env 模式，`LINAPRO_SOURCE_PLUGINS`未列入`globalEnv`，导致 Vite 插件页面扫描在 plugin-full 镜像构建中被关闭，最终`virtual:lina-plugin-pages`为空并显示插件页面未找到。修复：将`LINAPRO_SOURCE_PLUGINS`加入`apps/lina-vben/turbo.json`的`globalEnv`，并新增`linactl`回归测试校验该配置。验证命令：`node -e "const fs=require('fs'); const raw=fs.readFileSync('/tmp/linapro-turbo-dry.json','utf8').trim(); const data=JSON.parse(raw); const env=data.globalCacheInputs.environmentVariables.specified.env; console.log(env.join(',')); if (!env.includes('LINAPRO_SOURCE_PLUGINS')) process.exit(1);"`、`ruby -e 'require "json"; JSON.parse(File.read("apps/lina-vben/turbo.json")); puts "turbo json ok"'`、`cd hack/tools/linactl && go test ./... -run TestFrontendTurboAllowsSourcePluginBuildEnv -count=1`、`cd hack/tools/linactl && go test ./... -count=1`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- apps/lina-vben/turbo.json hack/tools/linactl/main_test.go openspec/changes/manual-nightly-image-build/tasks.md`、`cd apps/lina-vben && LINAPRO_SOURCE_PLUGINS=1 pnpm turbo run build --filter=@lina/web-antd --force`、`rg -l "system/notice|monitor/loginlog|platform/tenants|plugin-demo-source-sidebar-entry" apps/lina-vben/apps/web-antd/dist`。确认构建产物包含插件页面路由。i18n 影响：不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时业务缓存；本次只让 Turborepo 构建缓存将 source-plugin 模式环境变量纳入 hash 与环境透传。数据权限影响：不新增或修改 HTTP/API 数据操作接口或角色数据权限边界。开发工具与脚本影响：修改既有 Turborepo 配置和 Go 版`linactl`测试，不新增平台专属脚本或默认开发入口。
- [x] 2026-05-17: FB-2 验证通过。根因不是`monitor-loginlog`、`monitor-online`、`monitor-operlog`未被镜像构建或宿主 registry 未启用，而是这三个插件声明为`scope_nature=tenant_aware`且`default_install_mode=tenant_scoped`；启动期`plugin.autoEnable`只把它们安装并启用到宿主`sys_plugin`，没有在源码插件 provider 注册后给已有活跃租户写入`sys_plugin_state(state_key='__tenant_enabled__')`，菜单过滤因此在租户上下文隐藏这三个插件。`monitor-server`是`platform_only/global`，所以仍可显示。修复：在 source plugin 路由/provider 注册后、启动一致性校验前新增`ReconcileAutoEnabledTenantPlugins`阶段，将 auto-enabled tenant-scoped 插件设置为`auto_enable_for_new_tenants=true`，并通过 multi-tenant provider 调用`tenantplugin.ProvisionForTenant`为已有活跃租户补齐缺失启用行；该补齐只插入缺失行，不覆盖已有租户显式禁用状态。新增测试：`TestReconcileAutoEnabledTenantPluginsProvisionsTenantScopedEntries`覆盖宿主启动补偿与租户菜单启用判定；`TestProvisionAutoEnabledTenantPluginsAppliesExistingActiveTenants`覆盖已有活跃租户补齐、暂停租户跳过和显式禁用保留；`TestProvisionForTenantPreservesExistingTenantChoice`覆盖租户插件服务不覆盖已有禁用行；`TC0237-auto-enabled-monitor-menus.ts`覆盖租户视图`menus/all`包含登录日志、在线用户、操作日志和服务监控菜单路由。验证通过命令：`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -run 'TestReconcileAutoEnabledTenantPluginsProvisionsTenantScopedEntries|TestUpdateTenantProvisioningPolicySurvivesManifestSync' -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend/internal/service/provider ./backend/internal/service/tenantplugin -run 'TestProvisionAutoEnabledTenantPluginsAppliesExistingActiveTenants|TestProvisionForTenant(AutoEnablesPolicyEnabledTenantPlugins|PreservesExistingTenantChoice|SkipsAutoEnablePolicyWhenPluginDisabled)' -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test -p=1 ./backend ./backend/internal/service/provider ./backend/internal/service/tenantplugin -count=1`、`cd apps/lina-plugins/multi-tenant && npx --prefix ../../../hack/tests tsc --noEmit --project ../../../hack/tests/tsconfig.json`、`cd hack/tests && pnpm test:validate`、`cd hack/tests && E2E_BROWSER_CHANNEL=chrome pnpm exec playwright test apps/lina-plugins/multi-tenant/hack/tests/e2e/plugin-governance/TC0237-auto-enabled-monitor-menus.ts --workers=1`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_auto_enable.go apps/lina-core/internal/service/plugin/plugin_auto_enable_test.go apps/lina-core/internal/service/plugin/plugin_code.go apps/lina-core/internal/service/startupstats/startupstats.go apps/lina-core/internal/service/tenantcap/tenantcap_impl.go apps/lina-core/pkg/tenantcap/tenantcap.go openspec/changes/manual-nightly-image-build/tasks.md`、`cd apps/lina-plugins && git diff --check -- multi-tenant/backend/plugin.go multi-tenant/backend/internal/service/provider/provider.go multi-tenant/backend/internal/service/provider/provider_impl.go multi-tenant/backend/internal/service/provider/provider_provisioning_test.go multi-tenant/backend/internal/service/tenantplugin/tenantplugin.go multi-tenant/backend/internal/service/tenantplugin/tenantplugin_impl.go multi-tenant/backend/internal/service/tenantplugin/tenantplugin_test.go multi-tenant/hack/tests/support/multi-tenant-scenarios.ts multi-tenant/hack/tests/e2e/plugin-governance/TC0237-auto-enabled-monitor-menus.ts`。补充说明：曾尝试运行`cd apps/lina-core && go test -p=1 ./internal/service/plugin ./internal/service/tenantcap ./internal/service/startupstats ./pkg/tenantcap -count=1`，其中`tenantcap/startupstats/pkg/tenantcap`可编译通过，但`internal/service/plugin`受本地共享 PostgreSQL 测试状态中已有动态插件反向依赖数据影响，出现`Plugin <id> cannot be changed because installed plugins depend on it`类失败；与本次 auto-enable 租户补齐路径无直接关系，已用上述目标测试覆盖变更包与启动编排门禁。i18n 影响：不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源；新增 E2E 文本仅为测试描述。缓存一致性影响：复用既有`tenantplugin.ProvisionForTenant`写入租户启用行并更新`plugin-runtime/global`共享修订号；宿主启动阶段刷新 enabled snapshot，不新增缓存域。数据权限影响：不新增或修改 HTTP/API 数据操作接口；只在启动治理阶段写入插件租户启用状态，未改变业务数据权限过滤。开发工具与脚本影响：不新增或修改开发工具、脚本或跨平台入口。
- [x] 2026-05-17: FB-3 验证通过。根因是`apps/lina-vben/packages/@core/base/design/src/css/global.css`在 Tailwind v4 中使用`@import 'tailwindcss' source(none);`后只显式扫描`packages/@core/`、`packages/effects/`和`apps/web-antd/src/`，nightly 插件完整镜像开启`LINAPRO_SOURCE_PLUGINS=1`后会把`apps/lina-plugins/*/frontend`源码插件页面编入生产应用，但设计包 CSS 构建没有扫描源码插件目录，导致服务监控页面使用的插件专属 utility 与 Iconify 类在生产 CSS 中缺失；本地开发环境可正常展示，是因为开发态对源码插件页面的即时处理没有暴露该生产 CSS 扫描缺口。修复：在全局 Tailwind 入口新增`@source '../../../../../../../lina-plugins/';`，并新增`TestFrontendTailwindScansSourcePluginPages`防止后续移除该扫描范围；同时扩展`TC0052-server-monitor-page.ts`，断言节点展开/收起图标可见。验证命令：`cd apps/lina-vben/packages/@core/base/design && pnpm vite build`、`cd apps/lina-vben && LINAPRO_SOURCE_PLUGINS=1 pnpm turbo run build --filter=@lina/web-antd --force`、`rg -F "ant-design--caret-down-outlined" apps/lina-vben/apps/web-antd/dist/css`、`rg -F "ant-design--caret-right-outlined" apps/lina-vben/apps/web-antd/dist/css`、`rg -F "server-monitor-disk-table" apps/lina-vben/apps/web-antd/dist/css`、`rg -F "icon-[ant-design--caret-down-outlined]" apps/lina-vben/apps/web-antd/dist/js`、`rg -F "plugin.monitor-server.sections.database" apps/lina-vben/apps/web-antd/dist/js`、`cd hack/tools/linactl && go test ./... -run 'TestFrontend(TurboAllowsSourcePluginBuildEnv|TailwindScansSourcePluginPages)' -count=1`、`cd apps/lina-plugins/monitor-server && npx --prefix ../../../hack/tests tsc --noEmit --project ../../../hack/tests/tsconfig.json`、`cd hack/tests && E2E_BROWSER_CHANNEL=chrome pnpm exec playwright test apps/lina-plugins/monitor-server/hack/tests/e2e/TC0052-server-monitor-page.ts --workers=1`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- apps/lina-vben/packages/@core/base/design/src/css/global.css hack/tools/linactl/main_test.go openspec/changes/manual-nightly-image-build/tasks.md`、`cd apps/lina-plugins && git diff --check -- monitor-server/hack/tests/e2e/TC0052-server-monitor-page.ts`。确认生产构建产物已包含服务监控页面的折叠图标、缩进、边框和磁盘表样式，TC0052 全部 8 个子用例通过。i18n 影响：不新增或修改产品运行时文案、manifest i18n 或 apidoc i18n；新增 E2E 描述和断言不进入产品语言包。缓存一致性影响：不修改运行时业务缓存、缓存失效或跨实例同步策略；仅修复前端构建期 CSS 扫描范围。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：仅新增既有 Go 版`linactl`测试，不新增平台专属脚本或默认开发入口。
- [x] 2026-05-17: FB-4 验证通过。新增`hack/deploy/docker-compose.yaml`作为内存态 nightly 演示启动入口，默认使用`ghcr.io/linaproai/linapro:nightly`，可通过`LINAPRO_IMAGE`覆盖镜像、通过`LINAPRO_HTTP_PORT`覆盖宿主端口；容器启动时在`/run/linapro`生成运行时配置，使用`/app/data`内`SQLite`数据库并将`/app/data`与`/run/linapro`声明为`tmpfs`，不挂载宿主目录、不声明持久化 volume；启动命令按`init --rebuild=true`、`mock`、`HTTP`服务顺序执行，并默认自动启用`multi-tenant`、`org-center`、`content-notice`、监控插件和`demo-control`。验证通过：`docker compose -f hack/deploy/docker-compose.yaml config`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- hack/deploy/docker-compose.yaml openspec/changes/manual-nightly-image-build/specs/release-image-build/spec.md openspec/changes/manual-nightly-image-build/tasks.md`；并对未跟踪新文件执行`git diff --no-index --check -- /dev/null hack/deploy/docker-compose.yaml`，确认无空白错误输出，该命令因新文件存在差异返回`1`为预期行为。i18n 影响：不新增或修改产品运行时文案、前端语言包、manifest i18n 或 apidoc i18n；Compose 内配置沿用已存在语言元数据。缓存一致性影响：单容器内存态演示配置显式`cluster.enabled=false`，不新增分布式缓存或跨实例失效路径。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：新增`Docker Compose`部署编排，不新增宿主平台脚本；`/bin/sh`仅在 Alpine 容器内部执行初始化编排，不作为宿主机默认开发脚本入口。
- [x] 2026-05-17: FB-5 验证通过。为`hack/deploy/docker-compose.yaml`补充必要注释，覆盖演示启动命令、一次性内存态数据语义、`LINAPRO_IMAGE`镜像覆盖、`LINAPRO_HTTP_PORT`端口覆盖、`tmpfs`约束、运行时配置生成、每次启动重建 schema 与加载 mock 数据，以及`demo-control`公开演示保护用途；同步更新`release-image-build`增量规范，明确 Compose 启动文件必须包含这些关键注释。验证通过：`docker compose -f hack/deploy/docker-compose.yaml config`、`openspec validate manual-nightly-image-build --strict`。i18n 影响：仅新增配置文件注释和 OpenSpec 规范文本，不新增或修改产品运行时文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例同步策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：不新增宿主平台脚本；仍仅在 Alpine 容器内部通过`/bin/sh`执行初始化编排。
- [x] 2026-05-17: FB-6 验证通过。将原先内联在`hack/deploy/docker-compose.yaml`启动脚本中的运行时配置剥离为`hack/deploy/config.yaml`，并通过 Compose `configs`以只读方式挂载到容器`/run/linapro/config.yaml`；Compose 启动脚本只保留创建`tmpfs`目录、执行`init --rebuild=true`、加载`mock`数据和启动服务的编排。`/app/data`仍为`tmpfs`，SQLite、上传文件和 wasm 存储均不通过宿主数据目录或持久化 volume 保留。验证通过：`docker compose -f hack/deploy/docker-compose.yaml config`、`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "yaml ok"' hack/deploy/docker-compose.yaml hack/deploy/config.yaml`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- hack/deploy/docker-compose.yaml hack/deploy/config.yaml openspec/changes/manual-nightly-image-build/specs/release-image-build/spec.md openspec/changes/manual-nightly-image-build/tasks.md`，并分别对未跟踪新文件执行`git diff --no-index --check -- /dev/null hack/deploy/docker-compose.yaml`和`git diff --no-index --check -- /dev/null hack/deploy/config.yaml`确认无空白错误输出。i18n 影响：`config.yaml`只声明已有`en-US`、`zh-CN`语言元数据，不新增产品运行时文案、前端语言包、manifest i18n 或 apidoc i18n。缓存一致性影响：仍为单容器演示配置，显式`cluster.enabled=false`，不修改运行时缓存、缓存失效或跨实例同步策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：不新增宿主平台脚本；`/bin/sh`仍仅在 Alpine 容器内部执行初始化编排。
- [x] 2026-05-17: FB-7 验证通过。将`hack/deploy/docker-compose.yaml`调整为`postgres`与`linapro`两个服务，新增`postgres:14-alpine`演示数据库服务、`pg_isready`健康检查和`linapro.depends_on.postgres.condition=service_healthy`，确保 LinaPro 在 PostgreSQL 健康后执行`init --rebuild=true`、`mock`和`HTTP`启动；将`hack/deploy/config.yaml`数据库链接从 SQLite 切换为`pgsql:postgres:postgres@tcp(postgres:5432)/linapro?sslmode=disable`。`postgres`数据目录`/var/lib/postgresql/data`、应用运行期目录`/app/data`与`/run/linapro`均为`tmpfs`，不新增宿主数据目录挂载或持久化 volume。验证通过：`docker compose -f hack/deploy/docker-compose.yaml config`、`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "yaml ok"' hack/deploy/docker-compose.yaml hack/deploy/config.yaml`、`openspec validate manual-nightly-image-build --strict`、`git diff --check -- hack/deploy/docker-compose.yaml hack/deploy/config.yaml openspec/changes/manual-nightly-image-build/specs/release-image-build/spec.md openspec/changes/manual-nightly-image-build/tasks.md`。i18n 影响：不新增产品运行时文案、前端语言包、manifest i18n 或 apidoc i18n；仅修改部署配置注释和 OpenSpec 文本。缓存一致性影响：仍为单实例 LinaPro 演示，`cluster.enabled=false`不变，不新增分布式缓存、失效或跨实例同步策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口、数据库查询或角色数据权限边界。开发工具与脚本影响：不新增宿主平台脚本；`/bin/sh`仍仅在 Alpine 容器内部执行初始化编排。

## 审查记录

- [x] 2026-05-18: FB-8 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、`git diff -- hack/deploy/tests/docker-compose.yaml hack/deploy/tests/config.yaml openspec/changes/manual-nightly-image-build/specs/release-image-build/spec.md openspec/changes/manual-nightly-image-build/tasks.md`。确认测试 Compose 已从演示应用启动逻辑调整为 PostgreSQL 加长期驻留开发容器：`dev`默认使用`loads/ubuntu:24.04-npm`，挂载当前仓库到`/workspace/linapro`，设置`GF_GCFG_PATH`到配置目录，等待`postgres`健康后仅保持可进入状态，不自动执行`lina init`、`lina mock`或启动 HTTP 服务；`/app/data`和 PostgreSQL 数据目录均为`tmpfs`，未新增持久化卷。当前工作区还存在大量租户权限边界相关未提交/未跟踪变更，均不属于本次审查范围且未被回退或重写。本次不新增 Go 生产代码、业务 API、数据库 schema、产品运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或角色数据权限逻辑；无需 Go 编译门禁、单元测试或 E2E。Compose 配置解析、YAML 解析、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-17: `lina-review`审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、新增 workflow 与 OpenSpec 文件内容检查。确认`.github/workflows/manual-nightly-image-build.yml`仅支持`workflow_dispatch`触发，只有一个直接调用`.github/workflows/reusable-image-publish.yml`的`manual-nightly-image` job，未声明`needs`、测试 job、共享测试验证套件、单元测试、`E2E`或 smoke 测试依赖；镜像名称、平台、`tag-mode: nightly`、`floating-tag: nightly`和`plugins: "1"`与现有 nightly 发布语义一致。OpenSpec 提案、设计、增量规范和任务记录均使用中文，变更范围与`release-image-build`能力匹配。本次不新增或修改 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存、数据权限逻辑或默认开发工具脚本，因此无需 Go 编译门禁、功能 E2E、i18n 资源、缓存一致性实现或数据权限测试。严重问题 0；警告 0。
- [x] 2026-05-17: FB-1 `lina-review`审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、`git diff -- apps/lina-vben/turbo.json hack/tools/linactl/main_test.go openspec/changes/manual-nightly-image-build/tasks.md`。确认本次修复只将`LINAPRO_SOURCE_PLUGINS`加入`apps/lina-vben/turbo.json`的`globalEnv`，让 plugin-full 镜像前端构建在 Turbo strict env 模式下继续启用 source-plugin 页面扫描；新增`TestFrontendTurboAllowsSourcePluginBuildEnv`为自包含配置回归测试，覆盖`linactl`包。未修改 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时业务缓存或角色数据权限逻辑。Go 测试、Turbo dry-run 环境检查、plugin-full 前端构建、构建产物路由检查、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-17: FB-2 `lina-review`审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、源码插件子仓状态、未跟踪测试文件展开、`openspec status --change manual-nightly-image-build --json`、宿主与`multi-tenant`插件差异检查。确认本次修复将宿主 auto-enable 的 tenant-scoped 插件在源码插件 provider 注册后补齐为新租户默认策略，并通过 multi-tenant provider 为已有活跃租户插入缺失的`sys_plugin_state('__tenant_enabled__')`行；`ProvisionForTenant`改为只插入缺失行，保留租户显式禁用状态。Go 生产代码已运行覆盖变更包的目标测试和`apps/lina-core/internal/cmd`启动编排测试；源码插件 provider、tenantplugin 和 backend 包测试通过；TC0237 E2E、TypeScript 检查、E2E 文件治理校验、OpenSpec 严格校验和空白检查通过。宽范围`apps/lina-core/internal/service/plugin`包测试存在本地共享 PostgreSQL 反向依赖残留导致的历史状态失败，已在验证记录中说明，未发现与本次路径相关的阻断。i18n 审查：不新增或修改用户可见运行时文案、菜单翻译、manifest i18n 或 apidoc i18n，新增 E2E 描述不进入产品语言包。缓存一致性审查：复用`sys_plugin_state`唯一键、`InsertIgnore`幂等写入、`plugin-runtime/global`共享修订号和启动后 enabled snapshot 刷新；多实例重复启动最多产生幂等写入或重复修订号递增，不会覆盖租户显式状态。数据权限审查：未新增 HTTP API、业务查询或角色数据权限边界；启动治理写入仅作用于插件租户启用投影。开发工具审查：不新增或修改默认开发工具、脚本或平台专属入口。严重问题 0；警告 0。
- [x] 2026-05-17: FB-3 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、源码插件子仓状态、`openspec status --change manual-nightly-image-build --json`、`git diff -- apps/lina-vben/packages/@core/base/design/src/css/global.css hack/tools/linactl/main_test.go openspec/changes/manual-nightly-image-build/tasks.md`、`git -C apps/lina-plugins diff -- monitor-server/hack/tests/e2e/TC0052-server-monitor-page.ts`。确认本次修复只扩大 Tailwind v4 全局 CSS 构建扫描范围到源码插件目录，并用`linactl`配置测试和服务监控 E2E 图标断言锁定回归；未修改 Go 生产代码、业务 API、数据库 schema、产品运行时文案、manifest i18n、apidoc i18n、运行时缓存或角色数据权限逻辑。`pnpm vite build`、plugin-full 前端生产构建、构建产物 CSS/JS 标记检查、linactl Go 测试、monitor-server E2E TypeScript 检查、TC0052 E2E、OpenSpec 严格校验和空白检查均通过；本地 dist 产物未产生跟踪文件变更。严重问题 0；警告 0。
- [x] 2026-05-17: FB-4 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、新增`hack/deploy/docker-compose.yaml`和`release-image-build`增量规范。确认 Compose 文件未使用宿主 bind mount 或持久化 volume，运行期配置与数据均落在容器`tmpfs`，默认镜像和端口可通过环境变量覆盖，启动顺序为初始化、加载演示数据、启动`HTTP`服务；`demo-control`默认启用以适配公开演示只读保护。未修改 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或角色数据权限逻辑；无需新增单元测试或`E2E`。`docker compose config`、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-17: FB-5 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、`hack/deploy/docker-compose.yaml`和`release-image-build`增量规范差异。确认新增注释聚焦体验者需要理解的运行方式、内存态约束、可覆盖参数和初始化顺序，没有改变 Compose 服务结构、镜像、端口、`tmpfs`、健康检查或启动命令语义；OpenSpec 规范与任务记录保持中文一致。未修改 Go 生产代码、业务 API、数据库 schema、产品运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或角色数据权限逻辑；无需新增单元测试或`E2E`。`docker compose config`和 OpenSpec 严格校验均通过。严重问题 0；警告 0。
- [x] 2026-05-17: FB-6 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、`hack/deploy/docker-compose.yaml`、新增`hack/deploy/config.yaml`和`release-image-build`增量规范差异。确认运行时配置已从 Compose 启动脚本剥离为独立 YAML，并以 Compose `configs`只读注入；该配置挂载是部署配置输入，不是演示数据持久化路径，演示数据库、上传目录和动态插件存储仍全部位于`/app/data`的`tmpfs`。未修改 Go 生产代码、业务 API、数据库 schema、产品运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或角色数据权限逻辑；无需新增单元测试或`E2E`。Compose 配置解析、YAML 解析、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-17: FB-7 `lina-review`审查完成。审查范围来源：`git status --short --ignore-submodules=none`、`git ls-files --others --exclude-standard`、`openspec status --change manual-nightly-image-build --json`、`hack/deploy/docker-compose.yaml`、`hack/deploy/config.yaml`和`release-image-build`增量规范差异。确认演示部署已从 SQLite 切换为 Compose 内 PostgreSQL 服务，LinaPro 通过服务名`postgres`连接数据库，并通过`depends_on`健康条件等待数据库可用后再执行初始化；PostgreSQL 数据目录使用`tmpfs`，未引入宿主数据目录挂载或持久化 volume，符合内存态演示边界。未修改 Go 生产代码、业务 API、数据库 schema、产品运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或角色数据权限逻辑；无需新增单元测试或`E2E`。Compose 配置解析、YAML 解析、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
