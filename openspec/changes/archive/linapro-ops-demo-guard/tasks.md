## Summary

- [x] 修复`linapro-ops-demo-guard`最小会话白名单，覆盖`linapro-tenant-core`插件挂载路径下的租户选择与租户切换。
- [x] 删除已无真实 HTTP 入口的宿主租户选择和租户切换历史白名单。
- [x] 补充插件中间件单元测试，覆盖插件挂载路径放行、历史宿主路径拒绝和其他插件挂载写请求继续拒绝。
- [x] 更新插件自有 E2E 覆盖，验证启用演示保护后租户用户可选择租户登录，admin 可通过插件挂载路径切换租户。
- [x] 运行 OpenSpec、Go、E2E 治理和格式验证，并完成反馈审查记录。

**FB-1**: `linapro-ops-demo-guard`启用后误拦截租户核心插件挂载路径的租户选择和租户切换 -- 已修复
**FB-2**: 删除`linapro-ops-demo-guard`中已无真实 HTTP 入口的宿主租户选择和租户切换白名单 -- 已修复
