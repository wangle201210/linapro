## 1. OpenSpec 与 Codex 配置治理

- [x] 1.1 调整 `.gitignore`，允许提交 `.github/codex/config.template.toml` 和无密钥模板，同时继续忽略真实认证文件
- [x] 1.2 新增或整理 `.github/codex` 配置模板，确保 workflow 可复制配置但不会提交真实 `OPENAI_API_KEY`

## 2. Monthly Workflow 实现

- [x] 2.1 新增 `Monthly OpenSpec Archive` GitHub Actions workflow，支持 schedule 和 `workflow_dispatch`
- [x] 2.2 在 workflow 中通过临时 `CODEX_HOME` 注入 Codex 配置和 `OPENAI_API_KEY` secret
- [x] 2.3 实现 OpenSpec 完成状态预检查，无可归档变更时跳过 Codex 执行
- [x] 2.4 通过 `loads/codex:latest` 依次执行 `lina-auto-archive` 和条件性的 `lina-archive-consolidate`
- [x] 2.5 实现 OpenSpec 校验、变更范围保护和自动写回逻辑

## 3. 验证与审查

- [x] 3.1 运行 `openspec validate monthly-openspec-archive --strict`
- [x] 3.2 对新增 workflow 和 Codex 模板执行静态检查，确认 YAML、JSON 和 TOML 格式有效
- [x] 3.3 记录 i18n、缓存一致性、数据权限和 E2E 影响判断
- [x] 3.4 完成实现后调用 `lina-review` 审查本次变更

## Verification Notes

- [x] 2026-05-12: OpenSpec 与静态格式验证通过：`openspec validate monthly-openspec-archive --strict`；`python3 -m json.tool .github/codex/auth.template.json >/dev/null`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/monthly-openspec-archive.yml")'`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/codex .gitignore openspec/changes/monthly-openspec-archive`。确认 `.github/codex/auth.json` 仍被 `.gitignore` 忽略，`.github/codex/config.template.toml` 与 `.github/codex/auth.template.json` 可纳入版本控制。
- [x] 2026-05-12: `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、`git status --ignored --short -- .github/codex`。发现并修正 1 个 workflow 审查问题：原先变更范围保护仅在 `openspec/` 有变化时运行，若 Codex 产生非 OpenSpec 意外变更但未产生归档变化会被静默跳过；已调整为只要存在完成候选并执行过 Codex，就运行 `Guard Generated Changes`。复验通过：`openspec validate monthly-openspec-archive --strict`、Codex JSON/TOML 解析、workflow YAML 解析、`git diff --check`。i18n 影响：本轮仅新增 GitHub Actions workflow、Codex 配置模板、`.gitignore` 规则和 OpenSpec 文档，不新增或修改前端运行时语言包、宿主/插件 manifest i18n、apidoc i18n、菜单、按钮、表单、接口文档或用户可见运行时文案。缓存一致性影响：本轮不修改运行时代码、不新增缓存、不改变缓存权威数据源、失效触发、跨实例同步或故障降级策略。数据权限影响：本轮不新增或修改后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计，不影响角色数据权限边界。E2E 影响：本轮不涉及用户可观察页面、路由、表单、表格或端到端业务流程，使用 OpenSpec 与 workflow 静态验证即可，不新增 E2E。
- [x] 2026-05-12: 二次安全复核后收紧 monthly 自动写回范围：当前迭代可提交 `.github/workflows/monthly-openspec-archive.yml`、`.github/codex/config.template.toml`、`.github/codex/auth.template.json` 和 `.gitignore` 作为人工治理变更，但 workflow 运行时的 `Guard Generated Changes` 与 `Commit Archive Changes` 仅允许自动提交 `openspec/**`，防止 Codex 在无人值守环境中修改 workflow、Codex 配置或密钥忽略规则。复验命令：`openspec validate monthly-openspec-archive --strict`、Codex JSON/TOML 解析、workflow YAML 解析、`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/codex .gitignore openspec/changes/monthly-openspec-archive`。

## Feedback

- [x] **FB-1**: `.github/codex/config.template.toml` 中的 provider `base_url` 不应写入真实 endpoint，必须改为通过 GitHub Secret 在 monthly workflow 运行时注入
- [x] **FB-2**: `base_url` 模板占位符应与 `auth.template.json` 的占位符风格保持一致，使用 `${OPENAI_BASE_URL}`
- [x] **FB-3**: monthly OpenSpec archive workflow 应支持通过 GitHub Variables 中的 `AI_CODING_TOOL` 在 `codex` 和 `cc` 之间切换 AI Coding 工具
- [x] **FB-4**: monthly OpenSpec archive workflow 应通过工具专属 reusable workflow 封装 Codex 和 Claude Code 实现细节，主 workflow 只负责检测和路由
- [x] **FB-5**: monthly OpenSpec archive 的自动归档和归档聚合提示词应提取到 `.github/prompts/` 公共文件，由 Codex 和 Claude Code workflow 共同引用
- [x] **FB-6**: Codex 配置模板应使用 `.github/codex/config.template.toml` 文件名，并更新 workflow、忽略规则和 OpenSpec 引用
- [x] **FB-7**: monthly OpenSpec archive 产生文件修改时应创建或更新维护 PR，而不是直接推送到默认分支
- [x] **FB-8**: Codex 和 Claude Code monthly archive workflow 中重复的 runner 准备、归档差异检测和 PR 写回治理步骤应抽象复用
- [x] **FB-9**: 自动 OpenSpec archive workflow 应改为每月 1 日 00:00 Asia/Shanghai 触发一次，保留手动触发，并同步调整文件名与工作流名称
- [x] **FB-10**: monthly OpenSpec archive workflow 中的 `ANTHROPIC_BASE_URL`、`ANTHROPIC_CUSTOM_MODEL` 和 `OPENAI_BASE_URL` 应改为 GitHub Variables 读取
- [x] **FB-11**: Codex 和 Claude Code monthly archive workflow 的 `Run Lina Auto Archive` 与 `Run Lina Archive Consolidate` 必须将 AI 工具进程输出实时显示到 GitHub Actions step 日志
- [x] **FB-12**: monthly OpenSpec archive workflow 应支持通过 GitHub Copilot CLI 执行自动归档和归档聚合，并通过 GitHub Variables 配置 Copilot 模型
- [x] **FB-13**: GitHub Copilot CLI monthly archive workflow 应支持通过 GitHub Variables 配置推理等级并传递给 `--reasoning-effort`
- [x] **FB-14**: monthly OpenSpec archive workflow 任一阶段失败时必须立即失败退出，不得继续执行后续阶段
- [x] **FB-15**: monthly OpenSpec archive workflow 的 `openspec validate --all` 必须通过，避免归档工作流因既有主规范格式失败

### Feedback Verification

- [x] 2026-05-12: FB-1 验证通过。`.github/codex/config.template.toml` 已将 `base_url` 改为占位符；`Monthly OpenSpec Archive` workflow 在 `Prepare Codex Runtime` 中要求 `secrets.OPENAI_BASE_URL`，并仅在临时 `CODEX_HOME/config.toml` 中用 JSON 字符串编码后的 secret 替换占位符，真实 endpoint 不进入仓库工作区。验证命令：`python3 -m json.tool .github/codex/auth.template.json >/dev/null`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/monthly-openspec-archive.yml")'`；`openspec validate monthly-openspec-archive --strict`；`rg -n 'co\\.yes\\.vg|https://co' .github/codex .github/workflows/monthly-openspec-archive.yml openspec/changes/monthly-openspec-archive`；临时目录模拟替换并用 `tomllib` 解析包含特殊字符的 `OPENAI_BASE_URL`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/codex .gitignore openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions 配置、Codex 模板和 OpenSpec 文档，不涉及运行时用户可见文案或翻译资源。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-1 `lina-review` 审查完成。审查范围来源：`sed -n '1,260p' .github/workflows/monthly-openspec-archive.yml`、`sed -n '1,80p' .github/codex/config.template.toml`、`openspec status --change monthly-openspec-archive --json`、`git status --short -- .github/codex .github/workflows/monthly-openspec-archive.yml openspec/changes/monthly-openspec-archive .gitignore`。确认真实 `base_url` 已从仓库模板中移除，workflow 同时检查 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`，并只在 runner 临时 `CODEX_HOME` 中写入带真实 endpoint 的配置；模板占位符替换使用 `json.dumps(base_url)` 生成合法 TOML 字符串，避免特殊字符破坏配置。严重问题 0；警告 0。
- [x] 2026-05-12: FB-2 验证通过。`base_url` 模板占位符已统一为 `"${OPENAI_BASE_URL}"`，workflow 仍通过 Python 精确替换整个 TOML 字符串值，并使用 `json.dumps(base_url)` 保证特殊字符会被编码为合法 TOML 字符串；不依赖 shell 变量展开。验证命令：`python3 -m json.tool .github/codex/auth.template.json >/dev/null`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/monthly-openspec-archive.yml")'`；`openspec validate monthly-openspec-archive --strict`；临时目录模拟替换并用 `tomllib` 解析包含特殊字符的 `OPENAI_BASE_URL`；`rg -n 'co\\.yes\\.vg|https://co' .github/codex .github/workflows/monthly-openspec-archive.yml openspec/changes/monthly-openspec-archive`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/codex .gitignore openspec/changes/monthly-openspec-archive`。
- [x] 2026-05-12: FB-3 验证通过。workflow 顶层 `env` 通过 `${{ vars.AI_CODING_TOOL || 'codex' }}` 读取 GitHub Variables，准备阶段将合法值限制为 `codex` 和 `cc`：`codex` 使用 `loads/codex:latest`、`.github/codex/config.template.toml` 和 `codex exec`；`cc` 使用 `loads/cc:latest`、`.github/cc/settings.template.json` 和 `claude -p`。本地抽取 `Prepare AI Runtime` shell 脚本并验证 `bash -n` 通过；分别模拟 `AI_CODING_TOOL=codex`、`AI_CODING_TOOL=cc` 和非法值，确认两套临时配置生成成功且非法值会失败。验证命令：`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/monthly-openspec-archive.yml")'`；`python3 -m json.tool .github/cc/settings.template.json >/dev/null`；`python3 -m json.tool .github/codex/auth.template.json >/dev/null`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions 配置和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-3 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、`git diff -- .github/workflows/monthly-openspec-archive.yml openspec/changes/monthly-openspec-archive`。确认 workflow 通过白名单 `case` 限制 `AI_CODING_TOOL` 为 `codex` 或 `cc`，只校验所选工具对应的 secrets，日志 artifact 已从 Codex 专属命名改为通用 AI 日志；`actionlint` 容器检查、workflow YAML 解析、配置模板 JSON/TOML 解析、OpenSpec 严格校验和 `git diff --check` 均通过。严重问题 0；警告 0。审查时发现工作区还存在与本反馈无关的改动：`.github/workflows/main-ci.yml`、`.github/workflows/nightly-build.yml`、`openspec/changes/redis-cluster-coordination/tasks.md` 和未跟踪的 `hack/tests/scripts/run-redis-cluster-smoke.sh`，本次未修改或回退。
- [x] 2026-05-12: FB-4 验证通过。主 workflow `.github/workflows/monthly-openspec-archive.yml` 已简化为检测与路由：检测完成候选、解析 `AI_CODING_TOOL`，并按 `codex` 或 `cc` 调用固定 reusable workflow。新增 `.github/workflows/monthly-openspec-archive-codex.yml` 和 `.github/workflows/monthly-openspec-archive-cc.yml`，分别封装对应工具的 checkout、运行时准备、AI 归档、差异检测、条件性聚合、OpenSpec 校验、变更范围保护、提交和日志上传。验证命令：`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；本地抽取 Codex/Claude Code 准备脚本并执行 `bash -n` 与临时配置生成模拟；`python3 -m json.tool .github/cc/settings.template.json >/dev/null`；`python3 -m json.tool .github/codex/auth.template.json >/dev/null`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions 配置和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-5 验证通过。新增 `.github/prompts/monthly-openspec-auto-archive.zh-CN.md` 和 `.github/prompts/monthly-openspec-archive-consolidate.zh-CN.md` 作为公共提示词文件；Codex reusable workflow 通过 `codex exec - < .github/prompts/...` 读取，Claude Code reusable workflow 通过 `run-cc-task < .github/prompts/...` 读取。验证确认工具专属 workflow 中不再内联维护提示词正文，公共 prompt 文件保留原有无人值守归档/聚合约束和中文摘要输出要求。验证命令：`rg -n '这是无人值守的 monthly CI|只扫描 `openspec/changes/`|未指定变更列表时' .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/prompts`；`rg -n 'monthly-openspec-auto-archive|monthly-openspec-archive-consolidate' .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/prompts`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/prompts openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈新增的是 CI 提示词文件，不属于前端运行时语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-5 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、`git diff -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/prompts openspec/changes/monthly-openspec-archive`。确认两段提示词只在 `.github/prompts/` 中维护，Codex 与 Claude Code workflow 均通过 stdin 引用公共文件，未保留重复正文；公共 prompt 仍包含只扫描活跃变更、任务完成校验、禁止 `--no-validate`、范围限制、失败退出和中文摘要输出要求。`actionlint`、OpenSpec 严格校验和 `git diff --check` 均通过。严重问题 0；警告 0。审查时发现工作区还存在与本反馈无关的改动：`.github/workflows/main-ci.yml`、`.github/workflows/nightly-build.yml`、`.github/workflows/release-build.yml`、`.github/workflows/image-build/*` 删除、`.github/image/*` 新增、`openspec/changes/redis-cluster-coordination/tasks.md` 和未跟踪的 `hack/tests/scripts/run-redis-cluster-smoke.sh`，本次未修改或回退。
- [x] 2026-05-12: FB-6 验证通过。旧 Codex 配置模板已重命名为 `.github/codex/config.template.toml`；Codex reusable workflow 复制新模板到临时 `CODEX_HOME/config.toml`，`.gitignore` 白名单和 OpenSpec 设计/规范/任务引用均已同步。验证命令：`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`python3` 使用 `tomllib` 解析 `.github/codex/config.template.toml`；文件存在性检查确认旧模板不存在且新模板存在；排除历史任务记录后的旧仓库模板路径扫描确认 workflow、配置目录、忽略规则、设计文档和规范文档不再引用旧模板；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/codex .github/workflows/monthly-openspec-archive-codex.yml .gitignore openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 CI 配置模板文件名、workflow 引用、忽略规则和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-6 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/codex .github/workflows/monthly-openspec-archive-codex.yml .gitignore openspec/changes/monthly-openspec-archive`、`git diff -- .github/codex .github/workflows/monthly-openspec-archive-codex.yml .gitignore openspec/changes/monthly-openspec-archive`、旧路径残留扫描、OpenSpec 严格校验和 `actionlint`。确认仓库内 Codex 模板使用 `.template.toml` 命名，运行时仍生成工具期望的临时 `config.toml`，未改变密钥注入语义或自动提交范围保护。严重问题 0；警告 0。审查时发现工作区还存在与本反馈无关的改动：`.github/workflows/main-ci.yml`、`.github/workflows/nightly-build.yml`、`.github/workflows/release-build.yml`、`.github/workflows/image-build/*` 删除、`.github/image/*` 新增、`openspec/changes/redis-cluster-coordination/tasks.md` 和未跟踪的 `hack/tests/scripts/run-redis-cluster-smoke.sh`，本次未修改或回退。
- [x] 2026-05-12: FB-7 验证通过。主 workflow 和 Codex/Claude Code reusable workflow 均新增 `pull-requests: write` 权限；两个工具分支的写回步骤已从 `Commit Archive Changes` 改为 `Create or Update Archive Pull Request`，只把通过范围保护的 `openspec/**` 变更提交到固定维护分支 `automation/monthly-openspec-archive`，并通过 `gh pr list --state open` 精确查找打开中的维护 PR，再用 `gh pr edit/create` 更新或创建目标为默认分支的归档 PR。验证命令：`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml openspec/changes/monthly-openspec-archive`；扫描当前 workflow 与 OpenSpec 规范确认不再存在默认分支直推或直推语义。i18n 影响：本反馈仅修改 GitHub Actions 写回策略和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-7 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、`git diff -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml openspec/changes/monthly-openspec-archive`、默认分支直推语义扫描、OpenSpec 严格校验和 `actionlint`。确认 workflow 不再将归档结果直接推送到默认分支；仍需推送固定维护分支 `automation/monthly-openspec-archive` 以供 PR 使用，随后通过 GitHub CLI 创建或更新打开中的维护 PR。范围保护仍在写回前执行且仅允许 `openspec/**`；OpenSpec 文档已同步改为 PR 写回语义。严重问题 0；警告 0。本地环境未安装 `gh` 且无法拉取 `ghcr.io/cli/cli:latest` 帮助镜像，`gh` 参数未做本地 CLI 帮助验证；目标 runner 为 GitHub hosted `ubuntu-latest`，预期内置 GitHub CLI。
- [x] 2026-05-12: FB-8 验证通过。新增 `.github/actions/monthly-openspec-setup`、`.github/actions/monthly-openspec-detect-changes` 和 `.github/actions/monthly-openspec-finalize-pr` 三个本地 composite action，分别复用 checkout 后的 runner 准备、归档差异检测、OpenSpec 校验/范围保护/PR 写回治理步骤；Codex 和 Claude Code reusable workflow 仅保留各自认证配置、AI 容器调用和日志 artifact 上传差异。验证命令：`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；抽取 workflow 与 composite action 所有 `run` 脚本执行 `bash -n`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml openspec/changes/monthly-openspec-archive`；扫描确认重复的 `Setup Timezone`、`Setup Node`、`Validate OpenSpec`、`Guard Generated Changes`、`Create or Update Archive Pull Request` 和 `gh pr list` 逻辑只保留在 `.github/actions/` 下。i18n 影响：本反馈仅修改 GitHub Actions workflow、composite action 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口或权限边界。E2E 影响：不涉及前端页面或端到端用户流程，采用治理验证。
- [x] 2026-05-12: FB-8 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions openspec/changes/monthly-openspec-archive`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、workflow/composite action diff、OpenSpec 严格校验、workflow `actionlint`、YAML 解析和脚本语法检查。确认本地 composite action 不作为第一步使用，工具 workflow 先 checkout 仓库后再调用本地 action；公共治理步骤已集中，工具差异仍留在工具 workflow 内。严重问题 0；警告 0。
- [x] 2026-05-14: FB-9 验证通过。主路由 workflow 已重命名为 `.github/workflows/monthly-openspec-archive.yml`，workflow name 改为 `Monthly OpenSpec Archive`，Codex/Claude Code reusable workflow、`.github/actions/monthly-openspec-*` composite action、`.github/prompts/monthly-openspec-*` 提示词、维护分支 `automation/monthly-openspec-archive` 和日志 artifact 均同步为 monthly 命名。schedule 改为 UTC 月末 16:00 分组 cron：`0 16 31 1,3,5,7,8,10,12 *`、`0 16 30 4,6,9,11 *`、`0 16 28 2 *`、`0 16 29 2 *`，对应 Asia/Shanghai 每月 1 日 00:00；workflow 保留 `workflow_dispatch`，并通过 `github.event.schedule` 门禁在闰年跳过二月 28 日 UTC 的重复触发。验证命令：`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；抽取 workflow 与 composite action 所有 `run` 脚本执行 `bash -n`；Python 模拟 2026、2027、2028 年 schedule，确认每年只产生 12 次 Asia/Shanghai 每月 1 日 00:00 的有效运行且闰年跳过 `2028-02-28 16:00 UTC`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml .github/prompts/monthly-openspec-auto-archive.zh-CN.md .github/prompts/monthly-openspec-archive-consolidate.zh-CN.md openspec/changes/monthly-openspec-archive`；旧 nightly 路径和旧 cron 残留扫描通过。i18n 影响：本反馈仅修改 GitHub Actions workflow、composite action、CI prompt 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端用户流程，采用治理验证，不新增 E2E。
- [x] 2026-05-14: FB-9 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、新旧 monthly/nightly workflow、composite action、prompt 和 OpenSpec 变更文件的内容审查、全仓旧 `nightly-openspec` 可执行引用扫描、workflow `actionlint`、YAML 解析、脚本语法检查、cron 模拟、OpenSpec 严格校验和空白检查。确认主 workflow 只在默认分支执行，月度自动 schedule 对应 Asia/Shanghai 每月 1 日 00:00，`workflow_dispatch` 可按需手动触发，AI 工具路由仍限制为 `codex`/`cc`，reusable workflow 引用当前存在的 monthly 文件，公共治理 action 的 PR 写回仍仅允许 `openspec/**`。严重问题 0；警告 0。残余风险：月度自动归档会让完成变更最多停留在活跃目录约一个月，已通过手动触发入口缓解；`openspec validate --all` 当前仍受仓库既有主规范状态影响失败，但本变更 `openspec validate monthly-openspec-archive --strict` 已通过，且 `openspec validate --all` 输出中 `change/monthly-openspec-archive` 为通过。
- [x] 2026-05-17: FB-10 验证通过。主路由 workflow 已通过 `vars.OPENAI_BASE_URL`、`vars.ANTHROPIC_BASE_URL` 和 `vars.ANTHROPIC_CUSTOM_MODEL` 向工具专属 reusable workflow 传参；Codex reusable workflow 只保留 `OPENAI_API_KEY` secret，并通过 `inputs.openai_base_url` 注入临时 `config.toml`；Claude Code reusable workflow 只保留 `ANTHROPIC_AUTH_TOKEN` secret，并通过 `inputs.anthropic_base_url`、`inputs.anthropic_custom_model` 注入临时 `settings.json`。验证命令：`rg -n "secrets\\.OPENAI_BASE_URL|secrets\\.ANTHROPIC_BASE_URL|secrets\\.ANTHROPIC_CUSTOM_MODEL|OPENAI_BASE_URL secret|ANTHROPIC_BASE_URL secret|ANTHROPIC_CUSTOM_MODEL variable or secret|同名 secret" .github/workflows/monthly-openspec-archive*.yml openspec/changes/monthly-openspec-archive`（仅命中旧历史验证记录）；`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions workflow 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码、不新增缓存、不改变缓存失效或跨实例同步策略。数据权限影响：不涉及后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端业务流程，采用治理验证，不新增 E2E。
- [x] 2026-05-17: FB-10 `lina-review` 审查完成。审查范围来源：`git status --short`、`git status --short -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml openspec/changes/monthly-openspec-archive`、workflow diff、OpenSpec 严格校验、workflow YAML 解析、`actionlint` 和变量/密钥引用扫描。确认 `OPENAI_BASE_URL`、`ANTHROPIC_BASE_URL`、`ANTHROPIC_CUSTOM_MODEL` 均由主路由 workflow 通过 `vars.*` 传入 reusable workflow inputs，reusable workflow 不再声明或读取这些同名 secrets；`OPENAI_API_KEY` 与 `ANTHROPIC_AUTH_TOKEN` 仍作为 secrets 处理。严重问题 0；警告 0。该反馈不涉及 Go 生产代码、API 端点、运行时缓存、数据权限或前端 E2E 行为。
- [x] 2026-05-17: FB-11 验证通过。Claude Code reusable workflow 已移除 `claude -p ... > "$1"` 的直接重定向，改为在容器内通过 `tee` 将 `claude -p` stdout/stderr 同时写入当前 Actions step 日志和 `/tmp/cc-logs/<task>.md` artifact，并显式捕获和返回 `claude -p` 原始退出码；Codex reusable workflow 的 `Run Lina Auto Archive` 与 `Run Lina Archive Consolidate` 已增加 `set -euo pipefail`，并将 `docker run` stdout/stderr 通过 `tee` 同步写入 step 日志和 `${CODEX_LOGS}/*.console.log`，同时继续保留 `--output-last-message` artifact。验证命令：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive-cc.yml .github/workflows/monthly-openspec-archive-codex.yml openspec/changes/monthly-openspec-archive`；`rg -n "claude .* >|output-last-message|tee|console\\.log|set -euo pipefail|Run Lina Auto Archive|Run Lina Archive Consolidate" .github/workflows/monthly-openspec-archive-cc.yml .github/workflows/monthly-openspec-archive-codex.yml openspec/changes/monthly-openspec-archive/specs/monthly-openspec-archive/spec.md`；本地 shell smoke 确认 Codex 外层 `tee` 管道和 Claude Code 显式退出码捕获在模拟失败时均返回非 0。i18n 影响：本反馈仅修改 GitHub Actions workflow 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码、不新增缓存、不改变缓存失效或跨实例同步策略。数据权限影响：不涉及后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端业务流程，采用治理验证，不新增 E2E。
- [x] 2026-05-17: FB-12 验证通过。主路由 workflow 已将 `AI_CODING_TOOL` 白名单扩展为 `codex`、`cc`、`copilot`，并在 `copilot` 分支调用 `.github/workflows/monthly-openspec-archive-copilot.yml`；Copilot reusable workflow 使用 Node.js 24、`npm install --global @github/copilot`、临时 `openspec` shim、`copilot -p`、`secrets.COPILOT_GITHUB_TOKEN`、隔离 `COPILOT_HOME`、`vars.COPILOT_MODEL || 'auto'`、`--no-ask-user` 和有限 `--allow-tool` 规则执行自动归档与条件性归档聚合。`COPILOT_MODEL=auto` 时不传显式 `--model`，避免把 `auto` 当作具体模型名；非 `auto` 时传递 `--model=<value>`。验证命令：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；抽取主路由、Copilot workflow 和 monthly composite action 的所有 `run` 脚本执行 `bash -n`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/actions/monthly-openspec-setup/action.yml openspec/changes/monthly-openspec-archive`；`rg -n "Unsupported AI_CODING_TOOL|archive-with-copilot|monthly-openspec-archive-copilot|COPILOT_SELECTED_MODEL|COPILOT_MODEL|node-version: \\\"24\\\"|@github/copilot|--no-ask-user|--allow-tool write|copilot_bin|exec npx" .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/actions/monthly-openspec-setup/action.yml openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions workflow、composite action Node 版本和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码、不新增缓存、不改变缓存失效或跨实例同步策略。数据权限影响：不涉及后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端业务流程，采用治理验证，不新增 E2E。
- [x] 2026-05-17: FB-12 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、本次 workflow/composite action/OpenSpec 文档 diff、YAML 解析、`actionlint`、脚本语法检查、OpenSpec 严格校验和空白检查。审查发现并修正 1 个问题：初版 Copilot reusable workflow 未提供 `openspec` 命令 shim，但 `lina-auto-archive` 技能会直接调用 `openspec`；已补充临时 `copilot-bin/openspec` 并加入 `GITHUB_PATH`，语义与 Codex/Claude Code 分支一致。确认主路由只会运行一个匹配工具分支，Copilot 分支使用 `COPILOT_GITHUB_TOKEN` secret、隔离 `COPILOT_HOME`、`--secret-env-vars COPILOT_GITHUB_TOKEN`、实时日志 artifact 和最终 `openspec/**` 范围守卫；Node.js 24 升级用于兼容 `@github/copilot`，同时保留 OpenSpec `npx` 运行能力。严重问题 0；警告 0。该反馈不涉及 Go 生产代码、API 端点、运行时缓存、数据权限或前端 E2E 行为；开发工具/CI 入口变更已通过 Linux runner 目标语法与 actionlint 验证。
- [x] 2026-05-17: FB-13 验证通过。主路由 workflow 已通过 `vars.COPILOT_REASONING_EFFORT || ''` 向 GitHub Copilot CLI reusable workflow 传递推理等级；Copilot reusable workflow 新增 `copilot_reasoning_effort` input，运行前将输入归一化为小写，并只允许空值、`low`、`medium`、`high` 或 `xhigh`。空值时不传递 `--reasoning-effort`，合法非空值时在 `Run Lina Auto Archive` 和 `Run Lina Archive Consolidate` 两个 Copilot 调用中传递 `--reasoning-effort=<value>`，非法值会在执行 Copilot 前失败。验证命令：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "ok #{f}" }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml`；抽取主路由、Copilot workflow 和 monthly composite action 的所有 `run` 脚本执行 `bash -n`；`openspec validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-copilot.yml openspec/changes/monthly-openspec-archive`。i18n 影响：本反馈仅修改 GitHub Actions workflow 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码、不新增缓存、不改变缓存失效或跨实例同步策略。数据权限影响：不涉及后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端业务流程，采用治理验证，不新增 E2E。
- [x] 2026-05-17: FB-13 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、`.github/workflows/monthly-openspec-archive.yml`、`.github/workflows/monthly-openspec-archive-copilot.yml` 和 `openspec/changes/monthly-openspec-archive` 相关 diff。确认新增推理等级输入只影响 Copilot 分支，不改变 Codex 或 Claude Code 分支行为；`COPILOT_REASONING_EFFORT` 不作为 secret 处理，适合 GitHub Variables；模型参数和推理等级参数均通过数组方式传入 `copilot`，避免空值产生错误参数；非法推理等级会在安装运行时后、执行归档任务前失败并保留日志。严重问题 0；警告 0。该反馈不涉及 Go 生产代码、API 端点、运行时缓存、数据权限或前端 E2E 行为。
- [x] 2026-05-17: FB-14 验证通过。新增 `.github/actions/monthly-openspec-assert-archive-complete`，在 `Run Lina Auto Archive` 后重新执行 `openspec list --json`，如果仍存在 `complete`、`completed` 或 `done` 状态的活跃变更则立即失败并输出残留变更；新增 `.github/actions/monthly-openspec-validate`，并在 Codex、Claude Code、GitHub Copilot CLI 三个 reusable workflow 的自动归档后和归档聚合后立即执行 `openspec validate --all`，确保自动归档失败不会继续进入差异检测/聚合，聚合失败不会继续进入 PR 收尾。验证命令：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "ok #{f}" }' .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/actions/monthly-openspec-setup/action.yml .github/actions/monthly-openspec-detect-changes/action.yml .github/actions/monthly-openspec-assert-archive-complete/action.yml .github/actions/monthly-openspec-validate/action.yml .github/actions/monthly-openspec-finalize-pr/action.yml`；`docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest .github/workflows/monthly-openspec-archive.yml .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/workflows/monthly-openspec-archive-copilot.yml`；Ruby 抽取 9 个 workflow/action 文件中的 21 个 `run` block 并执行 `bash -n`；`npx -y "@fission-ai/openspec@1.3.1" validate monthly-openspec-archive --strict`；`git diff --check -- .github/workflows/monthly-openspec-archive-codex.yml .github/workflows/monthly-openspec-archive-cc.yml .github/workflows/monthly-openspec-archive-copilot.yml .github/actions/monthly-openspec-assert-archive-complete/action.yml .github/actions/monthly-openspec-validate/action.yml openspec/changes/monthly-openspec-archive`；本地 Node smoke 验证断言逻辑能识别完成状态残留变更。i18n 影响：本反馈仅修改 GitHub Actions workflow、composite action 和 OpenSpec 文档，不涉及运行时用户可见文案、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码、不新增缓存、不改变缓存失效或跨实例同步策略。数据权限影响：不涉及后端数据操作接口、服务数据访问路径、插件宿主服务适配器或聚合统计。E2E 影响：不涉及前端页面、路由、表单、表格或端到端业务流程，采用治理验证，不新增 E2E。开发工具/脚本影响：本反馈修改 GitHub Actions CI 入口和 composite action，目标运行环境为 GitHub hosted Linux runner；已通过 YAML/actionlint、shell 语法检查和静态扫描验证，未新增仓库默认本地开发脚本入口。
- [x] 2026-05-17: FB-15 验证通过。将 `openspec/specs/**/spec.md` 主规范统一为 OpenSpec 1.3.1 可识别结构：保留中文正文，仅把结构标题规范化为 `## Purpose`、`## Requirements`、`### Requirement:` 和 `#### Scenario:`；为缺少主规范容器的 `e2e-suite-execution-efficiency`、`framework-i18n-module-projection`、`framework-i18n-source-text-registry`、`runtime-message-i18n-governance`、`system-api-docs` 补充最小 Purpose/Requirements 包装；为少量旧中文正文补入 `SHALL` / `MUST` 规范词；移除 `user-auth` 中中断主 Requirements 区块的 `## 新增需求` 小节。验证命令：`npx -y "@fission-ai/openspec@1.3.1" validate --all`，结果 `Totals: 93 passed, 0 failed (93 items)`；静态扫描确认 `openspec/specs` 中不再存在旧结构标题 `## 目的`、`## 需求`、`## 新增需求`、`## ADDED Requirements`、`### 需求：`、`### Requirements：`、`### Requirements:`、`#### 场景：`；容器检查确认 79 个主规范均包含 `## Purpose` 和 `## Requirements`；`git diff --check -- openspec/specs` 通过。i18n 影响：本反馈仅调整 OpenSpec 规范文档结构标记和少量规范词，不修改运行时翻译资源、前端语言包、宿主/插件 manifest i18n 或 apidoc i18n。缓存一致性影响：不修改运行时代码或缓存逻辑。数据权限影响：不涉及后端数据操作接口或权限边界。E2E 影响：不涉及用户可观察页面或端到端业务流程，采用 OpenSpec 校验和静态扫描验证。
- [x] 2026-05-17: FB-15 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`openspec status --change monthly-openspec-archive --json`、CI 日志、`openspec/specs/**/spec.md` diff、OpenSpec 全量校验、旧结构标题扫描、主规范容器检查和 `git diff --check`。确认失败根因为既有主规范结构不符合 OpenSpec 1.3.1，而非 GitHub Copilot CLI 本身；当前 79 个主规范均有 `## Purpose` 与 `## Requirements`，`validate --all` 已从日志中的 `26 passed, 69 failed` 修复为 `93 passed, 0 failed`。严重问题 0；警告 0。该反馈不涉及 Go 生产代码、API 端点、运行时缓存、数据权限、前端运行时文案或 E2E 行为。
