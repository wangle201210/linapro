## 1. 旧升级语义清理

- [x] 1.1 删除或改写旧开发期升级入口相关规范、README、命令帮助、错误提示和 i18n 文案残留
- [x] 1.2 移除启动阶段待升级 fail-fast 语义，将源码插件待升级从启动错误改为运行时状态标记
- [x] 1.3 更新源码升级相关稳定 contract 注释和命名，避免继续表达开发期升级工具或启动阻断

## 2. 运行时状态模型

- [x] 2.1 设计并实现插件运行时状态字段或独立状态存储，覆盖 `normal`、`pending_upgrade`、`abnormal`、`upgrade_running`、`upgrade_failed`
- [x] 2.2 扩展插件列表和详情 DTO，返回 `runtimeState`、`effectiveVersion`、`discoveredVersion`、`upgradeAvailable`、`abnormalReason` 和最近失败信息
- [x] 2.3 调整源码插件和动态插件发现逻辑，保持有效 release 不被发现版本覆盖
- [x] 2.4 补充运行时状态枚举常量、bizerr 错误码和 apidoc/i18n 文案，禁止硬编码状态字符串和用户可见错误文案

## 3. 启动扫描与业务入口控制

- [x] 3.1 改造宿主启动插件扫描流程，按有效版本和发现版本写入运行时状态且不阻断启动
- [x] 3.2 实现 `pending_upgrade`、`abnormal` 和 `upgrade_failed` 下的插件路由、菜单、cron 或 hook 受控策略
- [x] 3.3 确保插件管理页、插件列表、升级预览和升级执行 API 在待升级/异常状态下仍可访问
- [x] 3.4 为启动扫描和受控业务入口补充后端单元测试，并运行覆盖变更包的 `go test`

## 4. 运行时升级 API 与服务编排

- [x] 4.1 新增 `GET /plugins/{id}/upgrade/preview`，返回升级前后 manifest、依赖检查、SQL 摘要、hostServices 变化和风险提示
- [x] 4.2 新增 `POST /plugins/{id}/upgrade`，执行权限校验、确认校验、状态重读和运行时升级编排
- [x] 4.3 实现升级编排顺序：加锁、预检、前置回调、自定义升级回调、upgrade SQL、治理资源同步、有效版本切换、缓存失效、后置事件
- [x] 4.4 实现升级失败记录和重试语义，保留失败阶段、错误码、manifest 快照和当前有效版本
- [x] 4.5 为运行时升级 API 和服务编排补充单元测试，并运行 `cd apps/lina-core && go test ./internal/service/plugin -count=1` 或更窄覆盖包测试

## 5. 生命周期回调统一

- [x] 5.1 设计并实现统一生命周期回调接口，覆盖 `BeforeInstall`、`BeforeUpgrade`、`Upgrade`、`AfterUpgrade`、`BeforeDisable`、`BeforeUninstall` 等核心点
- [x] 5.2 为源码插件新增升级回调请求结构，至少包含升级前 manifest 快照和目标 manifest 快照
- [x] 5.3 删除旧 lifecycle guard 与 `Can*` 注册/执行路径，统一使用 `Before*` 生命周期前置回调模型覆盖卸载、禁用和租户删除拦截行为
- [x] 5.4 更新示例源码插件或测试插件，覆盖升级回调和前置阻断回调
- [x] 5.5 为回调执行超时、panic、错误、阻断聚合和 `Before*` 生命周期前置回调补充单元测试

## 6. 前端插件管理页面

- [x] 6.1 扩展插件管理 API model，消费运行时状态、有效版本、发现版本、异常原因和失败信息
- [x] 6.2 调整插件列表状态标签和操作按钮，待升级插件显示升级，异常插件显示人工修复提示
- [x] 6.3 新增升级确认弹窗，展示版本差异、manifest 摘要、依赖检查、hostServices 变化、SQL 摘要和风险提示
- [x] 6.4 实现升级成功、失败和重试后的列表刷新与用户提示
- [x] 6.5 同步前端运行时语言包，确保新增按钮、状态、弹窗和错误提示无硬编码文案

## 7. 缓存、集群和权限治理

- [x] 7.1 在升级成功和失败路径中按插件 ID 精确失效插件状态、菜单、权限、路由、cron、i18n 和 apidoc 缓存
- [x] 7.2 在 `cluster.enabled=true` 时接入宿主统一集群协调、分布式锁或共享修订号，避免多节点并发升级
- [x] 7.3 明确升级 API 的权限校验和数据权限边界，插件自定义升级回调访问宿主服务时不得绕过租户和数据权限约束
- [x] 7.4 补充缓存一致性和集群并发相关单元测试或集成测试

## 8. E2E 与综合验证

- [x] 8.1 新增宿主级插件框架 E2E `TC0236-plugin-runtime-upgrade.ts`，覆盖待升级状态、升级按钮、升级确认和升级成功状态刷新
- [x] 8.2 新增或扩展 E2E 覆盖异常状态展示和人工修复提示
- [x] 8.3 运行前端单元测试、插件管理 E2E、后端变更包测试和启动绑定包测试
- [x] 8.4 运行 `openspec validate plugin-runtime-upgrade --strict`
- [x] 8.5 运行 `git diff --check` 覆盖本变更涉及的代码、测试、i18n 和 OpenSpec 文档
- [x] 8.6 完成实现后调用 `lina-review`，重点审查运行时升级幂等性、失败恢复、缓存/集群一致性、生命周期回调兼容性和 E2E 覆盖

## Feedback

- [x] **FB-1**: 插件安装和卸载弹窗依赖检查失败时重复显示全局网络异常和本地刷新失败提示
- [x] **FB-2**: 源码插件升级 manifest 快照投影使用 map 字符串键硬编码，缺少类型化构造契约
- [x] **FB-3**: 插件安装和卸载弹窗依赖检查接口未绑定导致所有插件显示刷新失败提示
- [x] **FB-4**: 全新项目无需保留旧 lifecycle guard 与 `Can*` 兼容接口
- [x] **FB-5**: 插件运行时 reconciler 与源码升级服务文件行数过多，需要按职责拆分
- [x] **FB-6**: `SourcePluginLifecycle` 接口方法注释过短，缺少作用和适用场景说明
- [x] **FB-7**: 动态插件缺少与源码插件一致命名的 `Before*` 生命周期前置机制
- [x] **FB-8**: 源码和动态示例插件需要注册生命周期回调并输出调试日志展示流程
- [x] **FB-9**: 动态插件缺少与源码插件对等的 `Upgrade` 和 `Uninstall` 执行阶段生命周期回调
- [x] **FB-10**: 租户级插件禁用和租户删除路径未统一触发源码插件与动态插件生命周期回调
- [x] **FB-11**: 插件 OpenAPI zh-CN apidoc 缺少运行时状态和升级失败详情字段翻译键
- [x] **FB-12**: GitHub Action plugin-full E2E 中动态插件 cron 授权预览和运行时热升级用例失败
- [x] **FB-13**: 插件 cron 列表方法命名和注释容易混淆，需要按执行、声明预览和已安装声明投影语义收敛
- [x] **FB-14**: GitHub Action 后端单元测试中未安装插件的已安装 cron 声明投影泄漏源码插件任务
- [x] **FB-15**: 插件租户过滤 contract 方法过多且字段限定参数语义不清，需要收敛为 `Context` 和 `Apply(ctx, model, qualifier)`
- [x] **FB-16**: i18n `ResourceLoader` 的 `LayoutMode` 枚举增加理解成本，应收敛为加载目录和递归开关
- [x] **FB-17**: SQLite 启动且 `plugin.autoEnable` 包含 `multi-tenant` 时，启动一致性校验早于 tenantcap provider 注册导致 panic
- [x] **FB-18**: demo-control 启用时多租户登录选择租户被误拦截为写操作
- [x] **FB-19**: 多租户插件清理数据卸载仍被租户存在前置条件阻断
- [x] **FB-20**: demo-control 启用时插件安装和卸载弹窗提交失败重复显示只读错误提示
- [x] **FB-21**: Nightly plugin-full E2E `TC0227` 仍按旧默认清理数据卸载语义等待生命周期前置条件错误，导致测试拿到成功响应后断言 `errorCode` 失败
- [x] **FB-22**: 插件管理页状态开关等待启停接口完成后才更新，接口较慢时像卡住

## Progress Notes

- 2026-05-19: 完成反馈 `FB-22`。根因是插件管理页状态 `Switch` 由 `row.enabled` 受控，但 `handleStatusChange` 只有在 `PUT /plugins/{id}/enable|disable` 完成后才写回 `row.enabled`；接口较慢时开关会停留在旧状态，用户感知为点击无响应。修复为启停请求开始前先记录旧值并将当前行乐观更新到目标状态，同时按插件 ID 显示开关 loading/disabled 防止重复点击；请求成功后继续刷新插件运行时注册表并显示成功提示，请求失败则回滚到旧状态，由既有请求错误提示显示失败原因。新增宿主级插件管理 E2E `TC0243-plugin-status-switch-feedback.ts`，覆盖慢启用接口下开关立即切到目标状态并显示加载态，以及启用失败后回滚到原状态。验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C apps/lina-vben --filter @lina/web-antd i18n:check`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`pnpm -C hack/tests test:validate`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0243-plugin-status-switch-feedback.ts --config playwright.config.ts --workers=1 --reporter=list`、`openspec validate plugin-runtime-loading --type spec --strict --no-interactive`、本轮相关文件 `git diff --check`。i18n 影响：复用既有状态和错误提示文案，不新增或修改运行时语言包、manifest i18n 或 apidoc 资源。缓存一致性影响：不新增缓存读写、失效策略或跨实例协调路径，成功后仍复用既有 `notifyPluginRegistryChanged()` 刷新前端插件注册表。数据权限影响：不新增 REST API 或业务数据访问面，插件启停仍走既有治理权限边界。开发工具脚本影响：无新增或修改默认开发工具、脚本或跨平台入口。
- 2026-05-19: 完成反馈 `FB-22` 的 `lina-review` 审查。审查范围覆盖 `apps/lina-vben/apps/web-antd/src/views/system/plugin/index.vue`、`hack/tests/e2e/extension/plugin/TC0243-plugin-status-switch-feedback.ts` 与 `openspec/changes/archive/2026-05-19-plugin-runtime-upgrade/tasks.md`。确认本轮只修改插件管理页前端交互和宿主级插件管理 E2E，不新增后端 Go 生产代码、REST API、数据库迁移、用户可见新文案、运行时 i18n/apidoc 资源、缓存失效策略、数据权限访问面或开发脚本。复验通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C apps/lina-vben --filter @lina/web-antd i18n:check`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`pnpm -C hack/tests test:validate`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0243-plugin-status-switch-feedback.ts --config playwright.config.ts --workers=1 --reporter=list`、`openspec validate plugin-runtime-loading --type spec --strict --no-interactive`、本轮相关文件 `git diff --check`。严重问题 0；警告 0。
- 2026-05-18: 完成反馈 `FB-21`。根因是 `FB-19` 已将 `multi-tenant` 的清理数据卸载调整为允许通过，以便执行卸载 SQL 清理插件租户表；但 `TC0227` 仍直接确认卸载弹窗的默认“清理插件自有存储数据”选项，导致首次 `DELETE /plugins/multi-tenant` 返回成功响应而不是 `PLUGIN_LIFECYCLE_PRECONDITION_VETOED`。修复为 `TC0227` 在触发首次卸载前显式取消清理数据勾选，覆盖“保留插件数据卸载时存在租户应弹出生命周期前置条件强制确认”的真实语义；force 卸载成功后先恢复插件安装启用状态，再清理测试租户，避免插件卸载期间租户删除接口不可用。验证通过：`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`E2E_BROWSER_CHANNEL=chrome CI=1 E2E_RETRIES=1 E2E_WORKERS=1 E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:8080/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:8080 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/multi-tenant/hack/tests/e2e/lifecycle-precondition/TC0227-plugin-uninstall-force-dialog.ts --config playwright.config.ts --workers=1 --reporter=list`、`openspec validate plugin-runtime-upgrade --strict`、根仓库与 `apps/lina-plugins` 子仓库相关文件 `git diff --check`。i18n 影响：仅调整 E2E 操作步骤和 OpenSpec 记录，不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例同步策略。数据权限影响：不新增或修改业务数据访问接口；测试租户仍通过既有平台租户 API 创建和清理。开发工具脚本影响：无新增或修改默认开发工具、脚本或跨平台入口。
- 2026-05-18: 完成反馈 `FB-21` 的 `lina-review` 审查。审查范围覆盖 `apps/lina-plugins/multi-tenant/hack/tests/e2e/lifecycle-precondition/TC0227-plugin-uninstall-force-dialog.ts`、`openspec/changes/plugin-runtime-upgrade/tasks.md`、根仓库和 `apps/lina-plugins` 子仓库状态、OpenSpec 状态与相关 diff。确认本轮只修正 E2E 对卸载清理策略的操作步骤和测试资源清理顺序，不修改生产 Go 代码、REST API、数据库迁移、运行时 UI 文案、manifest i18n、apidoc 资源、缓存失效策略、数据权限访问面或开发脚本；`TC0227` 已基于当前本地服务通过，E2E 清单校验、TypeScript 编译、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。

- 2026-05-15: 完成后端运行时状态投影切片。源码插件和动态插件文件版本高于数据库有效版本时，插件列表投影 `runtimeState=pending_upgrade`，保持 `sys_plugin.version` 与 `release_id` 不被发现版本覆盖；文件版本低于有效版本时投影 `runtimeState=abnormal` 与稳定原因码；失败目标 release 保留为 `upgrade_failed` 诊断状态。启动阶段 `ValidateSourcePluginUpgradeReadiness` 改为非阻断漂移扫描，不再因为待升级源码插件阻断宿主启动。
- 2026-05-15: 本轮已同步后端运行时 i18n、错误文案清理和 zh-CN apidoc 翻译；未完成项包括详情 DTO、业务入口受控、运行时升级预览/执行 API、统一生命周期回调、前端管理页和 E2E。
- 2026-05-15: 验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin ./pkg/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。残留扫描：`make upgrade`、`confirm=upgrade`、`source-plugin version before startup`、`before host startup`、`development upgrade tool`、`startup fail-fast`、`PLUGIN_SOURCE_UPGRADE_PENDING` 在当前变更涉及的活跃代码/规范范围内无命中。
- 2026-05-15: i18n 影响：本轮新增 `plugin.runtimeUpgrade.*` 后端运行时语言键，删除旧源码升级启动阻断 error 键，并更新插件管理 apidoc zh-CN 文案。缓存一致性影响：本轮只新增只读状态投影与 failed release 状态保留，不新增缓存写入、失效或跨实例协调路径；实际升级成功/失败缓存精确失效仍留在 7.x 任务。数据权限影响：本轮不新增 REST 写接口或业务数据访问，仅扩展插件治理列表响应；E2E 影响：前端交互尚未实现，本轮使用后端单元测试和启动绑定包测试覆盖。
- 2026-05-15: 完成业务入口受控切片。运行时状态判断下沉到 catalog 并由 integration/runtime 复用；`pending_upgrade`、`abnormal`、`upgrade_failed` 不再允许插件业务入口执行，动态路由返回 `PLUGIN_RUNTIME_UPGRADE_REQUIRED`，插件菜单/权限菜单过滤、source route、source cron、hook 派发和动态 cron 发现均复用 business-entry enabled 快照或运行时状态判断。补充 `PLUGIN_RUNTIME_UPGRADE_REQUIRED` bizerr 和中英文 error i18n；详情 DTO、升级预览/执行 API 和前端交互仍未实现。
- 2026-05-15: 业务入口受控验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/service/plugin ./pkg/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：新增 `error.plugin.runtime.upgrade.required` 中英文文案；缓存一致性影响：本轮只复用和刷新进程内 business-entry snapshot，未实现升级成功/失败后的跨实例精确失效，仍留在 7.x；数据权限影响：不新增业务数据读写接口；开发工具脚本影响：无新增或修改脚本。
- 2026-05-15: 完成插件详情 DTO 切片。新增只读 `GET /plugins/{id}`，详情响应复用 `PluginItem`，与列表一致返回 `runtimeState`、`effectiveVersion`、`discoveredVersion`、`upgradeAvailable`、`abnormalReason` 和 `lastUpgradeFailure`；新增 `PLUGIN_NOT_FOUND` bizerr 和中英文 error i18n；更新插件 apidoc zh-CN 翻译。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：新增详情 API apidoc 翻译和 `error.plugin.not.found`；缓存一致性影响：详情为只读投影，不新增缓存写入或失效；数据权限影响：插件治理详情不读取业务数据范围，仍受 `plugin:query` 权限控制。
- 2026-05-15: 完成运行时升级预览 API 切片。新增只读 `GET /plugins/{id}/upgrade/preview`，仅允许 `pending_upgrade` 插件返回预览，响应包含升级前后 manifest 快照、有效/发现版本、目标依赖检查、SQL 数量摘要、hostServices 差异和稳定风险提示；预览不执行 SQL、不切换 release、不写运行时状态、不调用插件回调。新增 `PLUGIN_RUNTIME_UPGRADE_PREVIEW_UNAVAILABLE`、`PLUGIN_RUNTIME_UPGRADE_SNAPSHOT_MISSING`、`PLUGIN_RUNTIME_UPGRADE_SNAPSHOT_INVALID` bizerr 和中英文 error i18n，补充插件 runtimeUpgrade 风险提示语言键与 zh-CN apidoc 翻译。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：新增预览 API、错误码和风险提示翻译；缓存一致性影响：本轮为只读预览，仅在读路径确认运行时缓存新鲜度，不新增缓存写入/失效或跨实例协调，升级成功/失败精确失效仍留在 7.x；数据权限影响：插件治理预览不读取业务数据范围，仍受 `plugin:query` 权限控制；开发工具脚本影响：无新增或修改脚本。
- 2026-05-15: 完成运行时升级执行 API 后端切片。新增 `POST /plugins/{id}/upgrade`，权限复用 `plugin:install`，请求必须携带 `confirmed=true`，服务端执行前重新读取 registry、发现 manifest 和运行时状态，仅允许 `pending_upgrade` 进入升级；源码插件委托既有 `UpgradeSourcePlugin`，动态插件新增显式 `UpgradeDynamicPluginRequest` 并复用内部 `applyUpgrade` 执行 upgrade SQL、治理资源同步、有效 release 切换、运行时缓存失效和变更通知。普通动态 `Install`/后台 reconciler 不再因为高版本 staged artifact 自动升级，只保留待升级状态。新增 `PLUGIN_RUNTIME_UPGRADE_CONFIRMATION_REQUIRED`、`PLUGIN_RUNTIME_UPGRADE_UNAVAILABLE`、`PLUGIN_RUNTIME_UPGRADE_TYPE_UNSUPPORTED`、`PLUGIN_RUNTIME_UPGRADE_EXECUTION_FAILED` bizerr 和中英文 error i18n，补充 zh-CN apidoc 翻译。失败语义本轮复用 target release `failed` 诊断与有效版本不切换，阶段细化、manifest 快照失败记录和幂等重试账本仍留在 4.4/后续任务。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`jq empty apps/lina-core/manifest/i18n/en-US/error.json apps/lina-core/manifest/i18n/zh-CN/error.json apps/lina-core/manifest/i18n/en-US/plugin.json apps/lina-core/manifest/i18n/zh-CN/plugin.json apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json`。i18n 影响：新增执行 API apidoc 和执行错误码翻译；缓存一致性影响：动态升级成功路径复用现有动态 runtime 插件粒度缓存失效和 cache revision 通知，源码升级继续复用 `source_plugin_upgraded` 修订通知，集群分布式锁和更细作用域失效仍留在 7.x；数据权限影响：升级 API 属于插件治理写操作，不读取业务数据范围，仍由接口权限控制，插件自定义回调尚未接入；开发工具脚本影响：无新增或修改脚本。
- 2026-05-15: 完成源码插件运行时升级生命周期回调和失败诊断切片。`pluginhost.SourcePluginLifecycle` 新增 `BeforeInstall`、`BeforeUpgrade`、`Upgrade`、`AfterUpgrade`、`BeforeDisable`、`BeforeUninstall` 注册接口，并发布 `SourcePluginLifecycleInput`、`ManifestSnapshot`、`SourcePluginUpgradeInput`；源码升级流程已接入 `BeforeUpgrade` 阻断、自定义 `Upgrade` 回调、升级 SQL、治理同步、有效 release 切换、runtime cache 修订通知、`AfterUpgrade` 回调和 `plugin.upgraded` hook。失败时目标 release 标记为 failed，非 SQL 阶段写入 `sys_plugin_migration phase=upgrade` 诊断记录，列表/详情通过 `lastUpgradeFailure.phase/errorCode/messageKey/detail` 暴露，`upgrade_failed` 可重新预览和重试。显式升级入口新增同进程按插件 ID 本地锁；`cluster.enabled=true` 的跨实例分布式锁仍保留在 7.2。验证通过：`cd apps/lina-core && go test ./internal/service/plugin -run 'TestRuntimeUpgradeLockSerializesSamePlugin|TestExecuteRuntimeUpgrade|TestUpgradeSourcePluginInvokesLifecycleCallbacks|TestUpgradeSourcePluginBeforeCallbackVetoes|TestUpgradeSourcePluginCallbackFailureIsRetryable' -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`jq empty apps/lina-core/manifest/i18n/en-US/error.json apps/lina-core/manifest/i18n/zh-CN/error.json apps/lina-core/manifest/i18n/en-US/plugin.json apps/lina-core/manifest/i18n/zh-CN/plugin.json apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：新增升级失败 detail 与生命周期阻断相关错误文案；缓存一致性影响：本轮完成成功路径已有插件粒度缓存修订通知和本地锁，跨节点锁/更细作用域失效仍待 7.x；数据权限影响：升级回调通过目标插件进程内代码运行，后续若通过 host services 访问业务数据必须继续遵守对应服务的数据权限边界；开发工具脚本影响：无新增或修改脚本。
- 2026-05-15: 完成旧 lifecycle guard 与 `Can*` 接口删除。源码插件安装、禁用、卸载路径改为只调用 `BeforeInstall`、`BeforeDisable`、`BeforeUninstall`；租户删除通过 `BeforeTenantDelete` 聚合前置条件；插件管理强制卸载弹窗、错误码和 E2E 命名统一为 lifecycle precondition。强制卸载在配置允许时仍可绕过 `BeforeUninstall`，保持人工修复能力。验证通过：`cd apps/lina-core && go test ./pkg/pluginhost -run 'TestRunLifecycleCallbacks(Aggregates|Treats|Recovers)' -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -run 'Test(SourceLifecycleBeforeInstallBlocksInstall|SourceLifecycleBeforeDisableBlocksDisable|SourceLifecycleBeforeUninstallBlocksUninstall|SourceLifecycleBeforeUninstallForceBypassesWhenConfigured)' -count=1`。
- 2026-05-15: 完成插件管理页运行时升级交互。前端 API model 增加 `runtimeState/effectiveVersion/discoveredVersion/upgradeAvailable/abnormalReason/lastUpgradeFailure` 与升级预览/执行 DTO；插件列表新增运行时状态列、版本漂移展示、待升级/失败升级按钮和异常人工修复提示；新增升级确认弹窗展示前后 manifest、依赖检查、SQL 摘要、hostServices 差异和风险提示，升级成功/失败均刷新列表并通过运行时语言包提示。验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C apps/lina-vben --filter @lina/web-antd i18n:check`、`pnpm -C apps/lina-vben test:unit apps/web-antd/src/locales/index.test.ts apps/web-antd/src/runtime/public-frontend.test.ts apps/web-antd/src/adapter/form.test.ts`。i18n 影响：同步维护 `zh-CN/pages.json` 与 `en-US/pages.json` 的运行时状态、按钮、弹窗、错误提示和风险提示文案，无新增硬编码用户文案。
- 2026-05-15: 完成运行时升级缓存、集群和权限治理。升级入口在单机模式使用进程内按插件 ID 锁；`cluster.enabled=true` 时通过宿主 `coordination.LockStore` 获取分布式锁，缺失锁后端失败关闭，不退化为本地锁。升级开始前写入 `current_state=upgrade_running` 作为可观察运行中标记，失败后恢复稳定宿主状态，使 failed target release 投影为 `upgrade_failed` 而不是长期运行中。成功和失败路径均按插件 ID 精确失效本节点 frontend bundle、WASM 动态模块和 i18n 插件扇区；源码插件 i18n 新增 `SourcePluginID` 作用域，动态插件继续使用 `DynamicPluginID` 作用域；跨实例通过既有 `plugin-runtime` shared revision 广播，其他节点在读路径 `EnsureFresh` 后刷新 enabled snapshot、frontend bundle、WASM 与 runtime i18n 缓存。权限边界：升级执行仍复用 `plugin:install` 管理权限，不读取业务数据范围；插件自定义升级回调只能通过既有 host services 访问宿主能力，继续受租户和数据权限边界约束。验证通过：`cd apps/lina-core && go test ./internal/service/i18n -run 'TestInvalidateRuntimeBundleCache(SourcePluginIsPluginScoped|DynamicPluginIsPluginScoped|HostSectorIsLocaleScoped)' -count=1`、`cd apps/lina-core && go test ./internal/service/plugin -run 'Test(ExecuteRuntimeUpgradeFailureKeepsEffectiveDynamicVersion|ExecuteRuntimeUpgradeUpgradesDynamicPlugin|UpgradeSourcePluginInvokesLifecycleCallbacks|UpgradeSourcePluginCallbackFailureIsRetryable|RuntimeUpgradeClusterLockRejectsMissingBackend|RuntimeUpgradeClusterLockSerializesAcrossServices)' -count=1`。
- 2026-05-15: 新增宿主级插件框架 E2E `hack/tests/e2e/extension/plugin/TC0236-plugin-runtime-upgrade.ts`，使用管理 API mock 覆盖待升级状态、升级按钮、升级确认弹窗、成功刷新为正常状态，以及异常状态的人工修复提示。验证通过：`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0236-plugin-runtime-upgrade.ts --config playwright.config.ts`。
- 2026-05-15: 综合验证通过：`cd apps/lina-core && go test ./internal/service/i18n ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C apps/lina-vben --filter @lina/web-antd i18n:check`、`pnpm -C apps/lina-vben test:unit apps/web-antd/src/locales/index.test.ts apps/web-antd/src/runtime/public-frontend.test.ts apps/web-antd/src/adapter/form.test.ts`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0236-plugin-runtime-upgrade.ts --config playwright.config.ts`、`jq empty apps/lina-core/manifest/i18n/en-US/error.json apps/lina-core/manifest/i18n/zh-CN/error.json apps/lina-core/manifest/i18n/en-US/plugin.json apps/lina-core/manifest/i18n/zh-CN/plugin.json apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json apps/lina-vben/apps/web-antd/src/locales/langs/zh-CN/pages.json apps/lina-vben/apps/web-antd/src/locales/langs/en-US/pages.json`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`、`rg "type ControllerV1 struct\\{\\}|func NewV1\\(\\)" apps/lina-core/internal/controller -n` 无命中。开发工具脚本影响：本轮未新增或修改默认开发工具入口；既有 unrelated 脚本变更未在本轮触碰。
- 2026-05-15: 完成 `lina-review` 终审。审查范围覆盖已跟踪和未跟踪变更，重点检查运行时升级幂等性、失败恢复、缓存/集群一致性、生命周期回调兼容性、权限/数据权限边界、i18n 作用域失效、E2E 用例独立性和 Go 文件注释规范。发现并修复 5 个变更范围内 Go 文件缺少文件用途注释的问题；未发现阻塞性实现缺陷。复验通过：`cd apps/lina-core && go test ./internal/service/i18n ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin/internal/integration ./internal/controller/plugin ./internal/service/plugin ./pkg/sourceupgrade ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`。
- 2026-05-15: 完成反馈 `FB-1`。根因是安装/卸载弹窗打开时刷新 `/plugins/{id}/dependencies`，请求失败先触发全局 request 网络错误提示，又被弹窗本地 catch 显示“刷新插件依赖检查结果失败”。新增 `silentErrorMessage` 请求配置和 `pluginDependencyCheckSilently`，仅让依赖检查刷新由弹窗显示单点本地提示；普通接口继续保留全局错误提示。更新 `TC0235-plugin-dependency-management.ts` 增加安装/卸载网络失败断言，确认不再出现“网络异常，请检查您的网络连接后重试。”。验证通过：`pnpm -C apps/lina-vben test:unit packages/effects/request/src/request-client/request-client.test.ts`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests test:validate`、`pnpm -C hack/tests exec tsc --noEmit`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0235-plugin-dependency-management.ts --config playwright.config.ts`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：复用既有 `pages.system.plugin.dependency.checkFailed` 和全局网络错误文案，无新增翻译键；缓存和数据权限影响：前端错误提示路径修复，不涉及缓存或后端数据访问边界。
- 2026-05-15: 完成反馈 `FB-2`。根因是源码插件升级回调的 manifest 快照投影直接在 `sourceupgrade` 中组装 `map[string]interface{}`，字段名与 `pluginhost.NewManifestSnapshot` 内部取值逻辑重复，存在拼写错误和契约漂移风险。修复新增 `pluginhost.ManifestSnapshotValues` 与 `NewManifestSnapshotFromValues` 类型化构造器，`sourceManifestSnapshotView` 改为填充结构体字段，旧 `NewManifestSnapshot(map[string]interface{})` 保留为兼容入口并集中使用内部字段常量。新增 `TestSourceManifestSnapshotViewPublishesTypedSnapshot` 覆盖宿主投影字段，新增 `TestNewManifestSnapshotKeepsMapConstructorCompatible` 覆盖旧 map 构造器兼容。验证通过：`cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/plugin/internal/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin ./pkg/pluginhost ./internal/service/plugin/internal/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：本轮只调整 Go 内部构造契约和单元测试，不新增或修改用户文案；缓存一致性影响：仅调整回调输入快照构造，不新增缓存读写、失效或跨实例协调路径；数据权限影响：不新增接口或业务数据访问；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-15: 完成反馈 `FB-3`。根因是新增的 `DependencyCheck` 后端接口只完成了 API/Controller/Service 定义，但未在 `bindHostAPIRoutes` 的受保护路由组中绑定，导致插件安装和卸载弹窗每次请求 `GET /plugins/{id}/dependencies` 都命中未发布路由并进入前端本地 `checkFailed` 提示，因此表现为所有插件都会显示“刷新插件依赖检查结果失败”。修复已补齐 `pluginCtrl.DependencyCheck` 路由绑定，并同步绑定同一运行时升级切片新增但漏挂的 `Detail`、`UpgradePreview`、`Upgrade` 端点；新增 `TestPluginManagementRuntimeRoutesAreBound` 覆盖这些路由存在且保留 `Auth` 与 `Permission` 中间件。验证通过：`cd apps/lina-core && go test ./internal/cmd -run 'TestPluginManagementRuntimeRoutesAreBound|TestUploadedFileAccessRouteIsPublic' -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`。i18n 影响：本轮不新增或修改用户文案；缓存一致性影响：仅修复 HTTP 路由发布，不改变缓存读写或失效；数据权限影响：新增发布的管理端点均继续处于受保护路由组并保留声明式权限中间件。
- 2026-05-15: 完成反馈 `FB-4`。项目为全新项目，无需保留旧 lifecycle guard 与 `Can*` 兼容层；删除旧 `lifecycle_guard` 文件、`RegisterLifecycleGuard`/`CanUninstall`/`CanDisable`/`CanTenantDelete` 等注册和执行路径，统一改为 `BeforeInstall`、`BeforeUpgrade`、`BeforeDisable`、`BeforeUninstall`、`BeforeTenantDisable`、`BeforeTenantDelete`、`BeforeInstallModeChange` 前置回调，以及升级专用 `Upgrade`/`AfterUpgrade` 回调。同步将宿主、multi-tenant 源码插件、前端弹窗、E2E 和错误码命名从 lifecycle guard 改为 lifecycle precondition；修复源码插件安装/禁用/卸载路径中根 facade 与源码插件专属执行重复触发同一 `Before*` 回调的问题，确保每个源插件操作只执行一次前置检查。验证通过：`cd apps/lina-core && go test ./internal/service/plugin -run 'TestSourceLifecycleBefore(InstallBlocksInstall|DisableBlocksDisable|UninstallBlocksUninstall|UninstallForceBypassesWhenConfigured)' -count=1`、`cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/plugin ./internal/service/plugin/internal/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、旧 guard/`Can*` 残留扫描无命中、相关 i18n JSON `jq empty` 通过。i18n 影响：已同步宿主 error i18n、multi-tenant 插件 error i18n、前端 pages 语言包和 packed 镜像；缓存一致性影响：仅变更生命周期前置检查命名和调用收敛，不新增缓存写入、失效或跨实例协调路径；数据权限影响：不新增业务数据接口或数据访问范围；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-16: 完成反馈 `FB-5`。按职责拆分过大的 Go 文件：`runtime/reconciler.go` 保留后台调度、请求入口、registry 决策和节点投影，新增 `reconciler_install.go`、`reconciler_upgrade.go`、`reconciler_uninstall.go`、`reconciler_rollback.go` 分别承载安装/Mock 数据、升级/刷新/状态切换、卸载/孤儿清理和回滚失败投影；`sourceupgrade/sourceupgrade.go` 保留服务契约与构造，新增 `sourceupgrade_status.go`、`sourceupgrade_execute.go`、`sourceupgrade_release.go`、`sourceupgrade_lifecycle.go`、`sourceupgrade_failure.go`、`sourceupgrade_result.go` 分别承载状态扫描、升级编排、release 切换、生命周期快照、失败诊断和结果消息。本轮为同包私有函数迁移，不改变导出接口、REST API、用户文案、缓存写入/失效策略、数据访问语义或开发工具脚本。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade -count=1`、`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：无用户可见文案变更；缓存一致性影响：仅移动既有缓存相关调用位置，不改变策略或作用域；数据权限影响：不新增或修改数据操作接口；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-16: 完成反馈 `FB-5` 的 `lina-review` 审查。审查确认本轮为同包函数迁移和文件职责拆分，未新增导出 API、运行时依赖、缓存策略、数据权限边界或用户可见文案；新增 Go 文件均符合组件前缀命名和文件顶部用途注释要求。复验通过：`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。
- 2026-05-16: 完成反馈 `FB-6`。扩展 `pluginhost.SourcePluginLifecycle` 每个注册方法的 GoDoc，明确各生命周期回调的作用、宿主调用时机、失败/阻断语义和适用场景，帮助源码插件开发者区分前置否决、升级执行、后置通知和卸载清理职责。验证通过：`cd apps/lina-core && go test ./pkg/pluginhost -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- apps/lina-core/pkg/pluginhost/pluginhost_source_plugin_contract.go openspec/changes/plugin-runtime-upgrade/tasks.md`。i18n 影响：仅修改开发者 GoDoc 和 OpenSpec 任务记录，不新增或修改用户可见文案、运行时语言包或 apidoc 资源；缓存一致性影响：不涉及缓存读写、失效或跨实例协调；数据权限影响：不新增接口或业务数据访问；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-16: 完成反馈 `FB-7`。动态插件新增专用 `lina.plugin.backend.lifecycle` artifact section 与 `backend/lifecycle/*.yaml` 打包入口，生命周期 operation 仅接受与源码插件一致的 `BeforeInstall`、`BeforeUpgrade`、`BeforeDisable`、`BeforeUninstall`、`BeforeTenantDisable`、`BeforeTenantDelete`、`BeforeInstallModeChange`，拒绝旧 `Can*` 命名；动态 install/upgrade/disable/uninstall/tenant delete 已接入相同 `PLUGIN_LIFECYCLE_PRECONDITION_VETOED` 前置阻断错误。`BeforeUpgrade` 在写入 `upgrade_running` 与切换 release 前执行；disable/uninstall/tenant delete 使用 active release manifest，避免 staged artifact 影响当前生效插件；install/upgrade lifecycle 执行时使用本次操作确认或已持久化的 hostServices 授权快照，未确认的资源型 host service 不会暴露给前置处理器，并由 `TestBuildLifecycleAuthorizedHostServicesDropsUnconfirmedResources` 与 `TestApplyTargetReleaseAuthorizedHostServicesFiltersMissingRelease` 覆盖。`build-wasm` 的临时动态插件测试已覆盖 `backend/lifecycle/001-before-install.yaml` 打包与 artifact section 解析。验证通过：`cd apps/lina-core && go test ./internal/service/plugin -run 'TestApplyTargetReleaseAuthorizedHostServicesFiltersMissingRelease|TestBuildLifecycleAuthorizedHostServicesDropsUnconfirmedResources|TestExecuteRuntimeUpgradeBeforeLifecycleBlocksBeforeRunningState' -count=1`、`cd apps/lina-core && go test ./pkg/pluginbridge ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime ./internal/service/plugin -count=1`、`cd hack/tools/build-wasm && go test ./... -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：复用既有 `PLUGIN_LIFECYCLE_PRECONDITION_VETOED` 错误码和文案，不新增用户可见文案或 apidoc；缓存一致性影响：本轮只在 lifecycle side effect 前增加前置判断，不新增缓存，成功/失败后的既有缓存失效路径不变；数据权限影响：不新增业务数据接口，动态 lifecycle 若通过 host services 访问数据仍受已授权 host service 与其既有租户/数据权限边界约束；开发工具脚本影响：仅修改 Go 版 `build-wasm` 工具并通过其单元测试，无新增平台脚本。
- 2026-05-16: 完成反馈 `FB-8`。源码示例插件 `plugin-demo-source` 已注册 `BeforeInstall`、`AfterInstall`、`BeforeUpgrade`、`Upgrade`、`AfterUpgrade`、`BeforeDisable`、`AfterDisable`、`BeforeUninstall`、`AfterUninstall`、`BeforeTenantDisable`、`AfterTenantDisable`、`BeforeTenantDelete`、`AfterTenantDelete`、`BeforeInstallModeChange` 和 `AfterInstallModeChange` 调试回调，并通过项目 logger 输出 operation、plugin、版本、租户和安装模式切换字段；动态示例插件 `plugin-demo-dynamic` 已通过 `backend/lifecycle/*.yaml` 声明相同命名的 14 个 `Before*`/`After*` lifecycle contract，controller 统一返回 `ok=true`，service 通过 runtime log 输出 `plugin-demo-dynamic lifecycle callback invoked` 及操作上下文。补充动态插件 artifact 构建验证，确认官方示例打包成功并包含 14 个 lifecycle contracts。验证通过：`cd apps/lina-core && go test ./pkg/pluginhost ./pkg/pluginbridge ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd hack/tools/build-wasm && go test ./... -count=1`、`cd hack/tools/build-wasm && go run . --plugin-dir ../../../apps/lina-plugins/plugin-demo-dynamic --output-dir <tmpdir>`、临时 `go.work` 覆盖 `apps/lina-core`、`apps/lina-plugins/plugin-demo-source`、`apps/lina-plugins/plugin-demo-dynamic` 后运行 `go test <plugin-demo-source>/... <plugin-demo-dynamic>/... -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。旧命名扫描仅剩 OpenSpec 历史说明和 `CanInstall` 负向测试用例，用于证明旧命名被拒绝；运行时代码和示例插件不再使用旧 guard/`Can*` 命名。i18n 影响：仅更新示例 README 和调试日志文本，不新增 UI runtime i18n、manifest i18n 或 apidoc 资源；缓存一致性影响：仅新增生命周期成功后通知和示例日志，不新增缓存读写、失效或跨实例协调路径；数据权限影响：不新增数据 API，动态 lifecycle 若通过 host services 访问宿主数据仍受既有授权、租户和数据权限边界约束；开发工具脚本影响：未新增平台脚本，`build-wasm` 仅作为 Go 工具验证入口。
- 2026-05-16: 完成反馈 `FB-8` 的 `lina-review` 审查。审查范围覆盖源码示例插件、动态示例插件、动态 lifecycle contract、源码/动态 `After*` 通知执行路径、相关单元测试和 OpenSpec 任务记录。审查修正 `pluginhost/lifecycle_precondition.go` 文件级注释和 `LifecycleHook` 注释，避免仍将承载 `After*` 的统一 lifecycle callback runner 描述为仅 precondition。未发现阻塞性实现缺陷；确认 `Before*` 仍为失败关闭前置阻断，`After*` 为成功后 best-effort 通知，失败只记录 warning，不回滚已经完成的生命周期操作。复验通过：`cd apps/lina-core && go test ./pkg/pluginhost ./pkg/pluginbridge ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/sourceupgrade ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。
- 2026-05-16: 完成反馈 `FB-9` 和 `FB-10`。动态插件生命周期契约新增与源码插件一致的 `Upgrade`、`Uninstall` 执行阶段 operation，bridge 请求补充 `purgeStorageData`、`fromManifest`、`toManifest`；动态升级在 `BeforeUpgrade` 允许后、upgrade SQL 前执行 `Upgrade`，失败后保持旧有效 release 并进入升级失败诊断；动态卸载仅在清理插件存储和数据时执行 `Uninstall`，失败后保持已安装状态，保留数据卸载会跳过该执行阶段。租户级插件禁用和租户删除统一通过宿主 `PluginLifecycleService` 编排，源码与动态插件均可执行 `BeforeTenantDisable`/`AfterTenantDisable`、`BeforeTenantDelete`/`AfterTenantDelete`；multi-tenant 源码插件在写入租户插件状态前调用前置检查，成功后发送 best-effort 通知。`lina-review` 发现动态卸载执行阶段失败会把低层 bridge/runtime 错误直接暴露给 API 调用方，已补 `PLUGIN_UNINSTALL_EXECUTION_FAILED` 稳定 bizerr 和中英文 error i18n，并保留既有依赖阻断、前置 veto、force 配置错误和 artifact missing 等业务错误原码。验证通过：`go test ./apps/lina-core/internal/service/plugin ./apps/lina-core/internal/service/plugin/internal/runtime ./apps/lina-core/pkg/pluginhost ./apps/lina-core/pkg/pluginbridge ./apps/lina-core/pkg/pluginservice/... ./apps/lina-core/internal/service/pluginhostservices -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend/internal/service/tenant ./backend/internal/service/tenantplugin ./backend/internal/service/lifecycleprecondition ./backend -count=1`、临时 `go.work` 覆盖 `apps/lina-core` 与插件模块后运行 `go test` 验证 `plugin-demo-dynamic` 和 `monitor-server` 相关包、`go test ./hack/tools/build-wasm/internal/builder -count=1`、`jq empty apps/lina-core/manifest/i18n/en-US/error.json apps/lina-core/manifest/i18n/zh-CN/error.json`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：除新增 `PLUGIN_UNINSTALL_EXECUTION_FAILED` 调用方可见错误翻译外，不新增 UI 文案、apidoc 文案或运行时语言包键；缓存一致性影响：租户插件状态写入仍复用既有 `plugin-runtime` shared revision bump，动态升级/卸载继续复用已有插件粒度缓存失效与跨实例通知，未新增独立缓存；数据权限影响：未新增 REST 数据操作接口，生命周期回调若通过 host services 访问宿主数据仍受既有授权、租户和数据权限边界约束；开发工具脚本影响：仅修改 Go 版 `build-wasm` lifecycle discovery，无新增平台脚本，并通过 builder 单元测试验证。
- 2026-05-16: 完成反馈 `FB-11`。补齐插件 OpenAPI zh-CN apidoc 中 `PluginDynamicItem.runtimeState` 与 `PluginUpgradeFailureItem.detail` 的结构化字段翻译，并同步更新 packed manifest 镜像，修复 `TestOpenAPII18nBundlesCoverCurrentMetadata/zh-CN` 报告的 missing key。验证通过：`cd apps/lina-core && go test ./internal/service/apidoc -run TestOpenAPII18nBundlesCoverCurrentMetadata -count=1`、`jq empty apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json apps/lina-core/internal/packed/manifest/i18n/zh-CN/apidoc/core-api-plugin.json`、`openspec validate plugin-runtime-upgrade --strict`。i18n 影响：仅补齐 apidoc zh-CN 字段翻译，不新增 UI 运行时语言包或错误码；缓存一致性影响：静态接口文档翻译资源补齐，不新增缓存读写或失效逻辑；数据权限影响：不新增或修改 API 行为；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-16: 完成反馈 `FB-11` 的 `lina-review` 审查。审查范围限定为插件 apidoc zh-CN 翻译资源、packed manifest 镜像和 OpenSpec 任务记录；确认新增键使用结构化 `core.api.plugin.v1.*` 命名空间，不使用源文本键，不修改英文占位 bundle，不引入运行时 API、缓存、权限或数据权限行为变更。复验通过：`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-plugin.json apps/lina-core/internal/packed/manifest/i18n/zh-CN/apidoc/core-api-plugin.json openspec/changes/plugin-runtime-upgrade/tasks.md`。
- 2026-05-16: 完成反馈 `FB-12`。GitHub Action `plugin-full` 失败根因分为两类：其一，动态插件 cron 声明发现被业务入口 enablement 过滤复用，导致安装授权弹窗无法在安装前展示 `plugin-demo-dynamic` 的 `heartbeat` cron 授权明细，且安装后未启用阶段无法把内置任务投影到任务管理；其二，`TC0070-plugin-hot-upgrade.ts` 仍把重复 `install` 当作热升级入口，而运行时升级变更后高版本上传只进入待升级状态，必须显式调用 `POST /plugins/{id}/upgrade`。修复将 cron 可见性拆分为三条路径：授权预览使用 `ListCronDeclarationsByPlugin`，已安装任务投影使用 `ListInstalledCronDeclarations`，handler 发布继续使用 `ListExecutableCronJobs` 并要求业务入口已启用；同时将 cron 服务对插件服务的依赖收窄为声明列表接口。E2E 热升级用例改为上传高版本后等待有效版本保持旧版本，再调用显式升级 API；失败升级场景改为断言升级 API 失败并保持稳定版本资源。补充真实 bundled Wasm artifact 的 integration 测试，覆盖插件列表授权预览路径在未安装前执行 `plugin-demo-dynamic` cron registration 并发现 `heartbeat`。验证通过：`go test ./apps/lina-core/internal/service/plugin/internal/integration -run 'TestListExecutableCronJobsSkipsPendingUpgradeDynamicPlugin|TestListCronDeclarationsDiscoversDisabledDynamicPlugin|TestListInstalledCronDeclarationsDiscoversInstalledDisabledDynamicPlugin|TestListCronDeclarationsDiscoversBundledDynamicSample' -count=1`、`go test ./apps/lina-core/internal/service/cron -run 'TestSyncBuiltinScheduledJobsRegistersDeclarationSnapshots|TestBuildPluginBuiltinJobsUsesInstalledDeclarations' -count=1`、`go test -p 1 ./apps/lina-core/internal/service/plugin/internal/integration ./apps/lina-core/internal/service/cron ./apps/lina-core/internal/controller/plugin ./apps/lina-core/internal/service/plugin -count=1`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`、`cd apps/lina-core && go test ./internal/cmd -count=1`。本地 `E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0067-runtime-wasm-lifecycle.ts hack/tests/e2e/extension/plugin/TC0070-plugin-hot-upgrade.ts --config playwright.config.ts` 在未重启后端服务的情况下表现为 `TC0070` 全部通过、`TC0067h/o` 仍命中旧服务进程而失败；本轮以重启后会生效的后端 integration 覆盖该链路。i18n 影响：不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc 资源；缓存一致性影响：仅读取已存在的运行时缓存新鲜度和安装状态来区分声明/投影/执行路径，不新增缓存写入、全量失效或跨实例协调策略；数据权限影响：不新增业务数据接口，插件管理和任务管理仍走既有权限/数据权限边界；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-16: 完成反馈 `FB-12` 的 `lina-review` 审查。审查范围限定为动态插件 cron 声明/投影/执行路径拆分、插件列表授权预览、cron 内置任务投影、`TC0070` 热升级 E2E 语义和本任务记录；确认 handler 发布仍只面向已启用插件，授权预览不会投影未安装插件任务，已安装未启用插件会在任务管理中以缺失 handler 的既有 `paused_by_plugin` 语义呈现。未发现阻塞性实现缺陷；注意 integration 测试包与部分菜单测试共享数据库/测试存储，包级验证需使用 `-p 1` 避免并行污染。复验通过：`go test -p 1 ./apps/lina-core/internal/service/plugin/internal/integration ./apps/lina-core/internal/service/cron ./apps/lina-core/internal/controller/plugin ./apps/lina-core/internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。
- 2026-05-17: 完成反馈 `FB-13`。将插件 cron 对外方法按语义重命名为 `ListExecutableCronJobs`、`ListExecutableCronJobsByPlugin`、`ListCronDeclarationsByPlugin` 和 `ListInstalledCronDeclarations`，并在 facade 与 integration 接口/实现上补充详细 GoDoc，明确执行态 handler 发布、授权预览声明、已安装声明投影三条路径的过滤条件和禁止混用边界；同步更新 cron、jobhandler、插件列表 controller 和相关测试桩/测试名。验证通过：`go test -p 1 ./apps/lina-core/internal/service/plugin/internal/integration ./apps/lina-core/internal/service/cron ./apps/lina-core/internal/service/jobhandler ./apps/lina-core/internal/controller/plugin ./apps/lina-core/internal/service/plugin -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/plugin.go apps/lina-core/internal/service/plugin/plugin_integration.go apps/lina-core/internal/service/plugin/internal/integration/integration.go apps/lina-core/internal/service/plugin/internal/integration/extensions_cron_managed.go apps/lina-core/internal/service/plugin/internal/integration/extensions_cron_test.go apps/lina-core/internal/controller/plugin/plugin_v1_list.go apps/lina-core/internal/service/cron/cron.go apps/lina-core/internal/service/cron/cron_managed_jobs.go apps/lina-core/internal/service/cron/cron_managed_jobs_test.go apps/lina-core/internal/service/jobhandler/jobhandler_plugin.go apps/lina-core/internal/service/jobhandler/jobhandler_test.go openspec/changes/plugin-runtime-upgrade/tasks.md`。i18n 影响：仅调整开发者 API 命名、GoDoc 和任务记录，不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc 资源；缓存一致性影响：不改变既有运行时缓存新鲜度确认、插件粒度失效或跨实例通知策略；数据权限影响：不新增或修改 REST API、业务数据访问或数据权限边界；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-13` 的 `lina-review` 审查。审查范围覆盖插件 cron facade、integration 接口/实现、插件列表授权预览、cron 内置任务投影、jobhandler 生命周期注册、相关测试桩和 OpenSpec 任务记录；确认三条路径命名和注释已区分“可执行 handler 发布”“声明预览”“已安装声明投影”，执行路径仍要求动态插件业务入口可用，声明预览不会被误用于 handler 发布，已安装声明投影仍排除未安装插件。未发现阻塞性问题；复验通过：`cd apps/lina-core && go test ./internal/cmd -count=1`、旧方法名静态扫描无代码残留、`openspec status --change plugin-runtime-upgrade --json` 状态正常。
- 2026-05-17: 完成反馈 `FB-14`。修复 `ListInstalledCronDeclarations` 的安装状态过滤只作用于动态插件的问题，将 `installedOnly` 过滤上移到 source/dynamic 共同扫描入口，确保未安装源码插件和未安装动态插件的 cron 声明都不会进入任务管理投影；保留授权预览 `ListCronDeclarationsByPlugin` 对未安装动态插件的声明发现能力。补充回归断言，创建未安装源码插件 fixture 并验证已安装声明投影不泄漏该插件。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/integration -run 'TestListCronDeclarationsDiscoversDisabledDynamicPlugin|TestListInstalledCronDeclarationsDiscoversInstalledDisabledDynamicPlugin' -count=1`、`make init confirm=init`、`make pack.assets`、`LINA_TEST_PGSQL_LINK='pgsql:postgres:postgres@tcp(127.0.0.1:5432)/linapro?sslmode=disable' make test.go plugins=1 race=true verbose=true`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/internal/integration/extensions_cron_managed.go apps/lina-core/internal/service/plugin/internal/integration/extensions_cron_test.go openspec/changes/plugin-runtime-upgrade/tasks.md`。i18n 影响：仅修改后端过滤逻辑、测试和任务记录，不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc 资源；缓存一致性影响：仅读取既有插件 registry 安装状态过滤投影，不新增缓存、失效或跨实例协调路径；数据权限影响：不新增 REST API 或业务数据访问，任务管理投影仍复用既有权限/数据权限边界；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-15`。将源码插件可见的 `TenantFilterService` 从 `Current`、`CurrentContext`、`Apply`、`ApplyColumn` 四方法收敛为 `Context(ctx)` 与 `Apply(ctx, model, qualifier)` 两方法；`tenant_id` 字段名继续由 contract 固定，`qualifier` 仅表达 join 查询中的表名或别名，空 qualifier 保持单表查询行为。同步迁移官方源码插件的 demo、登录日志、操作日志、通知、组织部门/岗位和 orgcap provider 调用点，直接写入租户字段时使用 `Context(ctx).TenantID`。验证通过：`cd apps/lina-core && go test ./pkg/pluginservice/... ./internal/service/pluginhostservices ./internal/service/auth -count=1`、临时 `go.work` 覆盖 `apps/lina-core` 与受影响官方插件模块后运行 `go test ./apps/lina-plugins/plugin-demo-source/backend/... ./apps/lina-plugins/monitor-loginlog/backend/... ./apps/lina-plugins/monitor-operlog/backend/... ./apps/lina-plugins/content-notice/backend/... ./apps/lina-plugins/org-center/backend/... -count=1`。i18n 影响：仅修改 Go contract、服务调用和测试，不新增或修改用户可见文案、manifest i18n、运行时语言包或 apidoc 资源；缓存一致性影响：不新增缓存读写、失效或跨实例协调路径；数据权限影响：不新增 REST API 或业务数据访问边界，保留既有租户隔离语义；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-16`。将 `pkg/i18nresource.ResourceLoader` 从 `LayoutMode` 枚举收敛为 `Subdir` + 可选 `LocaleSubdir` + `Recursive`，运行时 UI 语言包继续默认加载 `manifest/i18n/<locale>/*.json`，apidoc 通过 `LocaleSubdir=apidoc` 与 `Recursive=true` 加载 `manifest/i18n/<locale>/apidoc/**/*.json`；未迁移资源目录，未合并 runtime/apidoc 动态插件 artifact section。同步更新 `build-wasm` 内部 helper 命名和 `framework-i18n-foundation` 规范描述。验证通过：`go test ./apps/lina-core/pkg/i18nresource -count=1`、`cd apps/lina-core && go test ./internal/service/i18n ./internal/service/apidoc -count=1`、`cd hack/tools/build-wasm && go test ./... -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`。i18n 影响：仅调整 i18n 加载器内部契约和规范描述，不新增或修改用户可见文案、manifest i18n 资源或 apidoc 翻译内容；缓存一致性影响：不改变运行时语言包或 apidoc 缓存键、失效作用域、动态插件 section 或跨实例通知策略；数据权限影响：不新增 REST API 或业务数据访问；开发工具脚本影响：仅修改 Go 版 `build-wasm` 内部 helper 命名并通过其单元测试，无新增平台脚本。
- 2026-05-17: 完成反馈 `FB-16` 的 `lina-review` 审查。审查范围覆盖 `pkg/i18nresource` 加载器契约、运行时 i18n/apidoc 调用点、动态插件 `build-wasm` 资源扫描 helper、`framework-i18n-foundation` 规范同步和本任务记录；确认默认运行时加载仍为非递归 locale 根目录直属 JSON，apidoc 加载仍限定 `apidoc` 子目录并递归扫描，动态插件 runtime/apidoc artifact section 未合并，未新增 REST API、数据访问、用户可见文案或缓存失效策略变更。复验通过：`openspec validate plugin-runtime-upgrade --strict`、`git diff --check`；全量 `openspec validate --all --strict` 仍受既有大量历史 spec 格式问题阻断，其中 `framework-i18n-foundation` 的阻断原因为原文件使用中文 `## 目的` / `## 需求` 标题而非 CLI 要求的 `## Purpose` / `## Requirements`，非本次内容同步引入。
- 2026-05-17: 完成反馈 `FB-17`。根因是 HTTP 启动流程在 `BootstrapAutoEnable` 将 `multi-tenant` 安装并启用后，立刻执行插件启动一致性校验；而 `multi-tenant` 的真实 `tenantcap` provider 在源码插件 HTTP 路由注册回调中注册，校验早于路由注册导致 SQLite 启动场景下报出 `multi-tenant plugin is enabled but tenantcap provider is not registered`。修复将 HTTP 启动拆为源码路由注册前后两个阶段：先完成集群启动、插件 auto-enable 和源码升级就绪扫描，再注册宿主与源码插件 HTTP 路由，使 provider 在一致性校验和 cron 启动前就绪；一致性校验通过后再启动插件 lifecycle 绑定、cron、动态前端预热和 runtime reconciler。新增 `TestHTTPStartupRegistersSourceRoutesBeforeConsistencyValidation` 锁定启动顺序，避免回归。验证通过：`cd apps/lina-core && go test ./internal/cmd -run 'TestHTTPStartupRegistersSourceRoutesBeforeConsistencyValidation|TestValidateHTTPStartupPluginConsistencyPanicsOnInvalidState' -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- apps/lina-core/internal/cmd/cmd_http.go apps/lina-core/internal/cmd/cmd_http_runtime.go apps/lina-core/internal/cmd/cmd_http_routes.go apps/lina-core/internal/cmd/cmd_test.go openspec/changes/plugin-runtime-upgrade/tasks.md`。i18n 影响：仅调整后端启动编排和测试，不新增或修改用户可见文案、运行时语言包、manifest i18n 或 apidoc 资源；缓存一致性影响：不新增缓存读写或失效策略，动态 runtime reconciler 仍在一致性校验通过后启动，SQLite 仍保持单节点模式；数据权限影响：不新增 REST API 或业务数据访问，multi-tenant provider 仍复用既有租户与数据权限边界；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-17` 的 `lina-review` 审查。审查范围覆盖 HTTP 启动阶段拆分、源码插件路由注册时序、启动一致性校验、cron/runtime 后台启动顺序、回归测试和 OpenSpec 任务记录；确认本轮未新增 Controller、API DTO、数据操作接口、用户可见文案、i18n 资源、缓存失效策略或开发脚本。启动绑定包已基于当前工作区通过 `go test ./internal/cmd -count=1` 编译门禁，`openspec validate plugin-runtime-upgrade --strict` 与全量 `git diff --check` 通过。未发现阻塞性实现缺陷。
- 2026-05-17: 完成反馈 `FB-18`。根因是 `demo-control` 全局中间件只放行 `POST /api/v1/auth/login` 和 `POST /api/v1/auth/logout`，多租户登录页点击“进入租户”会发起 `POST /api/v1/auth/select-tenant` 以消费一次性 `preToken`、签发租户 token、创建在线会话并预热权限上下文，因此被误判为业务写操作。修复将 demo-control 会话白名单扩展为登录、token 刷新、租户选择、租户切换和登出，仍不放行平台租户代操作、租户管理 CRUD、插件管理写操作等业务写入路径；同步更新中英文 README，并扩展 `TC0105` 覆盖 demo-control 启用时多租户登录选择、refresh 和 switch-tenant 不应被拦截。验证通过：`cd apps/lina-plugins && GOWORK=off go test lina-plugin-demo-control/backend lina-plugin-demo-control/backend/internal/service/middleware -count=1`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`pnpm -C hack/tests test:validate`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check -- hack/tests/e2e/extension/plugin/TC0105-demo-control-guard.ts openspec/changes/plugin-runtime-upgrade/tasks.md`、`cd apps/lina-plugins && git diff --check -- demo-control/README.md demo-control/README.zh-CN.md demo-control/backend/internal/service/middleware/middleware_guard.go demo-control/backend/internal/service/middleware/middleware_guard_test.go`。本地 `E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0105-demo-control-guard.ts --config playwright.config.ts` 在当前 8080 服务未启用 `demo-control` 的情况下 7 条用例全部按前置条件跳过；尝试用当前代码临时启动插件完整后端到 18080 时仍被 `multi-tenant plugin is enabled but tenantcap provider is not registered` 启动一致性问题阻断，因此本轮未能在本机完成真实 demo-control 启用态 E2E 执行。i18n 影响：不新增运行时 UI、manifest i18n、错误码或 apidoc 资源，仅更新中英文 README 和 E2E 断言文本；缓存一致性影响：仅调整请求白名单判断，不新增缓存读写、失效或跨实例协调路径；数据权限影响：不新增 REST 数据操作接口，租户选择和切换仍走既有认证、租户成员校验和 token/session 边界；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-18` 的 `lina-review` 审查。审查范围覆盖 `demo-control` 全局中间件请求分类、会话白名单、插件管理写操作拒绝单测、`TC0105` demo-control E2E 和中英文 README；审查中同步修正 `TC-105f` 旧断言，使其与全局只读策略一致地验证插件治理写操作被拒绝。确认本轮未新增 Controller、API DTO、数据库变更、运行时 i18n/apidoc 资源、缓存失效策略、数据操作接口或开发脚本。复验通过：`cd apps/lina-plugins && GOWORK=off go test lina-plugin-demo-control/backend lina-plugin-demo-control/backend/internal/service/middleware -count=1`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`pnpm -C hack/tests test:validate`、`openspec validate plugin-runtime-upgrade --strict`；`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0105-demo-control-guard.ts --config playwright.config.ts` 在当前 8080 服务未启用 `demo-control` 的情况下 7 条用例按前置条件跳过，真实 demo-control 启用态 E2E 仍受当前本地服务未启用插件和临时后端启动一致性阻断限制，未完成端到端实跑。
- 2026-05-17: 完成反馈 `FB-19`。根因是源码插件 `BeforeUninstall` 前置回调只收到插件 ID 和 operation，无法区分管理员选择的是保留插件数据还是清理插件存储和数据；multi-tenant 因此在选择清理数据卸载时仍按“存在租户需先删除”阻断。修复为源码插件通用 lifecycle input 增加 `PurgeStorageData()` 策略快照，宿主在源码和动态 `BeforeUninstall`/`AfterUninstall` 前置及通知链路中透传 `UninstallOptions.PurgeStorageData`；multi-tenant 在 `purgeStorageData=true` 时允许卸载流程继续执行卸载 SQL 清理租户表，在保留数据卸载时仍阻断，避免孤立租户数据。补充 `pluginhost` 契约测试、源码插件卸载前置回调策略测试、动态 lifecycle request 测试和 multi-tenant lifecycleprecondition 单元测试，并将 multi-tenant 前置测试改为 fake counter，避免依赖本地数据库残留。验证通过：`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/plugin -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend ./backend/internal/service/lifecycleprecondition -count=1`、临时 `go.work` 覆盖 `apps/lina-core` 和 `apps/lina-plugins/plugin-demo-source` 后运行 `go test lina-plugin-demo-source/backend -count=1`、`openspec validate plugin-runtime-upgrade --strict`、`git diff --check` 覆盖本轮代码、测试和 OpenSpec 文件。i18n 影响：不新增或修改用户可见文案、错误码、manifest i18n、运行时语言包或 apidoc 资源；缓存一致性影响：仅透传卸载请求策略并调整前置判断，不新增缓存读写、失效或跨实例协调路径，卸载成功后仍复用既有插件运行时缓存失效；数据权限影响：不新增 REST API 或业务数据访问面，multi-tenant 清理数据卸载继续由宿主插件治理权限和既有卸载 SQL 控制；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-19` 的 `lina-review` 审查。审查范围覆盖源码插件 lifecycle input 契约、源码/动态卸载前置和通知参数传递、multi-tenant 前置判断、相关单元测试以及 OpenSpec 规范/任务记录；确认 `BeforeUninstall` 与 `AfterUninstall` 的 `purgeStorageData` 语义在源码和动态路径一致，保留数据卸载仍会被 multi-tenant 租户存在检查阻断，清理数据卸载会继续执行既有卸载 SQL。确认本轮未新增 Controller、REST API、数据库迁移、用户可见文案、i18n/apidoc 资源、缓存失效策略、数据权限访问面或开发脚本；Go 编译门禁和 `openspec validate plugin-runtime-upgrade --strict` 已基于当前工作区通过。审查未发现阻塞性问题。
- 2026-05-17: 追踪反馈 `FB-19` 复报。补充 Controller 层单元测试锁定 `DELETE /plugins/{id}` 的 `purgeStorageData` 请求参数映射：未传和 `1` 均进入 `UninstallOptions.PurgeStorageData=true`，`0` 保留插件数据，`force` 不改变清理策略；同步清理 multi-tenant 注释中的旧 `lifecycle guards` 术语。复查当前源码和新构建产物均不再包含用户复报中的旧错误文案 `blocked by lifecycle guards`，只包含 `blocked by lifecycle preconditions`；本机 8080 后端进程 PID 96017 启动于 2026-05-15 23:10:05，`txt` 指向 `/Users/john/.Trash/temp/bin/lina`，说明仍在运行被替换前的旧二进制，需要重启后端进程加载当前 `temp/bin/lina`。补充验证通过：`cd apps/lina-core && go test ./internal/controller/plugin -count=1`、`cd apps/lina-core && go test ./pkg/pluginhost ./internal/service/plugin ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend ./backend/internal/service/lifecycleprecondition ./backend/internal/service/tenantplugin -count=1`、`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./apps/lina-core/internal/cmd -count=1`、`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./apps/lina-core/internal/controller/plugin ./apps/lina-core/internal/service/plugin -count=1`。i18n 影响：不新增或修改用户可见文案、错误码键、manifest i18n、运行时语言包或 apidoc 资源；缓存一致性影响：仅验证和重启加载当前二进制，不改变缓存策略；数据权限影响：不新增 REST 数据访问面；开发工具脚本影响：无新增或修改默认开发工具入口。
- 2026-05-17: 完成反馈 `FB-19` 复报的 `lina-review` 审查与本地服务重启。审查范围覆盖新增 Controller 参数映射测试、源码/动态 lifecycle `purgeStorageData` 传播、multi-tenant 前置放行逻辑、官方插件 `GOWORK` 编译路径、旧错误文案扫描和 OpenSpec 记录；确认没有新增业务数据接口、数据库迁移、运行时 i18n/apidoc 资源、缓存失效路径、数据权限访问面或开发工具脚本。已通过 `linactl dev plugins=1 skip_wasm=true` 重建并重启本地 8080 后端，当前 PID 56587 的 `txt` 指向工作区 `temp/bin/lina`，不再指向 Trash 中旧二进制；磁盘新二进制不包含 `blocked by lifecycle guards` 旧错误文案。复验通过：`cd apps/lina-core && go test ./internal/controller/plugin ./pkg/pluginhost ./internal/service/plugin ./internal/service/plugin/internal/runtime ./internal/cmd -count=1`、`cd apps/lina-plugins/multi-tenant && GOWORK=off go test ./backend ./backend/internal/service/lifecycleprecondition ./backend/internal/service/tenantplugin -count=1`、`openspec validate plugin-runtime-upgrade --strict`、本轮相关文件 `git diff --check`。未对当前数据库执行真实 multi-tenant 卸载，以避免破坏本地租户数据。
- 2026-05-17: 完成反馈 `FB-20`。根因是插件生命周期写操作失败时，同一个后端错误响应会先经过全局 request 错误拦截器弹出一次错误提示，卸载/升级等弹窗随后又在局部 `catch` 中解析并弹出同一错误；安装弹窗为了处理 mock-data 回滚也需要接管安装错误，但普通安装失败没有明确的局部错误所有权，导致 demo-control 只读错误提示重复或归属不稳定。修复为插件安装、卸载和升级提交请求增加 `silentErrorMessage` 调用选项，让有局部错误处理的生命周期弹窗跳过全局 toast；安装弹窗在非 mock-data 回滚失败时使用后端 `messageKey` / `message` 本地显示一条错误，卸载和升级继续复用各自的局部错误解析。新增宿主级插件框架 E2E `TC0238-plugin-demo-control-single-error-toast.ts`，通过 mock 403 demo-control 响应覆盖安装弹窗和卸载弹窗提交失败时同一只读错误只出现一次，并确认没有全局网络/服务器兜底错误叠加。验证通过：`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`pnpm -C hack/tests exec tsc --noEmit --pretty false`、`pnpm -C hack/tests test:validate`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/extension/plugin/TC0238-plugin-demo-control-single-error-toast.ts --config playwright.config.ts`、`openspec validate plugin-runtime-upgrade --strict`、本轮相关文件 `git diff --check`。i18n 影响：不新增或修改产品运行时文案、manifest i18n 或 apidoc i18n，E2E 仅复用现有 demo-control 错误文案。缓存一致性影响：仅调整前端错误提示归属，不新增缓存读写、失效、跨实例通知或最大陈旧时间边界。数据权限影响：不新增 REST API 或业务数据访问面，插件治理写操作仍复用既有权限和 demo-control 拦截边界。开发工具脚本影响：无新增或修改默认开发工具、脚本或跨平台入口。
- 2026-05-17: 完成反馈 `FB-20` 的 `lina-review` 审查。审查范围覆盖插件生命周期 API `silentErrorMessage` 选项、安装授权弹窗本地错误解析、卸载/升级弹窗错误提示归属、`PluginPage` toast 计数 helper、新增 `TC0238` E2E 和 OpenSpec 任务记录；确认本轮未新增后端 Go 生产代码、REST API、数据库迁移、产品文案、运行时 i18n/apidoc 资源、缓存失效策略、数据权限访问面或开发工具脚本。前端 typecheck、E2E TypeScript、E2E 治理校验、`TC0238` Playwright、OpenSpec 严格校验和空白检查已通过。审查未发现阻塞性问题。
