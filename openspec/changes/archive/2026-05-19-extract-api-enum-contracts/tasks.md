# 任务

- [x] 1. 新增 `pkg/listorder`、`pkg/tenantoverride`、`pkg/statusflag` 小型公共契约组件，并补齐组件注释和单元测试。
- [x] 2. 调整 `apps/lina-core/api` 中重复的列表排序方向、租户覆盖模式、菜单类型、插件桥接类型和通用状态标志引用，保持外部 API 契约不变。
- [x] 3. 更新受影响控制器的类型转换和常量引用，确保服务层仍接收原有领域语义。
- [x] 4. 运行公共组件测试、API/控制器编译烟测和 OpenSpec 校验，并记录 i18n、缓存、数据权限无影响结论。
- [x] 5. 调用 `lina-review` 对变更进行代码和规范审查。

## Feedback

- [x] **FB-1**: 删除 API 包中对公共契约类型的兼容别名和常量转发，改为 DTO 字段直接引用 `pkg` 公共类型。
