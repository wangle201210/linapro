## 1. 主题交互与偏好更新

- [x] 1.1 保留主题按钮圆形`View Transition`揭幕的双向方向与节奏，删除尾帧强制跳过，主题事务期间暂停页面内容过渡，并适配减少动态效果偏好
- [x] 1.2 验证并保持未变化偏好分支的引用稳定性，按主题补丁字段定向更新颜色、圆角和字体变量
- [x] 1.3 将主题与语言提示值同步持久化、完整偏好快照防抖持久化，并确保加载时提示值覆盖旧快照
- [x] 1.4 补充偏好分支引用稳定性、定向变量更新和立即刷新恢复的单元测试

## 2. 主题消费者优化

- [x] 2.1 将`ECharts`主题切换改为复用实例并调用`setTheme()`，维护完整原始配置基线并将稳定`id`的数据增量折叠进基线，通过一次延迟绘制的完整更新统一换肤，禁止补丁日志、清空、销毁、重建和额外调整尺寸；停用实例延迟到激活时更新
- [x] 2.2 拆分`VXE`主题与语言监听器，使主题变化只更新表格主题
- [x] 2.3 补充`ECharts`实例稳定性与`VXE`主题、语言监听职责的单元测试

## 3. 回归验证

- [x] 3.1 新增仪表盘`TC007`主题切换性能回归用例，通过页面对象验证双向圆形方向、更新/就绪/完成耗时、动画开始边界图表非空、图表实例标识稳定、三态画布非空和立即刷新主题保持
- [x] 3.2 运行相关格式化、类型检查和单元测试，并修复发现的问题
- [x] 3.3 运行`TC007`并审查亮色、暗色、切回亮色截图，确认图表与页面视觉可读
- [x] 3.4 运行`OpenSpec`严格校验和`lina-review`等价代码审查，修复全部有效发现
