## 1. Workflow 基线与命名

- [x] 1.1 记录当前 `.github/workflows/nightly-test-and-build.yml` 与 `.github/workflows/release-build.yml` 的 job、权限、触发条件和 artifact 差异
- [x] 1.2 将 release workflow 调整为 `.github/workflows/release-test-and-build.yml`，并将 workflow name 改为 `Release Test and Build`
- [x] 1.3 移除或替换旧 `.github/workflows/release-build.yml`，确保仓库中不存在职责重叠的 release 镜像发布 workflow

## 2. 发布前测试门禁

- [x] 2.1 在 release workflow 中保留 Windows 命令冒烟 job
- [x] 2.2 在 release workflow 中加入 Go 单元测试 job，并优先复用 `.github/workflows/reusable-backend-unit-tests.yml`
- [x] 2.3 在 release workflow 中加入前端单元测试 job，并优先复用 `.github/workflows/reusable-frontend-unit-tests.yml`
- [x] 2.4 在 release workflow 中加入完整 E2E job，覆盖宿主和官方插件测试范围
- [x] 2.5 在 release workflow 中加入 Redis cluster smoke job，覆盖集群协调基础能力
- [x] 2.6 为 release E2E 和 Redis smoke 上传独立命名的日志与测试报告 artifact，避免与 nightly artifact 混淆

## 3. 官方插件完整验证

- [x] 3.1 为 release workflow 的 checkout 配置递归 submodule 初始化，兼容官方插件工作区迁移为 submodule 的形态
- [x] 3.2 增加官方插件工作区 preflight，检查 `apps/lina-plugins` 存在且至少包含官方插件 `plugin.yaml`
- [x] 3.3 增加插件 E2E 范围 preflight，确认 `apps/lina-plugins/<plugin-id>/hack/tests/e2e/TC*.ts` 可被发现
- [x] 3.4 确保 preflight 失败时在镜像构建前快速失败，并输出 `git submodule update --init --recursive` 初始化提示
- [x] 3.5 确认 release 完整 E2E 使用 `pnpm test` 或等价 plugin-full 入口，而不是 host-only 入口

## 4. 镜像发布依赖与行为保持

- [x] 4.1 为 `release-image` 增加 `needs`，依赖 Windows 命令冒烟、Go 单测、前端单测、完整 E2E 和 Redis cluster smoke 全部成功
- [x] 4.2 保留 release tag Docker tag 合法性校验，确保非法 Git tag 不会进入镜像构建
- [x] 4.3 保留 `make image config=.github/image/config.yaml platforms=linux/amd64,linux/arm64 push=1` 多架构镜像构建与推送入口
- [x] 4.4 保留 release tag 和 `latest` 浮动标签发布，并在发布后执行远端 manifest inspect
- [x] 4.5 确认任一测试 job 失败、取消或超时时，GHCR login、镜像 push 和 `latest` 更新均不会执行

## 5. 验证与审查

- [x] 5.1 运行 workflow YAML 静态检查或等价解析验证，确认 release workflow 语法有效
- [x] 5.2 运行 `openspec validate release-test-and-build --strict`
- [x] 5.3 运行 `git diff --check -- .github/workflows openspec/changes/release-test-and-build`
- [x] 5.4 记录本变更不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑，因此无需新增 i18n、缓存一致性或数据权限实现变更
- [x] 5.5 调用 `lina-review` 完成代码和规范审查，并修正审查发现

## 审查记录

- [x] 2026-05-13: `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows/release-build.yml .github/workflows/release-test-and-build.yml .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-frontend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml openspec/changes/release-test-and-build`、`git diff`、未跟踪文件展开、`openspec status --change release-test-and-build --json`。确认 release workflow 已替换为 `Release Test and Build`，镜像发布 job 通过 `needs` 等待 Windows 命令冒烟、Go 单测、前端单测、完整 E2E 和 Redis cluster smoke；release checkout 使用递归 submodule；E2E 与镜像发布前包含官方插件工作区 preflight；完整 E2E 使用 `pnpm test` 覆盖宿主与插件范围；测试失败、取消或超时会阻止 GHCR 登录、镜像 push 和 `latest` 更新。`actionlint`、OpenSpec 严格校验、YAML 解析和空白检查均通过。本变更只修改 GitHub Actions 和 OpenSpec 文档，不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑，因此无 i18n、缓存一致性或数据权限实现变更。严重问题 0；警告 0。

## Feedback

- [x] **FB-1**: `hack/tools/image-builder/main.go` 单文件内容过大，CLI、配置归一化、平台解析、镜像构建、文件 staging 和命令执行职责集中，影响维护和审查；已拆分为同一 `package main` 下的入口编排、配置、参数、平台、环境输出、镜像、产物 staging 和命令执行源码文件，未新增可被外部 import 的包边界
- [x] **FB-2**: `.github/workflows` 中 nightly/release workflow 的 E2E、Redis smoke、host-only build smoke、镜像发布等 job 仍包含大量内联步骤和重复 preflight 逻辑，影响 workflow 编排层可读性；已抽取为独立 reusable workflow 和 composite action，并保持测试门禁和发布语义不变
- [x] **FB-3**: `.github/workflows/release-test-and-build.yml` 未覆盖 `.github/workflows/nightly-test-and-build.yml` 的完整测试验证面，尤其缺少 host-only Windows/Go/前端、host-only build smoke 和 host-only E2E 门禁；已补齐 release 的 host-only 与 plugin-full 验证矩阵，并确保 `release-image` 等待全部验证 job 成功后才能发布镜像
- [x] **FB-4**: `.github/workflows` 中 workflow、job 与 step 的关键配置缺少统一中英文说明，影响维护者快速理解触发条件、权限、复用边界、验证目标与 artifact 产物；已为所有 workflow 的关键配置、每个 job 和每个内联 step 补充中英文注释，并保持 workflow 语义不变
- [x] **FB-5**: `make image-build` 目标名称不符合当前 dotted command 命名风格；已改为 `make image.build`，并同步 `linactl image.build`、帮助注册表、自动化测试和中英文文档引用
- [x] **FB-6**: `.github/workflows/main-ci.yml` 未对重要的 `make plugins.*` 根目录指令做 CI 冒烟验证；已新增可复用 Linux `plugin-command-smoke` workflow，覆盖真实仓库 `plugins.status` submodule 诊断，以及隔离 fixture 中 `plugins.init`、`plugins.install`、`plugins.update` 和 `plugins.status` 的基本行为，主 CI 仅通过 `uses` 引用
- [x] **FB-7**: `.github/workflows/main-ci.yml` 还应显式覆盖 `make dev`、`make status`、`make stop`、`make init`、`make mock` 和 `make wasm` 常用根目录指令，确保这些入口在主 CI 中可用且基本行为符合预期；已新增可复用 Linux `make-command-smoke` workflow，并将主 CI 中 OpenSpec 完成门禁、插件指令冒烟、常用 make 指令冒烟、Redis integration 与 SQLite smoke 等复杂 job 拆为独立 reusable workflow，主 CI 仅通过 `uses` 编排引用
- [x] **FB-8**: `.github/workflows/reusable-backend-unit-tests.yml` 在 `apps/lina-core` 子目录调用不存在的 `prepare-packed-assets` Make 目标，导致 GitHub Actions 报错 `make: *** No rule to make target 'prepare-packed-assets'.  Stop.`
- [x] **FB-9**: 资源打包缺少统一的 `make pack.assets` 根目录入口；`Linux`、`macOS` 与 `Windows` 的 make 包装入口应使用一致的 `pack.assets` 指令名，且不暴露 `make prepare-packed-assets`
- [x] **FB-10**: `.github/workflows` 中直接调用 `linactl` 的步骤在存在对应根 `make` 或 Windows `make.cmd` 指令时应改用 make 入口，避免 CI 绕过公开命令包装层
- [x] **FB-11**: `TestOpenAPII18nBundlesCoverCurrentMetadata/zh-CN` 报告 OpenAPI apidoc 中文翻译资源缺少共享 DTO 与官方插件结构化 key 覆盖，导致后端单元测试失败
- [x] **FB-12**: `.github/workflows/main-ci.yml` 的 SQLite smoke 仍会通过 `apps/lina-core/Makefile` 执行旧 `linactl prepare-packed-assets` 命令，导致 `linactl: unknown command "prepare-packed-assets"; run linactl help`
- [x] **FB-13**: `.github/workflows/reusable-plugin-command-smoke.yml` 仍按旧 `remote=current` key-value 输出断言 `plugins.status`，但当前 `linactl` 已改为 ASCII 表格输出，导致 Main CI 的 `Plugin command smoke / Verify Isolated Plugin Management Commands` 失败
- [x] **FB-14**: GitHub Actions 中 `make dev` 构建 `plugin-demo-dynamic` 动态 WASM 插件时失败，报错 `go: module lina-plugins appears multiple times in workspace`；同时插件完整模式的 Go workspace 解析失败需要保留 Go 原始诊断，避免只输出 `linactl: list Go workspace modules: exit status 1`
- [x] **FB-15**: GitHub Actions `E2E tests (host-only)` 先用 `npm ci`/`npx playwright install` 安装 `package-lock.json` 中的 Playwright 浏览器，再用 `pnpm test:host` 按 `pnpm-lock.yaml` 运行新版 Playwright，导致浏览器 revision 缺失并报 `Executable doesn't exist`
- [x] **FB-16**: `.github/workflows/main-ci.yml` 与 `.github/workflows/nightly-test-and-build.yml` 重复编排 host-only、plugin-full、构建冒烟、E2E 与 Redis cluster 等验证 job，维护成本高；已抽取统一测试验证套件并由两个 workflow 通过参数复用，同时 Main CI 不启用 E2E
- [x] **FB-17**: `.github/workflows/reusable-test-verification-suite.yml` 的部分验证 job 仍依赖分组开关或默认启用，调用方无法逐项显式声明启停状态；已为每个 job 提供独立必填 input 开关，并让 Main CI 与 Nightly 调用处显式传入所有开关
- [x] **FB-18**: GitHub Actions `E2E tests (host-only)` 与 `E2E tests (plugin-full)` 仍以 4 个 worker 执行包含共享后端状态的浏览器测试并行池，导致角色、用户、文件与部分 i18n 页面在 `.vxe-table` 等页面加载等待点级联超时；已将共享状态密集的 IAM 角色、IAM 用户、文件管理、用户设置、默认工作台跳转和部分 runtime i18n 回归用例纳入 E2E 串行隔离清单，并让 reusable E2E workflow 支持显式 `parallel-workers` 输入，Nightly、Release 和共享验证套件均显式使用 `1`
- [x] **FB-19**: GitHub Actions `E2E tests (host-only)` 在 `TC0012d` 字典类型删除和 `TC-228a` 用户批量编辑中仍存在重试状态丢失、删除响应监听不稳和全局加载遮罩拦截点击的问题，导致 nightly host-only E2E 失败
- [x] **FB-20**: `.github/workflows/release-test-and-build.yml` 仍手工展开测试 job，未像 nightly 一样复用共享测试验证套件，且 release 应采用 Main CI 的简要测试范围，不运行完整 E2E
- [x] **FB-21**: host-only E2E `TC0063-auth-menu` 仍通过中文菜单名硬编码创建受限角色，未显式包含 `system:user:query` 等页面实际依赖的按钮权限，导致普通用户访问 `/system/user` 时连续收到 `403` 并卡住 loading，从而让 `TC0063b` 与 `TC0063d` 失败
- [x] **FB-22**: host-only E2E 登录后把宿主普通 `platform` 顶层目录误判为多租户能力，导致 `TC0226-user-account-labels` 与 `TC0228-user-batch-edit` 的用户新增抽屉和批量编辑弹窗调用缺失的 `/auth/login-tenants` 路由并长期保持 loading 遮罩，从而让 `E2E tests (host-only)` 继续失败
- [x] **FB-23**: Nightly `Go unit tests (host-only)` 在默认包级并发下让插件单测共用同一个 PostgreSQL schema 和动态插件运行期产物目录，`internal/service/plugin` 会观察到 `internal/service/plugin/internal/integration` 的临时动态插件状态并失败；已让 `linactl test.go` 使用 `go test -p=1` 串行执行包，同时保留 `-race` 和包内常规测试行为
- [x] **FB-24**: host-only E2E `TC0223d` 仍通过写入 `linapro:tenant-state` localStorage 模拟源码多租户启用态，226/228 修复后登录态刷新会按真实 `user/info` 菜单重新计算并覆盖为非租户态，导致角色数据权限默认值从“本租户数据”变为“全部数据”；应让测试通过真实租户菜单能力信号模拟源码多租户启用，避免依赖旧误判或不可持久的本地存储状态
- [x] **FB-25**: Nightly plugin-full E2E 中 `TC0222`、`TC0005a`、`TC0005c` 和 `TC0062d` 在全量长链路后接近或超过单测试 60 秒超时；应拆分过长 E2E 流程并减少非断言目标的重复 UI 导航/清理，降低 CI runner 资源抖动下的超时概率
- [x] **FB-26**: Playwright E2E 全局默认单测试超时时间仍为 60 秒，CI runner 资源抖动或全量长链路后容易把可恢复的慢用例误判为失败；应将全局默认 test timeout 调整为 180 秒
- [x] **FB-27**: GitHub Actions `Go unit tests` 中 `TestRunDevStartsServicesAsAsyncProcessesAndPrintsFinalStatus` 与 `TestRunDevPassesRepositoryWasmOutputWhenPluginsEnabled` 在测试 fixture 未提供 Vite binary 时真实执行 `pnpm install`，导致未安装 `pnpm` 的 Go 单测环境失败

## Feedback Verification

- [x] 2026-05-16: FB-27 验证通过：分析 GitHub Actions run `25959756060` 的 `Go unit tests` job `76312960226`，失败集中在 `hack/tools/linactl` 的两个 `runDev` 单测，错误为缺少 `pnpm`。根因是测试 fixture 只创建了前端 workdir，未创建 `ensureFrontendDeps` 用于判断前端依赖已安装的 Vite binary；测试的 `execCommand` 替身尚未接管前，`ensureFrontendDeps` 会尝试真实执行 `pnpm install`。修复后新增 `writeFrontendDependencySentinel` helper，在这两个 runDev 单测的临时仓库中创建 `viteCommand(root)` 哨兵文件，使测试继续聚焦异步进程启动、最终状态输出和 wasm 输出目录传参，不依赖真实 `pnpm`。验证命令：`cd hack/tools/linactl && go test . -run 'TestRunDevStartsServicesAsAsyncProcessesAndPrintsFinalStatus|TestRunDevPassesRepositoryWasmOutputWhenPluginsEnabled' -count=1`、`cd hack/tools/linactl && go test ./... -count=1`、`openspec validate release-test-and-build --strict`、`git diff --check -- hack/tools/linactl/main_test.go openspec/changes/release-test-and-build/tasks.md`。i18n 影响：仅调整 Go 单测 fixture 和 OpenSpec 记录，不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例一致性策略。数据权限影响：不修改生产 API、服务、数据库查询或角色数据权限边界。开发工具与脚本影响：未新增或修改默认开发工具入口、脚本或 `linactl runDev` 生产逻辑；只让现有跨平台 Go 工具单测在缺少 `pnpm` 的 Go-only CI 环境中自包含。
- [x] 2026-05-16: FB-27 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- hack/tools/linactl/main_test.go openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 `hack/tools/linactl` 的 Go 单测 fixture 和 OpenSpec 反馈记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑变更。目标 runDev 单测、linactl 全包单测、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-16: FB-26 验证通过：将 `hack/tests/playwright.config.ts` 的 Playwright 全局默认 `timeout` 从 `60000` 调整为 `180000`，只扩大单个 test 的最大执行窗口，`expect.timeout` 仍保持 `10000`，避免定位或断言失败被无意义拉长。验证命令：`npx pnpm@11.0.9 -C hack/tests test:validate`、`npx pnpm@11.0.9 -C hack/tests exec tsc --noEmit -p tsconfig.json`、`openspec validate release-test-and-build --strict`、`git diff --check -- hack/tests/playwright.config.ts openspec/changes/release-test-and-build/tasks.md`。i18n 影响：仅调整 E2E 测试运行配置和 OpenSpec 记录，不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例一致性策略。数据权限影响：不修改生产 API、服务或角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本，仅修改既有 Playwright 测试配置。
- [x] 2026-05-16: FB-26 `lina-review` 审查完成。审查范围来源：`git status --short`、`git -C apps/lina-plugins status --short`、`git ls-files --others --exclude-standard`、`git -C apps/lina-plugins ls-files --others --exclude-standard`、`git diff -- hack/tests/playwright.config.ts openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次新增改动只将 Playwright 全局默认 `test.timeout` 从 60 秒调整为 180 秒，未修改 `expect.timeout`、worker、retry、生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限实现。E2E 清单校验、TypeScript 编译、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-16: FB-25 验证通过：分析 GitHub Actions run `25948058162` 的 plugin-full E2E job `76280206787`，失败集中在 `TC0222-management-workbench-routes`、`TC0005-user-crud` 的创建/编辑子用例以及 `TC0062-user-role` 的删除子用例，日志均显示单测试 `Test timeout of 60000ms exceeded` 后页面/上下文被 Playwright 关闭。修复后将 `TC0222` 从单个串联 platform tenant、system user、tenant switch、impersonation 和 removed route fallback 的长测试拆为 6 个独立子用例；`TC0005` 删除重复整页跳转、将非断言目标的 finally 清理改为 API 删除；`TC0062d` 删除重复整页跳转并保留 API 兜底清理，降低 CI runner 资源抖动下的单测试超时概率。验证命令：`npx pnpm@11.0.9 -C hack/tests test:validate`、`npx pnpm@11.0.9 -C hack/tests exec tsc --noEmit -p tsconfig.json`、`git diff --check -- apps/lina-plugins/multi-tenant/hack/tests/e2e/platform-admin/TC0222-management-workbench-routes.ts hack/tests/e2e/iam/user/TC0005-user-crud.ts hack/tests/e2e/iam/user/TC0062-user-role.ts openspec/changes/release-test-and-build/tasks.md`、本地 plugin-full 服务 `make dev plugins=1` 后运行 `CI=1 E2E_RETRIES=1 E2E_WORKERS=1 E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:8080/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:8080 npx pnpm@11.0.9 -C hack/tests exec playwright test apps/lina-plugins/multi-tenant/hack/tests/e2e/platform-admin/TC0222-management-workbench-routes.ts --workers=1 --reporter=list`，结果 6 个测试全部通过，最长子用例 `TC-222f` 为 10.9 秒；随后运行同环境 `npx pnpm@11.0.9 -C hack/tests exec playwright test hack/tests/e2e/iam/user/TC0005-user-crud.ts hack/tests/e2e/iam/user/TC0062-user-role.ts --workers=1 --reporter=list`，结果 8 个测试全部通过，`TC0005a` 10.3 秒、`TC0005c` 13.3 秒、`TC0062d` 12.4 秒。i18n 影响：仅调整 E2E 测试组织和清理方式，不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例一致性策略。数据权限影响：不修改生产 API、服务或角色数据权限边界；API 清理仅用于测试数据回收。开发工具与脚本影响：未新增或修改默认开发工具、CI 辅助脚本或平台脚本。
- [x] 2026-05-16: FB-25 `lina-review` 审查完成。审查范围来源：`git status --short`、`git -C apps/lina-plugins status --short`、`git ls-files --others --exclude-standard`、`git -C apps/lina-plugins ls-files --others --exclude-standard`、`git diff -- hack/tests/e2e/iam/user/TC0005-user-crud.ts hack/tests/e2e/iam/user/TC0062-user-role.ts openspec/changes/release-test-and-build/tasks.md`、`git -C apps/lina-plugins diff -- multi-tenant/hack/tests/e2e/platform-admin/TC0222-management-workbench-routes.ts`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响宿主和 multi-tenant 插件 E2E 测试组织、测试数据清理路径和 OpenSpec 反馈记录；`TC0222` 拆分后的每个子用例均独立准备 mock workbench 状态，`TC0005/TC0062` 的 API 清理只按当前测试生成的精确用户名删除，未扩大生产数据操作面。没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限实现变更。E2E 清单校验、TypeScript 编译、OpenSpec 严格校验、空白检查和目标 plugin-full E2E 均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-24 验证通过：分析 GitHub Actions run `25922298253` 的 `E2E tests (host-only)` job `76194219509`，失败集中在 `TC0223-role-data-scope-dict-select.ts` 的 `TC-223d`，期望“本租户数据”但实际为“全部数据”。该用例原先只写入 `linapro:tenant-state.enabled=true`，而 FB-22/226/228 修复后登录态刷新会根据真实 `user/info.menus` 重新计算租户能力，host-only 菜单没有具体租户页面时会正确覆盖为非租户态。修复后 `TC0223d` 继续将动态插件状态列表 mock 为空，但通过拦截 `/api/v1/user/info` 并保留真实响应、追加 `/platform/tenants` 租户菜单信号来模拟源码多租户能力启用，避免依赖旧误判或不可持久的 localStorage 状态。验证命令：`npm exec --yes --package=pnpm@11.0.9 -- pnpm -C hack/tests test:validate`；临时 Docker PostgreSQL `linapro-e2e-pg-223` 监听 `127.0.0.1:55435`，使用临时 `GF_GCFG_PATH=/tmp/linapro-e2e-fb24-config` 执行 `make init confirm=init rebuild=true`、`make mock confirm=mock`、`make dev plugins=0`；随后运行 `E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:8080/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:8080 E2E_DB_PORT=55435 E2E_BROWSER_CHANNEL=chrome npm exec --yes --package=pnpm@11.0.9 -- pnpm -C hack/tests exec playwright test e2e/iam/role/TC0223-role-data-scope-dict-select.ts --project=chromium --workers=1`，目标 2 个测试全部通过；重新初始化同一 Docker 数据库后运行 host-only 非 media 全量 E2E，范围为当前 host-only manifest 选中的 83 个文件中跳过用户要求无需运行的 `e2e/settings/file/TC0048-file-management.ts`、`e2e/settings/file/TC0151-uploads-route-requires-auth.ts`、`e2e/settings/file/TC0171-file-data-permission.ts`，实际运行 80 个文件，非串行段 34 passed / 2 skipped，串行段 177 passed / 1 skipped，全量无失败，其中 `TC0223d`、`TC0226-user-account-labels`、`TC0228-user-batch-edit` 和 `TC0063-auth-menu` 均通过。i18n 影响：仅调整 E2E 测试准备逻辑，不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存、缓存失效或跨实例一致性策略；测试仍显式 mock 动态插件状态缓存。数据权限影响：不修改生产 API、服务或角色数据权限边界，只验证既有角色数据权限字典展示。开发工具与脚本影响：未新增或修改默认开发工具、CI 辅助脚本或平台脚本；本地 Docker PostgreSQL 仅用于模拟 CI 干净数据库。
- [x] 2026-05-15: FB-24 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- hack/tests/e2e/iam/role/TC0223-role-data-scope-dict-select.ts openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 `TC0223` 的 E2E 测试准备逻辑和 OpenSpec 反馈记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或数据权限逻辑变更。更新后的 `TC0223d` 不再通过 localStorage 强行维持租户态，而是在保持动态插件状态列表为空的前提下，通过真实 `/user/info` 响应追加具体租户菜单能力信号，测试意图与 226/228 后的登录态租户能力判定一致。E2E 结构校验、OpenSpec 严格校验、空白检查、Docker PostgreSQL 下目标 TC0223 单文件验证和 host-only 非 media 全量 E2E 均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-23 验证通过：分析 GitHub Actions run `25922298253` 的 `Go unit tests (host-only)` job `76194219390`，失败集中在 `lina-core/internal/service/plugin`，包括 `TestDynamicPluginUninstallFailureRestoresStableRegistryFlags` 初始安装被其他已安装插件依赖阻断，以及 `TestDynamicPluginFollowerDefersUntilPrimaryReconciles` 对临时动态插件注册表调和时报 `plugin does not exist`；同一 run 的 plugin-full Go 单测已通过，说明问题不是生产插件功能本身，而是 host-only 默认 `go test ./...` 在同一数据库上并发执行不同 package 测试时的共享 fixture 污染。修复后 `linactl test.go` 统一追加 `-p=1`，仅串行化 package 进程，仍保留 `race=true`、`verbose=true` 和包内普通 Go 测试语义。验证命令：`cd hack/tools/linactl && go test ./... -run TestRunTestGoSerializesPackageExecution -count=1`、`cd hack/tools/linactl && go test ./... -count=1`、临时干净 Docker PostgreSQL `127.0.0.1:55434` 初始化后运行 `LINA_TEST_PGSQL_LINK=pgsql:postgres:postgres@tcp(127.0.0.1:55434)/linapro?sslmode=disable make test.go plugins=0 race=true verbose=true`，输出确认入口为 `go test -p=1 -race -v ./...` 且全量 host-only Go 单测通过，其中 GitHub 失败的两个插件用例均通过、`openspec validate release-test-and-build --strict`、`git diff --check -- hack/tools/linactl/command_ops.go hack/tools/linactl/main_test.go openspec/changes/release-test-and-build/tasks.md`。i18n 影响：不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不新增或修改运行时缓存、失效或跨实例一致性策略；本次只收敛测试进程调度。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：修改既有跨平台 Go 工具 `linactl test.go` 的默认 `go test` 参数并新增单元测试覆盖，不新增平台专属脚本；本地 Docker PostgreSQL 仅用于模拟 CI 干净数据库。
- [x] 2026-05-15: FB-23 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- hack/tools/linactl/command_ops.go hack/tools/linactl/main_test.go openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响跨平台 Go 工具 `linactl test.go` 的包级测试调度、对应单元测试和 OpenSpec 反馈记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑变更。新增 `TestRunTestGoSerializesPackageExecution` 自建临时 workspace、stub 子进程并断言 `go test -p=1 -race -v ./...`，不依赖外部测试顺序或数据库残留。linactl 单测、OpenSpec 严格校验、空白检查和干净 Docker PostgreSQL 下的 host-only Go 全量单测均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-22 验证通过：在 GitHub Actions run `25911233842` 对应的 `main` 分支失败日志中确认 host-only `TC0226-user-account-labels` 与 `TC0228-user-batch-edit` 均卡在用户新增抽屉/批量编辑弹窗 busy 遮罩；本地用干净 Docker PostgreSQL host-only 环境复现时，登录后的 `linapro:tenant-state` 因宿主普通 `platform` 顶层目录被 `hasMultiTenantMenu` 误判而写入 `enabled=true`，用户页随后打开弹窗会调用 host-only 后端不存在的 `/api/v1/auth/login-tenants?userId=1`，后端返回路由重定向并最终让弹窗初始化 Promise 失败，loading 未关闭。修复后多租户能力检测只识别具体租户页面路径（如 `platform/tenants`、`tenant/*`），不再把宿主 `platform` 目录作为多租户信号；用户新增抽屉与批量编辑弹窗的初始化流程均使用 `finally` 收敛 loading，避免初始化异常时长期遮罩。验证命令：`git diff --check -- apps/lina-vben/apps/web-antd/src/store/auth.ts apps/lina-vben/apps/web-antd/src/views/system/user/user-drawer.vue apps/lina-vben/apps/web-antd/src/views/system/user/user-batch-edit-modal.vue openspec/changes/release-test-and-build/tasks.md`、`openspec validate release-test-and-build --strict`、`pnpm -C apps/lina-vben/apps/web-antd run typecheck`、`pnpm -C hack/tests test:validate`、临时干净库 `GF_GCFG_PATH=/tmp/linapro-fb22-config go run main.go init --confirm=init --sql-source=local`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:8080/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:8080 E2E_DB_PORT=55433 E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test e2e/settings/user/TC0226-user-account-labels.ts e2e/settings/user/TC0228-user-batch-edit.ts --project=chromium --workers=1`，目标 2 个 E2E 用例全部通过，登录 storage state 中 `tenant-state.enabled=false`。i18n 影响：不新增或修改前端运行时文案、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不新增或修改运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增或修改默认开发工具、CI 辅助脚本或平台脚本；临时 Docker PostgreSQL 仅用于本地验证。
- [x] 2026-05-15: FB-22 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- apps/lina-vben/apps/web-antd/src/store/auth.ts apps/lina-vben/apps/web-antd/src/views/system/user/user-drawer.vue apps/lina-vben/apps/web-antd/src/views/system/user/user-batch-edit-modal.vue openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响前端登录态多租户能力判定、用户新增抽屉/批量编辑弹窗 loading 收敛和 OpenSpec 反馈记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑变更。现有 `TC0226-user-account-labels` 和 `TC0228-user-batch-edit` 已直接覆盖本次用户可观察 bug 的复现和修复，干净 host-only Docker PostgreSQL 环境中目标用例全部通过。当前工作区另有 `apps/lina-plugins` submodule 状态改动，本次审查未覆盖、回退或重写该独立改动。严重问题 0；警告 0。
- [x] 2026-05-15: FB-21 验证通过：本地复跑 `E2E_BROWSER_CHANNEL=chrome E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:host` 时，host-only 串行阶段新增复现 `TC0063b` 与 `TC0063d` 失败；失败表象为 `waitForBusyIndicatorsToClear` 超时，但根因是测试角色仅按中文菜单名分配“权限管理/用户管理”，未显式纳入用户管理页面真实依赖的 `system:user:query` 权限，普通用户进入 `/system/user` 后 `/api/v1/user` 与 `/api/v1/user/dept-tree` 持续返回 `403`，页面 loading 无法结束。修复后 `TC0063-auth-menu` 改为通过 `getMenuIdsByPermsWithAncestors` 以权限点装配角色菜单：基础场景显式包含 `system:user:list` 与 `system:user:query`，扩展场景额外包含 `system:role:list` 与 `system:role:query`，避免菜单显示断言依赖脆弱的中文名称推断和遗漏按钮权限。验证命令：`pnpm -C hack/tests test:validate`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/iam/menu/TC0063-auth-menu.ts --project=chromium --workers=1`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/iam/menu/TC0060-menu-crud.ts hack/tests/e2e/iam/menu/TC0063-auth-menu.ts --project=chromium --workers=1`。i18n 影响：仅调整 E2E 测试准备逻辑，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不修改生产数据权限实现；修复只让测试角色显式携带当前页面实际依赖的查询权限，以匹配现有权限模型。开发工具与脚本影响：未新增默认开发工具或平台脚本；本地使用系统 Chrome channel 运行 Playwright 验证。
- [x] 2026-05-15: FB-21 OpenSpec completion gate 补充验证通过：GitHub Actions run `25922220747` 的 `OpenSpec changes complete` job 报告 `release-test-and-build` 仍为 `status=in-progress,tasks=90/91`；复核任务记录确认唯一未完成项为 FB-21，且本文件已存在上方 FB-21 复现、修复和 E2E 验证记录。GitHub Actions run `25911233842` 中 host-only 与 plugin-full 的 `TC0063-auth-menu` 子用例也均已通过。本次仅同步 OpenSpec 任务勾选状态，不修改运行时代码、E2E 用例或 CI 编排。验证命令：`openspec validate release-test-and-build --strict`、干净 worktree 中执行 CI 同款 `npx -y @fission-ai/openspec@1.3.1 list --json` completion gate 脚本并确认 `All 9 active OpenSpec change(s) are complete.`、`git diff --check -- openspec/changes/release-test-and-build/tasks.md openspec/changes/plugin-workspace-management/tasks.md`。不新增 i18n、缓存、数据权限、REST API 或开发工具/脚本变更。
- [x] 2026-05-15: FB-19 验证通过：分析 GitHub Actions 日志 `/Users/john/Downloads/job-logs.txt`，确认 host-only E2E 最终失败为 `TC0012d` 字典类型删除和 `TC-228a` 用户批量编辑，`TC0054e` 首次失败但 retry 通过。修复后 `TC0012` 改为每个子用例自建自清理，避免 Playwright retry 只重跑失败子用例时丢失前序创建/编辑状态；`DictPage.deleteType` 改为先定位目标行再打开确认框，并用 `Promise.all` 将确认点击与 `/dict/type/{id}` DELETE 响应等待绑定，避免错过请求；通用 busy 等待改为等待所有可见 loading/overlay 消失，并覆盖 Vben `.bg-overlay-content` 遮罩；`UserPage.batchUpdateSelectedStatus` 在批量编辑弹窗 loading 清空后点击开关，并等待 `/user` PUT 响应完成。验证命令：`cd hack/tests && pnpm test:validate`、`pnpm -C hack/tests exec tsc --noEmit`、`openspec validate release-test-and-build --strict`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/settings/dict/TC0012-dict-type-crud.ts --project=chromium --workers=1`、`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test hack/tests/e2e/settings/dict/TC0012-dict-type-crud.ts hack/tests/e2e/settings/dict/TC0054-dict-type-import-upload.ts hack/tests/e2e/settings/user/TC0228-user-batch-edit.ts --project=chromium --workers=1`，相关 13 个 E2E 子用例全部通过。i18n 影响：仅调整 E2E 测试与测试 helper，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本；本地因 Playwright 自带 Chromium 未安装且下载受阻，使用系统 Chrome channel 完成行为验证，CI 仍由 pnpm Playwright 安装链路提供浏览器。
- [x] 2026-05-15: FB-19 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- hack/tests/e2e/settings/dict/TC0012-dict-type-crud.ts hack/tests/pages/DictPage.ts hack/tests/pages/UserPage.ts hack/tests/support/ui.ts openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响宿主 Playwright E2E、共享 POM/helper 和 OpenSpec 任务记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑变更。`TC0012` 子用例已消除跨子用例状态依赖并具备自清理；字典删除 helper 的响应等待与确认点击顺序符合 Playwright 并发等待模式；用户批量编辑等待覆盖 Vben loading overlay，避免遮罩拦截交互。E2E 清单校验、TypeScript 编译、OpenSpec 严格校验、相关 13 个 E2E 子用例和空白检查均通过。当前工作区另有 release workflow 与 OpenSpec 设计/规范改动，本次审查未覆盖、回退或重写这些独立改动。严重问题 0；警告 0。
- [x] 2026-05-15: FB-20 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "workflow yaml ok"' .github/workflows/*.yml .github/actions/*/action.yml`、自定义 Ruby 检查确认 `.github/workflows/main-ci.yml`、`.github/workflows/nightly-test-and-build.yml` 与 `.github/workflows/release-test-and-build.yml` 的 `verification-suite.with` 均覆盖 `.github/workflows/reusable-test-verification-suite.yml` 的全部必填 inputs、自定义 Ruby 检查确认 release 的 `verification-suite` 与 Main CI 简要测试开关一致且 `include-host-only-e2e-tests=false`、`include-plugin-full-e2e-tests=false`、`release-image.needs=[release-tag-version-check, verification-suite]`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/release-test-and-build.yml .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/release-test-and-build.yml openspec/changes/release-test-and-build`。确认 release workflow 已删除手工展开的 host-only/plugin-full 测试 job，改为复用 `reusable-test-verification-suite.yml`；release 仍保留 tag 校验和 plugin-full 镜像发布，镜像发布等待 tag 校验与共享验证套件成功后执行。i18n 影响：仅调整 GitHub Actions 编排和 OpenSpec 文档，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本，仅复用既有 reusable workflow 和 GitHub Actions 编排能力。
- [x] 2026-05-15: FB-20 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/release-test-and-build.yml openspec/changes/release-test-and-build/proposal.md openspec/changes/release-test-and-build/design.md openspec/changes/release-test-and-build/specs/e2e-suite-organization/spec.md openspec/changes/release-test-and-build/specs/release-image-build/spec.md openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 release workflow 编排和 OpenSpec 文档；release workflow 通过共享测试套件复用 Main CI 简要测试开关，显式关闭 host-only/plugin-full E2E，`release-image` 只等待 tag 校验和 `verification-suite`，并继续通过 `plugins: "1"` 发布 plugin-full 镜像。当前工作区另有 FB-19 相关 `hack/tests` E2E 文件改动，本次审查未覆盖、回退或重写这些独立改动。本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑。YAML 解析、actionlint、共享套件输入覆盖检查、release 简要测试开关检查、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-18 验证通过：分析 GitHub Actions 日志 `/Users/john/Downloads/logs_69102194533/8_E2E tests (host-only) _ E2E tests.txt` 和 `/Users/john/Downloads/logs_69102194533/2_E2E tests (plugin-full) _ E2E tests.txt`，确认失败集中在 `E2E_PARALLEL_WORKERS=4` 的并行池，host-only 报告 `selected=83 parallel=46 serial=37 parallelWorkers=4` 后出现 37 个失败，plugin-full 报告 `selected=216 parallel=46 serial=170 parallelWorkers=4` 后出现 49 个失败，主要表现为角色、用户、文件和部分 i18n 页面等待 `.vxe-table` 超时。修复后运行：`node -e "JSON.parse(require('fs').readFileSync('hack/tests/config/execution-manifest.json','utf8')); console.log('execution manifest json ok')"`、`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "workflow yaml ok"' .github/workflows/*.yml .github/actions/*/action.yml`、`cd hack/tests && pnpm test:validate`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/*.yml`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/reusable-e2e-tests.yml .github/workflows/reusable-test-verification-suite.yml .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/release-test-and-build.yml hack/tests/config/execution-manifest.json openspec/changes/release-test-and-build/tasks.md`、自定义 Ruby 检查确认 `.github/workflows/main-ci.yml` 与 `.github/workflows/nightly-test-and-build.yml` 的 `verification-suite.with` 覆盖 `.github/workflows/reusable-test-verification-suite.yml` 的全部 18 个 workflow_call inputs、自定义 Node 检查确认修复后 host-only 为 `selected=83 parallel=19 serial=64`，plugin-full 为 `selected=216 parallel=19 serial=197`。i18n 影响：仅调整 E2E 执行清单和 CI worker 参数，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不修改运行时缓存实现；仅把 runtime i18n cache 相关测试归入串行隔离，降低测试间共享缓存状态竞争。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界实现。开发工具与脚本影响：修改既有 Node E2E 清单和 GitHub Actions workflow 输入，不新增默认开发脚本或平台专属命令；本地治理验证覆盖 JSON/YAML/actionlint/E2E 清单。
- [x] 2026-05-15: FB-18 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/reusable-e2e-tests.yml .github/workflows/reusable-test-verification-suite.yml .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/release-test-and-build.yml hack/tests/config/execution-manifest.json openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 GitHub Actions E2E worker 输入、E2E 执行清单和 OpenSpec 任务记录；没有未跟踪文件，没有 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存实现或数据权限逻辑变更。`reusable-e2e-tests.yml` 默认 `parallel-workers=1`，Nightly、Release 和共享验证套件调用方均显式传入 `1`；失败日志中暴露的 IAM role/user、settings file/user、默认工作台跳转和部分 runtime i18n 用例已声明串行隔离原因。JSON/YAML 解析、E2E 清单治理、actionlint、OpenSpec 严格校验、workflow input 覆盖检查和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-17 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "all workflow yaml ok"' .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/*.yml`、自定义 Ruby 输入覆盖检查确认 `.github/workflows/main-ci.yml` 与 `.github/workflows/nightly-test-and-build.yml` 的 `verification-suite.with` 与 `.github/workflows/reusable-test-verification-suite.yml` 中所有 `required: true` inputs 完全一致且无缺失/多余项、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml openspec/changes/release-test-and-build/tasks.md`。确认共享验证套件为每个 job 暴露独立必填开关：OpenSpec 完成门禁、host-only Windows/Go/前端、plugin-full Windows/Go/前端、plugin command smoke、make command smoke、Redis integration、SQLite smoke、host-only build smoke、host-only E2E、plugin-full E2E 与 Redis cluster smoke；Main CI 和 Nightly 均显式传入所有开关值，不依赖默认启停。i18n 影响：仅调整 GitHub Actions 编排和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本，仅调整 GitHub Actions workflow input 契约。
- [x] 2026-05-15: FB-17 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 GitHub Actions 编排契约和 OpenSpec 任务记录；共享验证套件的每个 job 均由独立 `include-*` input 控制，且所有 job 开关均为 `required: true`，调用方无法通过默认值隐式启停；Main CI 与 Nightly 均显式列出所有开关。Main CI 仍关闭 host-only/plugin-full E2E，Nightly 仍开启 E2E 和 Redis cluster smoke，并保持镜像发布等待 `verification-suite` 成功。本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑。YAML 解析、actionlint、输入覆盖检查、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-16 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }; puts "all workflow yaml ok"' .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/*.yml`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml openspec/changes/release-test-and-build/tasks.md`、`wc -l .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml`。确认新增 `.github/workflows/reusable-test-verification-suite.yml` 统一编排 host-only、plugin-full、构建冒烟、可选 E2E、Redis cluster 以及主 CI 专属 OpenSpec/Linux 命令/Redis integration/SQLite smoke 门禁；`main-ci.yml` 通过参数禁用 E2E 并启用主 CI 专属门禁，`nightly-test-and-build.yml` 通过参数启用 E2E 并保持 nightly 镜像发布等待验证套件成功。i18n 影响：仅调整 GitHub Actions 编排和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本，仅复用既有 reusable workflow 和 GitHub Actions 编排能力。
- [x] 2026-05-15: FB-16 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/main-ci.yml .github/workflows/nightly-test-and-build.yml .github/workflows/reusable-test-verification-suite.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动只影响 GitHub Actions 编排和 OpenSpec 任务记录；新增共享验证套件没有复制底层测试步骤，而是继续调用既有细粒度 reusable workflow；Main CI 保持 E2E 关闭并补齐 nightly 的非 E2E plugin-full、host-only build smoke 与 Redis cluster smoke 覆盖，Nightly 保持 E2E 和镜像发布语义，镜像发布通过 `needs: verification-suite` 等待套件整体成功。本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑。YAML 解析、actionlint、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-13 验证通过：`cd hack/tools/linactl && go test ./... -run TestPluginsInstallUpdateAndStatusUseConfiguredSources -count=1`、`cd hack/tools/linactl && go test ./... -count=1`、`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/reusable-plugin-command-smoke.yml"); puts "yaml ok"'`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/reusable-plugin-command-smoke.yml`、从 workflow YAML 解析并执行 `Verify Isolated Plugin Management Commands` 的完整 `RUNNER_TEMP` 隔离脚本、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/reusable-plugin-command-smoke.yml hack/tools/linactl/command_plugin_management_test.go openspec/changes/release-test-and-build/tasks.md`。确认 CI 断言已匹配当前 `plugins.status` ASCII 表格行，不再查找旧 `remote=current` key-value 文本；新增 `linactl` 测试覆盖 `p=<plugin-id>` 过滤 status 输出并防止旧格式断言回归。i18n 影响：仅调整 GitHub Actions 断言、开发工具测试和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本，仍复用既有 `make`/`linactl` 入口。
- [x] 2026-05-15: FB-13 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/reusable-plugin-command-smoke.yml hack/tools/linactl/command_plugin_management_test.go openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认 workflow 断言已与 `plugins.status` 表格输出对齐，新增测试复用同一自包含临时 Git 仓库场景并覆盖过滤 status 输出；本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑。严重问题 0；警告 0。
- [x] 2026-05-15: FB-14 验证通过：`cd hack/tools/linactl && go test . -run 'TestPrepareOfficialPluginWorkspace|TestGoWorkspaceModules' -count=1 -v`、`cd hack/tools/linactl && go test ./... -count=1`、`go test ./apps/lina-core/internal/service/plugin/internal/testutil -count=1`、`GOWORK=$PWD/temp/go.work.plugins GOFLAGS='-tags=official_plugins' go list -m -f '{{.Dir}}'`、`cd apps/lina-core && GOWORK=$PWD/../../temp/go.work.plugins GOFLAGS='-tags=official_plugins' go list ./...`、`cd apps/lina-core && GOWORK=$PWD/../../temp/go.work.plugins GOFLAGS='-tags=official_plugins' go test ./internal/service/plugin/internal/testutil -count=1`、`make test.go plugins=1 race=false verbose=false`、`openspec validate release-test-and-build --strict`、`git diff --check -- apps/lina-core/internal/service/plugin/internal/testutil hack/tools/linactl openspec/changes/release-test-and-build/tasks.md`。确认 `linactl test.go` 的 workspace 模块枚举失败会保留 `go list` stderr 诊断；官方插件根目录自身提供 `module lina-plugins` 时复用该根模块并清理 stale fallback，缺少根聚合模块时生成 `temp/official-plugins` fallback；插件测试辅助生成 `temp/go.work.plugins` 时同样包含 `lina-plugins` 聚合模块，避免动态插件测试覆盖出缺失聚合模块的 workspace。i18n 影响：仅调整 Go 开发工具和后端测试辅助，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：修改既有跨平台 Go 工具 `linactl` 和 Go 测试辅助，未新增平台专属脚本。
- [x] 2026-05-15: FB-14 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- apps/lina-core/internal/service/plugin/internal/testutil/testutil_build.go apps/lina-core/internal/service/plugin/internal/testutil/testutil_build_test.go hack/tools/linactl/command_ops.go hack/tools/linactl/command_plugin_gowork.go hack/tools/linactl/main_test.go openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认变更只影响跨平台 Go 工具 `linactl`、后端测试辅助和 OpenSpec 任务记录；未新增业务 API、数据库 schema、前端运行时文案、运行时缓存控制或数据权限逻辑。临时 plugin-full workspace 现在对真实官方插件根 `module lina-plugins` 复用根模块，对缺少根聚合模块的 fixture 生成 fallback 模块，避免重复或缺失 `lina-plugins`；`go list` 失败会保留 Go 原始 stderr 诊断以便 CI 定位。新增和修改测试均自包含，使用临时目录构造 workspace 场景；Go 编译门禁、完整 plugin-full `make test.go`、OpenSpec 严格校验和空白检查均通过。严重问题 0；警告 0。
- [x] 2026-05-15: FB-15 验证通过：`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/reusable-e2e-tests.yml"); puts "yaml ok"'`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest -no-color .github/workflows/reusable-e2e-tests.yml`、`CI=true pnpm --dir hack/tests install --frozen-lockfile`、`pnpm --dir hack/tests exec playwright --version`、`pnpm --dir hack/tests exec playwright install --dry-run chromium`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/reusable-e2e-tests.yml openspec/changes/release-test-and-build/tasks.md`。确认 E2E workflow 的测试依赖和浏览器安装均改用 `hack/tests/pnpm-lock.yaml` 对应的 pnpm 链路，`pnpm exec playwright` 解析到 Playwright `1.59.1`，dry-run 显示会安装 `chromium_headless_shell-1217`，与失败日志中缺失的 revision 一致。i18n 影响：仅调整 GitHub Actions 安装命令和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或平台脚本；CI 仍通过既有 pnpm/Playwright 工具链执行。
- [x] 2026-05-15: FB-15 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/reusable-e2e-tests.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际修复只影响可复用 E2E workflow 和 OpenSpec 任务记录；`Install E2E Dependencies` 与 `Install Playwright Browsers` 已统一使用 pnpm 链路，避免 `package-lock.json` 与 `pnpm-lock.yaml` 的 Playwright revision 不一致。YAML 解析、actionlint、OpenSpec 严格校验、pnpm 锁文件安装验证、Playwright dry-run 和空白检查均通过。本次不新增或修改 Go 生产代码、业务 API、数据库 schema、前端运行时文案、manifest i18n、apidoc i18n、运行时缓存或数据权限逻辑。当前工作区仍存在 FB-14 相关 Go/linactl 改动，本次审查未回退或重写这些独立改动。严重问题 0；警告 0。

- [x] 2026-05-14: FB-1 验证通过：`cd hack/tools/image-builder && go test ./... -count=1`、`go run ./hack/tools/image-builder --preflight --tag=test-preflight`、`go run ./hack/tools/image-builder --print-build-env --tag=test-preflight`。确认 `main.go` 从 745 行降至约 100 行，最大拆分文件约 265 行，工具仍通过同一 `go run ./hack/tools/image-builder` 入口使用。i18n 影响：仅拆分开发工具源码文件，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。E2E 影响：无前端可见行为变化，使用工具单元测试、preflight 和 build env 输出验证覆盖。
- [x] 2026-05-14: FB-1 `lina-review` 审查完成。审查范围来源：`git status --short -- hack/tools/image-builder openspec/changes/release-test-and-build/tasks.md`、`git ls-files --others --exclude-standard hack/tools/image-builder`、`git diff -- hack/tools/image-builder/main.go openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认拆分仅改变 `image-builder` 源码组织方式，`main.go` 保留入口和流程编排，其余职责保持同一 `package main` 下的不可 import 文件；未新增后端运行期依赖、HTTP/API、数据库 schema、前端运行时文案、缓存控制或数据权限逻辑。新增源码文件具备文件职责注释，函数/类型注释完整，未发现直接 `g.Log()`、隐式关键服务构造、未处理错误或用户可见接口错误风险。严重问题 0；警告 0。
- [x] 2026-05-14: FB-2 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/*.yml`、新增 composite action `run` 脚本 `bash -n` 语法检查、`git diff --check -- .github/workflows .github/actions openspec/changes/release-test-and-build/tasks.md`、`openspec validate release-test-and-build --strict`。确认 `.github/workflows/nightly-test-and-build.yml` 从 655 行收敛为 111 行，`.github/workflows/release-test-and-build.yml` 从 359 行收敛为 80 行；E2E、Redis cluster smoke、host-only build smoke、镜像发布和官方插件 workspace preflight 已迁移到命名明确的 reusable workflow/composite action。i18n 影响：仅调整 GitHub Actions 组织结构，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。E2E 影响：不改变测试用例内容或门禁范围，使用 workflow 静态解析、actionlint、OpenSpec 严格校验和空白检查覆盖。
- [x] 2026-05-14: FB-2 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard .github/workflows .github/actions openspec/changes/release-test-and-build`、`git diff -- .github/workflows/nightly-test-and-build.yml .github/workflows/release-test-and-build.yml .github/workflows/reusable-windows-command-smoke.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`，并显式纳入新增未跟踪文件 `.github/actions/host-only-artifact-smoke/action.yml`、`.github/actions/verify-official-plugin-workspace/action.yml`、`.github/workflows/reusable-e2e-tests.yml`、`.github/workflows/reusable-host-only-build-smoke.yml`、`.github/workflows/reusable-image-publish.yml`、`.github/workflows/reusable-redis-cluster-smoke.yml`。确认 nightly/release 顶层 workflow 仅保留触发、权限、并行 job 编排、`needs` 门禁和 artifact/tag 参数；发布镜像仍依赖 Windows 命令冒烟、Go 单测、前端单测、完整 E2E 与 Redis cluster smoke 全部成功；plugin-full E2E/image 仍强制递归 submodule 与官方插件 workspace preflight。新增 composite action 已标注仅用于 Linux GitHub Actions job，不作为跨平台默认开发工具入口；本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑。严重问题 0；警告 0。
- [x] 2026-05-14: FB-3 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/nightly-test-and-build.yml .github/workflows/release-test-and-build.yml .github/workflows/reusable-e2e-tests.yml .github/workflows/reusable-host-only-build-smoke.yml .github/workflows/reusable-image-publish.yml .github/workflows/reusable-redis-cluster-smoke.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-frontend-unit-tests.yml`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows/release-test-and-build.yml openspec/changes/release-test-and-build`，并用 YAML 静态矩阵检查确认 release 覆盖 nightly 的 10 个验证 job 且 `release-image.needs` 等待全部验证 job。i18n 影响：仅调整 GitHub Actions 编排和 OpenSpec 文档，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。E2E 影响：不改变测试用例内容，release 现在同时运行 host-only E2E 与 plugin-full E2E。
- [x] 2026-05-14: FB-3 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows/release-test-and-build.yml openspec/changes/release-test-and-build`、`git diff -- .github/workflows/release-test-and-build.yml openspec/changes/release-test-and-build/specs/release-image-build/spec.md openspec/changes/release-test-and-build/specs/e2e-suite-organization/spec.md openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认 release workflow 的验证 job 与 nightly 对齐：host-only Windows 命令冒烟、Go 单测、前端单测、host-only build smoke、host-only E2E、plugin-full Windows 命令冒烟、Go 单测、前端单测、plugin-full E2E 和 Redis cluster smoke；`release-image` 通过 `needs` 等待全部 10 个验证 job。严重问题 0；警告 0。
- [x] 2026-05-14: FB-4 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/*.yml`、注释覆盖扫描确认 13 个 workflow 文件均有文件级说明、每个 job 和每个内联 step 前均有注释、`rg -n "\bEN:|\bZH:" .github/workflows || true` 确认未保留语言前缀、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows openspec/changes/release-test-and-build/tasks.md`。本次仅新增 GitHub Actions YAML 注释，并修正当前工作区 `monthly-openspec-archive.yml` 对已存在 `monthly-openspec-archive-{codex,cc}.yml` 的 reusable workflow 引用以通过 actionlint；不改变测试命令、服务、权限、镜像发布逻辑或 E2E 用例内容。i18n 影响：仅维护 CI 注释和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增默认开发工具或脚本入口；workflow 仍使用既有 CI 命令，使用 YAML 解析、actionlint、注释扫描、空白检查和 OpenSpec 严格校验覆盖。
- [x] 2026-05-14: FB-4 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows openspec/changes/release-test-and-build/tasks.md`、`git ls-files --others --exclude-standard .github/workflows`、`git diff --name-status -- .github/workflows openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认当前工作区 13 个 workflow 文件均已补充文件级、job 级和内联 step 级中英文注释；注释未使用 `EN:`/`ZH:` 前缀；YAML 解析、actionlint、OpenSpec 严格校验、空白检查和注释覆盖扫描均通过。当前工作区存在既有 `.github/workflows/nightly-openspec-archive*.yml` 删除与 `.github/workflows/monthly-openspec-archive*.yml` 未跟踪重命名状态，本次审查按当前磁盘 workflow 集合验证，并确认 monthly 路由引用当前存在的 monthly reusable workflow。严重问题 0；警告 0。
- [x] 2026-05-14: FB-5 验证通过：`cd hack/tools/linactl && go test ./... -count=1`、`cd hack/tools/linactl && go run . help`、`make -n image.build`、`openspec validate release-test-and-build --strict`、`rg -n "(make|linactl) image-build|## image-build|\\.PHONY: image-build|^image-build:" README.md README.zh-CN.md AGENTS.md hack/makefiles hack/tools/linactl Makefile make.cmd`、`git diff --check -- hack/makefiles/image.mk hack/tools/linactl/command.go hack/tools/linactl/main_test.go README.md README.zh-CN.md AGENTS.md openspec/changes/release-test-and-build/tasks.md`。确认 `make image.build` 转发到 `linactl image.build`，`linactl help` 展示 `image.build`，旧 `make image-build`/`linactl image-build` 入口未在当前入口与文档中残留。i18n 影响：仅调整开发命令名和文档引用，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。E2E 影响：无前端可见页面行为变化，使用 `linactl` 单元测试和命令 dry-run 覆盖。
- [x] 2026-05-14: FB-5 `lina-review` 审查完成。审查范围来源：`git status --short -- hack/makefiles/image.mk hack/tools/linactl/command.go hack/tools/linactl/main_test.go README.md README.zh-CN.md AGENTS.md openspec/changes/release-test-and-build/tasks.md`、`git ls-files --others --exclude-standard`、`git diff -- hack/makefiles/image.mk hack/tools/linactl/command.go hack/tools/linactl/main_test.go README.md README.zh-CN.md AGENTS.md openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际改动将 Make 目标、`linactl` 注册表与帮助用法统一为 `image.build`，新增注册表测试防止 `image-build` 回归；未新增后端运行期依赖、HTTP/API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑。当前审查文件中存在前序工作区改动，本次审查仅覆盖命令改名相关行，并未回退或重写其他改动。严重问题 0；警告 0。
- [x] 2026-05-14: FB-6 验证通过：`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/main-ci.yml"); puts "ok"'`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/main-ci.yml`、本地模拟 GitHub Actions `RUNNER_TEMP` 后运行 `make help`、`make plugins.status`、隔离临时仓库中的 `make -C "$smoke_root" plugins.init`、`make -C "$smoke_root" plugins.install p=demo-ci`、`make -C "$smoke_root" plugins.update p=demo-ci`、`make -C "$smoke_root" plugins.status p=demo-ci`。确认 `main-ci.yml` 新增 `plugin-command-smoke` job，真实仓库只运行只读 `plugins.status`，写入型插件命令均在临时 fixture 中使用本地插件源验证，不污染真实 `apps/lina-plugins`。i18n 影响：仅调整 CI 编排和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增仓库默认开发工具或平台脚本，GitHub Actions Linux job 使用既有 `make`/`linactl` 入口并通过 YAML 解析、actionlint、OpenSpec 严格校验和命令 smoke 覆盖。
- [x] 2026-05-14: FB-6 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows/main-ci.yml openspec/changes/release-test-and-build/tasks.md`、`git diff -- .github/workflows/main-ci.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认 `plugin-command-smoke` job 通过根 `make` 入口验证 `plugins.*` 指令；真实仓库仅执行只读 `make plugins.status` 并覆盖 submodule 诊断；`plugins.init/install/update/status` 的写入行为在 `$RUNNER_TEMP` 隔离仓库中使用本地插件源验证，不会改写真实 `apps/lina-plugins`。新增 shell 仅存在于 GitHub Actions Linux job，不作为默认跨平台开发工具入口；本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑。严重问题 0；警告 0。
- [x] 2026-05-14: FB-7 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/main-ci.yml .github/workflows/reusable-plugin-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml .github/workflows/reusable-openspec-changes-complete.yml .github/workflows/reusable-redis-integration-tests.yml .github/workflows/reusable-sqlite-smoke.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/main-ci.yml .github/workflows/reusable-plugin-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml .github/workflows/reusable-openspec-changes-complete.yml .github/workflows/reusable-redis-integration-tests.yml .github/workflows/reusable-sqlite-smoke.yml`、本地模拟 GitHub Actions `RUNNER_TEMP` 后运行插件指令 fixture、`make -C "$smoke_root" dev plugins=0 BACKEND_PORT=18080 FRONTEND_PORT=15666`、`make -C "$smoke_root" status BACKEND_PORT=18080 FRONTEND_PORT=15666`、`make -C "$smoke_root" stop BACKEND_PORT=18080 FRONTEND_PORT=15666`、隔离动态插件 fixture 中真实执行 `make -C "$smoke_root" wasm p=demo-dynamic` 并断言 `temp/output/demo-dynamic.wasm` 产物存在。确认 `.github/workflows/main-ci.yml` 仅保留 reusable workflow `uses` 编排，不再内联 OpenSpec、插件指令、常用 make 指令、Redis integration 或 SQLite smoke 的 `steps`/`services`；`make init` 与 `make mock` 在 `reusable-make-command-smoke.yml` 中通过 PostgreSQL service container 执行，未在本机直接改写本地数据库。i18n 影响：仅调整 GitHub Actions 编排和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增仓库默认开发工具或平台脚本，GitHub Actions Linux job 使用既有 `make`/`linactl` 入口；复杂 job 已拆为 reusable workflow 供主 CI 引用。
- [x] 2026-05-14: FB-7 `lina-review` 审查完成。审查范围来源：`git status --short -- .github/workflows/main-ci.yml .github/workflows/reusable-plugin-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml .github/workflows/reusable-openspec-changes-complete.yml .github/workflows/reusable-redis-integration-tests.yml .github/workflows/reusable-sqlite-smoke.yml openspec/changes/release-test-and-build/tasks.md`、`git ls-files --others --exclude-standard -- .github/workflows/reusable-plugin-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml .github/workflows/reusable-openspec-changes-complete.yml .github/workflows/reusable-redis-integration-tests.yml .github/workflows/reusable-sqlite-smoke.yml`、`git diff`、`openspec status --change release-test-and-build --json`。确认 `main-ci.yml` 只保留 reusable workflow 编排引用，复杂 job 已拆入独立 YAML；`reusable-plugin-command-smoke.yml` 覆盖 `plugins.*`，`reusable-make-command-smoke.yml` 覆盖 `init/mock/dev/status/stop/wasm`，`reusable-openspec-changes-complete.yml`、`reusable-redis-integration-tests.yml` 和 `reusable-sqlite-smoke.yml` 承接原有 OpenSpec/Redis/SQLite 逻辑且命令语义不变。YAML 解析、actionlint、OpenSpec 严格校验和空白检查均通过。本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑；新增 shell 仅存在于 GitHub Actions Linux workflow，不作为默认跨平台开发工具入口。严重问题 0；警告 0。
- [x] 2026-05-14: FB-8 验证通过：`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/reusable-backend-unit-tests.yml"); puts "yaml ok"'`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/reusable-backend-unit-tests.yml`、`go run ./hack/tools/linactl prepare-packed-assets`、`openspec validate release-test-and-build --strict`。确认 backend unit test workflow 改为从仓库根目录调用跨平台 `linactl prepare-packed-assets` 入口，不再在 `apps/lina-core` 子目录调用不存在的 `prepare-packed-assets` Make 目标。i18n 影响：仅调整 GitHub Actions 命令和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增仓库默认开发工具或平台脚本，复用既有 Go 工具链入口并通过实际命令 smoke 覆盖。
- [x] 2026-05-14: FB-8 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/reusable-backend-unit-tests.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际修复仅将 backend unit test workflow 的资源准备步骤切换到仓库根目录的 `go run ./hack/tools/linactl prepare-packed-assets`，与 Windows command smoke 的既有入口保持一致；仓库中不再存在 `.github/workflows/reusable-backend-unit-tests.yml` 调用 `make -C apps/lina-core prepare-packed-assets` 的路径。当前工作区还存在 `.github/workflows/main-ci.yml`、`AGENTS.md`、`CONTRIBUTING.md` 和未跟踪 `CONTRIBUTING.zh-CN.md` 等前序改动，本次审查未回退或重写这些无关变更。本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑。严重问题 0；警告 0。
- [x] 2026-05-14: FB-9 验证通过：`make pack.assets`、`cd hack/tools/linactl && go test ./... -count=1`、`make help`、`make -n pack.assets`、`make -n prepare-packed-assets`（预期失败）、`go run ./hack/tools/linactl prepare-packed-assets`（预期失败）、`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml`、`go run ./hack/tools/linactl test.scripts`、`openspec validate release-test-and-build --strict`、`git diff --check -- hack/makefiles/build.mk hack/tools/linactl .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml openspec/changes/release-test-and-build/tasks.md`。确认 `make pack.assets` 转发到 `linactl pack.assets` 并成功生成 `apps/lina-core/internal/packed/manifest`；`make help` 展示 `pack.assets`；`make -qp` 中只存在 `pack.assets`，不存在 `prepare-packed-assets` Make 目标；Windows workflow 同步验证 `make.cmd pack.assets` 与 `.\make.cmd pack.assets`，Linux workflow 同步使用 `make pack.assets`。i18n 影响：仅调整开发命令名、CI 命令和工具文档，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：新增根 Make 兼容包装目标，实际编排仍复用跨平台 Go 工具 `linactl`。
- [x] 2026-05-14: FB-9 `lina-review` 审查完成。审查范围来源：`git status --short -- hack/makefiles/build.mk hack/tools/linactl/README.md hack/tools/linactl/README.zh-CN.md hack/tools/linactl/command.go hack/tools/linactl/main_test.go .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml openspec/changes/release-test-and-build/tasks.md`、`git diff --name-status -- hack/makefiles/build.mk hack/tools/linactl/README.md hack/tools/linactl/README.zh-CN.md hack/tools/linactl/command.go hack/tools/linactl/main_test.go .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认根 Make 入口、Windows `make.cmd` 包装入口、`linactl` 注册表、CI 和中英文工具文档均统一使用 `pack.assets`；`prepare-packed-assets` 不再作为 `linactl` 或 Make 公共入口；新增注册表单元测试覆盖 `pack.assets` 存在和旧命令未注册。当前工作区存在前序 `.github/workflows/main-ci.yml`、`AGENTS.md`、`CONTRIBUTING.md` 与若干未跟踪 reusable workflow/中文文档改动，本次审查未回退或重写这些无关变更。本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑；严重问题 0；警告 0。
- [x] 2026-05-14: FB-10 验证通过：`ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f) }' .github/*.yaml .github/workflows/*.yml .github/actions/*/action.yml`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/*.yml`、`openspec validate release-test-and-build --strict`、`git diff --check -- .github/workflows hack/makefiles openspec/changes/release-test-and-build/tasks.md`、`rg -n "go run (\\./)?hack/tools/linactl|cd hack/tools/linactl|go run \\. .*release\\.tag\\.check|go run \\. .*test\\.go|go run \\. .*wasm|go run \\. .*pack\\.assets" .github/workflows -g '*.yml' -g '*.yaml'`（预期无命中）、`make release.tag.check print-version=1`、`make -n release.tag.check`、`make -n test.go plugins=1 race=true verbose=true`、`make -n wasm dry-run=true p=demo-dynamic`。确认 `create-release-tag.yml`、`release-test-and-build.yml`、`reusable-backend-unit-tests.yml` 和 `reusable-windows-command-smoke.yml` 中已有对应 make 包装的直接 `linactl` 执行均改为 `make` 或 `make.cmd` 入口；`test.go` 保留 `plugins/race/verbose` 参数透传，`wasm` 保留 `dry-run` 参数透传。i18n 影响：仅调整 GitHub Actions 命令、Make 参数透传和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：未新增平台脚本，根 Makefile 与 Windows `make.cmd` 仍作为 `linactl` Go 工具的薄包装入口。
- [x] 2026-05-14: FB-10 补充验证通过：`go run ./hack/tools/linactl test.scripts`、`make help`、`go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/create-release-tag.yml .github/workflows/release-test-and-build.yml .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml`。确认 Make 包装层帮助输出正常，仓库工具 smoke 通过，受影响 workflow 的 actionlint 复核无问题。
- [x] 2026-05-14: FB-10 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- .github/workflows/create-release-tag.yml .github/workflows/release-test-and-build.yml .github/workflows/reusable-backend-unit-tests.yml .github/workflows/reusable-windows-command-smoke.yml hack/makefiles/build.mk hack/makefiles/test.mk openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次只将已有对应 make 包装的 workflow 命令切换到 `make` 或 `make.cmd`，并补齐 `test.go` 的 `race/verbose` 与 `wasm` 的 `dry-run` 参数透传；剩余 `.github/workflows` 中的 `linactl` 命中仅为注释或隔离 fixture 路径，不是直接执行入口。本次不新增业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑；不涉及 Go 生产代码编译门禁。严重问题 0；警告 0。
- [x] 2026-05-14: FB-11 验证通过：`make pack.assets`、`go test ./apps/lina-core/internal/service/apidoc -run 'TestOpenAPII18nBundlesCoverCurrentMetadata/zh-CN' -count=1`、`go test ./apps/lina-core/internal/service/apidoc -count=1`、`openspec validate release-test-and-build --strict`、`git diff --check -- apps/lina-core/manifest/i18n/zh-CN/apidoc apps/lina-core/internal/packed/manifest/i18n/zh-CN/apidoc apps/lina-plugins/monitor-operlog/manifest/i18n/zh-CN/apidoc openspec/changes/release-test-and-build/tasks.md`。确认宿主 `zh-CN` apidoc 补齐共享 DTO 结构化 key，`monitor-operlog` 插件补齐插件命名空间下的共享操作日志 DTO key，`make pack.assets` 已同步生成当前工作区 packed manifest 资源，目标 apidoc 单测和同包测试均通过。i18n 影响：本次仅新增 OpenAPI apidoc 中文翻译资源，不修改前端运行时语言包、插件运行时 manifest/i18n 或接口源文案；资源按宿主 `core.*` 与插件 `plugins.monitor_operlog.*` 命名空间拆分。缓存一致性影响：仅静态翻译资源补齐，不新增或修改运行时缓存、缓存失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：仅运行既有 `make pack.assets` 生成资源，不新增或修改默认开发工具或平台脚本。
- [x] 2026-05-14: FB-11 `lina-review` 审查完成。审查范围来源：`git status --short -- apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-shared-dtos.json openspec/changes/release-test-and-build/tasks.md apps/lina-plugins`、`git -C apps/lina-plugins status --short -- monitor-operlog/manifest/i18n/zh-CN/apidoc/plugin-api-shared-dtos.json`、`git diff -- openspec/changes/release-test-and-build/tasks.md apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-shared-dtos.json`、`git -C apps/lina-plugins diff -- monitor-operlog/manifest/i18n/zh-CN/apidoc/plugin-api-shared-dtos.json`、`openspec status --change release-test-and-build --json`。确认新增 apidoc JSON 能被解析，宿主新增 key 均位于 `core.*` 命名空间，插件新增 key 均位于 `plugins.monitor_operlog.*` 命名空间，宿主 manifest 与 packed `core-api-shared-dtos.json` 内容一致，`TestOpenAPII18nBundlesCoverCurrentMetadata/zh-CN` 和 apidoc 包测试已覆盖本次缺失项。本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、运行时缓存、数据权限逻辑或默认开发脚本；严重问题 0；警告 0。
- [x] 2026-05-14: FB-12 验证通过：`make -C apps/lina-core -n pack.assets`、`make -C apps/lina-core pack.assets`、`cd hack/tools/linactl && go test ./... -count=1`、`bash -n hack/tests/scripts/run-sqlite-smoke.sh`、`LINAPRO_SQLITE_SMOKE_PORT=18081 ./hack/tests/scripts/run-sqlite-smoke.sh`、`openspec validate release-test-and-build --strict`、`git diff --check -- apps/lina-core/Makefile openspec/changes/release-test-and-build/tasks.md`、`rg -n "go run \\.\\./\\.\\./hack/tools/linactl prepare-packed-assets|linactl prepare-packed-assets|make -C .*prepare-packed-assets|make .*prepare-packed-assets" apps/lina-core/Makefile .github/workflows hack/tests/scripts hack/makefiles Makefile make.cmd`（预期无命中）。确认 `apps/lina-core` 的 `pack.assets` 目标已改为执行 `linactl pack.assets`，SQLite smoke 的实际初始化、mock、资源打包、后端构建、健康检查和登录验证均通过。i18n 影响：仅调整开发命令包装入口和 OpenSpec 任务记录，不新增或修改前端运行时文案、接口文档、manifest i18n 或 apidoc i18n 资源。缓存一致性影响：不涉及运行时缓存、失效或跨实例一致性策略。数据权限影响：不新增或修改 HTTP/API 数据操作接口，不涉及角色数据权限边界。开发工具与脚本影响：修改既有 `apps/lina-core` Make 包装目标，底层仍复用跨平台 Go 工具 `linactl`；未新增平台脚本。
- [x] 2026-05-14: FB-12 `lina-review` 审查完成。审查范围来源：`git status --short`、`git ls-files --others --exclude-standard`、`git diff -- apps/lina-core/Makefile openspec/changes/release-test-and-build/tasks.md`、`openspec status --change release-test-and-build --json`。确认本次实际修复仅将 `apps/lina-core` 子目录的 `pack.assets` Make 包装目标从旧 `linactl prepare-packed-assets` 切换为 `linactl pack.assets`，与当前 `linactl` 注册表、根 `make pack.assets` 和 SQLite smoke 调用链保持一致；静态扫描未发现 CI、Make 或 smoke 脚本仍调用旧命令。本次不新增 Go 生产代码、业务 API、数据库 schema、前端运行时文案、运行时缓存或数据权限逻辑；当前工作区存在前序 workflow 改动和未跟踪 `apps/lina-core/manifest/i18n/zh-CN/apidoc/core-api-shared-dtos.json`，本次审查未回退或重写这些无关变更。严重问题 0；警告 0。
