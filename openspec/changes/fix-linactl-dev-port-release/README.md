# fix-linactl-dev-port-release

Fix `linactl dev` so restarting managed backend and frontend development services waits briefly for their ports to be released before reporting an occupied-port error.
